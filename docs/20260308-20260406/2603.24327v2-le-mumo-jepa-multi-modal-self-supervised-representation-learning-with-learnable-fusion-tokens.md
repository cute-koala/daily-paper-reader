---
title: "Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens"
title_zh: Le MuMo JEPA：基于可学习融合 Token 的多模态自监督表示学习
authors: "Ciem Cornelissen, Sam Leroux, Pieter Simoens"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24327v2"
tags: ["query:q9"]
score: 10.0
evidence: 带有可学习融合标记的多模态JEPA
tldr: 本研究提出 Le MuMo JEPA，一种多模态自监督学习框架，旨在通过 RGB 图像及其对齐模态（如 LiDAR 深度或热成像）学习统一表征。该方法在共享 Transformer 中引入可学习的融合标记作为潜在瓶颈，通过剪枝融合策略强制跨模态信息整合。在 Waymo、nuScenes 和 FLIR 等自动驾驶数据集上的实验表明，该模型在检测、深度估计和分割任务中实现了最优的性能与效率平衡，显著降低了计算和内存开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24327v2/fig-001.webp\", \"caption\": \"Figure 1. Overview of Le MuMo JEPA. The companion modality is represented as a spatially aligned signal and fused with RGB through learnable fusion tokens, which act as a latent bottleneck inside a shared transformer. The default training objective applies SIGReg to the joint multimodal CLS embedding.\", \"page\": 2, \"index\": 1, \"width\": 458, \"height\": 573}, {\"url\": \"assets/figures/arxiv/2603.24327v2/fig-002.webp\", \"caption\": \"Table 3. Patch-probe comparison on Waymo with all encoders trained from scratch. CenterNet and dense prediction heads are trained on frozen patch embeddings taken from models that were all self-supervised from scratch on Waymo. “Training Data” denotes the modalities seen during SSL pretraining; for dual-stream baselines, the frozen patch probes use the camera-aligned RGB patch readout described in Section 4.1. Best results are in bold; second best are underlined. ↑ means higher is better and ↓ means lower is better.\", \"page\": 7, \"index\": 2, \"width\": 744, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2603.24327v2/fig-003.webp\", \"caption\": \"Figure 4. Waymo qualitative probe output. The figure shows the RGB input together with predictions from the three probe families used in the paper: dense depth estimation, segmentation, and 3D detection boxes. It provides a direct qualitative view of the same patch-level capabilities summarized by the quantitative probe tables.\", \"page\": 8, \"index\": 3, \"width\": 492, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2603.24327v2/fig-004.webp\", \"caption\": \"Figure 3. Waymo patch-embedding visualization. t-SNE projections of final-layer patch embeddings are shown with classoriented structure and depth-oriented structure, illustrating how the learned patch space organizes both semantic grouping and geometric variation across methods. The left column uses Le MuMo JEPA fusion-token embeddings, and the right column uses LeJEPA patch embeddings. A planar fit to the plotted depth gradient gives an R2 score of 0.463 for Le MuMo JEPA versus 0.086 for LeJEPA.\", \"page\": 8, \"index\": 4, \"width\": 471, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2603.24327v2/fig-005.webp\", \"caption\": \"Figure 2. Waymo dataset showcase. Example synchronized supervision used in our experiments: (a) RGB image, (b) cameraview segmentation, (c) the aligned companion-modality signal shown in depth form for the driving setting, and (d) projected 3D bounding boxes.\", \"page\": 5, \"index\": 5, \"width\": 487, \"height\": 330}]"
motivation: 现有的自监督学习多局限于单一模态，未能充分利用异构传感器提供的互补结构信息。
method: 扩展 LeJEPA 架构，利用可学习的融合标记作为模态特定路径间的潜在瓶颈，并结合剪枝融合策略与 SIGReg 正则化。
result: 在 Waymo 和 nuScenes 等数据集上，该方法在下游检测和深度估计任务中表现优异，且在 FLIR 热成像基准测试中取得了最佳结果。
conclusion: Le MuMo JEPA 在保持高精度的同时，显著提升了多模态表征学习的计算效率和内存利用率。
---

## 摘要
自监督学习已成为一种无需人工标注即可学习视觉表示的强大范式，但大多数方法仍运行在单一模态上，因此错失了异构传感器提供的互补结构信息。我们提出了 Le MuMo JEPA，这是一个从 RGB 图像及其对齐的伴随模态中学习统一表示的自监督框架。在我们的驾驶实验中，第二种模态是与相机对齐的 LiDAR 深度；我们还在 Teledyne FLIR ADAS 基准测试中评估了 RGB-热成像训练和迁移。我们的方法通过学习融合 Token（fusion tokens）将 LeJEPA 扩展到多模态设置，这些 Token 在共享 Transformer 内部的模态特定 patch stem 之间充当潜瓶颈（latent bottleneck）。我们的默认模型采用了一种剪枝融合策略：在初始跨模态注意力层之后，丢弃模态特定的 Token，迫使跨模态信息进入共享的融合 Token 网格，将其作为高效的潜瓶颈，随后对联合多模态 CLS 嵌入应用草图各向同性高斯正则化（SIGReg）。在 Waymo 数据集上，Le MuMo JEPA 在从零开始训练的多模态基准模型中，在下游 patch probe 任务上表现出最强的性能-效率权衡，提升了 CenterNet 检测和稠密深度估计性能，同时在分割任务上保持了竞争力。在 nuScenes 的从零训练下，Le MuMo JEPA 依然是最强的模型，并且在 FLIR 上也取得了最佳结果，尤其是在经过 Waymo 初始化的微调之后。在计算量、内存占用和预估训练时间大幅降低的情况下，它在我们的研究中仍保持了最佳的整体准确率-效率平衡。

## Abstract
Self-supervised learning has emerged as a powerful paradigm for learning visual representations without manual annotations, yet most methods still operate on a single modality and therefore miss the complementary structure available from heterogeneous sensors. We present Le MuMo JEPA, a self-supervised framework that learns unified representations from RGB images and aligned companion modalities. In our driving experiments, the second modality is camera-aligned LiDAR depth; we also evaluate RGB-thermal training and transfer on the Teledyne FLIR ADAS benchmark. Our approach extends LeJEPA to the multi-modal setting by learning fusion tokens that act as a latent bottleneck between modality-specific patch stems inside a shared transformer. Our default model employs a pruned fusion strategy: after an initial cross-modal attention layer, modality-specific tokens are dropped, forcing cross-modal information into the shared fusion-token grid as an efficient latent bottleneck before Sketched Isotropic Gaussian Regularization (SIGReg) is applied to the joint multimodal CLS embedding. On Waymo, Le MuMo JEPA gives the strongest performance-efficiency trade-off on downstream patch probes among the from-scratch multimodal baselines, improving CenterNet detection and dense depth while remaining competitive on segmentation. Under from-scratch training on nuScenes, Le MuMo JEPA remains the strongest model, and it also gives the best FLIR results, especially after Waymo-initialized fine-tuning. It also retains the best overall accuracy-efficiency balance in our study at substantially lower compute, memory, and estimated training time.