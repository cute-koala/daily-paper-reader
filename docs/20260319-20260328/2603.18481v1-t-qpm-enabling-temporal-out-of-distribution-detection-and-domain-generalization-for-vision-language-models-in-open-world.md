---
title: "T-QPM: Enabling Temporal Out-Of-Distribution Detection and Domain Generalization for Vision-Language Models in Open-World"
title_zh: T-QPM：在开放世界中实现视觉语言模型的时序分布外检测与领域泛化
authors: "Aditi Naiknaware, Salimeh Sekeh"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18481v1"
tags: ["query:vlm"]
score: 9.0
evidence: 类似CLIP的视觉语言模型与离群检测
tldr: 针对视觉语言模型在开放世界中面临的时间偏移和协变量偏移挑战，本文提出了T-QPM框架。该框架将传统的双模式匹配扩展为时间四重模式匹配，通过引入跨模态一致性模式来精细化决策边界，并利用轻量级融合权重和ATC正则化来应对动态分布变化，显著提升了模型在非平稳环境下的OOD检测性能和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18481v1/fig-001.webp\", \"caption\": \"Table 1: OOD detection results across 3 ID datasets and 5 OOD datasets using ViT16 architecture Each cell shows FPR95↓ / AUROC↑. Early (t=2) and late (t=8) times.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-002.webp\", \"caption\": \"Table 2: OOD detection results across 3 ID datasets and 5 OOD datasets using ViT-32 architecture. Each cell shows FPR95↓ / AUROC↑. Early (t=2) and late (t=8) times.\", \"page\": 11, \"index\": 2, \"width\": 725, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-003.webp\", \"caption\": \"Table 4: ID classification accuracy (clean and blur) for T-QPM vs. DPM across all ID datasets and timesteps under Gaussian Blur covariate shift\", \"page\": 26, \"index\": 3, \"width\": 579, \"height\": 654}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-004.webp\", \"caption\": \"Fig. 2: ID classification accuracy on clean (left) and Gaussian blur-shifted (right) test sets across all timesteps. T-QPM consistently outperforms DPM under both conditions, with the gap widening substantially under covariate shift. ID dataset: Clear100, OOD dataset: COCO with captions\", \"page\": 10, \"index\": 4, \"width\": 725, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-005.webp\", \"caption\": \"Fig. 3: ID classification accuracy on clean (left) and JPEG shifted-shifted (right) test sets across all timesteps. T-QPM consistently outperforms DPM under both conditions, with the gap widening substantially under covariate shift. ID dataset: Clear100, OOD dataset: COCO with captions\", \"page\": 10, \"index\": 5, \"width\": 725, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-006.webp\", \"caption\": \"Table 3: Ablation study of the Loss on Clear100 as ID and COCO as OOD on Vit-16.\", \"page\": 12, \"index\": 6, \"width\": 494, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-007.webp\", \"caption\": \"Table 5: ID classification accuracy under JPEG compression corruption for T-QPM vs. DPM across all ID datasets and timesteps.\", \"page\": 27, \"index\": 7, \"width\": 552, \"height\": 654}, {\"url\": \"assets/figures/arxiv/2603.18481v1/fig-008.webp\", \"caption\": \"Fig. 1: T-QPM Overview: At each timestep, ID images and their covariate-shifted views are encoded to build timestep-specific visual prototypes alongside a fixed ID Text Bank. At inference, four cross-modal scores between the test image, caption, and ID representations are fused to produce the final OOD decision.\", \"page\": 5, \"index\": 8, \"width\": 725, \"height\": 310}]"
motivation: 现有的视觉语言模型OOD检测方法通常假设环境静态，难以应对现实世界中的时间分布偏移和协变量偏移问题。
method: 提出时间四重模式匹配（T-QPM）框架，结合跨模态一致性推理与基于平均阈值置信度（ATC）正则化的动态权重融合技术。
result: 在时间分区基准测试中，该方法显著超越了现有的静态基准模型，展现出更强的鲁棒性和时间一致性。
conclusion: T-QPM为动态开放世界环境下的多模态OOD检测提供了一个有效且稳定的技术方案。
---

## 摘要
分布外（OOD）检测仍然是开放世界学习中的一个关键挑战，模型必须适应不断演化的数据分布。虽然最近的视觉语言模型（VLM）（如 CLIP）通过双模式匹配（DPM）实现了多模态 OOD 检测，但现有方法通常存在两个主要缺点：（1）它们依赖于固定的融合规则并假设环境是静态的，在时序漂移下会失效；（2）它们对协变量偏移的输入缺乏鲁棒性。在本文中，我们提出了一个新颖的两步框架，以增强动态设置下的 OOD 检测和协变量分布偏移鲁棒性。我们将双模式机制扩展为时序四重模式匹配（T-QPM）。首先，通过将 OOD 图像与文本描述配对，我们引入了 ID 和 OOD 信号之间的跨模态一致性模式，通过联合图文推理来优化决策边界。其次，我们通过学习轻量级融合权重来解决时序分布偏移，从而优化组合语义匹配和视觉典型性。为了确保稳定性，我们实施了基于平均阈值置信度（ATC）的显式正则化，防止性能随着分布演化而下降。在时序划分的基准测试上的实验表明，我们的方法显著优于静态基准模型，为非平稳环境下的多模态 OOD 检测提供了一个鲁棒且时序一致的框架。

## Abstract
Out-of-distribution (OOD) detection remains a critical challenge in open-world learning, where models must adapt to evolving data distributions. While recent vision-language models (VLMS) like CLIP enable multimodal OOD detection through Dual-Pattern Matching (DPM), existing methods typically suffer from two major shortcomings: (1) They rely on fixed fusion rules and assume static environments, failing under temporal drift; and (2) they lack robustness against covariate shifted inputs. In this paper, we propose a novel two-step framework to enhance OOD detection and covariate distribution shift robustness in dynamic settings. We extend the dual-pattern regime into Temporal Quadruple-Pattern Matching (T-QPM). First, by pairing OOD images with text descriptions, we introduce cross-modal consistency patterns between ID and OOD signals, refining the decision boundary through joint image-text reasoning. Second, we address temporal distribution shifts by learning lightweight fusion weights to optimally combine semantic matching and visual typicality. To ensure stability, we enforce explicit regularization based on Average Thresholded Confidence (ATC), preventing performance degradation as distributions evolve. Experiments on temporally partitioned benchmarks demonstrate that our approach significantly outperforms static baselines, offering a robust, temporally-consistent framework for multimodal OOD detection in non-stationary environments.

---

## 论文详细总结（自动生成）

以下是对论文《T-QPM: Enabling Temporal Out-Of-Distribution Detection and Domain Generalization for Vision-Language Models in Open-World》的深度结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现有的视觉语言模型（VLM，如CLIP）在开放世界（Open-World）部署时面临三大挑战：
    1.  **时序漂移（Temporal Drift）**：数据分布随时间动态演化，静态模型难以适应。
    2.  **协变量偏移（Covariate Shift）**：输入图像可能存在噪声或损坏（如模糊、压缩），导致模型性能下降。
    3.  **多模态信号利用不足**：现有方法（如DPM）主要关注图像输入，忽略了测试样本自带的文本描述（Caption）中蕴含的丰富语义。
*   **研究动机**：旨在开发一个能够同时处理语义分布外（OOD）检测、领域泛化（针对协变量偏移）以及时序动态适应的统一框架。

### 2. 论文提出的方法论：T-QPM
T-QPM（时序四重模式匹配）框架分为四个阶段：
*   **阶段 I：文本模式构建**：利用冻结的CLIP文本编码器，通过提示词集成（Prompt Ensembling）为每个类别生成固定的ID文本库（Text Bank），作为稳定的语义锚点。
*   **阶段 II：时序视觉模式构建**：针对每个时间步（Timestep），利用空间注意力机制提取图像特征，并计算类条件视觉原型（Visual Prototypes），使模型能感知视觉分布的随时间的变化。
*   **阶段 III：四重跨模态评分**：融合了四种匹配得分：
    1.  **图像-文本匹配**：测试图像与ID文本库的对齐度。
    2.  **图像-视觉典型性**：测试图像与当前时间步视觉原型的KL散度。
    3.  **标题-文本对齐**：测试样本标题与ID文本库的语义重叠。
    4.  **标题-视觉对齐**：测试样本标题与当前时间步视觉统计的一致性。
*   **阶段 IV：训练与阈值校准**：
    *   **轻量化学习**：仅学习2个标量融合权重（$\beta, \eta$），冻结整个CLIP骨干网络。
    *   **损失函数**：结合了平衡分类损失、协变量一致性损失（惩罚原始与损坏图像间的得分差异）以及**时序漂移惩罚**（利用ATC正则化稳定跨时间步的置信度边界）。

### 3. 实验设计
*   **数据集与场景**：
    *   **ID（分布内）数据集**：CLEAR100、CLEAR10（包含10个时间桶）和Core50（10个不同环境的会话），用于模拟时序演化。
    *   **OOD（分布外）数据集**：COCO、ImageNet-1K-VL、Flickr30K、CC12M、Visual Genome。
    *   **协变量偏移**：对ID测试集施加高斯模糊和JPEG压缩损坏。
*   **Benchmark与对比方法**：主要对比基准是 **DPM（Dual-Pattern Matching）**，这是目前VLM在OOD检测领域的先进方法。
*   **评估指标**：FPR95（假阳性率）、AUROC（曲线下面积）以及分类准确率。

### 4. 资源与算力
*   **算力说明**：论文明确提到实验运行在**单张 NVIDIA GPU**上。
*   **训练细节**：每个时间步仅训练5个Epoch，学习率为 $3 \times 10^{-3}$。由于仅优化2个参数且冻结了骨干网络，该方法具有极高的参数效率和计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了3个时序ID基准和5个多模态OOD基准。
    *   测试了两种模型架构（ViT-B/16 和 ViT-B/32）。
    *   进行了详尽的消融实验（验证四个损失组件的必要性）和超参数敏感性分析（$\beta, \eta, \gamma_{cap}$）。
*   **充分性评价**：实验设计非常充分且客观。通过在10个连续时间步上展示性能曲线，清晰地证明了该方法在处理长期漂移时的稳定性。对比实验在相同骨干网络和设置下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能领先**：T-QPM在所有基准测试中均显著优于DPM。在FPR95指标上，T-QPM通常能将错误率降低2-3倍。
*   **时序鲁棒性**：随着时间推移，DPM的性能往往大幅波动或下降，而T-QPM通过时序原型更新和ATC正则化保持了极高的稳定性。
*   **协变量抗性**：在图像受损（模糊/压缩）的情况下，T-QPM的分类准确率比DPM高出约5%-6%，证明了其在领域泛化方面的优势。
*   **理论支撑**：论文通过数学证明（定理1）建立了时序一致性与泛化误差界限之间的联系。

### 7. 优点
*   **多模态融合创新**：首次在时序OOD检测中引入“四重匹配”，充分挖掘了测试样本的标题信号。
*   **参数极其高效**：仅需训练2个参数即可实现强大的适应能力，非常适合端侧或实时部署。
*   **双重鲁棒性**：同时解决了语义OOD检测和协变量偏移下的领域泛化问题。

### 8. 不足与局限
*   **标题依赖性**：该方法假设测试样本带有文本标题（Caption）。在某些纯视觉传感器应用（如简单的监控摄像头）中，可能无法实时获取高质量的标题。
*   **阈值固定化**：虽然得分是动态融合的，但决策阈值 $\delta$ 在 $t=0$ 时刻固定。在极端长期的环境中，固定阈值可能仍难以应对剧烈的分布突变。
*   **计算开销**：虽然训练开销极低，但在推理时需要对标题进行实时编码，相比纯视觉方法增加了一定的计算延迟。

（完）
