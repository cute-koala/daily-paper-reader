---
title: "LanteRn: Latent Visual Structured Reasoning"
title_zh: LanteRn：潜空间视觉结构化推理
authors: "André G. Viveiros, Nuno Gonçalves, Matthias Lindemann, André Martins"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25629v1"
tags: ["query:vlm"]
score: 9.0
evidence: 多模态大模型中的潜空间视觉推理
tldr: 针对当前多模态大模型在视觉推理中过度依赖文本描述而缺乏细粒度空间理解的问题，本文提出 LanteRn 框架。该框架允许模型在推理过程中交替使用语言和紧凑的潜空间视觉表示，直接在潜空间进行视觉推理。通过监督微调和强化学习两阶段训练，LanteRn 在多个感知基准测试中显著提升了视觉定位和细粒度推理能力，为高效多模态推理提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25629v1/fig-001.webp\", \"caption\": \"Table 1: SFT evaluation results on V ⋆, Blink and a subset of VisCoT datasets. DA = Direct Attribution, RP = Relative Position, OL = Object Localization.\", \"page\": 7, \"index\": 1, \"width\": 827, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2603.25629v1/fig-002.webp\", \"caption\": \"Table 2: RL evaluation results on VStar, Blink and a subset of Viscot datasets. DA = Direct Attribution, RP = Relative Position, OL = Object Localization.\", \"page\": 8, \"index\": 2, \"width\": 827, \"height\": 191}]"
motivation: 现有模型将视觉内容转化为文本会丢失细粒度空间信息，而直接在像素空间推理则面临巨大的计算开销。
method: 引入连续的视觉思维嵌入，使模型能在潜空间进行推理，并采用监督微调与强化学习两阶段策略进行训练。
result: "在 VisCoT、V* 和 Blink 等多个感知基准测试中，该方法在视觉定位和细粒度推理任务上均取得了显著提升。"
conclusion: 利用内部潜空间表示进行视觉推理是提升多模态模型效率和准确性的有效途径。
---

## 摘要
尽管语言推理模型在许多任务中表现出色，但视觉推理对于当前的大型多模态模型（LMMs）而言仍具挑战性。因此，大多数 LMM 默认将感知内容转化为文本，这对于需要细粒度空间和视觉理解的任务来说是一个严重的局限。虽然最近的方法通过调用工具或生成中间图像在“以图思考”方面取得了进展，但它们要么依赖外部模块，要么因直接在像素空间进行推理而产生不必要的计算开销。在本文中，我们介绍了 LanteRn，这是一个使 LMM 能够将语言与紧凑的潜视觉表示交织在一起的框架，从而允许视觉推理直接在潜空间中进行。LanteRn 增强了视觉-语言 Transformer 的能力，使其在推理过程中能够生成并关注连续的视觉思维嵌入。我们分两个阶段训练该模型：首先通过监督微调将视觉特征锚定在潜状态中，随后通过强化学习使潜推理与任务级效用对齐。我们在三个以感知为中心的基准测试（VisCoT、V* 和 Blink）上评估了 LanteRn，观察到在视觉定位和细粒度推理方面有一致的提升。这些结果表明，内部潜表示为更高效的多模态推理提供了一个充满前景的方向。

## Abstract
While language reasoning models excel in many tasks, visual reasoning remains challenging for current large multimodal models (LMMs). As a result, most LMMs default to verbalizing perceptual content into text, a strong limitation for tasks requiring fine-grained spatial and visual understanding. While recent approaches take steps toward thinking with images by invoking tools or generating intermediate images, they either rely on external modules, or incur unnecessary computation by reasoning directly in pixel space. In this paper, we introduce LanteRn, a framework that enables LMMs to interleave language with compact latent visual representations, allowing visual reasoning to occur directly in latent space. LanteRn augments a vision-language transformer with the ability to generate and attend to continuous visual thought embeddings during inference. We train the model in two stages: supervised fine-tuning to ground visual features in latent states, followed by reinforcement learning to align latent reasoning with task-level utility. We evaluate LanteRn on three perception-centric benchmarks (VisCoT, V*, and Blink), observing consistent improvements in visual grounding and fine-grained reasoning. These results suggest that internal latent representations provide a promising direction for more efficient multimodal reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《LanteRn: Latent Visual Structured Reasoning》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：当前的大型多模态模型（LMMs）在处理视觉任务时，通常采用“关于图像的思考（Thinking about images）”模式，即将视觉信息转化为文本描述后再进行推理。这种方式会导致高维视觉信息的丢失，尤其在需要细粒度空间理解和复杂感知任务中表现不佳。
*   **现有方案局限**：
    *   **工具调用类**：依赖外部模块（如检测器、裁剪工具），计算开销大且受限于预定义工具。
    *   **像素生成类**：在推理链中生成中间图像，但在像素空间进行推理会浪费大量算力在无关的图像细节上。
*   **核心问题**：如何让模型直接在“潜空间（Latent Space）”中进行高效、结构化的视觉推理，实现真正的“以图思考（Thinking with images）”？

### 2. 方法论（LanteRn 框架）
LanteRn 允许模型在推理过程中交替生成文本和连续的潜视觉表示（Latent Visual States）。
*   **核心思想**：在 Transformer 的词表中增加控制标记（如 `<|lvr_start|>`），当模型进入“潜视觉模式”时，不再通过语言头预测概率，而是直接输出 $K$ 个隐藏层向量作为“视觉思维嵌入”。
*   **两阶段训练流程**：
    1.  **监督微调（SFT）——视觉锚定**：
        *   利用预训练视觉编码器作为“教师”。
        *   **损失函数**：$L = L_{text} + \gamma L_{latent}$。其中 $L_{latent}$ 是模型生成的潜向量与视觉编码器提取的感兴趣区域（ROI）特征之间的均方误差（MSE）。
        *   **目的**：让模型学会将潜状态与具体的视觉概念（如边界框内的特征）对齐。
    2.  **强化学习（RL）——任务对齐**：
        *   采用 **GRPO（组相对策略优化）** 算法。
        *   **混合动作空间**：处理离散文本和连续潜向量。为了稳定训练，引入了“潜状态回放（Latent State Replay）”机制。
        *   **奖励设计**：结合准确率奖励（$R_{acc}$）和格式奖励（$R_{fmt}$）。
        *   **目的**：使潜表示从单纯的“视觉重建”转向提升最终答案正确性的“任务驱动型推理”。

### 3. 实验设计
*   **基准模型**：以 Qwen2.5-VL-3B 为基础架构。
*   **数据集**：
    *   **SFT 阶段**：基于 Visual-CoT 构建的合成数据集（包含推理轨迹和 ROI 标注）。
    *   **RL 阶段**：使用 VIRL-39k 数据集（包含多样化的视觉推理问题，无显式区域标注）。
*   **评估基准（Benchmarks）**：
    *   **VisCoT**：评估多步视觉推理。
    *   **V\***：评估视觉搜索和空间关系理解。
    *   **Blink**：评估核心视觉感知能力（如物体定位、相对位置）。
*   **对比方法**：
    *   **Qwen2.5-VL-3B**：原始预训练模型。
    *   **LanteRn-NTP**：仅使用文本预测（Next Token Prediction）的基准，将潜向量位置替换为普通文本标记，以验证连续潜表示的独特价值。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$（SFT）和 $5 \times 10^{-6}$（RL），采用了余弦学习率调度和 TRL 库进行强化学习训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对潜向量长度 $K$ 做了消融实验（$K \in \{4, 8, 16, 32\}$）。
    *   在三个主流感知基准（VisCoT, V\*, Blink）的多个子任务上进行了测试。
*   **充分性评价**：实验设计较为**充分且客观**。通过引入 NTP 基准，排除了“推理步数增加”带来的性能提升干扰，证明了“潜空间表示”本身的有效性。但在模型规模上，仅在 3B 参数级别进行了验证，尚未展示在更大规模模型（如 7B 或 72B）上的扩展性。

### 6. 主要结论与发现
*   **潜推理的优越性**：LanteRn 在感知密集型任务上显著优于纯文本推理模型，尤其在物体定位（Blink OL）和相对位置（Blink RP）任务中提升明显。
*   **RL 的关键作用**：SFT 仅能实现视觉特征的初步对齐，而 RL 阶段能显著提升模型利用潜表示解决复杂问题的能力（如 Blink RP 从 0.68 提升至 0.81）。
*   **效率优势**：相比于生成像素或调用外部工具，潜空间推理在保持视觉结构信息的同时，计算开销更低。

### 7. 优点与亮点
*   **架构创新**：提出了一种优雅的、内生式的“以图思考”机制，无需外部工具或昂贵的像素解码。
*   **训练策略**：结合了监督学习的“特征锚定”和强化学习的“效用对齐”，解决了潜表示缺乏直接监督信号的问题。
*   **性能提升**：在 3B 规模的模型上实现了与某些 7B 模型相当的感知推理性能。

### 8. 不足与局限
*   **固定容量限制**：目前使用固定的潜向量长度 $K$，无法根据任务复杂度动态调整推理所需的视觉资源。
*   **数据领域局限**：训练数据主要集中在特定的视觉推理领域，跨领域的泛化能力（如医学影像、遥感）尚待验证。
*   **可解释性挑战**：潜空间中的“视觉思维”对人类而言是不可见的，缺乏直观的视觉化手段来解释模型到底在“想”什么。
*   **算力细节缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估复现成本。

（完）
