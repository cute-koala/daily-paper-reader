---
title: ROI-Driven Foveated Attention for Unified Egocentric Representations in Vision-Language-Action Systems
title_zh: ROI驱动的中心凹注意力：用于视觉-语言-动作系统中统一的第一人称视角表征
authors: "Xinhai Sun, Xiang Shi, Menglin Zou, Wenlong Huang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20668v1"
tags: ["query:vlm"]
score: 9.0
evidence: 视觉-语言-动作系统与统一的第一视角表示
tldr: 针对具身智能中数据采集成本高和跨机器人对齐难的问题，本文提出一种ROI驱动的视网膜中心化注意力机制。通过正向运动学将末端执行器位姿投影至外部相机，生成与动作对齐的手部ROI，在保留全局上下文的同时维持关键接触区域的高分辨率。该方法实现了跨异构机器人的统一表征，为大规模数据复用和视觉-语言-动作（VLA）系统的跨具身学习提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20668v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF HAND-CENTRIC DATA COLLECTION STRATEGIES\", \"page\": 7, \"index\": 1, \"width\": 1056, \"height\": 233}]"
motivation: 现有的VLA模型受限于高昂的数据采集成本、有限的跨具身对齐能力以及互联网视觉数据向机器人控制迁移困难的问题。
method: 利用正向运动学将机器人末端位姿投影到单目外部相机，提取高分辨率的手部ROI，并结合全局上下文构建统一的几何对齐表征。
result: 建立了一套包含标定、同步和ROI生成的标准化流程，实现了在不依赖腕部相机的情况下获取高质量的第一视角动作数据。
conclusion: ROI驱动的自我中心表征是一种实用的数据抽象方式，能有效促进跨异构机器人的数据复用和大规模具身智能学习。
---

## 摘要
具身智能系统的发展日益受到物理交互数据的可用性和结构的限制。尽管视觉-语言-动作（VLA）模型近期取得了进展，但当前的流程仍面临数据采集成本高、跨具身对齐受限以及从互联网规模的视觉数据到机器人控制的迁移效果差等问题。我们提出了一种由感兴趣区域（ROI）驱动的工程工作流，引入了一种基于几何的第一人称视角数据表征。通过正向运动学（FK）将末端执行器姿态投影到单个外部摄像头中，我们在无需腕部摄像头或多视图系统的情况下，推导出与运动对齐的以手为中心的 ROI。与直接对全帧进行下采样不同，ROI 在调整大小之前从原始图像中裁剪，从而在保留全局上下文的同时，为接触关键区域保留了高局部信息密度。我们展示了一个可复现的流程，涵盖了校准、同步、ROI 生成、确定性边界处理和元数据治理。由此产生的表征实现了具身对齐和视角归一化，从而支持在异构机器人之间进行数据复用。我们认为，第一人称视角 ROI 是可扩展采集和跨具身学习的一种实用数据抽象，桥接了互联网规模的感知与特定机器人的控制。

## Abstract
The development of embodied AI systems is increasingly constrained by the availability and structure of physical interaction data. Despite recent advances in vision-language-action (VLA) models, current pipelines suffer from high data collection cost, limited cross-embodiment alignment, and poor transfer from internet-scale visual data to robot control.   We propose a region-of-interest (ROI) driven engineering workflow that introduces an egocentric, geometry-grounded data representation. By projecting end-effector poses via forward kinematics (FK) into a single external camera, we derive movement-aligned hand-centric ROIs without requiring wrist-mounted cameras or multi-view systems. Unlike directly downsampling the full frame, ROI is cropped from the original image before resizing, preserving high local information density for contact-critical regions while retaining global context.   We present a reproducible pipeline covering calibration, synchronization, ROI generation, deterministic boundary handling, and metadata governance. The resulting representation is embodiment-aligned and viewpoint-normalized, enabling data reuse across heterogeneous robots. We argue that egocentric ROI serves as a practical data abstraction for scalable collection and cross-embodiment learning, bridging internet-scale perception and robot-specific control.

---

## 论文详细总结（自动生成）

### 论文总结：ROI驱动的中心凹注意力用于VLA系统中的统一第一人称表征

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能（Embodied AI）的发展受限于物理交互数据的稀缺性与结构差异。现有的视觉-语言-动作（VLA）模型在实际部署中面临三大挑战：
*   **数据采集成本高**：依赖多摄像头同步或昂贵的腕部相机（Eye-in-Hand）。
*   **跨具身对齐难**：不同机器人的传感器配置和机械结构导致数据难以复用。
*   **感知与控制脱节**：互联网规模的视觉数据缺乏机器人控制所需的局部精细特征。
本文提出了一种**ROI（感兴趣区域）驱动的工程工作流**，旨在通过单目外部相机生成“虚拟”的手部中心视角，从而在降低硬件复杂度的同时，提供高信息密度的第一人称表征。

#### 2. 方法论：核心思想与关键技术细节
核心思想是利用机器人的**正向运动学（FK）**将末端执行器的空间位姿投影到二维图像平面，从而动态裁剪出以手部为中心的ROI。

*   **FK-to-ROI 流程**：
    1.  **位姿计算**：通过关节编码器数据计算末端执行器在基座坐标系下的位姿 $^BT_E(t)$。
    2.  **坐标变换**：利用标定好的外参将位姿转换至相机坐标系 $^Cp_E(t)$。
    3.  **图像投影**：通过针孔相机模型将三维点投影为像素坐标 $(u, v)$。
    4.  **具身感知偏移（Embodiment-aware Offset）**：为了避免ROI中心仅停留在手腕，引入偏移量 $\beta$ 指向实际接触区域（如指尖）。
    5.  **自适应缩放与填充**：根据末端执行器的深度 $Z_c(t)$ 动态调整裁剪尺寸，并对超出图像边界的部分进行确定性零填充（Zero Padding）。
*   **中心凹注意力（Foveated Attention）**：
    *   将裁剪出的 ROI 调整为固定分辨率（如 256x256），其局部采样密度比直接下采样的全局图高出约 5 倍。
    *   在 Transformer 架构中，ROI 作为独立的 Token 序列输入，通过增加 Token 数量和空间分辨率，实现“结构化注意力强制”，使模型更关注交互区域。

#### 3. 实验设计
本文更侧重于**工程协议与标准化的定义**，而非单一的算法基准测试。
*   **评估协议**：提出了两种评估路径：
    *   **路径 A（数据集改造）**：在现有包含标定信息的离线数据集上生成 ROI，对比“仅全局图”与“全局+ROI”的训练效率。
    *   **路径 B（仿真/回放）**：在仿真环境中验证投影逻辑在跨具身（不同机械臂、夹具）时的鲁棒性。
*   **对比对象**：对比了单外部相机、原生腕部相机、多视角相机阵列以及本文的 FK-ROI 方法（见论文 Table I）。
*   **指标体系**：涵盖工程指标（标定耗时、带宽）、几何指标（投影残差、填充率）和学习代理指标（动作稳定性、数据效率）。

#### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   原因在于本文定位为“工程工作流（Engineering Workflow）”和“数据治理架构”，旨在指导 VLA 系统的开发实践，而非报告特定模型的训练开销。文中提到的 SaiVLA-0 是该团队的前期背景工作。

#### 5. 实验数量与充分性
*   **实验规模**：论文未提供大规模的消融实验数据表，而是通过几何推导和工程逻辑论证了 ROI 的有效性。
*   **充分性评价**：从系统工程角度看，论文详细定义了元数据字段、质量门控（Quality Gates）和治理模式，逻辑严密。但从实证角度看，缺乏在多个真实机器人任务上的量化对比结果，这在文中被列为“正在进行的工作”。
*   **客观性**：论文诚实地讨论了标定漂移和遮挡对该方法的影响，表现出较好的学术客观性。

#### 6. 主要结论与发现
*   **ROI 是高效的数据抽象**：通过 FK 投影生成的 ROI 能够桥接互联网感知数据与机器人特定控制，实现跨异构机器人的表征统一。
*   **降低硬件负担**：无需维护腕部相机硬件，通过软件算法即可获得高质量的第一人称视角。
*   **结构化偏置优于显式监督**：通过在 Token 空间重复表示 ROI 区域，可以诱导模型产生对交互区域的注意力偏置，无需修改 Transformer 架构或增加辅助损失函数。

#### 7. 优点
*   **工程落地性强**：提供了一套完整的从标定、同步到元数据管理的标准化流程。
*   **信息密度高**：通过局部裁剪保留了接触瞬间的关键视觉细节（如微小位姿变化）。
*   **跨平台兼容**：通过视角归一化，使得不同品牌、不同构型的机器人数据可以放入同一个 VLA 模型中训练。

#### 8. 不足与局限
*   **依赖标定精度**：ROI 的质量高度依赖于相机外参和 FK 的准确性，一旦发生机械形变或相机位移，投影会失效。
*   **遮挡风险**：在复杂操作中，机械臂自身或物体可能遮挡末端执行器，导致 ROI 失去关键信息。
*   **上下文丢失**：过度关注 ROI 可能导致模型忽略全局环境中的长程规划信息（需结合全局图使用）。
*   **实证数据欠缺**：论文缺乏最终的端到端任务成功率对比，更多是方法论的倡议。

（完）
