---
title: "Pixelis: Reasoning in Pixels, from Seeing to Acting"
title_zh: Pixelis：像素级推理，从观看到行动
authors: Yunpeng Zhou
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25091v1"
tags: ["query:vlm"]
score: 9.0
evidence: 使用视觉语言系统进行跟踪和定位的像素空间智能体
tldr: 针对视觉语言系统仅作为静态观察者、缺乏行动能力且难以适应环境变化的问题，本文提出 Pixelis。这是一个在像素空间运行的智能体，通过缩放、分割、跟踪等可执行操作直接处理图像和视频。它采用三阶段训练：监督微调学习工具语法，奖励微调优化好奇心与连贯性，以及测试时强化学习实现无标签自适应。实验表明，Pixelis 在多个基准测试中显著提升了性能，实现了更具物理落地感的视觉推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25091v1/fig-001.webp\", \"caption\": \"Table 1. Results on public benchmarks. We compare Pixelis to answer-only self-consistency, VLM test-time adaptation methods, process-supervised baselines, and a replicated Pixel Reasoner variant for each training phase. The upper block lists proprietary, non-directly comparable models; all claims use the same 8B backbone. Scores are reported on Tool-needed subsets with matched compute, sampling budget, acceptance rate, and token-level KL; † denotes a win on the Tool-needed subset.\", \"page\": 6, \"index\": 1, \"width\": 986, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2603.25091v1/fig-002.webp\", \"caption\": \"Figure 1. Pixelis uses executable pixel tools to act on images and videos. SFT learns tool syntax; CC-RFT shapes exploration with curiosity, coherence, and efficiency; Pixel TTRL adapts online via retrieval and trajectory voting under KL/EMA safety, producing shorter, auditable toolchains.\", \"page\": 2, \"index\": 2, \"width\": 912, \"height\": 645}, {\"url\": \"assets/figures/arxiv/2603.25091v1/fig-003.webp\", \"caption\": \"Figure 2. Three-phase training of Pixelis. SFT learns a tool-use grammar; CC-RFT shapes exploration with curiosity, coherence, and a light efficiency prior; Pixel TTRL adapts at test time by retrieving neighbours and updating toward behaviourally consistent trajectories under a KL-to-EMA safety constraint, turning raw tool traces into shorter, structured pixel toolchains.\", \"page\": 4, \"index\": 3, \"width\": 958, \"height\": 360}]"
motivation: 现有的视觉语言模型多为静态观察者，缺乏通过行动在物理世界中进行推理和自我优化的能力。
method: 提出一种像素空间智能体，通过三阶段训练（SFT、好奇心-连贯性奖励微调、测试时强化学习）学习并优化一系列可执行的像素级操作链。
result: "在六个图像和视频基准测试中，相比 8B 基线模型平均提升了 4.08% 的相对增益，并生成了更短且可审计的工具链。"
conclusion: 将推理过程置于像素级的行动中，能有效增强多模态感知的物理落地性，并支持在无外部反馈的情况下进行具身自适应。
---

## 摘要
大多数视觉语言系统是静态观察者：它们描述像素，但不采取行动，且在分布偏移下无法安全地改进。这种被动性限制了可泛化的、具有物理基础的视觉智能。在精选数据之外，通过行动而非静态描述进行学习至关重要。我们提出了 Pixelis，这是一个像素空间智能体，它通过一组紧凑的可执行操作（缩放/裁剪、分割、跟踪、OCR、时间定位）直接作用于图像和视频，并从其结果中学习。Pixelis 的训练分为三个阶段：(1) 有监督微调，通过带有掩码模仿损失的思维-行动链（Chain-of-Thought-Action）轨迹学习像素工具语法，该损失增加了操作/参数标记的权重，并利用辅助头来稳定像素落地的参数；(2) 好奇心-连贯性奖励微调，优化了一个结合了预测误差好奇心、相邻步骤连贯性以及在 KL 锚点下的轻度效率先验的双驱动目标，从而产生简短、有效且结构化的工具链；(3) 像素测试时强化学习，通过检索邻居、对完整轨迹而非答案进行投票，并向简短、高保真的样本进行更新，同时利用 KL-to-EMA 安全控制来约束漂移，从而实现无标签自适应。在六个公开的图像和视频基准测试中，Pixelis 取得了持续的改进：与相同的 8B 基线相比，平均相对增益为 +4.08%（在 VSI-Bench 上达到峰值 +6.03%），同时生成了更短、可审计的工具链，并在测试时学习期间保持了合理的 KL 散度。在像素内而非抽象标记中采取行动，使多模态感知落地于物理世界，将视觉推理与可执行的结果联系起来，并实现了无需外部反馈的具身自适应。

## Abstract
Most vision-language systems are static observers: they describe pixels, do not act, and cannot safely improve under shift. This passivity limits generalizable, physically grounded visual intelligence. Learning through action, not static description, is essential beyond curated data. We present Pixelis, a pixel-space agent that operates directly on images and videos via a compact set of executable operations (zoom/crop, segment, track, OCR, temporal localization) and learns from its consequences. Pixelis trains in three phases: (1) Supervised Fine-Tuning learns a pixel-tool grammar from Chain-of-Thought-Action traces with a masked imitation loss that upweights operation/argument tokens and auxiliary heads to stabilize pixel-grounded arguments; (2) Curiosity-Coherence Reward Fine-Tuning optimizes a dual-drive objective marrying prediction-error curiosity with adjacent-step coherence and a mild efficiency prior under a KL anchor, yielding short, valid, structured toolchains; (3) Pixel Test-Time RL performs label-free adaptation by retrieving neighbors, voting over complete trajectories rather than answers, and updating toward short, high-fidelity exemplars while constraining drift with a KL-to-EMA safety control. Across six public image and video benchmarks, Pixelis yields consistent improvements: the average relative gain is +4.08% over the same 8B baseline (peaking at +6.03% on VSI-Bench), computed as (ours-baseline)/baseline, while producing shorter, auditable toolchains and maintaining in-corridor KL during test-time learning. Acting within pixels, rather than abstract tokens, grounds multimodal perception in the physical world, linking visual reasoning with actionable outcomes, and enables embodied adaptation without external feedback.

---

## 论文详细总结（自动生成）

以下是对论文《Pixelis: Reasoning in Pixels, from Seeing to Acting》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉语言模型（VLM）大多是“静态观察者”，它们仅能描述像素而无法主动操作像素，导致在面对领域偏移（Domain Shift）时，定位、阅读和时间定位等错误会持续存在。
*   **核心问题**：如何打破“感知-行动”脱节的僵局，让模型能够通过一组可验证的像素级工具（如缩放、分割、跟踪）进行推理，并在测试阶段安全地进行自我优化。
*   **整体含义**：论文提出了 **Pixelis**，一个在像素空间运行的智能体。它不仅能组合可审计的工具链，还能通过在线学习闭合感知与行动的环路，将多模态感知落地于物理世界。

### 2. 论文提出的方法论
Pixelis 的训练和运行分为三个关键阶段：
*   **阶段 1：监督微调 (SFT)**
    *   **核心思想**：从“思维-行动链”（CoTA）轨迹中学习工具调用的语法。
    *   **技术细节**：采用掩码模仿损失，对操作和参数标记（Tokens）进行加权处理；引入辅助头（Auxiliary Heads）来稳定像素落地的参数（如坐标、掩码）。
*   **阶段 2：好奇心-连贯性奖励微调 (CC-RFT)**
    *   **核心思想**：通过强化学习优化工具链的结构。
    *   **关键技术**：
        *   **好奇心奖励**：基于预测误差，鼓励模型探索视觉状态。
        *   **连贯性先验**：通过计算相邻步骤嵌入向量的余弦相似度，惩罚无意义的工具切换，诱导生成简短且逻辑严密的计划。
        *   **效率先验**：在 KL 锚点约束下，倾向于生成更短的有效路径。
*   **阶段 3：像素测试时强化学习 (Pixel TTRL)**
    *   **核心思想**：在无标签的情况下，利用测试样本进行在线自适应。
    *   **算法流程**：检索相似的邻居样本 -> 对完整的推理轨迹（而非仅最终答案）进行行为投票 -> 选择高置信度且行为一致的轨迹作为伪标签进行更新 -> 使用 **KL-to-EMA** 安全控制防止模型在持续更新中产生漂移。

### 3. 实验设计
*   **数据集/场景**：涵盖了 6 个主流的图像和视频基准测试，包括：
    *   图像类：V* Bench, MMBench v1.1, InfoVQA (测试集), VSI Bench。
    *   视频类：MVBench, Video-MMMU。
*   **基准模型 (Backbone)**：主要基于 **Qwen3-VL-8B-Instruct**。
*   **对比方法**：
    *   闭源模型：GPT-5 (minimal), Gemini-2.5-Pro。
    *   开源模型：Seed-1.5-VL, InternVL3.5, Qwen3-VL-30B。
    *   消融/变体：Pixel Reasoner 复制版、仅答案级的自一致性方法、现有的 VLM 测试时自适应方法（如 StatA, RA-TTA）以及过程监督基线（PRM）。

### 4. 资源与算力
*   **算力消耗**：论文明确指出，对于 8B 规模的模型，总训练成本为 **805.2 GPU·h**。
*   **运行效率**：推理延迟主要由像素工具（如分割和跟踪）占据（约 87%），在线自适应（检索和投票）的中位额外开销约为 0.35 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同维度的基准测试上进行了验证，涵盖了空间推理、时间定位、OCR 等多种任务。
*   **消融实验**：非常充分。包括对 SFT/RFT/TTRL 各阶段的拆解、工具必要性分析（逐一禁用工具）、结构先验对比（L2 平滑 vs. 连贯性）、以及对检索泄露和去重的严格审计。
*   **客观性**：通过引入 RaPR（像素推理率）和 RaCPR（复合像素推理率）等过程指标，不仅关注最终准确率，还量化了推理过程的质量。

### 6. 主要结论与发现
*   **性能提升**：Pixelis 在 8B 规模下，相比基线平均取得了 **+4.08%** 的相对增益，在 VSI-Bench 上最高提升 **+6.03%**。
*   **工具链优化**：平均推理步骤从约 6 步缩短至 **3.7 步**，且生成的工具链更具可审计性。
*   **安全性**：Pixel TTRL 成功将测试时的 KL 散度控制在 0.2 以内，有效防止了无监督更新下的模型崩溃。
*   **像素落地的价值**：实验证明，直接在像素空间操作（而非仅依赖文本启发式搜索）是提升多模态模型物理落地能力的关键。

### 7. 优点
*   **可审计性**：通过可执行的工具链，模型给出的答案具有清晰的视觉证据支撑。
*   **闭环学习**：提出的 TTRL 机制允许模型在部署后根据环境反馈持续进化，且具备极强的安全性（KL-to-EMA）。
*   **创新的评价指标**：RaPR 和 RaCPR 为评估视觉智能体的推理过程提供了新的工具。

### 8. 不足与局限
*   **阶段性耦合**：目前三个训练阶段是松散耦合的，而非单一的端到端目标，可能导致 SFT 阶段的偏差在后续阶段持续存在。
*   **工具依赖性**：系统高度依赖非微分工具（如 SAM2, PP-OCR），如果底层工具在极端光照或模糊下失效，错误会沿推理链传播。
*   **计算开销**：虽然推理步骤缩短了，但调用复杂的视觉工具（如视频跟踪）相比纯文本推理仍有显著的延迟增加。

（完）
