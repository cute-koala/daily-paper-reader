---
title: "Fusion-Poly: A Polyhedral Framework Based on Spatial-Temporal Fusion for 3D Multi-Object Tracking"
title_zh: Fusion-Poly：一种基于时空融合的 3D 多目标跟踪多面体框架
authors: "Xian Wu, Yitao Wu, Xiaoyu Li, Zijia Li, Lijun Zhao, Lining Sun"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08199v1"
tags: ["query:vot"]
score: 8.0
evidence: 利用时空融合的3D多目标跟踪
tldr: 针对LiDAR与相机采样频率不一致导致异步数据被浪费的问题，本文提出Fusion-Poly框架。该框架通过时空融合技术，在同步时间戳关联多模态观测，在异步时间戳关联单模态观测，实现了更高频率的运动和存在状态更新。主要贡献包括频率感知级联匹配、轨迹估计及全状态观测对齐模块，显著提升了轨迹一致性和跟踪可靠性。
selection_source: carryover_cache
motivation: 现有的3D多目标跟踪方法通常只在同步时间戳进行空间融合，忽略了大量异步观测数据中蕴含的丰富信息。
method: 提出包含频率感知级联匹配、高频轨迹估计和全状态观测对齐三个核心组件的时空融合框架。
result: "在nuScenes测试集上达到了76.5%的AMOTA，刷新了基于检测的3D多目标跟踪方法的最优性能记录。"
conclusion: 充分利用异步传感器数据进行高频状态更新，能有效提升3D多目标跟踪的鲁棒性和准确性。
---

## 摘要
激光雷达-相机 3D 多目标跟踪 (MOT) 结合了丰富的视觉语义和精确的深度线索，以提高轨迹一致性和跟踪可靠性。然而，在实践中，激光雷达和相机的采样率不同。为了保持时间对齐，现有的数据流水线通常同步异构传感器流，并以降低的共享频率对其进行标注，这迫使大多数先前的方法仅通过基于投影或可学习的跨传感器关联在同步时间戳执行空间融合。结果，大量的异步观测数据仍未得到充分利用，尽管它们具有在短时间间隔内支持更频繁的关联和更稳健的轨迹估计的潜力。为了解决这一局限性，我们提出了 Fusion-Poly，这是一个用于 3D MOT 的时空融合框架，它集成了异步激光雷达和相机数据。Fusion-Poly 在同步时间戳将轨迹与多模态观测相关联，在异步时间戳与单模态观测相关联，从而实现了运动和存在状态的高频更新。该框架包含三个关键组件：一个频率感知级联匹配模块，根据可用的检测模态自适应同步和异步帧；一个频率感知轨迹估计模块，通过高频运动预测、差分更新和置信度校准的生命周期管理来维护轨迹；以及一个全状态观测对齐模块，通过优化图像投影误差来提高同步时间戳下的跨模态一致性。在 nuScenes 测试集上，Fusion-Poly 达到了 76.5% 的 AMOTA，在基于检测的跟踪 3D MOT 方法中树立了新的最先进水平。广泛的消融实验进一步验证了各组件的有效性。代码将会开源。

## Abstract
LiDAR-camera 3D multi-object tracking (MOT) combines rich visual semantics with accurate depth cues to improve trajectory consistency and tracking reliability. In practice, however, LiDAR and cameras operate at different sampling rates. To maintain temporal alignment, existing data pipelines usually synchronize heterogeneous sensor streams and annotate them at a reduced shared frequency, forcing most prior methods to perform spatial fusion only at synchronized timestamps through projection-based or learnable cross-sensor association. As a result, abundant asynchronous observations remain underexploited, despite their potential to support more frequent association and more robust trajectory estimation over short temporal intervals.   To address this limitation, we propose Fusion-Poly, a spatial-temporal fusion framework for 3D MOT that integrates asynchronous LiDAR and camera data. Fusion-Poly associates trajectories with multi-modal observations at synchronized timestamps and with single-modal observations at asynchronous timestamps, enabling higher-frequency updates of motion and existence states. The framework contains three key components: a frequency-aware cascade matching module that adapts to synchronized and asynchronous frames according to available detection modalities; a frequency-aware trajectory estimation module that maintains trajectories through high-frequency motion prediction, differential updates, and confidence-calibrated lifecycle management; and a full-state observation alignment module that improves cross-modal consistency at synchronized timestamps by optimizing image-projection errors.   On the nuScenes test set, Fusion-Poly achieves 76.5% AMOTA, establishing a new state of the art among tracking-by-detection 3D MOT methods. Extensive ablation studies further validate the effectiveness of each component. Code will be released.

---

## 论文详细总结（自动生成）

以下是对论文《Fusion-Poly: A Polyhedral Framework Based on Spatial-Temporal Fusion for 3D Multi-Object Tracking》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 3D 多目标跟踪（MOT）中，激光雷达（LiDAR）和相机具有不同的采样频率。现有的主流方法通常为了对齐模态，将数据同步并降频至较低的统一频率（如 nuScenes 的 2Hz 标注频率），这导致大量高频的异步（Asynchronous）观测数据被浪费。
*   **研究动机**：作者认为，引入异步传感器线索可以实现更频繁的关联和融合，从而在更短的时间间隔内获得更稳健的轨迹估计，减少因运动位移过大导致的关联失败。

### 2. 方法论
Fusion-Poly 是一个基于 **Tracking-By-Detection (TBD)** 范式的学习无关（Learning-free）框架，核心思想是构建一个能同时处理同步多模态数据和异步单模态数据的时空融合体系。其关键组件包括：

*   **几何感知对齐模块 (GAAM)**：
    *   利用 2D 检测的高精度边界来约束 3D 框。通过最小化投影 3D 框与 2D 框之间的 IoU 残差，利用非线性最小二乘法（TRF 方法）优化 3D 目标的中心、尺寸和朝向，增强跨模态空间一致性。
*   **频率感知级联匹配 (FACM)**：
    *   **同步帧**：采用三阶段级联匹配（混合检测关联 -> 纯 3D 关联 -> 纯 2D 关联），优先处理高可靠性的多模态融合结果。
    *   **异步帧**：仅利用高频相机 2D 检测进行关联，以维持轨迹在非关键帧的时间连续性。
*   **频率感知轨迹估计 (FATE)**：
    *   **运动预测**：使用卡尔曼滤波（KF），并根据帧间隔动态调整预测步长。
    *   **状态更新**：针对同步和异步帧采用差异化的观测噪声建模（通过缩放因子 $\gamma$ 抑制异步测量的置信度）。
    *   **生命周期管理**：引入基于 **Noisy-OR** 公式的置信度校准策略，融合多模态得分，并根据在线平均得分进行轨迹的初始化与终止。

### 3. 实验设计
*   **数据集**：使用大规模自动驾驶数据集 **nuScenes**（包含 1000 个场景，传感器包括 6 个相机、1 个 LiDAR 等）。
*   **Benchmark**：主要评价指标为 **AMOTA**（平均多目标跟踪准确度），此外还包括 MOTA、IDS（身份切换次数）等。
*   **对比方法**：对比了包括 EagerMOT、Poly-MOT、CAMO-MOT、DINO-MOT、EMMS-MOT 等在内的多种 SOTA（先进）方法。
*   **输入配置**：LiDAR 3D 检测频率为 2Hz，相机 2D 检测频率提升至 4Hz（通过在关键帧间插入中间帧实现）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：由于该方法是**学习无关（Learning-free）**的，它不需要大规模的深度学习训练过程，主要开销在于推理阶段的几何优化和关联算法。作者提到该框架使用 Python 实现，并强调了其在 TBD 范式下的灵活性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 nuScenes **Test 集**上进行了最终性能对比。
    *   在 **Val 集**上进行了详尽的消融实验（共 8 组核心对比，验证了异步数据、FACM、FATE、GAAM 各组件的增益）。
    *   进行了**超参数敏感性分析**（如得分衰减因子、融合权重等）。
    *   设计了**传感器故障模拟实验**（在相机外参中加入高斯噪声），验证系统的鲁棒性。
*   **充分性评价**：实验设计非常充分且客观。通过对比同步与异步设置下的性能差异，清晰地证明了高频异步数据对减少 IDS 和提升轨迹连续性的贡献。

### 6. 主要结论与发现
*   **性能领先**：Fusion-Poly 在 nuScenes 测试集上达到了 **76.5% AMOTA**，是目前基于 TBD 范式的 3D MOT 方法中的最优性能（SOTA）。
*   **异步数据的价值**：直接引入异步数据若无特殊处理会降低性能，但配合频率感知模块（FACM 和 FATE）后，能显著提升跟踪的鲁棒性。
*   **空间对齐的重要性**：GAAM 模块通过 2D-3D 联合优化，有效减少了投影误差，提升了检测输入的质量。

### 7. 优点与亮点
*   **时空融合新视角**：突破了传统方法只在同步时间戳做空间融合的限制，将融合扩展到了异步时间维度。
*   **即插即用**：作为一种 TBD 框架，它可以无缝集成各种先进的 3D 和 2D 检测器。
*   **鲁棒性强**：实验证明其对传感器校准误差（外参噪声）具有较强的容忍度，优于 EagerMOT 等经典方法。
*   **数学严谨性**：对得分融合更新策略进行了方差分析，从理论上证明了其差分更新策略的优越性。

### 8. 不足与局限
*   **检测器依赖**：性能高度依赖于上游 2D/3D 检测器的质量，若检测器在异步帧产生大量假阳性，可能会干扰轨迹维护。
*   **异步数据获取**：在实际部署中，获取高质量的异步 2D 检测可能需要额外的计算资源（如更高频率的视觉推理），论文未深入讨论这种计算负载的权衡。
*   **场景局限**：主要在 nuScenes 数据集上验证，对于极端恶劣天气或传感器完全失效的长时段表现尚待更多验证。

（完）
