---
title: "TrackDeform3D: Markerless and Autonomous 3D Keypoint Tracking and Dataset Collection for Deformable Objects"
title_zh: TrackDeform3D：面向变形物体的无标记自主三维关键点跟踪与数据集采集
authors: "Yeheng Zong, Yizhou Chen, Alexander Bowler, Chia-Tung Yang, Ram Vasudevan"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17068v1"
tags: ["query:vot"]
score: 8.0
evidence: 变形物体的3D关键点跟踪
tldr: 针对变形物体3D表示提取难及大规模数据集匮乏的问题，本文提出TrackDeform3D框架。该框架仅利用RGB-D相机即可实现无标记、自动化的3D关键点识别与轨迹跟踪。通过引入运动一致性约束，确保了数据的时空连贯性。实验证明其在几何和跟踪精度上优于现有方法，并贡献了包含6类物体、总计110分钟的高质量变形物体轨迹数据集。
selection_source: fresh_fetch
motivation: 现有的变形物体3D数据采集依赖昂贵的设备或繁重的人工标注，导致大规模高质量数据集稀缺。
method: 提出一种基于RGB-D相机的自动化框架，通过运动一致性约束实现鲁棒的3D关键点识别与轨迹跟踪。
result: 在多种物体类别上的评估显示，该方法在几何精度和跟踪准确性方面均优于当前最先进的跟踪算法。
conclusion: TrackDeform3D为变形物体提供了一种低成本、高效的3D数据采集方案，并发布了大规模高质量轨迹数据集。
---

## 摘要
结构化三维表示（如关键点和网格）为变形物体提供了紧凑且具有表现力的描述，能够同时捕捉对动力学建模和运动规划等下游任务至关重要的几何与拓扑信息。然而，鲁棒地提取此类表示仍然具有挑战性，因为当前的感知方法难以处理复杂的变形。此外，大规模三维数据采集仍然是一个瓶颈：现有方法要么需要极高的数据采集成本（如劳动密集型标注或昂贵的动作捕捉设备），要么依赖于在非结构化环境中失效的简化假设。因此，针对变形物体的大规模三维数据集和基准测试仍然稀缺。为了应对这些挑战，本文提出了一种仅使用 RGB-D 相机即可采集变形物体三维数据集的低成本自主框架。所提方法能够识别三维关键点并鲁棒地跟踪其轨迹，通过引入运动一致性约束来生成时间平滑且几何相干的数据。TrackDeform3D 在多种物体类别上与几种最先进的跟踪方法进行了对比评估，结果表明其在几何精度和跟踪精度方面均有一致的提升。利用该框架，本文发布了一个包含 6 种变形物体、总计 110 分钟轨迹数据的高质量大规模数据集。

## Abstract
Structured 3D representations such as keypoints and meshes offer compact, expressive descriptions of deformable objects, jointly capturing geometric and topological information useful for downstream tasks such as dynamics modeling and motion planning. However, robustly extracting such representations remains challenging, as current perception methods struggle to handle complex deformations. Moreover, large-scale 3D data collection remains a bottleneck: existing approaches either require prohibitive data collection efforts, such as labor-intensive annotation or expensive motion capture setups, or rely on simplifying assumptions that break down in unstructured environments. As a result, large-scale 3D datasets and benchmarks for deformable objects remain scarce. To address these challenges, this paper presents an affordable and autonomous framework for collecting 3D datasets of deformable objects using only RGB-D cameras. The proposed method identifies 3D keypoints and robustly tracks their trajectories, incorporating motion consistency constraints to produce temporally smooth and geometrically coherent data. TrackDeform3D is evaluated against several state-of-the-art tracking methods across diverse object categories and demonstrates consistent improvements in both geometric and tracking accuracy. Using this framework, this paper presents a high-quality, large-scale dataset consisting of 6 deformable objects, totaling 110 minutes of trajectory data.

---

## 论文详细总结（自动生成）

### TrackDeform3D：面向变形物体的无标记自主三维关键点跟踪与数据集采集

#### 1. 核心问题与研究动机
*   **研究背景**：变形物体（如电线束、织物）在家庭和工厂中无处不在，但由于其具有无限自由度，机器人对其进行操纵和建模极其困难。
*   **核心问题**：现有的三维表示（关键点、网格）提取方法存在瓶颈：
    *   **成本高昂**：动作捕捉系统（MoCap）需要昂贵的设备和繁重的人工后期处理。
    *   **一致性差**：现有的 RGB-D 跟踪方法往往缺乏初始化一致性（不同批次采集的关键点位置不统一）和轨迹一致性（关键点间距随时间剧烈波动）。
    *   **数据匮乏**：缺乏大规模、高质量且具有拓扑一致性的变形物体三维轨迹数据集。
*   **研究动机**：开发一种低成本、自主化且能保证几何一致性的框架，用于大规模采集变形物体的 3D 数据。

#### 2. 方法论
TrackDeform3D 采用统一的约束优化框架来处理关键点的初始化与跟踪：
*   **核心思想**：将关键点提取转化为一个受约束的能量最小化问题，通过锚点（Anchors）固定几何特征，通过边长约束维持拓扑结构。
*   **关键技术流程**：
    1.  **自主分割**：利用点云差分法（移动机械臂前后的点云对比）自动隔离物体，并结合机械臂模型过滤干扰点。
    2.  **物体分类**：基于局部几何特征（协方差矩阵特征值）自动区分 1D（线状）和 2D（面状）物体。
    3.  **初始化**：
        *   **1D 物体**：通过骨架化提取中轴线，识别端点和分叉点作为锚点。
        *   **2D 物体**：沿轮廓分布锚点，利用双线性插值生成内部网格。
    4.  **优化求解**：目标函数最小化相邻关键点间的边长误差，约束条件包括：关键点必须落在观测点云上、锚点位置固定。采用类似 Gauss-Seidel 的迭代投影法求解。
    5.  **鲁棒跟踪**：以每一帧的优化结果作为下一帧的初值（Warm-start），并应用时间平滑滤波器减少抖动。

#### 3. 实验设计
*   **实验场景**：双臂机器人（Franka Panda）操纵变形物体的实验台，配备单个 RGB-D 相机。
*   **数据集/物体**：涵盖 6 类物体，包括绳索（Rope）、分叉绳索（Branched Rope）、织物（Fabric）、电线（Wire）、电线束（Wire Harness）和 T 恤。
*   **对比基准（Baselines）**：
    *   **CoTracker3**：基于视频的多点跟踪方法。
    *   **CDCPD2**：强制执行几何约束的点云变形跟踪方法。
    *   **SpatialTrackerV2**：最新的三维点跟踪方法。
*   **评估指标**：绝对边长误差（RMSE）、倒角距离（Chamfer Distance）、F-score（衡量点云覆盖率）。

#### 4. 资源与算力
*   **硬件设备**：两台 Franka Emika Panda 机械臂，一台 RGB-D 相机（30Hz, 720x1080 分辨率）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、训练时长或推理延迟。由于该方法主要基于几何优化而非深度学习训练（虽然对比了学习型基准），其核心算法可能更侧重于 CPU 上的迭代求解或轻量级 GPU 加速。

#### 5. 实验数量与充分性
*   **实验规模**：采集了总计 **110 分钟** 的轨迹数据，包含 **1320 个运动序列**。
*   **消融实验**：针对锚点约束、边长斥力约束、点云投影约束分别进行了消融，验证了各组件对初始化和跟踪一致性的贡献。
*   **充分性评价**：实验覆盖了从简单线缆到复杂分叉结构及 2D 织物的多种拓扑，对比了视觉和点云领域的先进方法，实验设计较为全面、客观，数据量显著超过了同类现有数据集（如 DOT 和 Mocap）。

#### 6. 主要结论与发现
*   **性能领先**：TrackDeform3D 在所有评估指标上均优于现有基准，尤其是在处理动态操纵和复杂拓扑（如分叉结构）时表现更稳健。
*   **一致性优势**：通过引入锚点和边长约束，该方法能有效防止关键点漂移，确保了跨会话的初始化一致性。
*   **数据集价值**：发布了目前规模最大、种类最全的变形物体 3D 轨迹数据集，填补了分叉变形物体（BDLO）的数据空白。

#### 7. 优点
*   **低成本且自主**：仅需单相机即可运行，无需昂贵的标记系统或人工逐帧标注。
*   **几何感知力强**：通过优化框架显式维护了物体的物理拓扑（如边长不变性），生成的轨迹更适合物理仿真和动力学学习。
*   **通用性**：一套框架同时兼容 1D、分叉 1D 和 2D 物体。

#### 8. 不足与局限
*   **拓扑假设**：对于 2D 物体，目前假设其具有类似矩形的拓扑结构，对于更复杂的非规则形状（如带孔洞的织物）可能需要改进。
*   **遮挡限制**：依赖单相机观测，虽然有一定的鲁棒性，但在严重自遮挡或机械臂遮挡时，跟踪精度可能会下降。
*   **环境要求**：实验在相对受控的机器人工作站进行，在极度杂乱或光照剧烈变化的环境下的表现尚待验证。

（完）
