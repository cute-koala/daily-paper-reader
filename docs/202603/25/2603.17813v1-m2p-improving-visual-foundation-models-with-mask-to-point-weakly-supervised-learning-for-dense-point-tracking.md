---
title: "M2P: Improving Visual Foundation Models with Mask-to-Point Weakly-Supervised Learning for Dense Point Tracking"
title_zh: M2P：通过掩码到点弱监督学习提升视觉基础模型在密集点追踪中的表现
authors: "Qiangqiang Wu, Tianyu Yang, Bo Fang, Jia Wan, Matias Di Martino, Guillermo Sapiro, Antoni B. Chan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17813v1"
tags: ["query:vot"]
score: 9.0
evidence: 稠密点跟踪的最新进展
tldr: 针对视觉基础模型（VFM）在视频密集点追踪（TAP）中因静态图像预训练导致的特征提取不足问题，本文提出Mask-to-Point (M2P) 弱监督学习框架。该框架利用视频对象分割（VOS）掩码，引入局部结构一致性、掩码标签一致性和边界约束，显著提升了模型捕捉时间对应关系的能力。在TAP-Vid-DAVIS基准上，M2P相比DINOv2和DINOv3取得了大幅性能提升，展现了其作为点追踪通用预训练骨干网络的潜力。
selection_source: fresh_fetch
motivation: 现有的视觉基础模型主要基于静态图像预训练，难以有效捕捉视频中密集的时序对应关系。
method: 提出M2P学习框架，通过局部结构一致性、掩码标签一致性及边界约束，利用VOS掩码对VFM进行弱监督微调。
result: "在TAP-Vid-DAVIS基准上，M2P使DINOv2-B/14和DINOv3-B/16的性能分别提升了12.8%和14.6%。"
conclusion: M2P证明了利用掩码信息进行弱监督学习能显著增强基础模型的点追踪能力，可作为该领域的通用预训练方案。
---

## 摘要
任意点追踪（Tracking Any Point, TAP）已成为视频理解的基础工具。当前的方法通过离线微调或测试时优化来适配 DINOv2 等视觉基础模型（VFMs）。然而，这些视觉基础模型依赖于静态图像预训练，这在捕捉视频中的密集时间对应关系方面本质上是次优的。为解决这一问题，我们提出了掩码到点（Mask-to-Point, M2P）学习，利用丰富的视频对象分割（VOS）掩码标注来提升视觉基础模型在密集点追踪中的性能。我们的 M2P 为弱监督表示学习引入了三种新的基于掩码的约束。首先，我们提出了一种局部结构一致性损失，利用普鲁克分析（Procrustes analysis）来建模局部结构内点的内聚运动，从而实现更可靠的点对点匹配学习。其次，我们提出了掩码标签一致性（MLC）损失，强制要求采样的前景点在不同帧之间严格匹配前景区域。所提出的 MLC 损失可被视为一种正则化，能够稳定训练并防止收敛到平凡解。最后，应用掩码边界约束来显式监督边界点。我们证明，仅使用 3.6K 个 VOS 训练视频，我们的弱监督 M2P 模型通过高效训练显著优于基准视觉基础模型。值得注意的是，在 TAP-Vid-DAVIS 基准测试中，M2P 相比 DINOv2-B/14 和 DINOv3-B/16 分别实现了 12.8% 和 14.6% 的性能提升。此外，所提出的 M2P 模型被用作测试时优化和离线微调 TAP 任务的预训练骨干网络，展示了其作为点追踪通用预训练模型的潜力。代码将在论文接收后公开发布。

## Abstract
Tracking Any Point (TAP) has emerged as a fundamental tool for video understanding. Current approaches adapt Vision Foundation Models (VFMs) like DINOv2 via offline finetuning or test-time optimization. However, these VFMs rely on static image pre-training, which is inherently sub-optimal for capturing dense temporal correspondence in videos. To address this, we propose Mask-to-Point (M2P) learning, which leverages rich video object segmentation (VOS) mask annotations to improve VFMs for dense point tracking. Our M2P introduces three new mask-based constraints for weakly-supervised representation learning. First, we propose a local structure consistency loss, which leverages Procrustes analysis to model the cohesive motion of points lying within a local structure, achieving more reliable point-to-point matching learning. Second, we propose a mask label consistency (MLC) loss, which enforces that sampled foreground points strictly match foreground regions across frames. The proposed MLC loss can be regarded as a regularization, which stabilizes training and prevents convergence to trivial solutions. Finally, mask boundary constrain is applied to explicitly supervise boundary points. We show that our weaklysupervised M2P models significantly outperform baseline VFMs with efficient training by using only 3.6K VOS training videos. Notably, M2P achieves 12.8% and 14.6% performance gains over DINOv2-B/14 and DINOv3-B/16 on the TAP-Vid-DAVIS benchmark, respectively. Moreover, the proposed M2P models are used as pre-trained backbones for both test-time optimized and offline fine-tuned TAP tasks, demonstrating its potential to serve as general pre-trained models for point tracking. Code will be made publicly available upon acceptance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **M2P (Mask-to-Point)** 的弱监督学习框架，旨在通过利用视频对象分割（VOS）数据集中的掩码（Mask）标注，提升视觉基础模型（VFM，如 DINOv2/v3）在密集点追踪（TAP）任务中的表现。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的任意点追踪（TAP）方法主要依赖视觉基础模型（VFM）。然而，这些模型（如 DINOv2）大多是在静态图像上预训练的，缺乏捕捉视频中密集时间对应关系（Temporal Correspondence）的能力。
*   **研究动机**：
    *   现有的 TAP 模型通常在合成数据集（如 Kubric）上进行全监督训练，与真实场景存在领域差距。
    *   视频对象分割（VOS）数据集拥有丰富的真实视频掩码标注，但这些“对象级”的信息尚未被有效转化为“点级”的追踪监督信号。
*   **整体含义**：论文试图弥合 VOS 和 TAP 之间的鸿沟，证明通过弱监督学习，对象掩码可以显著增强基础模型的时序匹配特征。

### 2. 方法论：核心思想与关键技术
M2P 的核心思想是**利用掩码约束来引导点特征的学习**，主要包含以下三个关键技术：
*   **查询点采样**：在起始帧掩码区域内，先通过 K-Means 聚类划分空间组，再利用最远点采样（FPS）获取空间分布均匀且具有代表性的查询点。
*   **局部结构一致性（LSC）损失**：
    *   假设局部区域内的点运动具有相干性。
    *   利用**普鲁克分析（Procrustes Analysis）**，根据少数高置信度的匹配点估计出一个相似变换（旋转、平移、缩放），并将其应用于组内其他点，生成伪标签进行监督。
*   **掩码标签一致性（MLC）损失**：
    *   强制要求预测点必须落在目标帧的对应前景掩码内。
    *   这作为一种正则化手段，防止模型在训练中收敛到错误的背景区域或产生平凡解。
*   **掩码边界约束（MBC）**：
    *   显式监督靠近边界的点，要求它们到掩码边界的相对距离在不同帧之间保持一致，从而提升边缘区域的追踪精度。
*   **架构实现**：采用 **LoRA（低秩自适应）** 技术对 DINOv2/v3 的 Transformer 块进行参数高效微调，仅更新极少量参数。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：YouTube-VOS 2019 和 DAVIS-2017 训练集（共约 3.4K 视频）。
    *   **评估基准**：TAP-Vid-DAVIS、TAP-Vid-Kinetics、RGB-Stacking 以及 BADJA。
*   **对比方法**：
    *   **基础模型**：DINOv2 (S/B), DINOv3 (S/B/L), CLIP, SAM, SAM2 等。
    *   **TAP 专用模型**：TAPNet, CoTracker, TAPIR, Chrono, DINO-Tracker 等。
*   **评估模式**：包括查询第一帧（First-query）和跨步查询（Strided-query）模式。

### 4. 资源与算力
*   **算力设备**：使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **训练时长**：预训练过程大约需要 **1 天**。
*   **训练细节**：Batch size 为 16，学习率为 2e-5，迭代次数为 150,000 次。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的对比实验和消融实验。
    *   **对比实验**：覆盖了从零样本（Zero-shot）到全监督（Fully-supervised）的多种主流模型。
    *   **消融实验**：详细分析了 LSC、MLC、MBC 三种损失函数的贡献，以及 LoRA 的秩（Rank）、采样点数量 $K$、训练数据量对结果的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了 M2P 作为独立特征提取器的效果，还将其集成到现有的 TAP 框架（如 Chrono 和 DINO-Tracker）中，证明了其作为通用预训练骨干网络的有效性。

### 6. 主要结论与发现
*   **性能大幅提升**：在 TAP-Vid-DAVIS 基准上，M2P 使 DINOv2-B 提升了 **12.8%**，使 DINOv3-B 提升了 **14.6%**。
*   **弱监督的有效性**：仅使用 3.6K 个真实视频的掩码标注，其效果就能超过在海量合成数据上训练的全监督模型。
*   **特征判别力增强**：可视化结果显示，M2P 产生的相关性图（Correlation Maps）比原始 DINO 更尖锐、更聚焦，能有效抑制语义相似的干扰物。
*   **下游适配性强**：作为预训练模型，它能显著加快测试时优化（Test-time optimization）的收敛速度（减少 75% 的迭代次数）。

### 7. 优点
*   **高效性**：利用 LoRA 进行微调，计算开销小，且不需要昂贵的点级轨迹标注。
*   **创新性**：首次系统性地探索了如何将 VOS 的掩码信息转化为 TAP 的弱监督信号，特别是引入普鲁克分析来建模局部运动一致性。
*   **通用性**：不改变 VFM 的基本结构，易于迁移到不同的基础模型（如从 DINOv2 迁移到 DINOv3）。

### 8. 不足与局限
*   **对极端变形的鲁棒性**：虽然引入了局部结构一致性，但在面对极端的非刚性变形或剧烈遮挡时，基于相似变换的假设可能会失效。
*   **分辨率限制**：受限于基础模型的 Patch Size（如 14 或 16），在处理极细微的坐标定位（如 $\delta_0$ 指标）时，相比小 Patch 模型仍有微弱劣势。
*   **数据潜力尚未挖尽**：目前仅使用了 3.6K 视频，尚未在更大规模的 VOS 数据集（如 SA-V）上进行验证。

（完）
