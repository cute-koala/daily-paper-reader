---
title: "PC-CrossDiff: Point-Cluster Dual-Level Cross-Modal Differential Attention for Unified 3D Referring and Segmentation"
title_zh: PC-CrossDiff：用于统一 3D 指代与分割的点-簇双层跨模态差分注意力
authors: "Wenbin Tan, Jiawen Lin, Fangyong Wang, Yuan Xie, Yong Xie, Yachao Zhang, Yanyun Qu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17753v1"
tags: ["query:vlm"]
score: 10.0
evidence: 3D视觉定位与指代指称理解
tldr: 针对3D视觉定位在复杂多物体场景中因隐式线索解析不足和空间干扰导致的性能下降问题，本文提出PC-CrossDiff框架，统一处理3DREC和3DRES任务。该框架通过点级和聚类级双层跨模态差分注意力机制，自适应提取定位线索并抑制无关干扰，在ScanRefer等主流基准上达到SOTA水平，显著提升了复杂场景下的定位精度。
selection_source: fresh_fetch
motivation: 现有方法在复杂场景中难以有效解析隐式定位线索且易受共存物体干扰，限制了3D视觉定位在现实世界中的应用。
method: 提出包含点级差分注意力（PLDA）和聚类级差分注意力（CLDA）的双层架构，分别用于提取细粒度隐式线索和优化层级空间关系。
result: "在ScanRefer、NR3D和SR3D上均获SOTA，其中ScanRefer隐式子集的Overall@0.50指标大幅提升了10.16%。"
conclusion: PC-CrossDiff通过双层差分注意力机制增强了模型对隐式空间线索的解析能力，为解决复杂场景下的3D视觉定位问题提供了有效方案。
---

## 摘要
3D 视觉定位 (3DVG) 旨在通过两个核心任务定位自然语言指代词的指代对象：3D 指代理解 (3DREC) 和分割 (3DRES)。虽然现有方法在简单的单物体场景中取得了较高的准确率，但在现实世界中常见的复杂多物体场景中，其性能会严重下降，阻碍了实际部署。现有方法在复杂多物体场景中面临两个关键挑战：对消除视觉相似物体歧义至关重要的隐式定位线索解析不足，以及对共存物体的动态空间干扰抑制无效，导致定位准确率下降。为了应对这些挑战，我们提出了 PC-CrossDiff，这是一个具有双层跨模态差分注意力架构的统一双任务框架，用于 3DREC 和 3DRES。具体而言，该框架引入了：(i) 点级差分注意力 (PLDA) 模块，在文本和点云之间应用双向差分注意力，通过可学习权重自适应地提取隐式定位线索，以提高判别性表示；(ii) 簇级差分注意力 (CLDA) 模块，建立了一种分层注意力机制，通过定位感知差分注意力块自适应地增强与定位相关的空间关系，同时抑制模糊或无关的空间关系。我们的方法在 ScanRefer、NR3D 和 SR3D 基准测试中达到了最先进的性能。值得注意的是，在 ScanRefer 的 Implicit 子集上，它将 3DREC 任务的 Overall@0.50 分数提高了 +10.16%，突显了其解析隐式空间线索的强大能力。

## Abstract
3D Visual Grounding (3DVG) aims to localize the referent of natural language referring expressions through two core tasks: Referring Expression Comprehension (3DREC) and Segmentation (3DRES). While existing methods achieve high accuracy in simple, single-object scenes, they suffer from severe performance degradation in complex, multi-object scenes that are common in real-world settings, hindering practical deployment. Existing methods face two key challenges in complex, multi-object scenes: inadequate parsing of implicit localization cues critical for disambiguating visually similar objects, and ineffective suppression of dynamic spatial interference from co-occurring objects, resulting in degraded grounding accuracy. To address these challenges, we propose PC-CrossDiff, a unified dual-task framework with a dual-level cross-modal differential attention architecture for 3DREC and 3DRES. Specifically, the framework introduces: (i) Point-Level Differential Attention (PLDA) modules that apply bidirectional differential attention between text and point clouds, adaptively extracting implicit localization cues via learnable weights to improve discriminative representation; (ii) Cluster-Level Differential Attention (CLDA) modules that establish a hierarchical attention mechanism to adaptively enhance localization-relevant spatial relationships while suppressing ambiguous or irrelevant spatial relations through a localization-aware differential attention block. Our method achieves state-of-the-art performance on the ScanRefer, NR3D, and SR3D benchmarks. Notably, on the Implicit subsets of ScanRefer, it improves the Overall@0.50 score by +10.16% for the 3DREC task, highlighting its strong ability to parse implicit spatial cues.

---

## 论文详细总结（自动生成）

### 论文总结：PC-CrossDiff —— 用于统一 3D 指代与分割的点-簇双层跨模态差分注意力

#### 1. 核心问题与整体含义（研究动机和背景）
3D 视觉定位（3DVG）包含指代理解（3DREC，实例级定位）和指代分割（3DRES，点云分割）两个任务。现有方法在简单、单物体场景中表现良好，但在**复杂多物体场景**中性能大幅下降。主要瓶颈在于：
*   **隐式定位线索解析不足**：难以处理如“在笔记本电脑旁边”这类非显式的空间描述。
*   **动态空间干扰**：多物体共存产生的模糊空间关系会引入噪声，现有方法缺乏有效的干扰抑制机制。
*   **多任务冲突**：在联合训练 3DREC 和 3DRES 时，存在尺度差异和梯度冲突。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PC-CrossDiff**，一个统一的双任务框架，核心在于**双层跨模态差分注意力机制**：
*   **点级差分注意力 (PLDA)**：
    *   在文本和点云之间应用双向差分注意力。
    *   利用可学习的差分系数 $\lambda$ 自动过滤无关的视觉/文本特征，从而精准提取隐藏在完整句子中的隐式定位线索。
*   **簇级差分注意力 (CLDA)**：
    *   **空间关系建模 (SRM)**：通过采样和聚类捕获全局位置上下文。
    *   **定位感知差分注意力 (LDA)**：采用“过滤-增强”策略，利用差分注意力选择性地增强与目标相关的空间特征，同时抑制来自无关物体的干扰。
*   **双几何任务协调损失 ($L_{DGTL}$)**：
    *   引入双几何一致性约束（框与掩码的互导损失）。
    *   通过动态权重平衡和梯度对齐惩罚项，减少多任务间的“串扰”，实现参数的高效共享。

#### 3. 实验设计
*   **数据集与场景**：在三大主流基准 **ScanRefer**、**NR3D** 和 **SR3D** 上进行评估。
*   **细分测试**：特别关注 **Multiple**（多物体）和 **Implicit**（隐式描述）子集。论文还利用 LLM（Qwen-turbo）辅助构建了一个包含 501 条隐式描述的验证子集。
*   **对比方法**：包括多任务基准 **MCLN**，以及单任务 SOTA 方法如 **X-RefSeg3D**、**3D-STMN**、**RefMask3D**、**G3-LQ**、**VPP-Net** 等。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA 3090 GPU** 上运行。
*   **软件/参数**：使用 AdamW 优化器；视觉编码器学习率为 $2 \times 10^{-3}$，其余层为 $2 \times 10^{-4}$。
*   **效率**：推理速度达到 **4.07 FPS**，在保证精度的同时兼顾了实时性。
*   **未明确说明**：文中未明确给出总训练时长或使用的 GPU 总数量。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3DREC 和 3DRES 两个任务在三个数据集上的全面对比。
*   **消融实验**：非常详尽，分别验证了 $L_{DGTL}$、PLDA、CLDA 的有效性，并探讨了聚类大小（$N_{clust}$）对性能的影响，以及差分注意力相对于标准 Transformer 注意力的优越性。
*   **客观性**：通过在 Multiple 和 Implicit 子集上的针对性测试，客观证明了模型在处理复杂干扰和隐式语义方面的鲁棒性。

#### 6. 主要结论与发现
*   **性能领先**：在所有基准上均达到 SOTA。在 ScanRefer 的 3DRES 任务中，Overall@0.50 达到 52.52%。
*   **隐式线索突破**：在 ScanRefer 的 Implicit 子集上，3DREC 的 Overall@0.50 提升了 **10.16%**，证明了差分注意力在语义解耦方面的极强能力。
*   **多物体鲁棒性**：在 Multiple 子集上的表现显著优于 MCLN，验证了 CLDA 抑制空间干扰的有效性。

#### 7. 优点（亮点）
*   **双层架构设计**：将细粒度的点级特征与宏观的簇级空间关系结合，逻辑清晰且互补。
*   **差分注意力应用**：首次将差分注意力引入 3D 视觉定位，有效解决了噪声抑制问题。
*   **统一框架**：通过精心设计的损失函数成功平衡了定位与分割两个互补任务，提升了整体效率。
*   **轻量化**：PLDA 模块仅增加 2.10M 参数，对推理速度影响极小。

#### 8. 不足与局限
*   **性能差距依然存在**：尽管在多物体场景下有显著提升，但其准确率（约 43-47%）仍远低于单物体场景（约 70%+），说明复杂场景下的 3DVG 仍有巨大挑战。
*   **聚类依赖性**：CLDA 依赖于 FPS 采样和 KNN 聚类，在极度稀疏或极度拥挤的点云中，聚类的质量可能会影响空间建模的稳定性。
*   **应用限制**：目前主要针对室内场景（ScanNet 衍生数据集），在室外大规模场景（如自动驾驶）中的泛化能力尚未验证。

（完）
