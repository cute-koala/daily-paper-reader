---
title: "CoVFT: Context-aware Visual Fine-tuning for Multimodal Large Language Models"
title_zh: CoVFT：面向多模态大语言模型的上下文感知视觉微调
authors: "Nan Zhou, Huiqun Wang, Yaoyan Zheng, Di Huang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21077v1"
tags: ["query:vlm"]
score: 9.0
evidence: 多模态大模型的视觉微调方法
tldr: 本研究针对多模态大模型中视觉编码器微调不稳定的问题，指出其根源在于忽略上下文导致的视觉偏好冲突。为此提出了CoVFT框架，通过引入上下文向量提取（CVE）和上下文专家混合（CoMoE）模块，将多模态上下文显式融入视觉适配过程。实验证明，CoVFT在12个基准测试中表现优异且稳定性高，使7B模型性能超越了13B模型，充分挖掘了视觉编码器的优化潜力。
selection_source: fresh_fetch
motivation: 现有视觉微调方法因缺乏上下文感知导致在不同多模态任务下产生参数更新冲突，性能表现不稳定。
method: 提出CoVFT框架，通过CVE模块提取上下文特征并结合CoMoE模块，实现针对不同上下文的动态视觉参数微调。
result: 在12个多模态基准测试中达到SOTA水平，且微调后的7B模型平均性能超过了参数量更大的13B模型。
conclusion: 显式引入上下文感知机制能有效解决视觉微调中的优化冲突，是提升多模态大模型视觉感知能力的关键。
---

## 摘要
多模态大语言模型（MLLMs）在跨模态感知和推理方面取得了显著进展，但一个基本问题仍未解决：视觉编码器应该被微调还是冻结？尽管 LLaVA 和 Qwen-VL 等模型取得了成功，但设计选择的不一致和训练设置的异构性阻碍了对 MLLM 中视觉微调（VFT）的统一理解。通过配置对齐的基准测试，我们发现现有的 VFT 方法在各种多模态任务中无法始终优于冻结基线。我们的分析表明，这种不稳定性源于视觉偏好冲突，即视觉编码器的上下文无关特性在多样化的多模态上下文下引发了发散的参数更新。为了解决这个问题，我们提出了上下文感知视觉微调（CoVFT）框架，该框架将多模态上下文显式地融入视觉适配中。通过集成上下文向量提取（CVE）和上下文混合专家（CoMoE）模块，CoVFT 分解了冲突的优化信号，并实现了稳定且上下文敏感的视觉更新。在 12 个多模态基准测试上的广泛实验表明，CoVFT 以卓越的稳定性实现了最先进的性能。值得注意的是，使用 CoVFT 微调的 7B MLLM 超过了其 13B 对应模型的平均性能，揭示了 MLLM 中视觉编码器优化方面巨大的未开发潜力。

## Abstract
Multimodal large language models (MLLMs) achieve remarkable progress in cross-modal perception and reasoning, yet a fundamental question remains unresolved: should the vision encoder be fine-tuned or frozen? Despite the success of models such as LLaVA and Qwen-VL, inconsistent design choices and heterogeneous training setups hinder a unified understanding of visual fine-tuning (VFT) in MLLMs. Through a configuration-aligned benchmark, we find that existing VFT methods fail to consistently outperform the frozen baseline across multimodal tasks. Our analysis suggests that this instability arises from visual preference conflicts, where the context-agnostic nature of vision encoders induces divergent parameter updates under diverse multimodal context. To address this issue, we propose the Context-aware Visual Fine-tuning (CoVFT) framework, which explicitly incorporates multimodal context into visual adaptation. By integrating a Context Vector Extraction (CVE) and a Contextual Mixture-of-Experts (CoMoE) module, CoVFT decomposes conflicting optimization signals and enables stable, context-sensitive visual updates. Extensive experiments on 12 multimodal benchmarks demonstrate that CoVFT achieves state-of-the-art performance with superior stability. Notably, fine-tuning a 7B MLLM with CoVFT surpasses the average performance of its 13B counterpart, revealing substantial untapped potential in visual encoder optimization within MLLMs.

---

## 论文详细总结（自动生成）

以下是对论文《CoVFT: Context-aware Visual Fine-tuning for Multimodal Large Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了多模态大语言模型（MLLM）中一个长期存在争议的基础问题：**视觉编码器（Vision Encoder）在指令微调阶段应该被冻结还是微调？**
*   **研究动机**：作者发现现有的视觉微调（VFT）方法（如全量微调、LoRA等）在多模态任务中表现极不稳定，往往无法稳胜“冻结视觉编码器”的基线。
*   **核心发现**：这种不稳定性源于**“视觉偏好冲突（Visual Preference Conflicts）”**。由于视觉编码器是上下文无关的，当面对不同的指令（如定位物体 vs. 描述全局）时，不同任务产生的优化信号会拉扯参数向冲突的方向更新，导致泛化性能下降。

### 2. 论文提出的方法论：CoVFT 框架
为了解决上述冲突，论文提出了**上下文感知视觉微调（CoVFT）**框架，核心思想是将多模态上下文显式引入视觉适配过程。
*   **上下文向量提取（CVE）**：利用冻结的文本编码器（如 BERT）提取指令特征，通过交叉注意力机制（Cross-Attention）在视觉编码器内部逐层聚合多模态信息，生成一个紧凑的上下文向量 $c$。
*   **上下文混合专家（CoMoE）**：将视觉编码器深层的前馈网络（FFN）替换为 MoE 结构。
    *   **关键技术**：路由器的权重由上下文向量 $c$ 动态决定。
    *   **作用**：通过上下文感知的路由，将冲突的优化信号分解到不同的专家子空间中。相似上下文的任务共享专家，冲突任务则通过不同专家进行参数更新，从而实现稳定的适配。

### 3. 实验设计
*   **数据集与基准测试**：在 **12 个多模态基准测试**上进行了评估，涵盖三个领域：
    1.  **通用问答**：MME-Perception, MMBench (EN/CN), GQA。
    2.  **知识与 OCR**：ScienceQA-Image, AI2D, TextVQA。
    3.  **视觉中心任务**：MMVP, RealWorldQA, CV-Bench (COCO, ADE, Omni3D)。
*   **对比方法**：
    *   **基线**：Freeze（冻结视觉编码器）。
    *   **微调方法**：Full Fine-tuning（全量微调）、LoRA、BitFit。
    *   **PEFT 方法**：VPT、SVPT。
*   **模型架构**：主要基于 LLaVA-1.5 范式（Vicuna-7B/13B + CLIP-ViT-L/14），并验证了 SigLIP、DINOv3 和 InternVL 2.0 的通用性。

### 4. 资源与算力
*   **算力设备**：实验使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：
    *   预训练阶段（558K 数据）：约 2 小时 38 分钟。
    *   指令微调阶段（665K 数据）：CoVFT 耗时约 5 小时 32 分钟（比全量微调多约 18 分钟）。
*   **显存占用**：CoVFT 的峰值显存比全量微调高约 13.5%（约 49.9GB）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了非常充分的实验。除了 12 个主流榜单的对比外，还包括：
    *   **消融实验**：针对 CVE 的设计（仅图像、仅文本、拼接等）和 CoMoE 的路由策略（随机、均匀、稀疏、稠密）进行了详细对比。
    *   **数据可扩展性**：测试了从 12.5% 到 100% 不同训练数据量下的表现。
    *   **架构泛化性**：在不同视觉编码器和不同 MLLM 框架（如 InternVL）上验证了有效性。
*   **客观性**：所有对比均在统一的 LLaVA 训练配置下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能卓越**：CoVFT 在 12 个基准测试中均优于冻结基线，平均提升 2.15%。
*   **以小博大**：**微调后的 7B 模型平均性能超过了冻结视觉编码器的 13B 模型**，证明了视觉编码器优化的巨大潜力（尽管其参数量占比不足 5%）。
*   **稳定性**：通过可视化梯度相似度和上下文向量聚类，证实了 CoVFT 能有效缓解梯度冲突，使优化过程更稳定。

### 7. 优点
*   **理论深刻**：首次明确提出了“视觉偏好冲突”这一概念，解释了为何简单微调视觉编码器在 MLLM 中效果不佳。
*   **设计精巧**：CVE 和 CoMoE 的结合既实现了上下文感知，又通过 MoE 结构巧妙地化解了参数更新冲突。
*   **高效性**：在仅增加极少训练开销（约 4% 时间）的情况下，获得了显著的性能增益。

### 8. 不足与局限
*   **计算开销**：虽然增加不多，但 MoE 结构确实带来了额外的显存压力和推理延迟（每样本增加约 10ms）。
*   **模型规模限制**：实验主要集中在 7B 和 13B 规模，尚未在超大规模模型（如 70B+）或更大规模的数据集（如数十亿级）上验证其上限。
*   **专家数量**：对于不同任务多样性的最优专家数量仍需经验性调整，缺乏自动化的专家分配机制。

（完）
