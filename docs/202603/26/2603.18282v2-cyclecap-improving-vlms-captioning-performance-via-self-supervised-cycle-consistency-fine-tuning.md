---
title: "CycleCap: Improving VLMs Captioning Performance via Self-Supervised Cycle Consistency Fine-Tuning"
title_zh: CycleCap：通过自监督循环一致性微调提升视觉语言模型（VLMs）的图像描述性能
authors: "Marios Krestenitis, Christos Tzelepis, Konstantinos Ioannidis, Stefanos Vrochidis, Ioannis Kompatsiaris, Georgios Tzimiropoulos, Shaogang Gong, Ioannis Patras"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18282v2"
tags: ["query:vlm"]
score: 9.0
evidence: 通过循环一致性改进VLM图文对齐
tldr: 针对视觉语言模型（VLM）在图像描述中存在的对齐偏差和幻觉问题，本文提出 CycleCap 微调方案。该方法利用循环一致性原理，通过预训练的文本生成图像模型将 VLM 生成的描述还原为图像，并以原始图像与重构图像的相似度作为奖励信号，采用 GRPO 算法进行自监督微调。CycleCap 无需人工标注数据，仅凭原始图像即可显著提升模型描述的准确性并减少幻觉。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在生成图像描述时容易产生幻觉或过于笼统，且微调过程通常依赖昂贵的人工标注数据集。
method: 提出一种自监督微调框架，通过将生成的文本重新映射回图像空间，并利用原始图像与重构图像的相似度作为 GRPO 算法的奖励信号。
result: 在多个 1B 至 7B 参数规模的模型上，CycleCap 在图像描述准确性和幻觉抑制方面均优于现有的监督学习方法。
conclusion: 该研究证明了循环一致性可以作为一种强大的自监督信号，在无需标注数据的情况下有效提升 VLM 的视觉对齐能力。
---

## 摘要
视觉语言模型（VLMs）在图像描述、视觉问答和视觉推理方面取得了显著进展。然而，它们仍容易出现视觉-语言失配问题，经常生成过于笼统或存在幻觉的描述。现有方法通常通过指令微调（需要昂贵的大规模标注数据集）或复杂的测试时框架（用于描述细化）来解决这一问题。在本研究中，我们从循环一致性（cycle consistency）的角度重新审视图像-文本对齐：给定一张图像和由图像转文本模型生成的描述，通过文本转图像模型的反向映射应能重建出一张与原图高度匹配的图像。在我们的设置中，VLM 作为图像转文本组件，而预训练的文本转图像模型通过根据生成的描述重建图像来闭合循环。基于此，我们提出了 CycleCap，这是一种利用群体相对策略优化（GRPO）来改进图像描述的微调方案，其奖励基于实时计算的原始图像与重建图像之间的相似度。与以往使用循环一致性损失构建偏好数据集的工作不同，我们的方法直接将循环一致性作为自监督训练信号。这使得仅使用原始图像成为可能，消除了对精选图像-文本数据集的需求，同时引导 VLM 生成更准确且有据可依（grounded）的文本描述。将 CycleCap 应用于参数量从 1B 到 7B 的四种 VLM，在图像描述和幻觉基准测试中均取得了持续的改进，超越了依赖监督循环一致性训练的最先进方法。

## Abstract
Visual-Language Models (VLMs) have achieved remarkable progress in image captioning, visual question answering, and visual reasoning. Yet they remain prone to vision-language misalignment, often producing overly generic or hallucinated descriptions. Existing approaches address this via instruction tuning-requiring costly, large-scale annotated datasets or via complex test-time frameworks for caption refinement. In this work, we revisit image-text alignment through the lens of cycle consistency: given an image and a caption generated by an image-to-text model, the backward mapping through a text-to-image model should reconstruct an image that closely matches the original. In our setup, a VLM serves as the image-to-text component, while a pre-trained text-to-image model closes the loop by reconstructing the image from the generated caption. Building on this, we introduce CycleCap, a fine-tuning scheme to improve image captioning using Group Relative Policy Optimization (GRPO) with a reward based on the similarity between the original and reconstructed images, computed on-the-fly. Unlike previous work that uses cycle consistency loss for preference dataset construction, our method leverages cycle consistency directly as a self-supervised training signal. This enables the use of raw images alone, eliminating the need for curated image-text datasets, while steering the VLM to produce more accurate and grounded text descriptions. Applied to four VLMs ranging from 1B to 7B parameters, CycleCap yields consistent improvements across captioning and hallucination benchmarks, surpassing state-of-the-art methods that rely on supervised cycle consistency training.

---

## 论文详细总结（自动生成）

以下是对论文《CycleCap: Improving VLMs Captioning Performance via Self-Supervised Cycle Consistency Fine-Tuning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉语言模型（VLMs）在生成图像描述时，经常出现**视觉-语言失配**（Misalignment）问题，表现为描述过于笼统或产生“幻觉”（生成图中不存在的内容）。
*   **研究背景**：现有的改进方案通常依赖大规模的人工标注数据集（成本高昂）或复杂的推理时细化框架（计算开销大）。
*   **核心动机**：利用“循环一致性”（Cycle Consistency）原理，即：如果一段描述是准确的，那么根据这段描述重新生成的图像应该与原图高度相似。作者旨在通过这种自监督的方式，无需人工标注即可提升 VLM 的对齐能力。

### 2. 方法论
*   **核心思想**：构建一个“图像 $\rightarrow$ 文本 $\rightarrow$ 图像”的闭环。VLM 生成描述，预训练的文本转图像（T2I）模型根据描述重构图像，通过比较原图与重构图的相似度来评价描述质量。
*   **关键技术细节**：
    *   **循环一致性奖励（Cycle Consistency Reward）**：使用 **DreamSim** 指标衡量原图 $x$ 与重构图 $G(F(x))$ 之间的感知和语义相似度。
    *   **GRPO 优化算法**：采用群体相对策略优化（Group Relative Policy Optimization）。对于每张图，模型生成一组（$n=8$）候选描述，计算组内相对奖励（优势函数），从而引导模型向高相似度描述的方向进化。
    *   **自监督训练**：训练过程中，T2I 模型（如 Stable Diffusion 3 或 FLUX.1）保持冻结，仅通过 LoRA 策略微调 VLM 的参数。
*   **流程**：输入图像 $\rightarrow$ VLM 生成多个描述 $\rightarrow$ T2I 重构多张图 $\rightarrow$ 计算相似度奖励 $\rightarrow$ GRPO 算法更新 VLM。

### 3. 实验设计
*   **数据集**：使用 **COCO 2014 训练集**（约 8.3 万张图像）作为原始图像来源，**完全不使用其自带的标签**。
*   **评估基准（Benchmarks）**：
    *   **CompreCap**：评估描述的完整性、属性准确性和关系。
    *   **CAPability**：从对象类别、颜色、空间关系等 9 个维度进行综合评估。
    *   **CapsBench**：通过 LLM 回答基于描述的“是/否”问题来测试视觉扎根能力。
    *   **MMHal**：专门评估模型产生幻觉的程度。
*   **对比方法**：
    *   各模型的 Baseline（原始版本）。
    *   **CyclePref**：使用 DPO 算法和预构建的循环一致性偏好数据集的方法。
    *   **RICO-Flash**：利用 GPT-4o 进行迭代细化并构建偏好数据集的方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA A100 GPU**。
*   **训练时长**：总训练时间根据模型大小不同，范围在 **270（1B 模型）到 430（7B 模型）GPU 小时**之间。
*   **参数设置**：采用 LoRA 微调（Rank=64），学习率为 $10^{-5}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种不同规模的主流 VLM（InternVL3-1B, Qwen2-VL-2B, Qwen2.5-VL-3B, Qwen2-VL-7B）。
    *   在 4 个权威的图像描述和幻觉基准上进行了测试。
*   **消融实验**：
    *   对比了不同的相似度度量指标（LPIPS, CLIP, DreamSim）。
    *   对比了不同的后端 T2I 模型（SD3, FLUX.1-schnell）。
    *   分析了 GRPO 生成数量 $n$ 对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型规模、跨评估维度以及与最先进（SOTA）的有监督/偏好学习方法对比，有力证明了自监督循环一致性的有效性。

### 6. 主要结论与发现
*   **性能提升**：CycleCap 在所有测试模型和基准上均实现了持续的性能增长，尤其在描述的详细程度和准确性上表现突出。
*   **超越有监督方法**：尽管 CycleCap 是完全自监督的，但其表现优于依赖昂贵偏好数据集（如 CyclePref）或外部大模型反馈（如 RICO-Flash）的方法。
*   **抑制幻觉**：在 MMHal 基准上的得分提升表明，循环一致性约束能有效减少模型凭空捏造内容的倾向。
*   **可扩展性**：实验证明，使用更强大的 T2I 模型或更先进的感知度量指标，可以进一步提升微调效果。

### 7. 优点
*   **零标注依赖**：完全利用原始图像进行自监督学习，极大地降低了数据获取成本。
*   **训练效率高**：相比于构建数十万条偏好对的 DPO 方法，CycleCap 通过 GRPO 在线生成奖励，流程更简洁。
*   **通用性强**：该框架不依赖特定的 VLM 架构，可广泛应用于各种规模的多模态模型。
*   **即插即用**：可以作为现有模型（如 RICO-Flash）的二次增强手段。

### 8. 不足与局限
*   **对 T2I 模型的依赖**：微调效果受限于后端 T2I 模型的生成能力。如果 T2I 模型无法准确还原某些复杂细节，可能会给 VLM 提供错误的奖励信号。
*   **图像空间局限性**：目前的奖励完全基于图像空间的相似度，可能无法捕捉到文本中某些抽象的语义逻辑（虽然 DreamSim 缓解了这一点）。
*   **计算开销**：虽然不需要标注，但训练过程中需要频繁调用 T2I 模型生成图像，这在训练阶段会产生一定的计算负担。

（完）
