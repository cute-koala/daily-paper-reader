---
title: "FineViT: Progressively Unlocking Fine-Grained Perception with Dense Recaptions"
title_zh: FineViT：通过密集重描述逐步解锁细粒度感知
authors: "Peisen Zhao, Xiaopeng Zhang, Mingxing Xu, Ruoyu Sun, Zewei Du, Dunzheng Wang, Guanghao Zheng, Haohang Xu, Zhibo Zhang, Yuhang Zhang, Yi Ai, Lin Liu, Qi Tian"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17326v1"
tags: ["query:vlm"]
score: 9.0
evidence: 使用密集重描述提升多模态大模型视觉编码器的细粒度感知
tldr: FineViT 针对多模态大模型中视觉编码器对细粒度感知不足的问题，提出了一种渐进式训练范式。通过使用高分辨率图像和密集的重标注数据，FineViT 首先在大规模全局重标注数据上建立语义基础，随后利用包含 4.5 亿条高质量局部描述的 FineCap-450M 数据集增强局部感知能力。该模型在零样本识别、长上下文检索及 MLLM 任务中均表现优异，为细粒度视觉感知提供了强力基准。
selection_source: fresh_fetch
motivation: 传统的 CLIP 视觉编码器受限于低分辨率预训练和噪声严重的粗糙图文对，难以处理复杂的空间细节任务。
method: 采用渐进式训练策略，先在高分辨率下利用全局重标注数据进行预训练，再通过 FineCap-450M 局部描述数据集进行 LLM 对齐以增强局部感知。
result: FineViT 在零样本识别和检索任务中达到 SOTA 水平，且在集成到 MLLM 后性能显著超越了 SigLIP2 和 Qwen-ViT。
conclusion: FineViT 证明了高质量密集重标注数据和渐进式训练对提升视觉编码器细粒度感知能力的有效性。
---

## 摘要
尽管多模态大语言模型（MLLMs）取得了飞速进展，但其视觉编码器往往仍是性能瓶颈。传统的基于 CLIP 的编码器在处理密集空间任务时表现不佳，这是由于低分辨率预训练导致的视觉细节丢失，以及对噪声大、粗粒度的网络爬取图文对的依赖。为了克服这些局限性，我们推出了 FineViT，这是一种专门为解锁细粒度感知而设计的新型视觉编码器。通过使用密集重描述替代粗糙的网络数据，我们通过一种渐进式训练范式系统地减轻了信息损失：首先，该编码器在数十亿个全局重描述图文对上以高原生分辨率从头开始训练，从而建立起稳健且细节丰富的语义基础。随后，我们利用精心策划的 FineCap-450M 数据集（包含超过 4.5 亿条高质量局部描述），通过 LLM 对齐进一步增强其局部感知能力。广泛的实验验证了该渐进式策略的有效性。FineViT 实现了最先进的零样本识别和检索性能，特别是在长上下文检索方面；当集成到 MLLMs 中时，其表现始终优于 SigLIP2 和 Qwen-ViT 等多模态视觉编码器。我们希望 FineViT 能为细粒度视觉感知提供一个强大的新基准。

## Abstract
While Multimodal Large Language Models (MLLMs) have experienced rapid advancements, their visual encoders frequently remain a performance bottleneck. Conventional CLIP-based encoders struggle with dense spatial tasks due to the loss of visual details caused by low-resolution pretraining and the reliance on noisy, coarse web-crawled image-text pairs. To overcome these limitations, we introduce FineViT, a novel vision encoder specifically designed to unlock fine-grained perception. By replacing coarse web data with dense recaptions, we systematically mitigate information loss through a progressive training paradigm.: first, the encoder is trained from scratch at a high native resolution on billions of global recaptioned image-text pairs, establishing a robust, detail rich semantic foundation. Subsequently, we further enhance its local perception through LLM alignment, utilizing our curated FineCap-450M dataset that comprises over $450$ million high quality local captions. Extensive experiments validate the effectiveness of the progressive strategy. FineViT achieves state-of-the-art zero-shot recognition and retrieval performance, especially in long-context retrieval, and consistently outperforms multimodal visual encoders such as SigLIP2 and Qwen-ViT when integrated into MLLMs. We hope FineViT could serve as a powerful new baseline for fine-grained visual perception.

---

## 论文详细总结（自动生成）

### FineViT：通过密集重描述逐步解锁细粒度感知 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）的性能往往受限于其视觉编码器。传统的视觉编码器（如基于 CLIP 的模型）存在三大瓶颈：
*   **信息丢失**：低分辨率预训练（如 224px 或 336px）导致 OCR 和小物体识别所需的细节丢失。
*   **数据质量差**：依赖于噪声大、描述简略的网络爬取图文对（如 LAION-400M），缺乏密集的语义信息。
*   **模态对齐鸿沟**：对比学习（全局对齐）与 LLM 的自回归目标（局部/序列对齐）之间存在训练目标的差异。
**FineViT** 的研究动机在于通过**渐进式训练范式**和**大规模密集重描述（Dense Recaptions）数据**，从头构建一个具备强细粒度感知能力的视觉基础模型。

#### 2. 核心方法论
FineViT 采用了一种三阶段的渐进式训练策略，旨在从几何感知过渡到语义理解，最后实现细粒度对齐：

*   **核心思想**：利用高质量的 AI 生成重描述（Recaption）替代原始网络文本，并在训练过程中逐步提升分辨率和任务复杂度。
*   **关键技术细节**：
    *   **架构设计**：基于 ViT 架构（约 8.6 亿参数），引入 **2D RoPE（旋转位置嵌入）** 以支持原生分辨率处理，Patch Size 为 14x14。
    *   **Stage I：MIM 初始化**。使用掩码图像建模（Masked Image Modeling）在 18 亿无标签图像上预训练，通过重建 DINOv3 的特征空间来建立初步的空间几何推理能力。
    *   **Stage II：大规模对比学习**。在 15.6 亿个**全局重描述**图文对上进行训练，采用 SigLIP 损失函数。此阶段保持 448x448 的原生分辨率，并将文本上下文长度从 64 扩展到 256 词元。
    *   **Stage III：LLM 对齐与多粒度感知**。引入 **FineCap-450M 数据集**，在 1K 分辨率下进行自回归训练。任务涵盖全局描述、区域描述（bbox-to-string）、定位（string-to-bbox）及 OCR 任务。
*   **数据工程**：构建了 **FineCap-450M**，包含 4.5 亿个高质量局部标注，涵盖自然场景、富文本图像和文档。

#### 3. 实验设计
*   **数据集与场景**：
    *   **判别式任务**：ImageNet-1k/v2/R（分类）；COCO、Flickr30k、DCI、IIW、Urban-1k（检索，特别是长文本检索）。
    *   **生成式任务（MLLM）**：涵盖通用 VQA（MMBench）、推理（MMMU、MathVista）、OCR 与图表（DocVQA、ChartQA）、定位与计数（RefCOCO、CountBenchQA）。
*   **Benchmark 与对比方法**：
    *   对比了视觉编码器基准：**SigLIP、SigLIP2、Seed-ViT**。
    *   对比了集成后的 MLLM 基准：**Qwen3-VL、Intern3.5-VL、Aquila-VL**。

#### 4. 资源与算力
*   **算力说明**：论文提到了训练细节，包括使用 **DeepSpeed ZeRO-2** 优化器、**BF16** 精度、AdamW 优化器以及余弦学习率调度。
*   **具体规模**：Stage I 批大小 4096；Stage II 批大小 49152（处理了 93 亿样本）；Stage III 批大小 4096。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）、GPU 数量以及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：实验设计非常全面，涵盖了从基础视觉指标（分类、检索）到高级多模态推理指标的数十个 Benchmark。
*   **消融实验**：
    *   验证了三个训练阶段（MIM -> CL -> Align）的各自贡献，证明 Stage III 对局部感知有显著提升。
    *   对比了视觉主干网络“冻结”与“非冻结”对性能的影响。
    *   测试了 FineViT 在不同规模 LLM（1.7B 和 8B）下的泛化能力。
*   **客观性**：使用了标准的 VLMEvalKit 框架进行评估，确保了与现有 SOTA 模型对比的公平性。

#### 6. 主要结论与发现
*   **细粒度优势**：FineViT 在 OCR、文档理解和物体定位任务上显著优于 SigLIP2。例如，在 DocVQA 上比 SigLIP2 高出 18.52%。
*   **长文本检索能力**：得益于密集重描述数据，FineViT 在处理复杂长描述的图像检索任务（如 DCI 数据集）中表现卓越，远超专门优化的 FixCLIP。
*   **渐进式训练的必要性**：单纯的对比学习会抑制实例级的空间敏感性，而第三阶段的局部对齐训练是解锁细粒度感知的关键。
*   **MLLM 适配性**：作为视觉插件，FineViT 在多个 LLM 后端上均表现出比 Qwen-ViT 更强的性能。

#### 7. 优点（亮点）
*   **数据质量**：构建并承诺发布 FineCap-450M，这是目前已知最大的细粒度重描述数据集，解决了视觉训练数据“脏、乱、简”的问题。
*   **训练范式**：从头开始（From Scratch）训练，避免了预训练模型的偏差，且全流程支持原生高分辨率。
*   **长上下文支持**：成功将视觉编码器的文本处理能力扩展到长描述，增强了复杂场景的匹配精度。

#### 8. 不足与局限
*   **模态限制**：目前 FineViT 仅限于**静态图像**，尚未扩展到视频领域的时序推理。
*   **算力成本**：从头训练 0.86B 参数的模型并处理数十亿级样本，其算力消耗巨大，普通研究机构难以复现。
*   **偏差风险**：重描述数据高度依赖于现有的强 MLLM（如 Qwen3-32B），可能存在模型幻觉的传递或特定风格的偏好。

（完）
