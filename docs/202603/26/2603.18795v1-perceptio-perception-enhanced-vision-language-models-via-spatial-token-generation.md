---
title: "Perceptio: Perception Enhanced Vision Language Models via Spatial Token Generation"
title_zh: Perceptio：通过空间标记生成增强感知的视觉语言模型
authors: "Yuchen Li, Amanmeet Garg, Shalini Chaudhuri, Rui Zhao, Garin Kessler"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18795v1"
tags: ["query:vlm"]
score: 10.0
evidence: 通过空间标记生成实现视觉语言任务中的空间推理
tldr: 针对大型视觉语言模型（LVLM）在细粒度空间感知方面的不足，本文提出了Perceptio。该模型通过在自回归序列中直接生成显式的2D语义分割和3D深度标记，增强了空间推理能力。研究利用VQ-VAE蒸馏深度信息并结合SAM2分割标记，配合多任务协同训练和新型损失函数，显著提升了模型在空间理解和定位任务上的表现，证明了显式空间思维链的有效性。
selection_source: fresh_fetch
motivation: 现有的LVLM在处理复杂几何关系时缺乏显式的空间解释，导致细粒度空间定位和3D感知能力受限。
method: 在LLM中集成SAM2分割标记和VQ-VAE深度标记，通过复合深度损失函数和多任务协同训练，使模型先生成空间标记再输出答案。
result: "在RefCOCO系列、HardBLINK（准确率提升10.3%）和MMBench等多个基准测试中均取得了SOTA性能。"
conclusion: 引入显式的空间思维链（Spatial Chain-of-Thought）能实质性地增强视觉语言模型的空间感知与推理能力。
---

## 摘要
大型视觉语言模型 (LVLMs) 在语义理解方面表现出色，但在细粒度空间定位方面面临挑战，因为模型必须在不产生空间解释的情况下隐式推断复杂的几何结构。我们提出了 Perceptio，这是一种具有 2D 和 3D 空间推理能力的感知增强型 LVLM，通过在自回归序列中直接生成显式的语义分割标记和深度标记来实现。具体而言，我们 (i) 从强大的单目教师模型中蒸馏出 VQVAE 深度码本，将密集深度标记化为紧凑序列，以及 (ii) 在 LLM 中集成基于 SAM2 的语义分割标记和 VQ-VAE 深度标记，使模型先输出空间标记，然后再进行回答。为了稳定深度标记的生成，我们引入了新型的复合深度标记目标（标记位、标记和计数损失）以及一种用于可微重建的软合并技术。我们在不同数据集上采用了多任务协同训练策略，让模型学习感知标记以应对多个下游任务。基于 InternVL，Perceptio 在各项基准测试中均达到了最先进的性能：在 RefCOCO/+/g 上将指代性表达分割提高了 +0.8/+1.4/+1.1 cIoU，HardBLINK 空间理解准确率提高了 10.3%，MMBench 准确率提高了 1.0%，证明了显式的空间思维链实质性地增强了 LVLM 中的空间定位能力。

## Abstract
Large Vision Language Models (LVLMs) excel at semantic understanding but struggle with fine grained spatial grounding, as the model must implicitly infer complex geometry without ever producing a spatial interpretation. We present Perceptio, a perception enhanced LVLM with 2D and 3D spatial reasoning abilities, enabled via explicit semantic segmentation tokens and depth tokens generated directly within the autoregressive sequence. Concretely, we (i) distill a VQVAE depth codebook from a strong monocular teacher to tokenize dense depth into compact sequences, and (ii) integrate SAM2 based semantic segmentation tokens and VQ-VAE depth tokens inside the LLM so the model first emits spatial tokens and then answers. To stabilize depth token generation, we introduce novel composite depth-token objectives (marker, token, and count losses) and a soft-merging technique for differentiable reconstruction. We adopt a multi-task co-training strategy across diverse datasets, letting the model learn perception tokens to tackle multiple downstream tasks. Building on InternVL, Perceptio achieves state-of-the-art performance across benchmarks: improving referring expression segmentation by +0.8/+1.4/+1.1 cIoU on RefCOCO/+/g HardBLINK spatial understanding accuracy by 10.3%, and MMBench accuracy by 1.0%, demonstrating that explicit spatial chain-of-thought materially strengthens spatial grounding in LVLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Perceptio: Perception Enhanced Vision Language Models via Spatial Token Generation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大型视觉语言模型（LVLM）在语义理解上表现优异，但在细粒度空间定位和3D感知（如深度、距离、比例）方面仍然薄弱。
*   **核心问题**：现有模型通常将空间推理视为一种隐式的涌现能力，缺乏显式的几何解释过程。
*   **整体含义**：本文提出了 **Perceptio**，通过在自回归序列中引入显式的2D语义分割标记和3D深度标记，构建了一种“空间思维链（Spatial Chain-of-Thought）”，强制模型在回答问题前先进行空间感知，从而显著增强其空间推理能力。

### 2. 方法论
*   **核心思想**：将感知任务（分割与深度）转化为标记（Token）生成任务，并集成到LLM的统一输出序列中。
*   **关键技术细节**：
    *   **序列结构**：输出格式定义为 `[seg tokens] [depth tokens] [text tokens]`。模型必须先生成分割和深度信息，再生成最终文本答案。
    *   **2D分割**：集成SAM2编码器，通过特殊的 `[seg]` 标记触发分割解码器生成掩码。
    *   **3D深度标记化**：利用 **VQ-VAE** 将来自“Depth Anything V2”教师模型的连续深度图离散化为100个深度标记（Codebook大小为128）。
    *   **复合深度损失函数**：
        *   **Marker Loss**：确保深度序列的起始和结束标记位置准确。
        *   **Token Loss**：监督深度标记值的准确性。
        *   **Count Loss**：惩罚生成序列长度的偏差。
    *   **软合并技术（Soft-merging）**：在训练中采用可微的重构方式，通过预测码本概率分布的加权平均来形成“软标记”，使深度重构损失能反向传播。

### 3. 实验设计
*   **数据集**：
    *   **指代性分割**：RefCOCO、RefCOCO+、RefCOCOg。
    *   **通用VQA/理解**：MME、MMBench、SEED-Bench。
    *   **科学/图表推理**：AI2D、MMStar、ScienceQA。
    *   **空间推理**：HardBLINK（包含3/4/5个点的相对深度比较）。
*   **对比方法**：包括 InternVL2、LLaVA-1.5、Sa2VA、LLaVA-Aurora、PixelLM 等主流及感知增强型模型。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 NVIDIA A100 GPU**。
*   **训练时长**：约 **24 小时**。
*   **实现细节**：基于 InternVL2.5 架构，采用 LoRA 微调（Rank=256），有效 Batch Size 为 512，最大序列长度 8192。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从像素级分割到高层语义推理的 10 多个基准测试，实验覆盖面广。
*   **消融实验**：
    *   对比了“仅2D感知”、“仅3D感知”以及“无感知标记”的情况。
    *   验证了不同深度损失项（重构损失 vs. 生成损失）的贡献。
*   **充分性评价**：实验设计较为客观、公平，通过跨维度的对比证明了 2D 和 3D 感知标记的互补性。

### 6. 主要结论与发现
*   **SOTA 性能**：Perceptio-8B 在 RefCOCO 系列数据集上刷新了纪录，并在 MMBench 上提升了 1.0%。
*   **空间推理飞跃**：在 HardBLINK 深度推理任务中，Perceptio 比之前的最佳模型（LLaVA-Aurora）平均准确率高出 **10.3%**。
*   **感知驱动推理**：消融实验显示，移除深度标记会导致空间推理能力大幅塌陷（HardBLINK 准确率下降 25.8%），证明了显式深度建模的必要性。

### 7. 优点
*   **统一框架**：首次在单个自回归序列中同时优化 2D 语义和 3D 几何感知。
*   **空间思维链**：通过“先感知、后回答”的逻辑，为 LVLM 提供了一种可解释的空间推理路径。
*   **端到端可微**：引入软合并技术解决了离散标记与连续图像重构之间的训练断层。
*   **推理效率**：尽管增加了感知标记，但推理开销增加极小（与 Sa2VA 相当）。

### 8. 不足与局限
*   **优化张力**：实验观察到加入深度标记后，在某些纯文本任务上存在轻微的性能权衡（Optimization Tension）。
*   **静态限制**：目前仅限于静态图像，尚未扩展到视频领域（需解决时间一致性问题）。
*   **教师依赖**：模型性能受限于教师模型（SAM2 和 Depth Anything V2）的质量，教师模型的噪声会传播给学生模型。

（完）
