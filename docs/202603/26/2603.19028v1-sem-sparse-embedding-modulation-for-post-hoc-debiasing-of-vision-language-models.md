---
title: "SEM: Sparse Embedding Modulation for Post-Hoc Debiasing of Vision-Language Models"
title_zh: SEM：用于视觉-语言模型事后去偏的稀疏嵌入调制
authors: "Quentin Guimard, Federico Bartsch, Simone Caldarella, Rahaf Aljundi, Elisa Ricci, Massimiliano Mancini"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19028v1"
tags: ["query:vlm"]
score: 9.0
evidence: CLIP视觉语言模型的后置去偏
tldr: 针对视觉语言模型（如CLIP）中存在的社会和虚假偏见，本文提出了一种名为SEM的后置零样本去偏框架。该方法利用稀疏自编码器（SAE）将密集的CLIP嵌入分解为解耦的稀疏特征，从而在稀疏潜空间中精准识别并调节偏见相关的神经元，同时保留任务相关的语义信息。实验证明，SEM在多个基准测试中显著提升了检索和分类的公平性，为多模态模型的公平性研究提供了新视角。
selection_source: fresh_fetch
motivation: 现有去偏方法在密集的嵌入空间中操作，导致偏见与任务信息高度纠缠，难以在不损害语义保真度的情况下有效去除偏见。
method: 提出稀疏嵌入调制（SEM），利用稀疏自编码器将嵌入分解为解耦特征，并针对性地调制偏见神经元而保留查询相关神经元。
result: 在四个基准数据集和两种CLIP骨干网络上的实验表明，SEM在检索和零样本分类任务中均实现了显著的公平性增益。
conclusion: 研究证明稀疏潜表征能为视觉语言模型的后置去偏提供比传统密集空间更有效且精准的操作基础。
---

## 摘要
连接视觉和语言的模型（如 CLIP）是多模态人工智能的关键组件，但其大规模、未经处理的训练数据引入了严重的社会偏差和伪偏差。现有的事后去偏方法通常直接在稠密的 CLIP 嵌入空间中操作，而偏差与任务相关信息在该空间中高度纠缠。这种纠缠限制了它们在不降低语义保真度的情况下消除偏差的能力。在这项工作中，我们提出了稀疏嵌入调制（SEM），这是一个在稀疏自编码器（SAE）潜空间中运行的事后、零样本去偏框架。通过将 CLIP 文本嵌入分解为解耦特征，SEM 能够识别并调制与偏差相关的神经元，同时保留与查询相关的神经元。这实现了更精确的非线性干预。在四个基准数据集和两个 CLIP 主干网络上，SEM 在检索和零样本分类中实现了显著的公平性提升。我们的结果表明，稀疏潜表示为视觉-语言模型的事后去偏提供了有效的基础。

## Abstract
Models that bridge vision and language, such as CLIP, are key components of multimodal AI, yet their large-scale, uncurated training data introduce severe social and spurious biases. Existing post-hoc debiasing methods often operate directly in the dense CLIP embedding space, where bias and task-relevant information are highly entangled. This entanglement limits their ability to remove bias without degrading semantic fidelity. In this work, we propose Sparse Embedding Modulation (SEM), a post-hoc, zero-shot debiasing framework that operates in a Sparse Autoencoder (SAE) latent space. By decomposing CLIP text embeddings into disentangled features, SEM identifies and modulates bias-relevant neurons while preserving query-relevant ones. This enables more precise, non-linear interventions. Across four benchmark datasets and two CLIP backbones, SEM achieves substantial fairness gains in retrieval and zero-shot classification. Our results demonstrate that sparse latent representations provide an effective foundation for post-hoc debiasing of vision-language models.

---

## 论文详细总结（自动生成）

这是一份关于论文《SEM: Sparse Embedding Modulation for Post-Hoc Debiasing of Vision-Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（如 CLIP）在海量未清洗数据上训练，容易习得社会偏见（如性别、种族）和虚假相关性（如背景与物体的关联）。
*   **研究背景**：现有的“事后（Post-hoc）”去偏方法通常直接在 CLIP 的**稠密嵌入空间**中操作。然而，在该空间中，语义内容与偏见信息高度“纠缠”，导致简单的线性投影（如正交投影）难以在不损害模型性能的情况下精准去除偏见。
*   **整体含义**：本文旨在通过引入**稀疏自编码器（SAE）**，将稠密嵌入分解为解耦的稀疏特征，从而在神经元级别实现更精准、非线性的去偏干预。

### 2. 方法论：核心思想与关键技术
SEM 框架的核心思想是将去偏操作从稠密空间转移到稀疏潜空间。
*   **核心流程**：
    1.  **投影**：利用预训练的 SAE 将 CLIP 文本嵌入 $z$ 映射到高维稀疏潜空间 $h$。
    2.  **神经元评分（Scoring）**：
        *   **内容相关性评分 ($S_{concept}$)**：通过比较查询词与一组多样化中性提示词（Diverse Prompts）的激活程度，识别哪些神经元代表核心语义。
        *   **偏见敏感性评分 ($S_{bias}$)**：利用特定的偏见提示词（如“男人的照片”），识别哪些神经元对特定偏见属性敏感。
    3.  **调制（Modulation）**：计算调制系数 $M$，放大内容神经元，衰减偏见神经元。
    4.  **重构**：将调制后的稀疏向量通过 SAE 解码器还原为去偏后的稠密嵌入 $z_{debias}$。
*   **三种变体**：
    *   **SEM-i (Bias-Agnostic)**：仅利用 LLM 生成的改写词估计内容神经元，衰减其他可能包含噪声的特征。
    *   **SEM-b (Bias-Aware)**：利用偏见提示词列表进行结构化的偏见神经元识别。
    *   **SEM-bi (Full)**：结合上述两者，实现最强干预。

### 3. 实验设计
*   **数据集**：
    *   **社会偏见**：FairFace、UTKFace、CelebA（涉及种族、性别、发色等属性）。
    *   **虚假相关性**：Waterbirds（测试背景对鸟类分类的影响）。
*   **任务场景**：跨模态检索（Cross-modal Retrieval）和零样本分类（Zero-shot Classification）。
*   **对比方法（Baselines）**：
    *   **零样本/事后方法**：RoboShot、Orth-Proj（正交投影）、Orth-Cali、PRISM-MINI。
    *   **非零样本方法（参考）**：PRISM、ZS-Debias、BendVLM（需要参考图像集）。
*   **骨干网络**：ViT-B/16、ViT-L/14@336px，以及 ResNet-50/101。

### 4. 资源与算力
*   **算力设备**：使用了单张 **NVIDIA A100 GPU (64GB)**。
*   **训练时长**：在 CC12M-cleaned 数据集上训练一个 SAE 大约需要 **1.5 小时**。
*   **推理开销**：由于是事后方法且 SAE 结构简单，推理过程非常高效。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 个主流基准数据集。
    *   测试了 4 种不同的 CLIP 骨干网络（ViT 和 ResNet 架构均有覆盖）。
    *   采用了 **10 折交叉验证**，确保结果的统计显著性。
*   **消融实验**：详细分析了调制公式中各项（如内容增强项、衰减项）的作用，验证了 SAE 空间相比 CLIP 原始空间的解耦优势。
*   **客观性**：实验不仅关注公平性指标（KL 散度、MaxSkew、Gap），还关注了性能指标（准确率、精度），证明了该方法在提升公平性的同时较好地保留了语义。

### 6. 主要结论与发现
*   **解耦优势**：SAE 潜空间在性别属性上的解耦能力是原始空间的 1.7-2.6 倍，在种族属性上更是达到了 5.6-5.7 倍。
*   **性能提升**：SEM 在**最差组准确率（Worst-Group Accuracy, WGA）**上表现优异。例如在 Waterbirds 任务中，SEM-bi 将 WGA 从 39.6% 提升至 67.6%。
*   **模块化与互补性**：SEM 可以作为其他方法的预处理步骤。将 SEM 与 BendVLM 结合，在 28 个指标中的 24 个上达到了 SOTA。

### 7. 优点（亮点）
*   **精准干预**：突破了传统线性投影的局限，实现了神经元级别的非线性调制。
*   **零样本特性**：不需要下游任务的标注数据，仅需预训练好的 SAE 和简单的提示词。
*   **可解释性**：通过稀疏特征，可以直观地观察到哪些神经元代表“医生”，哪些代表“男性”。
*   **通用性**：对社会偏见和环境虚假相关性均有效，且适配多种模型架构。

### 8. 不足与局限
*   **SAE 依赖性**：去偏效果高度依赖于 SAE 的训练质量和重构保真度。如果 SAE 未能捕捉到某些细微特征，去偏可能会导致语义丢失。
*   **提示词敏感性**：虽然使用了 LLM 增强，但方法仍依赖于人工或 LLM 定义的偏见提示词列表。
*   **计算前置**：虽然推理快，但需要针对每个模型骨干预先训练一个 SAE（尽管只需训练一次）。

（完）
