---
title: Heuristic-inspired Reasoning Priors Facilitate Data-Efficient Referring Object Detection
title_zh: 启发式引导的推理先验助力数据高效的指代目标检测
authors: "Xu Zhang, Zhe Chen, Jing Zhang, Dacheng Tao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24166v1"
tags: ["query:vlm"]
score: 10.0
evidence: 利用推理先验实现高效的数据引用对象检测
tldr: 针对指代目标检测（ROD）在机器人等领域面临的标注数据稀缺问题，本文提出了数据高效指代目标检测（De-ROD）任务基准。研究者开发了HeROD框架，通过在DETR类模型的提议排序、预测融合和匈牙利匹配三个阶段引入启发式空间与语义推理先验，显著提升了模型在少样本情况下的学习效率和性能，为视觉语言理解提供了新思路。
selection_source: fresh_fetch
motivation: 现有的指代目标检测模型高度依赖大规模标注数据，在数据稀缺的实际应用场景中难以有效学习空间和语义结构。
method: 提出HeROD框架，将基于指代短语的启发式空间和语义推理先验注入到DETR流水线的提议排序、预测融合和匈牙利匹配阶段。
result: 在RefCOCO、RefCOCO+和RefCOCOg数据集的低数据量实验中，HeROD一致优于现有的强基准模型。
conclusion: 集成简单且可解释的推理先验是实现高效视觉语言理解的一种实用且可扩展的有效途径。
---

## 摘要
大多数指代目标检测（ROD）模型，特别是现代定位检测器，是针对数据丰富的场景设计的，但在机器人、增强现实和其他专业领域等许多实际部署中，往往面临严重的标签稀缺问题。在这种情况下，端到端定位检测器需要从零开始学习空间和语义结构，从而浪费了宝贵的样本。我们提出了一个简单的问题：当数据稀缺时，显式推理先验能否帮助模型更高效地学习？为了探索这一问题，我们首先引入了数据高效指代目标检测（De-ROD）任务，这是一个用于衡量低数据和少样本设置下 ROD 性能的基准协议。随后，我们提出了 HeROD（启发式引导的 ROD），这是一个轻量级且与模型无关的框架。它将显式的、受启发式启发的空间和语义推理先验（即基于指代短语推导出的可解释信号）注入到现代 DETR 风格流水线的三个阶段中：候选框排序、预测融合和匈牙利匹配。通过在训练和推理过程中使模型偏向合理的候选目标，这些先验有望提高标签效率和收敛性能。在 RefCOCO、RefCOCO+ 和 RefCOCOg 数据集上，HeROD 在标签稀缺的情况下始终优于强大的定位基准模型。更广泛地说，我们的结果表明，集成简单、可解释的推理先验为实现更好的数据高效视觉语言理解提供了一条实用且可扩展的路径。

## Abstract
Most referring object detection (ROD) models, especially the modern grounding detectors, are designed for data-rich conditions, yet many practical deployments, such as robotics, augmented reality, and other specialized domains, would face severe label scarcity. In such regimes, end-to-end grounding detectors need to learn spatial and semantic structure from scratch, wasting precious samples. We ask a simple question: Can explicit reasoning priors help models learn more efficiently when data is scarce? To explore this, we first introduce a Data-efficient Referring Object Detection (De-ROD) task, which is a benchmark protocol for measuring ROD performance in low-data and few-shot settings. We then propose the HeROD (Heuristic-inspired ROD), a lightweight, model-agnostic framework that injects explicit, heuristic-inspired spatial and semantic reasoning priors, which are interpretable signals derived based on the referring phrase, into 3 stages of a modern DETR-style pipeline: proposal ranking, prediction fusion, and Hungarian matching. By biasing both training and inference toward plausible candidates, these priors promise to improve label efficiency and convergence performance. On RefCOCO, RefCOCO+, and RefCOCOg, HeROD consistently outperforms strong grounding baselines in scarce-label regimes. More broadly, our results suggest that integrating simple, interpretable reasoning priors provides a practical and extensible path toward better data-efficient vision-language understanding.

---

## 论文详细总结（自动生成）

### 论文总结：启发式引导的推理先验助力数据高效的指代目标检测 (HeROD)

#### 1. 核心问题与整体含义
*   **研究动机**：指代目标检测（ROD）旨在根据自然语言描述定位图像中的特定对象。虽然现有模型（如 Grounding DINO）在数据充足时表现优异，但在机器人、增强现实等实际应用中，标注数据往往极度匮乏。
*   **核心挑战**：端到端模型在数据稀缺时，必须从零开始学习基础的空间关系（如“左边”）和语义属性，这导致样本利用率低且容易过拟合。
*   **研究目标**：探索显式的“推理先验”（Reasoning Priors）能否帮助模型在有限数据下更高效地学习，减少对大规模标注的依赖。

#### 2. 方法论
论文提出了 **HeROD** 框架，其核心思想是将受启发式算法（如 A* 搜索）启发的空间和语义信号注入到 DETR 风格的检测流水线中。
*   **推理先验构建**：
    *   **空间先验 ($H_s$)**：从文本中提取方位词（如“left”, “top”），映射到预定义的空间概率图上。
    *   **语义先验 ($H_v$)**：利用预训练的 CLIPSeg 模型生成文本相关的视觉显著图，衡量候选框与描述的语义一致性。
*   **三阶段注入技术**：
    1.  **对象参考生成阶段**：在 Top-N 候选框筛选时，将先验分数与检测器置信度相加，引导模型关注更合理的区域。
    2.  **最终预测阶段**：使用轻量级 MLP 模块，自适应地融合学习到的特征与显式先验，进行最终的 Top-1 决策。
    3.  **训练目标优化**：修改匈牙利匹配（Hungarian Matching）的代价函数，加入先验项以稳定早期训练的分配过程，并引入置信度损失（$L_{conf}$）进行监督。

#### 3. 实验设计
*   **数据集**：RefCOCO、RefCOCO+、RefCOCOg。
*   **任务基准 (De-ROD)**：
    *   **低数据制度 (Low-data)**：分别抽取 0.1%、0.2%、0.5%、1%、2%、5% 的训练数据。
    *   **少样本制度 (Few-shot)**：在充足的“人”类别（支持类）上预训练，在极少量的“非人”对象（新类）上进行微调。
*   **对比方法**：以 **Grounding DINO** 和 **UNINEXT** 作为强基准模型（Baseline），对比注入 HeROD 框架前后的性能提升。

#### 4. 资源与算力
*   **算力细节**：论文提到使用 AdamW 优化器，Batch Size 为 8。
*   **显存与延迟**：
    *   **显存**：Batch Size 为 8 时，显存占用从 4742MB 升至 5810MB。
    *   **延迟**：推理速度从 0.362 s/iter 变为 0.435 s/iter（增加了约 20% 的开销，主要来自冻结的 CLIPSeg 模块）。
*   **未明确说明**：文中未明确提及具体的 GPU 型号（如 A100 或 V100）及总训练时长（小时/天），但给出了训练轮数（5-10 Epochs）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了 6 种不同比例的低数据设置、多种少样本划分、全量数据对比以及针对 RefCOCO 清洗版数据集的验证。
*   **消融实验**：分别验证了空间先验、语义先验、不同注入阶段（参考生成、预测融合、匹配损失）以及损失权重对结果的影响。
*   **充分性评价**：实验设计非常充分且客观。通过在两个不同的 SOTA 基准（DINO 和 UNINEXT）上验证，证明了方法的通用性（Model-agnostic）。

#### 6. 主要结论与发现
*   **性能飞跃**：在极度缺乏数据的 0.1% 设置下，HeROD 在 RefCOCO 验证集上比 Grounding DINO 提升了 **12.9%**。
*   **收敛加速**：显式先验充当了“软引导”，使模型无需从头学习方位等基础概念，显著提升了标签效率。
*   **全量数据兼容**：在全量数据训练下，HeROD 依然能带来 0.7%~1.0% 的小幅提升，证明先验信息与学习特征是互补的，而非冲突的。

#### 7. 优点
*   **可解释性强**：引入的空间和语义先验具有明确的物理含义，易于调试和理解。
*   **模型无关性**：作为一个轻量级插件，可以轻松集成到任何 DETR 类的多模态检测器中。
*   **任务定义领先**：首次系统性地定义了 De-ROD 任务，填补了 ROD 领域在数据高效性研究上的空白。

#### 8. 不足与局限
*   **空间词汇有限**：目前的空间先验仅支持基本的方位词（如上下左右），对于复杂的相对关系（如“在...后面”、“靠近...”）处理能力有限。
*   **依赖预训练模型**：语义先验高度依赖 CLIPSeg 等基础模型的质量，在某些特定领域（如医学影像）可能因缺乏相应的预训练模型而失效。
*   **推理开销**：虽然是轻量级的，但引入额外的视觉编码器（CLIPSeg）仍会带来一定的计算延迟和显存增长。

（完）
