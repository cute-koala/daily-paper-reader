---
title: "Meanings and Measurements: Multi-Agent Probabilistic Grounding for Vision-Language Navigation"
title_zh: 意义与度量：用于视觉语言导航的多智能体概率对齐
authors: "Swagat Padhan, Lakshya Jain, Bhavya Minesh Shah, Omkar Patil, Thao Nguyen, Nakul Gopalan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19166v1"
tags: ["query:vlm"]
score: 9.0
evidence: 视觉语言导航中度量语义语言查询的概率定位
tldr: 针对机器人视觉语言导航中视觉语言模型（VLM）难以处理复杂度量约束的问题，本文提出了MAPG框架。该框架通过多智能体协作将语言查询分解为结构化子组件，利用VLM进行语义定位，并结合概率组合生成符合度量一致性的3D空间决策。实验证明MAPG在HM-EQA及新提出的MAPG-Bench基准上表现优异，并成功应用于真实机器人。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理包含精确度量约束（如“冰箱右侧两米”）的复杂语义查询时表现不佳。
method: 提出一种多智能体概率定位框架，将查询分解后利用VLM定位，再通过概率组合实现度量一致的决策。
result: 在HM-EQA和新基准MAPG-Bench上均取得显著性能提升，并完成了真实世界的机器人演示。
conclusion: MAPG有效地结合了VLM的语义能力与概率推理的度量精度，提升了机器人在复杂物理环境中的语言理解与执行能力。
---

## 摘要
与人类协作的机器人必须将自然语言目标转化为可执行的、物理对齐的决策。例如，执行诸如“走到冰箱右侧两米处”之类的指令，需要对 3D 场景中的语义引用、空间关系和度量约束进行对齐。虽然近期的视觉语言模型 (VLM) 展示了强大的语义对齐能力，但它们并非专门为推理物理定义空间中的度量约束而设计。在这项工作中，我们通过实验证明，最先进的基于 VLM 的对齐方法在处理复杂的度量-语义语言查询时表现不佳。为了解决这一局限性，我们提出了 MAPG（多智能体概率对齐），这是一个智能体框架，它将语言查询分解为结构化的子组件，并调用 VLM 对每个组件进行对齐。随后，MAPG 以概率方式组合这些对齐输出，从而在 3D 空间中生成度量一致且可执行的决策。我们在 HM-EQA 基准测试上评估了 MAPG，并展示了其相对于强基准模型的持续性能提升。此外，我们引入了一个新的基准测试 MAPG-Bench，专门用于评估度量-语义目标对齐，填补了现有语言对齐评估中的空白。我们还展示了一个真实世界的机器人演示，表明当存在结构化场景表示时，MAPG 可以从仿真环境迁移到现实世界。

## Abstract
Robots collaborating with humans must convert natural language goals into actionable, physically grounded decisions. For example, executing a command such as "go two meters to the right of the fridge" requires grounding semantic references, spatial relations, and metric constraints within a 3D scene. While recent vision language models (VLMs) demonstrate strong semantic grounding capabilities, they are not explicitly designed to reason about metric constraints in physically defined spaces. In this work, we empirically demonstrate that state-of-the-art VLM-based grounding approaches struggle with complex metric-semantic language queries. To address this limitation, we propose MAPG (Multi-Agent Probabilistic Grounding), an agentic framework that decomposes language queries into structured subcomponents and queries a VLM to ground each component. MAPG then probabilistically composes these grounded outputs to produce metrically consistent, actionable decisions in 3D space. We evaluate MAPG on the HM-EQA benchmark and show consistent performance improvements over strong baselines. Furthermore, we introduce a new benchmark, MAPG-Bench, specifically designed to evaluate metric-semantic goal grounding, addressing a gap in existing language grounding evaluations. We also present a real-world robot demonstration showing that MAPG transfers beyond simulation when a structured scene representation is available.

---

## 论文详细总结（自动生成）

### 论文总结：意义与度量——用于视觉语言导航的多智能体概率对齐（MAPG）

#### 1. 核心问题与研究动机
在机器人视觉语言导航（VLN）中，机器人需要将人类的自然语言指令（如“走到冰箱右侧两米处”）转化为物理空间中的精确动作。这类指令被称为**度量-语义查询（Metric-Semantic Queries）**，涉及语义引用（冰箱）、空间关系（右侧）和度量约束（两米）。
*   **研究动机**：现有的视觉语言模型（VLM）虽然语义对齐能力强，但在处理精确的物理度量和几何约束时表现脆弱，且难以在导航过程中维持一致的坐标参考框架。
*   **核心问题**：如何结合 VLM 的语义推理能力与概率几何推理，以实现高精度的 3D 空间目标定位。

#### 2. 方法论：MAPG 框架
MAPG（Multi-Agent Probabilistic Grounding）采用模块化、多智能体协作的思想，将复杂查询分解并概率化组合：
*   **编排器（Orchestrator）**：将自然语言解析为结构化的“空间描述子句（SDC）”，提取出锚点对象（Anchor）、空间谓词（Relation）和度量约束（Metric）。
*   **定位智能体（Grounding Agent）**：利用语义场景图（3D Scene Graph）和 CLIP 等模型，将解析出的锚点（如“冰箱”）关联到环境中的具体物体实例。
*   **空间智能体（Spatial Agent）**：为每个约束生成连续的概率密度函数（PDF）：
    *   **方向内核**：使用 **von Mises-Fisher 分布**建模空间谓词（如“左”、“前”）。
    *   **度量内核**：使用 **径向高斯分布（Radial Gaussian）**建模距离约束。
*   **概率组合与规划**：通过在对数空间叠加这些解析内核，生成全局坐标系下的目标分布图。导航规划器（如 RRT*）从该分布中采样高概率点作为执行航点。

#### 3. 实验设计
*   **数据集与基准**：
    *   **HM-EQA**：现有的具身问答基准，用于测试语义推理。
    *   **MAPG-Bench（新提出）**：专门针对度量-语义定位设计的基准，包含 30 个 HM3D 室内场景和 100 个标注查询，重点评估 3D 坐标定位精度。
*   **对比方法**：
    *   **GraphEQA**：基于场景图的具身问答基准模型。
    *   **SRGPT**：专门的视觉空间推理模型。
    *   **通用 VLM 基准**：包括 GPT-4o、Gemini、Claude 等不同版本的变体。
*   **评估指标**：目标定位距离误差（O-O/O-W Error）、航向/俯仰角误差、任务成功率（TSR）及轨迹效率。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）及训练时长。
*   由于该框架主要依赖于现成的预训练大模型（如 GPT-5.2、Gemini 3 Pro 等，注：文中出现的版本号较高，可能为模拟未来场景或最新内部版本）进行推理，而非从头训练大规模模型，其算力消耗主要集中在在线推理和 3D 场景图的实时构建上。

#### 5. 实验数量与充分性
*   **实验规模**：在 30 个复杂的 3D 室内场景中进行了测试，涵盖了 100 组人工标注的复杂指令。
*   **消融实验**：对比了“显式空间推理”与“仅靠思维链（CoT）提示词”的效果，并测试了在物体遮挡（Occlusion）情况下的鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅有仿真环境的大规模测试，还包含了真实世界的机器人演示。通过对比多种 SOTA 模型，客观地证明了概率组合方法在度量精度上的优势。

#### 6. 主要结论与发现
*   **精度大幅提升**：MAPG 将物体到世界的定位误差从基准模型的 5.82 米降低到了 **0.07 米**（降低了 98.8%），方向误差也显著下降。
*   **结构化优于提示词**：消融实验证明，简单的 Prompt Engineering（如 CoT）无法替代显式的概率几何推理，后者在处理遮挡和复杂空间关系时成功率更高。
*   **多视图证据积累**：MAPG 能够通过多视角观察延迟决策，直到证据足以确定锚点物体，从而提高了任务成功率。

#### 7. 优点与亮点
*   **模块化与可解释性**：将语言理解与几何计算分离，使得定位过程透明且可调试。
*   **规划器友好**：输出的是连续的概率分布而非单一坐标点，更符合机器人路径规划的需求。
*   **填补空白**：提出的 MAPG-Bench 为具身智能领域提供了一个急需的、关注度量精度的评估工具。
*   **跨环境迁移**：展示了从仿真到真实物理环境的零样本（Zero-shot）迁移能力。

#### 8. 不足与局限
*   **依赖场景图质量**：如果 3D 场景图构建不完整或物体识别错误，MAPG 的性能会受限。
*   **参考系歧义**：对于“在……前面”这类依赖物体自身朝向的谓词，当物体固有方向不明时，仍存在推理偏差。
*   **地图对齐风险**：自由空间地图与语义场景图之间的微小错位可能导致生成的航点在物理上不可达。

（完）
