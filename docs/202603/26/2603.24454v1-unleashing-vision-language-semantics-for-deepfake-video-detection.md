---
title: Unleashing Vision-Language Semantics for Deepfake Video Detection
title_zh: 释放视觉-语言语义在深度伪造视频检测中的潜力
authors: "Jiawen Zhu, Yunqi Miao, Xueyi Zhang, Jiankang Deng, Guansong Pang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24454v1"
tags: ["query:vlm"]
score: 9.0
evidence: 利用 CLIP 等预训练视觉语言模型进行深度伪造检测
tldr: 针对深度伪造视频检测（DFD），现有基于视觉语言模型（VLM）的方法往往忽略了跨模态语义。本文提出VLAForge框架，通过ForgePerceiver捕捉细微伪造线索并保留预训练对齐知识，同时引入身份感知的VLA评分，结合身份先验文本提示增强判别力。实验证明该方法在多种伪造场景下均优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有基于VLM的深度伪造检测方法仅利用视觉特征，未能充分挖掘其潜在空间中丰富的视觉-语言跨模态语义。
method: 提出VLAForge框架，利用ForgePerceiver捕捉多尺度伪造线索，并结合身份先验增强的文本提示计算身份感知VLA评分。
result: 在经典换脸和最新全脸生成等多个视频深度伪造基准测试中，VLAForge在帧级和视频级表现均显著优于现有最先进方法。
conclusion: 充分释放视觉-语言模型的跨模态语义潜力，并结合身份先验信息，能有效提升深度伪造视频检测的泛化能力和判别性能。
---

## 摘要
最近的深度伪造视频检测（DFD）研究表明，预训练的视觉-语言模型（VLM）（如 CLIP）在检测跨不同身份的伪造痕迹方面表现出强大的泛化能力。然而，现有方法仅侧重于利用视觉特征，忽视了它们最显著的优势——嵌入在潜空间中丰富的视觉-语言语义。我们提出了 VLAForge，这是一种新型的 DFD 框架，它释放了这种跨模态语义的潜力，以增强模型在深度伪造检测中的判别能力。本项工作：i) 通过 ForgePerceiver 增强了 VLM 的视觉感知，ForgePerceiver 作为一个独立的学习器，能够从细粒度和整体两个维度捕捉多样且细微的伪造线索，同时保留预训练的视觉-语言对齐（VLA）知识；ii) 提供了一种互补的判别线索——身份感知 VLA 分数，该分数通过将跨模态语义与 ForgePerceiver 学习到的伪造线索相结合而得出。值得注意的是，VLA 分数通过身份先验信息的文本提示得到增强，以捕捉针对每个身份定制的真实性线索，从而实现更具判别性的跨模态语义。在视频 DFD 基准测试（包括经典的换脸伪造和最近的全脸生成伪造）上进行的全面实验表明，我们的 VLAForge 在帧级和视频级上均显著优于现有最先进的方法。代码可在 https://github.com/mala-lab/VLAForge 获取。

## Abstract
Recent Deepfake Video Detection (DFD) studies have demonstrated that pre-trained Vision-Language Models (VLMs) such as CLIP exhibit strong generalization capabilities in detecting artifacts across different identities. However, existing approaches focus on leveraging visual features only, overlooking their most distinctive strength -- the rich vision-language semantics embedded in the latent space. We propose VLAForge, a novel DFD framework that unleashes the potential of such cross-modal semantics to enhance model's discriminability in deepfake detection. This work i) enhances the visual perception of VLM through a ForgePerceiver, which acts as an independent learner to capture diverse, subtle forgery cues both granularly and holistically, while preserving the pretrained Vision-Language Alignment (VLA) knowledge, and ii) provides a complementary discriminative cue -- Identity-Aware VLA score, derived by coupling cross-modal semantics with the forgery cues learned by ForgePerceiver. Notably, the VLA score is augmented by an identity prior-informed text prompting to capture authenticity cues tailored to each identity, thereby enabling more discriminative cross-modal semantics. Comprehensive experiments on video DFD benchmarks, including classical face-swapping forgeries and recent full-face generation forgeries, demonstrate that our VLAForge substantially outperforms state-of-the-art methods at both frame and video levels. Code is available at https://github.com/mala-lab/VLAForge.

---

## 论文详细总结（自动生成）

以下是对论文《Unleashing Vision-Language Semantics for Deepfake Video Detection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着生成式模型的飞速发展，高保真的面部伪造视频（Deepfake）带来了严重的社会安全和伦理挑战。现有的检测方法正从捕捉低级伪造痕迹转向利用预训练视觉-语言模型（VLM，如 CLIP）的强大泛化能力。
*   **核心痛点**：现有的基于 VLM 的检测方法大多仅利用其**视觉编码器**（Visual Encoder），通过适配器（Adapter）或微调来提取特征，却忽视了 VLM 最核心的优势——**跨模态语义对齐（Vision-Language Alignment, VLA）**。
*   **研究目标**：释放 VLM 潜空间中丰富的视觉-语言语义潜力，通过引入身份先验和细粒度的跨模态对齐，提升模型在跨数据集场景下的判别力和鲁棒性。

### 2. 方法论
论文提出了 **VLAForge** 框架，包含两个核心组件：
*   **ForgePerceiver（伪造感知器）**：
    *   作为一个独立的轻量级学习器，它通过一组可学习的查询令牌（Query Tokens）与 VLM 的视觉令牌交互。
    *   **全局学习**：生成多样化的“伪造感知掩码”（Forgery-aware Masks），通过正交约束（Orthogonality Constraint）确保捕捉到互补的伪造特征，并将其注入 VLM 的自注意力机制中，增强全局类令牌（Class Token）的判别力。
    *   **局部学习**：生成粗粒度的“伪造定位图”（Forgery Localization Map），为后续的局部真实性推理提供空间引导。
*   **Identity-Aware VLA Scoring（身份感知 VLA 评分）**：
    *   **身份先验提示**：在文本提示词（如 "This is a real/fake photo of <id> person"）中嵌入身份令牌，该令牌由 VLM 提取的身份特征动态填充。
    *   **细粒度对齐**：计算身份感知的文本特征与视觉图像块（Patch）令牌之间的相似度，生成高分辨率的 VLA 注意力图。
    *   **多尺度融合**：将 VLA 注意力图与 ForgePerceiver 的定位图进行元素级融合，输出最终的局部判别分数。

### 3. 实验设计
*   **数据集**：
    *   **经典换脸伪造**：FaceForensics++ (FF++)、CelebDF v1/v2、DFDC、DeepfakeDetection (DFD)。
    *   **全脸生成伪造**：基于 GAN 和扩散模型（VQGAN, StyleGAN-XL, SiT, DiT, PixArt）生成的最新高保真数据集。
*   **Benchmark 与对比方法**：
    *   采用**跨数据集评估协议**：在 FF++ (c23 压缩版) 上训练，在其余 9 个数据集上测试。
    *   对比了 16 种最先进（SOTA）方法，包括传统 CNN 方法（Xception）、基于增强的方法（SBI, ProDet）以及最新的 VLM 适配方法（ForAda, RepDFD, UDD）。
*   **评价指标**：帧级（Frame-level）和视频级（Video-level）的 AUROC。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **模型配置**：主干网络采用 OpenCLIP 的 **ViT-L/14**（参数冻结）。
*   **训练细节**：Batch size 为 32，使用 Adam 优化器，初始学习率 2e-5，训练时长为 **15 个 epoch**。
*   **复杂度**：VLAForge 的可训练参数量仅为 **3.28M**，比同类 SOTA 方法 ForAda (5.7M) 更轻量。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个主流基准数据集上进行了详尽测试，涵盖了从早期换脸到最新扩散模型生成的多种伪造技术。
*   **消融实验**：针对 ForgePerceiver、身份先验提示、VLA 评分模块、损失函数（正交损失、Dice 损失）以及超参数（查询令牌数量 $q$、融合权重 $\alpha$）做了多组对比实验。
*   **可视化分析**：提供了 t-SNE 特征分布图、VLA 注意力图对比以及伪造掩码可视化，直观展示了模型如何定位伪造区域。
*   **客观性**：实验遵循标准跨数据集协议，并使用了多个随机种子重复实验以验证稳定性，结果表现出高度的一致性和客观性。

### 6. 主要结论与发现
*   **性能领先**：VLAForge 在所有测试数据集上均显著优于现有方法。在最具挑战性的 DFDC 数据集上，帧级 AUROC 提升了约 2.7%。
*   **跨模态优势**：实验证明，仅仅依靠视觉特征是不够的，引入身份感知的文本语义能显著提升模型对细微伪造痕迹的捕捉能力。
*   **身份先验的重要性**：注入身份信息后的 VLA 注意力图在空间上更连贯，能更精准地覆盖整个伪造面部区域，减少误报。

### 7. 优点
*   **创新视角**：首次深入挖掘并释放了 VLM 的**跨模态对齐语义**用于深度伪造检测，而非简单的视觉特征提取。
*   **轻量高效**：通过轻量级的 ForgePerceiver 实现了强大的特征增强，参数量极小，推理效率高。
*   **泛化性强**：在完全未见的扩散模型生成（Diffusion-generated）人脸上表现出极佳的迁移能力。

### 8. 不足与局限
*   **主干网络依赖**：模型性能仍受限于预训练 CLIP 的原始能力，若 CLIP 未能很好地建模某些极端光照或低分辨率场景，检测效果可能受限。
*   **身份提取风险**：虽然论文使用了类令牌作为身份先验，但在极端遮挡或非人脸区域，身份信息的提取质量可能会干扰文本提示的准确性。
*   **时序建模较弱**：目前主要基于帧级预测后取平均，未充分利用视频中的长程时序不一致性特征。

（完）
