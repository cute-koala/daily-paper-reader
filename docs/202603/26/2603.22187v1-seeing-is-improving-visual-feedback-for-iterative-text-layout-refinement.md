---
title: "Seeing is Improving: Visual Feedback for Iterative Text Layout Refinement"
title_zh: 见即改进：用于迭代文本布局优化的视觉反馈
authors: "Junrong Guo, Shancheng Fang, Yadong Qu, Hongtao Xie"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22187v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于自动布局生成和视觉反馈的多模态大语言模型
tldr: 针对多模态大模型在文本布局生成中缺乏视觉反馈、难以保证美观和可读性的问题，本文提出了视觉反馈布局模型（VFLM）。该框架通过强化学习和基于OCR准确率的视觉奖励模型，实现了自适应的反思性生成，使模型能够根据渲染结果进行迭代优化。实验证明，VFLM在多个基准测试中显著优于现有的代码生成基线和多模态模型，强调了视觉反馈在设计导向任务中的关键作用。
selection_source: fresh_fetch
motivation: 现有的布局生成方法主要依赖代码生成，由于缺乏对渲染结果的视觉感知，难以保证最终设计的可读性和美学质量。
method: 提出VFLM框架，利用视觉反馈进行迭代优化，并通过结合OCR准确率的视觉奖励模型进行强化学习，激发模型的反思生成能力。
result: 在多个基准测试中，VFLM的表现一致优于先进的多模态大模型、现有布局模型以及仅依赖代码的基线方法。
conclusion: 视觉反馈是提升设计导向型多模态大模型性能的关键因素，能够有效引导模型通过迭代实现更高质量的布局生成。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展使得从自然语言描述中自动生成结构化布局成为可能。现有方法通常遵循“仅代码”范式，即生成表示布局的代码，再由图形引擎渲染出最终图像。然而，这些方法无法感知渲染后的视觉结果，难以保证可读性和美观性。在本文中，我们将视觉反馈视为布局生成的关键因素，并提出了视觉反馈布局模型（VFLM），这是一个利用视觉反馈进行迭代优化的自我改进框架。VFLM 能够执行自适应反思生成，利用视觉信息反思之前存在的问题，并迭代生成输出，直到达到满意的质量。这是通过强化学习实现的，并结合了一个包含 OCR 准确率的视觉落地奖励模型。通过仅对最终生成结果进行奖励，我们可以有效地激发模型的迭代和反思生成能力。在多个基准测试上的实验表明，VFLM 始终优于先进的 MLLMs、现有的布局模型以及仅代码的基准模型，证明了视觉反馈对于面向设计的 MLLMs 至关重要。我们的代码和数据可在 https://github.com/FolSpark/VFLM 获取。

## Abstract
Recent advances in Multimodal Large Language Models (MLLMs) have enabled automated generation of structured layouts from natural language descriptions. Existing methods typically follow a code-only paradigm that generates code to represent layouts, which are then rendered by graphic engines to produce final images. However, they are blind to the rendered visual outcome, making it difficult to guarantee readability and aesthetics. In this paper, we identify visual feedback as a critical factor in layout generation and propose Visual Feedback Layout Model (VFLM), a self-improving framework that leverages visual feedback iterative refinement. VFLM is capable of performing adaptive reflective generation, which leverages visual information to reflect on previous issues and iteratively generates outputs until satisfactory quality is achieved. It is achieved through reinforcement learning with a visually grounded reward model that incorporates OCR accuracy. By rewarding only the final generated outcome, we can effectively stimulate the model's iterative and reflective generative capabilities. Experiments across multiple benchmarks show that VFLM consistently outperforms advanced MLLMs, existing layout models, and code-only baselines, establishing visual feedback as critical for design-oriented MLLMs. Our code and data are available at https://github.com/FolSpark/VFLM.

---

## 论文详细总结（自动生成）

### 论文总结：Seeing is Improving: Visual Feedback for Iterative Text Layout Refinement

#### 1. 核心问题与整体含义
论文针对当前多模态大语言模型（MLLM）在**自动化文本布局生成**中的局限性提出了挑战。现有方法（如生成 SVG 或 JSON 代码）多采用“仅代码”范式，模型在生成代码后无法感知渲染出的视觉效果，导致最终设计经常出现文字重叠、对比度差、可读性低或美感不足等问题。
**研究动机**：通过引入“视觉反馈”闭环，赋予模型“边看边改”的能力，弥补代码生成与视觉感知之间的鸿沟。

#### 2. 方法论
论文提出了 **VFLM（Visual Feedback Layout Model）** 框架，核心思想是构建一个“生成-渲染-反思-优化”的迭代循环。
*   **迭代流程**：模型首先生成初始 SVG 代码，通过渲染工具转为图像；模型随后观察该图像，反思布局缺陷（如重叠、大小不当），并生成修正后的代码，直至满意。
*   **两阶段训练**：
    1.  **冷启动监督微调（SFT）**：利用高级 MLLM（如豆包-Seed-1.6）作为教师模型，合成包含“错误-反思-修正”过程的多轮对话数据，使模型初步具备工具调用和自我修正能力。
    2.  **强化学习（RL）**：采用 **GRPO 算法**。核心创新在于设计了一个综合奖励函数：
        *   **布局奖励（$R_{layout}$）**：训练一个专门的奖励模型，通过四级质量梯度（从高质量真值到严重扰动的布局）学习审美和协调性。
        *   **内容奖励（$R_{ocr} + R_{svg}$）**：利用 OCR 准确率和代码字符匹配确保文本内容的正确性。
*   **关键发现**：简单的**基于结果的奖励（Outcome-based Reward）**比复杂的**基于过程的奖励（Process-oriented Reward）**更能有效激发模型的自我改进能力，避免“奖励作弊（Reward Hacking）”。

#### 3. 实验设计
*   **数据集**：
    *   **TextLayout**：自建数据集（200K 样本），包含背景、文本、SVG 和渲染图。
    *   **Crello** 与 **DesignerIntention**：行业标准基准测试集。
*   **对比方法**：
    *   **先进 MLLM**：GPT-4o, Claude 3.7, Qwen2.5-VL-72B 等。
    *   **图像编辑模型**：GPT-4o (edit), Qwen-Image-Edit, FLUX-Kontext。
    *   **专用布局模型**：OpenCOLE, IGD。
*   **评估指标**：OCR F1 分数（准确性）、Rali/Rove/Rcom（对齐/重叠/平滑度）、GPT-4o 评分（美学/和谐度）以及人工评价。

#### 4. 资源与算力
*   **硬件**：使用了 **16 台 NVIDIA H200 GPU** 组成的集群。
*   **训练细节**：
    *   SFT 阶段：8k 轨迹数据，训练 2 个 epoch，学习率 $1 \times 10^{-5}$。
    *   RL 阶段：准备了 32k 样本，学习率 $1 \times 10^{-6}$，每个样本进行 8 次采样（rollouts）。
    *   奖励模型：基于 Qwen2.5-VL-3B，在 1.2M 个偏好对上训练。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度对比**：涵盖了从通用大模型到专用设计模型的所有主流基准。
*   **消融实验**：对比了冷启动模型、单轮 RL、无 SFT 的 RL、直接输出 SFT 等多种变体，验证了迭代反馈的必要性。
*   **奖励机制分析**：深入探讨了结果奖励与过程奖励对模型收敛和性能的影响。
*   **人工验证**：引入 16 名参与者的盲测，增强了客观指标之外的可信度。

#### 6. 主要结论与发现
*   **视觉反馈至关重要**：VFLM 的最终输出显著优于其第一轮输出（Step-1），证明了“反思”过程确实提升了质量。
*   **性能领先**：VFLM 在 OCR 准确率和图形质量指标上均达到了 SOTA 水平，甚至在某些维度上超过了 Claude 3.7 和 GPT-4o。
*   **自适应能力**：模型能根据任务复杂度自动调整反思轮数（简单任务轮数少，复杂任务轮数多）。
*   **奖励设计启示**：在多模态推理任务中，清晰的结果导向信号比繁琐的中间步骤引导更稳健。

#### 7. 优点
*   **闭环系统**：首次将视觉反馈引入 MLLM 布局生成，解决了“盲目生成”的痛点。
*   **高质量奖励模型**：通过四级分层数据构造，解决了布局质量难以量化评估的问题。
*   **自我进化**：框架具有自我改进的潜力，不完全依赖于昂贵的人工标注。

#### 8. 不足与局限
*   **推理延迟**：由于需要多次迭代和渲染，VFLM 的推理时间（约 8.6s）显著高于单轮生成模型（如 IGD 约 3.7s），在实时性要求极高的场景下受限。
*   **依赖渲染器**：模型的表现高度依赖于底层 SVG 渲染引擎的能力，若渲染器不支持某些复杂图形特性，模型也无法优化。
*   **背景修改限制**：与图像编辑模型不同，VFLM 仅能调整文字布局，无法对背景图像进行协同修改。

（完）
