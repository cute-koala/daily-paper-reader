---
title: "COVTrack++: Learning Open-Vocabulary Multi-Object Tracking from Continuous Videos via a Synergistic Paradigm"
title_zh: COVTrack++：通过协同范式从连续视频中学习开放词汇多目标跟踪
authors: "Zekun Qian, Wei Feng, Ruize Han, Junhui Hou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24016v1"
tags: ["query:vot"]
score: 10.0
evidence: 连续视频中的开放词汇多目标跟踪
tldr: 针对开放词汇多目标跟踪（OVMOT）中连续标注数据匮乏及检测与关联协同不足的问题，本文构建了首个连续标注数据集C-TAO，并提出COVTrack++框架。该框架通过多线索融合、多粒度聚合及时间置信度传播机制，实现了检测与关联的深度协同，显著提升了对未知类别的跟踪性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有的开放词汇多目标跟踪研究受限于缺乏连续标注的视频训练数据，以及缺乏能有效协同检测与关联任务的定制化框架。
method: 提出了包含多线索自适应融合、多粒度层次聚合和时间置信度传播三个模块的协同框架，并构建了标注密度提升26倍的C-TAO数据集。
result: 在TAO数据集上取得了SOTA性能，显著提升了未知类别的关联与定位精度，并在BDD100K数据集上展现出强大的零样本泛化能力。
conclusion: 通过结合高质量连续标注数据与协同式算法框架，COVTrack++有效解决了开放词汇场景下复杂目标的持续跟踪难题。
---

## 摘要
多目标跟踪（MOT）传统上侧重于少数特定类别，限制了其在涉及多样化对象的现实场景中的适用性。开放词汇多目标跟踪（OVMOT）通过实现对任意类别（包括训练期间未见的新颖对象）的跟踪解决了这一问题。然而，目前的进展受到两个挑战的制约：缺乏用于训练的连续标注视频数据，以及缺乏能够协同处理检测与关联的定制化 OVMOT 框架。我们通过构建 C-TAO 解决了数据瓶颈，这是首个用于 OVMOT 的连续标注训练集，其标注密度比原始 TAO 提高了 26 倍，并捕捉了平滑的运动动力学和中间对象状态。针对框架瓶颈，我们提出了 COVTrack++，这是一个协同框架，通过三个模块实现了检测与关联之间的双向互惠机制：（1）多线索自适应融合（MCF）动态平衡外观、运动和语义线索，用于关联特征学习；（2）多粒度分层聚合（MGA）利用密集检测中的分层空间关系，其中可见的子节点（如对象部件）辅助被遮挡的父对象（如整体身体）进行关联特征增强；（3）时间置信度传播（TCP）通过高置信度跟踪对象提升跨帧的低置信度候选对象，从而恢复闪烁的检测结果并稳定轨迹。在 TAO 上的广泛实验证明了其最先进的性能，新颖类别的 TETA 在验证集和测试集上分别达到 35.4% 和 30.5%，相比之前的方法，新颖类别的 AssocA 提高了 4.8%，LocA 提高了 5.8%，并在 BDD100K 上表现出强大的零样本泛化能力。代码和数据集将公开。

## Abstract
Multi-Object Tracking (MOT) has traditionally focused on a few specific categories, restricting its applicability to real-world scenarios involving diverse objects. Open-Vocabulary Multi-Object Tracking (OVMOT) addresses this by enabling tracking of arbitrary categories, including novel objects unseen during training. However, current progress is constrained by two challenges: the lack of continuously annotated video data for training, and the lack of a customized OVMOT framework to synergistically handle detection and association. We address the data bottleneck by constructing C-TAO, the first continuously annotated training set for OVMOT, which increases annotation density by 26x over the original TAO and captures smooth motion dynamics and intermediate object states. For the framework bottleneck, we propose COVTrack++, a synergistic framework that achieves a bidirectional reciprocal mechanism between detection and association through three modules: (1) Multi-Cue Adaptive Fusion (MCF) dynamically balances appearance, motion, and semantic cues for association feature learning; (2) Multi-Granularity Hierarchical Aggregation (MGA) exploits hierarchical spatial relationships in dense detections, where visible child nodes (e.g., object parts) assist occluded parent objects (e.g., whole body) for association feature enhancement; (3) Temporal Confidence Propagation (TCP) recovers flickering detections through high-confidence tracked objects boosting low-confidence candidates across frames, stabilizing trajectories. Extensive experiments on TAO demonstrate state-of-the-art performance, with novel TETA reaching 35.4% and 30.5% on validation and test sets, improving novel AssocA by 4.8% and novel LocA by 5.8% over previous methods, and show strong zero-shot generalization on BDD100K. The code and dataset will be publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《COVTrack++: Learning Open-Vocabulary Multi-Object Tracking from Continuous Videos via a Synergistic Paradigm》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的视频多目标跟踪（MOT）局限于预定义的少数类别，无法应对现实世界中海量的未知类别。开放词汇多目标跟踪（OVMOT）旨在跟踪任意类别的目标，但面临两大瓶颈：
    1.  **数据瓶颈**：现有数据集（如 TAO）标注稀疏（仅 1 FPS），导致模型难以学习连续的运动动力学；而图像数据集缺乏时间结构。
    2.  **框架瓶颈**：缺乏能够协同处理检测（定位与分类）和关联（跨帧匹配）的专用框架，子任务之间缺乏互惠机制。
*   **整体含义**：本文通过构建高质量的连续标注数据集并提出协同增强框架，试图打破 OVMOT 在数据和算法上的双重限制。

### 2. 方法论
论文提出了 **COVTrack++** 框架，核心思想是建立检测与关联之间的双向互惠机制。
*   **C-TAO 数据集构建**：对原始 TAO 训练集进行逐帧重标注，将标注密度提升 26 倍，捕捉平滑的运动轨迹和中间状态（如遮挡过程）。
*   **多线索自适应融合 (MCF)**：
    *   提取外观、运动（位置）和语义三种线索。
    *   引入**双视角置信度估计**：**帧内置信度**（通过 SGN 网络学习线索间的互补性）和**帧间置信度**（利用循环一致性评估特征稳定性）。
    *   动态调整各线索权重，避免在未知类别上因语义或运动噪声导致的跟踪失败。
*   **多粒度层次聚合 (MGA)**：
    *   利用 OVMOT 中常见的嵌套框关系（如“人”包含“背包”）。
    *   通过**空间包含掩码**识别父子节点，利用交叉注意力机制，用稳定的子节点特征增强被遮挡或模糊的父节点特征。
*   **时间置信度传播 (TCP)**：
    *   针对检测“闪烁”问题，构建跨帧二分图。
    *   利用前一帧的高置信度目标，通过**类条件置信度传播**，恢复当前帧中低于阈值的候选框，从而维持轨迹连续性。

### 3. 实验设计
*   **数据集**：
    *   **TAO (Validation & Test)**：主要基准，包含 833 个类别，分为 Base（已知）和 Novel（新颖/未知）类。
    *   **BDD100K**：用于验证模型的零样本（Zero-shot）跨域泛化能力。
*   **评价指标**：采用 TETA（包含 LocA 定位精度、AssocA 关联精度、ClsA 分类精度）。
*   **对比方法**：包括 OVTrack、MASA、SLAck、OVSORT、TRACT、OVTR 等最先进的 OVMOT 方法，以及 ByteTrack、OC-SORT 等通用跟踪器。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA RTX 3090 GPU**。
*   **训练细节**：在 C-TAO 数据集上训练了 **10 个 epoch**。
*   **模型基础**：采用 Faster R-CNN (ResNet-50) 作为检测器骨干网络，且在训练关联模块时冻结了检测器参数。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 TAO 验证集和测试集上进行了全面的性能对比。
    *   **消融实验**：涵盖了模块级（MCF, MGA, TCP 的组合）、组件级（各种置信度、聚合策略、传播约束）的深入分析。
    *   **数据密度分析**：对比了不同标注比例（3.7% vs 7.4% vs 100%）对性能的影响。
    *   **跨域验证**：在 BDD100K 上进行了零样本测试。
*   **充分性评价**：实验设计非常充分且客观。通过控制变量法（如使用相同的检测器）确保了算法改进的公平性，且定性分析（可视化）直观展示了各模块在复杂场景下的作用。

### 6. 主要结论与发现
*   **性能领先**：在 TAO 验证集上，Novel TETA 达到 **35.4%**，比之前最好的方法提升了约 4%。
*   **关键提升点**：AssocA（关联精度）和 LocA（定位精度）提升显著，分别比 SLAck 提高了 4.8% 和 5.8%。
*   **数据价值**：证明了连续标注（C-TAO）对学习运动动力学至关重要，即使是简单的模型在 C-TAO 上训练也能获得显著增益。
*   **协同效应**：检测质量的提升（TCP）能提供更好的跟踪源，而强大的关联特征（MCF/MGA）又能反过来引导检测恢复，形成正向循环。

### 7. 优点
*   **数据贡献**：填补了 OVMOT 领域缺乏高质量连续标注视频数据的空白。
*   **自适应性**：MCF 模块通过双重置信度解决了不同场景下线索可靠性变化的问题，增强了鲁棒性。
*   **结构洞察**：MGA 模块敏锐地捕捉到了开放词汇场景下“父子嵌套”的特殊空间结构，并将其转化为特征增强的优势。
*   **即插即用**：TCP 模块作为一种后处理增强手段，无需重新训练检测器即可改善检测闪烁。

### 8. 不足与局限
*   **分类精度瓶颈**：尽管 TETA 总分提升显著，但 Novel 类的 **ClsA（分类精度）依然极低**（约 3.5%），这反映了开放词汇识别本身依然是该领域的巨大挑战。
*   **计算开销**：引入交叉注意力（MGA）和二分图匹配（TCP）会增加推理时的计算负担，可能影响实时性。
*   **依赖预训练**：模型性能在很大程度上仍受限于冻结的检测器骨干和 CLIP 蒸馏特征的质量。

（完）
