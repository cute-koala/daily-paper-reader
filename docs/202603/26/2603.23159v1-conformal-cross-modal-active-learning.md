---
title: Conformal Cross-Modal Active Learning
title_zh: 符合性跨模态主动学习
authors: "Huy Hoang Nguyen, Cédric Jung, Shirin Salehi, Tobias Glück, Anke Schmeink, Andreas Kugi"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23159v1"
tags: ["query:vlm"]
score: 9.0
evidence: 利用预训练视觉语言模型进行跨模态主动学习
tldr: 本研究针对现有主动学习（AL）方法忽视视觉语言模型（VLM）多模态知识的问题，提出了符合性跨模态采集（CCMA）框架。该框架采用教师-学生架构，利用预训练VLM作为教师，通过符合性预测生成语义一致的不确定性估计，指导纯视觉学生模型的样本选择。结合多样性感知策略，CCMA在多个基准测试中显著提升了数据效率，超越了现有的SOTA基准。
selection_source: fresh_fetch
motivation: 现有的主动学习方法未能充分利用现代视觉语言模型中丰富的多模态知识来优化样本选择。
method: 提出CCMA框架，利用VLM教师提供的符合性校准不确定性评分，结合多样性策略指导学生模型学习。
result: 在多个基准测试中，该方法在数据效率上一致优于仅依赖不确定性或多样性指标的现有SOTA方法。
conclusion: 通过结合多模态符合性评分与多样性选择，可以显著提升视觉模型在有限标注数据下的学习性能。
---

## 摘要
视觉基础模型凭借强大的预训练表示和出色的零样本能力改变了视觉识别领域，但其在高效数据学习方面的潜力仍有待进一步挖掘。主动学习（AL）旨在通过策略性地选择最具信息量的样本进行标注来最小化标注成本，但现有方法在很大程度上忽略了现代视觉语言模型（VLM）中蕴含的丰富多模态知识。我们提出了符合性跨模态采集（CCMA），这是一种通过教师-学生架构连接视觉和语言模态的新型主动学习框架。CCMA 利用预训练的 VLM 作为教师模型，提供具有语义基础的不确定性估计，并经过符合性校准，以指导纯视觉学生模型的样本选择。通过将多模态符合性评分与多样性感知选择策略相结合，CCMA 在多个基准测试中实现了卓越的数据效率。我们的方法始终优于最先进的主动学习基准方法，证明了其相较于仅依赖不确定性或多样性指标的方法具有明显优势。

## Abstract
Foundation models for vision have transformed visual recognition with powerful pretrained representations and strong zero-shot capabilities, yet their potential for data-efficient learning remains largely untapped. Active Learning (AL) aims to minimize annotation costs by strategically selecting the most informative samples for labeling, but existing methods largely overlook the rich multimodal knowledge embedded in modern vision-language models (VLMs). We introduce Conformal Cross-Modal Acquisition (CCMA), a novel AL framework that bridges vision and language modalities through a teacher-student architecture. CCMA employs a pretrained VLM as a teacher to provide semantically grounded uncertainty estimates, conformally calibrated to guide sample selection for a vision-only student model. By integrating multimodal conformal scoring with diversity-aware selection strategies, CCMA achieves superior data efficiency across multiple benchmarks. Our approach consistently outperforms state-of-the-art AL baselines, demonstrating clear advantages over methods relying solely on uncertainty or diversity metrics.

---

## 论文详细总结（自动生成）

### 论文总结：符合性跨模态主动学习 (Conformal Cross-Modal Active Learning, CCMA)

#### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管视觉基础模型（如 DINOv2）和视觉语言模型（VLM，如 CLIP）在特征表示上非常强大，但在**主动学习（Active Learning, AL）**领域，现有方法大多仅依赖纯视觉特征，忽视了 VLM 中丰富的文本-图像对齐语义。
*   **核心挑战**：VLM 的输出通常存在校准不良（mis-calibrated）的问题，且其概率分布与特定任务的分类器不直接兼容，难以直接作为不确定性指标。
*   **核心问题**：如何利用**符合性预测（Conformal Prediction）**技术，将 VLM 的多模态语义结构转化为可靠的、无分布假设的校准不确定性信号，以指导视觉模型的样本选择？

#### 2. 论文提出的方法论 (CCMA)
CCMA 采用**教师-学生架构**，通过五个阶段实现高效样本采集：
*   **多样性子池选择**：在 CLIP 图像嵌入空间进行 k-Means 聚类，从原始未标记池中筛选出一个代表性子池，以降低计算开销并保证初步覆盖。
*   **双预测器构建**：
    *   **教师 (Teacher)**：冻结的 VLM (CLIP)，利用文本原型生成类别概率。
    *   **学生 (Student)**：冻结的视觉骨干 (DINOv2) + 可训练的线性分类头。
*   **符合性校准 (Split Conformal Calibration)**：在留出集上分别校准教师和学生模型，生成**符合性预测集（Prediction Sets）**。这保证了在给定置信度下，预测集包含真实标签的概率，解决了模型校准不良的问题。
*   **跨模态不确定性评分**：
    *   计算教师和学生在预测集并集上的**Jensen-Shannon (JS) 散度**。
    *   引入**参数化置信门控 (Confidence Gate)**：根据教师和学生的 Top-1 置信度动态平衡 JS 散度与学生模型熵（Entropy）的权重。
*   **不确定性加权覆盖选择**：在子池中通过最大化“不确定性加权覆盖目标函数”来选择最终的标注批次，平衡了样本的信息量（不确定性）与代表性（多样性）。

#### 3. 实验设计
*   **数据集**：CIFAR100, Food101, DomainNet-Real, Caltech101, Caltech256。
*   **Benchmark 与对比方法**：
    *   **基准**：Random（随机采样）。
    *   **不确定性方法**：Entropy, Margins, Uncertainty, BALD, pBALD, BADGE, Alfa-mix。
    *   **代表性方法**：Coreset, Typiclust, ProbCover。
    *   **最新方法**：UHerding。
*   **实验设置**：20 轮主动学习迭代，每轮采集固定数量样本（如 CIFAR100 每轮 100 个），重复 5 次实验取均值。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率提示**：作者强调了该方法的计算效率，因为视觉骨干网络（DINOv2 和 CLIP）是冻结的，仅需训练简单的线性分类头。此外，通过子池化策略（Subpooling），显著降低了每轮评分的计算复杂度。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同领域和规模的图像分类基准，对比了 11 种主流 AL 算法，实验设置较为全面。
*   **消融实验**：详细探讨了超参数 $\kappa$（过采样因子）、$\tau$（教师模型温度系数）以及子池化策略、多样性组件对性能的影响。
*   **公平性**：所有方法均在相同的冻结特征空间下运行，确保了对比的客观性。通过 5 个随机种子运行并报告标准差，增强了结果的可信度。

#### 6. 论文的主要结论与发现
*   **性能优越性**：CCMA 在所有数据集上均一致优于 SOTA 基准，尤其在标注预算较低的早期阶段表现突出。
*   **教师-学生协同**：CCMA 在教师和学生存在显著预测差异时效果最好。随着学生模型逐渐对齐教师，AL 的重点会从不确定性转向覆盖优化。
*   **标注效率**：在 Food101 和 DomainNet-Real 上，CCMA 达到目标精度所需的标注样本数显著少于其他方法（例如，在 Food101 上达到 90% 精度仅需不到 1.8K 样本）。

#### 7. 优点
*   **鲁棒的校准**：引入符合性预测，使得跨模态的不确定性估计不再依赖于模型本身的校准质量，具有分布无关的特性。
*   **动态平衡**：置信门控机制无需人工调参，能自动在“信任教师语义”和“信任学生自身判断”之间切换。
*   **计算友好**：通过子池化和冻结骨干网络，适合在大规模数据集上部署。

#### 8. 不足与局限
*   **依赖预训练 VLM**：方法的有效性高度依赖于教师模型（CLIP）的质量。如果 VLM 在特定领域（如医学影像或高度专业化工业数据）表现不佳，其引导信号可能失效。
*   **性能饱和风险**：实验分析显示，当学生模型迅速与教师模型达成一致（如在 Caltech 数据集上）时，跨模态不确定性的增益会迅速消失。
*   **应用范围**：目前仅在图像分类任务上验证，尚未扩展到目标检测或分割等更复杂的视觉任务。

（完）
