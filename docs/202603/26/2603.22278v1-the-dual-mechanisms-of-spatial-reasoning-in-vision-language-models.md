---
title: The Dual Mechanisms of Spatial Reasoning in Vision-Language Models
title_zh: 视觉语言模型中空间推理的双重机制
authors: "Kelly Cui, Nikhil Prakash, Ayush Raina, David Bau, Antonio Torralba, Tamar Rott Shaham"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22278v1"
tags: ["query:vlm"]
score: 10.0
evidence: 视觉语言模型中的空间推理与目标关联机制
tldr: 本研究探讨了视觉语言模型（VLM）如何处理物体间的空间关系。研究发现模型依赖两种并行机制：一是语言模型骨干网络在中间层处理空间关系，但仅起次要作用；二是视觉编码器作为主导，其生成的表示直接编码了物体布局。关键发现是空间信号在视觉标记中呈全局分布，甚至延伸至背景区域。通过增强视觉编码器的全局空间表示，可以有效提升模型在自然图像上的空间推理能力，揭示了视觉编码器在空间认知中的核心地位。
selection_source: fresh_fetch
motivation: 旨在探究视觉语言模型在处理物体属性与空间关系关联时的内部计算位置与机制。
method: 通过分析模型内部表示，对比研究了语言模型骨干网络与视觉编码器在空间推理中的贡献。
result: 发现视觉编码器是空间信息的主导来源，且空间信号全局分布于包括背景在内的所有视觉标记中。
conclusion: 视觉编码器在空间推理中发挥核心作用，增强其全局空间表示能显著改善模型性能。
---

## 摘要
许多多模态任务（如图像描述和视觉问答）要求视觉语言模型 (VLM) 将物体与其属性及空间关系相关联。然而，目前尚不清楚这些关联在 VLM 内部是如何以及在何处计算的。在这项工作中，我们展示了 VLM 依赖两种并发机制来表示此类关联。在语言模型主干中，中间层在对应于物体的视觉 token 之上表示与内容无关的空间关系。然而，这种机制在塑造模型预测方面仅起次要作用。相反，空间信息的主要来源源自视觉编码器，其表示编码了物体的布局，并被语言模型主干直接利用。值得注意的是，这种空间信号全局分布在视觉 token 中，从物体区域延伸到周围的背景区域。我们表明，在所有图像 token 中全局增强这些源自视觉的空间表示，可以提高在自然图像上的空间推理性能。总之，我们的结果阐明了 VLM 内部如何计算空间关联，并强调了视觉编码器在实现空间推理中的核心作用。

## Abstract
Many multimodal tasks, such as image captioning and visual question answering, require vision-language models (VLMs) to associate objects with their properties and spatial relations. Yet it remains unclear where and how such associations are computed within VLMs. In this work, we show that VLMs rely on two concurrent mechanisms to represent such associations. In the language model backbone, intermediate layers represent content-independent spatial relations on top of visual tokens corresponding to objects. However, this mechanism plays only a secondary role in shaping model predictions. Instead, the dominant source of spatial information originates in the vision encoder, whose representations encode the layout of objects and are directly exploited by the language model backbone. Notably, this spatial signal is distributed globally across visual tokens, extending beyond object regions into surrounding background areas. We show that enhancing these vision-derived spatial representations globally across all image tokens improves spatial reasoning performance on naturalistic images. Together, our results clarify how spatial association is computed within VLMs and highlight the central role of vision encoders in enabling spatial reasoning.

---

## 论文详细总结（自动生成）

这篇论文《The Dual Mechanisms of Spatial Reasoning in Vision-Language Models》深入探讨了视觉语言模型（VLM）在处理空间关系时的内部工作机制。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 VLM 在多模态任务中表现优异，但空间推理（如识别“绿色方块左边的物体是什么”）仍是其薄弱环节。
*   **核心问题**：模型内部是在哪里以及如何将物体与其空间位置及属性进行“绑定”（Variable Binding）的？这种能力是源自视觉编码器（Vision Encoder），还是由语言模型（LM）主干网络在推理过程中构建的？
*   **研究背景**：先前研究发现纯语言模型通过“顺序表示”来绑定实体，本研究旨在确认 VLM 是否继承或演化出了类似的机制。

### 2. 方法论
*   **核心思想**：通过**因果中介分析（Causal Mediation Analysis）**和**线性探测（Linear Probing）**来定位和验证空间信息的来源。
*   **关键技术细节**：
    *   **线性探测**：训练轻量级分类器，从视觉 token 的嵌入中提取物体的空间顺序信息。
    *   **交换干预（Interchange Intervention）**：也称为“激活补丁”（Activation Patching）。将一个样本（如“左边是红球”）的内部激活值替换到另一个样本（如“右边是蓝球”）中，观察模型输出是否随之改变，以此确立因果关系。
    *   **双重机制模型**：
        1.  **视觉端机制（主导）**：视觉编码器生成的表示直接编码了全局布局。
        2.  **语言端机制（辅助）**：LM 主干在中间层形成与内容无关的顺序表示，作为视觉信号缺失时的备份。
    *   **空间增强干预**：基于探测到的空间方向，通过公式 $emb_t \leftarrow emb_t + \alpha \cdot probe_i$ 增强视觉嵌入中的空间信号，从而提升推理性能。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：Squares（颜色方块）、Shapes（几何形状）、Objects（真实物体图像），用于受控环境下的机制分析。
    *   **自然数据集**：使用 **What’sUp** 基准测试的控制子集，评估模型在真实图像上的空间推理能力。
*   **对比模型**：Qwen2-VL-7B-Instruct 和 Gemma-3-4b-it。
*   **实验对比**：对比了“仅修补物体 token”与“修补全局条带 token”的效果，以及在移除视觉空间信息前后的模型表现。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **计算负载分析**：由于该研究主要侧重于模型推理阶段的干预实验和轻量级线性探测器的训练（仅需少量样本），其计算需求远低于模型全量微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个数据集上进行了详尽测试。
    *   每项干预实验通常使用 50 对精心构造的“清洁-反事实”样本。
    *   探测器训练使用了 90 张图像，并在 30 张图像上进行了验证。
*   **充分性评价**：实验设计非常充分且严谨。研究者不仅观察到了相关性（通过探测），还通过消融实验（移除背景信息、移除视觉顺序信息）和因果干预验证了结论的可靠性。跨模型的测试证明了结论的普适性。

### 6. 主要结论与发现
*   **视觉编码器是核心**：空间信息的主要来源是视觉编码器，而非 LM 主干。
*   **全局分布特性**：空间信号并非只存在于物体所在的 token 中，而是**全局分布**在包括背景在内的所有视觉 token 中，呈现出“条带状”的分布模式。
*   **LM 的备份作用**：当视觉编码器的空间信息被人为移除时，LM 主干能在中间层自主生成顺序信息来尝试完成任务。
*   **性能提升**：通过简单地增强视觉 token 中的空间探测方向，在不进行任何微调的情况下，纠正了 What’sUp 数据集上 30% 到 50% 的错误预测。

### 7. 优点
*   **机制揭示深入**：首次清晰地界定了 VLM 空间推理中视觉与语言组件的分工。
*   **发现新现象**：揭示了空间信息在背景 token 中的“条带状”分布，挑战了“信息仅存在于物体区域”的直觉。
*   **实用性强**：提出的增强干预方法无需重新训练模型，为提升 VLM 性能提供了新思路。

### 8. 不足与局限
*   **场景复杂度有限**：实验主要基于简单的水平或垂直排列，对于物体高度重叠、遮挡或复杂的 3D 空间关系探讨较少。
*   **数据集偏差**：What’sUp 数据集的实验主要集中在水平方向，缺乏对全方位空间关系的自然图像验证。
*   **模型范围**：虽然测试了两个先进模型，但尚未在超大规模模型（如 70B 以上）或闭源模型（如 GPT-4o）上验证该机制的一致性。

（完）
