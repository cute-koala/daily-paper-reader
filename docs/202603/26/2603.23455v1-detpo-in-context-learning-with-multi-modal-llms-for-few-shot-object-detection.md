---
title: "DetPO: In-Context Learning with Multi-Modal LLMs for Few-Shot Object Detection"
title_zh: DetPO：基于多模态大语言模型的小样本目标检测上下文学习
authors: "Gautam Rajendrakumar Gare, Neehar Peri, Matvei Popov, Shruti Jain, John Galeotti, Deva Ramanan"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23455v1"
tags: ["query:vlm"]
score: 9.0
evidence: 利用多模态大模型进行上下文学习以实现视觉定位和目标检测
tldr: 针对多模态大模型（MLLM）在处理分布外目标检测任务时难以有效利用上下文示例的问题，本文提出了DetPO。这是一种无梯度的测试时提示优化方法，旨在通过少样本视觉示例来精炼文本提示，并校准预测置信度。该方法无需对模型进行昂贵的微调，在Roboflow20-VL和LVIS等基准测试中显著提升了检测精度，为黑盒模型在少样本场景下的应用提供了高效方案。
selection_source: fresh_fetch
motivation: 现有大语言模型在少样本目标检测中难以有效利用上下文示例，且对黑盒模型进行微调成本过高。
method: 提出DetPO，一种无梯度的测试时优化方法，通过在少样本示例上最大化检测准确率并校准置信度来精炼文本提示。
result: "在Roboflow20-VL和LVIS数据集上，DetPO显著提升了通用多模态大模型的检测性能，最高比现有黑盒方法提升了9.7%。"
conclusion: DetPO为提升黑盒多模态大模型的少样本目标检测能力提供了一种高效且无需微调的提示优化方案。
---

## 摘要
多模态大语言模型（MLLMs）在 OdinW-13 和 RefCOCO 等热门目标检测基准上展现出强大的视觉定位能力。然而，最先进的模型在泛化到其预训练中不常见的分布外类别、任务和成像模态时仍然面临挑战。虽然上下文提示是提升多样化任务性能的常用策略，但我们发现，与仅使用类别名称进行提示相比，它往往导致更低的检测准确率。这表明当前的 MLLMs 尚无法有效利用小样本视觉示例和丰富的文本描述进行目标检测。由于前沿 MLLMs 通常仅能通过 API 访问，且在消费级硬件上微调最先进的开源权重模型成本极高，因此我们探索了针对小样本目标检测的黑盒提示优化。为此，我们提出了检测提示优化（DetPO），这是一种无梯度的测试时优化方法，通过在小样本视觉训练示例上最大化检测准确率并校准预测置信度来精炼纯文本提示。我们提出的方法在 Roboflow20-VL 和 LVIS 上使通用 MLLMs 获得了持续的改进，性能优于之前的黑盒方法高达 9.7%。我们的代码可在 https://github.com/ggare-cmu/DetPO 获取。

## Abstract
Multi-Modal LLMs (MLLMs) demonstrate strong visual grounding capabilities on popular object detection benchmarks like OdinW-13 and RefCOCO. However, state-of-the-art models still struggle to generalize to out-of-distribution classes, tasks and imaging modalities not typically found in their pre-training. While in-context prompting is a common strategy to improve performance across diverse tasks, we find that it often yields lower detection accuracy than prompting with class names alone. This suggests that current MLLMs cannot yet effectively leverage few-shot visual examples and rich textual descriptions for object detection. Since frontier MLLMs are typically only accessible via APIs, and state-of-the-art open-weights models are prohibitively expensive to fine-tune on consumer-grade hardware, we instead explore black-box prompt optimization for few-shot object detection. To this end, we propose Detection Prompt Optimization (DetPO), a gradient-free test-time optimization approach that refines text-only prompts by maximizing detection accuracy on few-shot visual training examples while calibrating prediction confidence. Our proposed approach yields consistent improvements across generalist MLLMs on Roboflow20-VL and LVIS, outperforming prior black-box approaches by up to 9.7%. Our code is available at https://github.com/ggare-cmu/DetPO

---

## 论文详细总结（自动生成）

以下是对论文《DetPO: In-Context Learning with Multi-Modal LLMs for Few-Shot Object Detection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多模态大语言模型（MLLM）在通用目标检测上表现优异，但在面对**分布外（OOD）**任务（如医疗影像、工业缺陷、航拍等）时泛化能力差。
*   **核心矛盾**：
    *   **上下文学习（ICL）失效**：在 Prompt 中直接加入视觉示例（Few-shot images）往往反而降低检测精度，说明模型无法有效利用多模态上下文。
    *   **微调成本高昂**：前沿模型（如 Gemini）多为闭源 API，而开源大模型（如 Qwen-VL 72B）在消费级硬件上微调成本极高。
*   **整体含义**：本文旨在探索一种**黑盒提示优化（Black-box Prompt Optimization）**方法，通过纯文本提示词的迭代精炼，让 MLLM 在不改变权重的情况下，仅凭少量示例即可实现高效的小样本目标检测。

### 2. 方法论：核心思想与关键技术
DetPO（Detection Prompt Optimization）是一种无梯度的测试时优化框架，其核心流程如下：
*   **对比提示精炼（Contrastive Prompt Refinement）**：
    1.  **初始生成**：让模型观察正样本图片，总结该类别的视觉特征，并与负样本类别对比，生成初步的文本描述。
    2.  **错误驱动迭代**：在训练集上运行推理，识别出最严重的**假阳性（FP）**和**假阴性（FN）**。
    3.  **提示词修正**：引导模型修改描述——要求其“包含”被漏掉的 FN 特征，并“排除”导致 FP 的干扰特征。
*   **置信度校准（Confidence Score Estimation）**：
    *   由于 MLLM 默认不输出检测框分数，DetPO 探索了两种方案：
        *   **自报分数**：直接在 Prompt 中要求模型为每个框输出 0.0-1.0 的分数。
        *   **VQA Score**：将预测框叠加在原图上，询问模型“框内是否为某类物体？”，利用“Yes/No”Token 的 Logits 概率作为最终置信度。
*   **候选筛选**：在验证集上评估迭代过程中的多个 Prompt 候选，选择 mAP 最高的作为最终提示词。

### 3. 实验设计
*   **数据集**：
    *   **Roboflow20-VL (RF20-VL)**：包含 20 个子数据集，涵盖航拍、文档、动植物、工业、医疗、体育等领域。
    *   **LVIS Rare 50**：从 LVIS 中选取 50 个最稀有的类别进行 10-shot 测试。
*   **对比方法**：
    *   **专家模型**：GroundingDINO, LLMDet, SAM3, MQ-GLIP, YOLO-E。
    *   **通用模型基准**：Qwen2.5-VL (7B/72B), Qwen3-VL (8B/30B), Gemini 3 Pro。
    *   **提示优化框架**：GEPA, MIPROv2。
*   **评价指标**：COCO 风格的 mAP。

### 4. 资源与算力
*   **算力说明**：论文**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：文中提到 DetPO 相比通用优化器 GEPA，在特定任务上 Token 消耗减少了 **81%**，运行速度快了 **1.2 倍**（例如在航拍数据集上优化过程约需 10 分钟）。
*   **硬件背景**：作者强调该方法旨在规避昂贵的微调，因此更侧重于 API 调用和轻量化推理。

### 5. 实验数量与充分性
*   **实验规模**：在 20 个不同领域的子数据集上进行了详尽测试，并扩展到了 LVIS 长尾数据集。
*   **消融实验**：针对置信度估计方法（自报 vs VQA Score vs SigLIPv2）、K-shot 数量（3/5/10-shot）、迭代次数等进行了多组消融。
*   **客观性**：使用了 TIDE 工具进行错误诊断，分析了 FP 和 FN 的分布变化，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能飞跃**：DetPO 显著提升了 MLLM 的检测能力，在 Gemini 3 Pro 上达到 26.4 mAP，优于所有专家模型和基准 MLLM。
*   **ICL 悖论解决**：通过将视觉示例转化为精炼的文本描述，解决了直接输入图片导致性能下降的问题。
*   **置信度关键性**：VQA Score 虽增加了计算量，但对降低假阳性、提升 mAP 至关重要。
*   **趋势**：虽然目前仍略逊于全量微调的专家模型，但随着闭源模型能力的提升，DetPO 展示了超越微调模型的潜力。

### 7. 优点
*   **黑盒适用性**：无需访问模型权重或梯度，非常适合通过 API 使用的闭源模型。
*   **任务针对性**：相比通用的提示优化工具（如 DSPy 中的 MIPROv2），DetPO 专门针对检测任务中的 FP/FN 进行优化，效率更高。
*   **可解释性**：优化的结果是自然语言描述，用户可以直观看到模型学习到了哪些区分性特征。

### 8. 不足与局限
*   **优化开销**：虽然推理成本不变，但“提示词发现”阶段需要多次迭代推理，对于类别极多的任务，时间成本较高。
*   **VQA Score 延迟**：VQA 验证步骤的耗时随预测框数量线性增长，限制了实时应用。
*   **数据隐私/泄露**：依赖闭源 API 进行优化可能存在数据被厂商收集用于后续训练的风险。
*   **绝对精度**：在某些复杂场景下，其定位精度（Localization）仍是瓶颈，且整体性能尚未全面碾压经过深度微调的专家模型。

（完）
