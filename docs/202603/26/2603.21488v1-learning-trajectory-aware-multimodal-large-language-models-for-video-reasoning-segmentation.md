---
title: Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation
title_zh: 学习轨迹感知的多模态大语言模型用于视频推理分割
authors: "Jingnan Luo, Mingqi Gao, Jun Liu, Bin-Bin Gao, Feng Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21488v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于视频推理分割和轨迹感知的多模态大模型
tldr: 针对视频推理分割中传统方法在复杂动态场景下轨迹感知能力不足的问题，本文提出了TrajSeg框架。该框架基于多模态大模型，引入了双向文本-轨迹对齐机制，通过定位和描述任务增强模型对物体轨迹的理解。结合帧级内容集成模块与统一掩码解码器，实现了高效的端到端掩码生成。实验表明，TrajSeg在多个视频分割基准上均达到SOTA水平，显著提升了复杂动态下的分割性能。
selection_source: fresh_fetch
motivation: 现有方法依赖单向且隐式的文本-轨迹对齐，在面对剧烈视频动态时难以准确感知物体轨迹。
method: 引入双向文本-轨迹对齐机制，并利用帧级内容集成模块和统一掩码解码器实现从轨迹到掩码的端到端生成。
result: TrajSeg在引用和推理视频分割数据集的所有指标上均超越了现有方法，展现了卓越的性能。
conclusion: 通过增强轨迹感知和简化模型架构，TrajSeg为视频推理分割任务提供了一个高效且统一的解决方案。
---

## 摘要
多模态大语言模型（MLLMs）的繁荣激发了对视频推理分割的需求，该任务旨在根据人类指令对视频对象进行分割。以往的研究依赖于单向且隐式的文本-轨迹对齐，在面对剧烈的视频动态时难以进行轨迹感知。在这项工作中，我们提出了 TrajSeg，这是一个基于 MLLMs 构建的简单且统一的框架。具体而言，我们引入了双向文本-轨迹对齐，其中 MLLMs 接受定位意图（文本到轨迹）和描述意图（轨迹到文本）的指令。通过这种方式，MLLMs 可以从增强的对应关系中获益，并更好地感知视频中的对象轨迹。从轨迹生成掩码是通过帧级内容整合（FCI）模块和统一掩码解码器实现的。前者将 MLLM 解析的轨迹级标记（token）适配到特定帧的信息。后者将所有帧的分割统一到一个单一结构中，使所提出的框架得以简化并实现端到端训练。在指代和推理视频分割数据集上的广泛实验证明了 TrajSeg 的有效性，它在所有指标上都优于现有的视频推理分割方法。代码将在 https://github.com/haodi19/TrajSeg 公开。

## Abstract
The prosperity of Multimodal Large Language Models (MLLMs) has stimulated the demand for video reasoning segmentation, which aims to segment video objects based on human instructions. Previous studies rely on unidirectional and implicit text-trajectory alignment, which struggles with trajectory perception when faced with severe video dynamics. In this work, we propose TrajSeg, a simple and unified framework built upon MLLMs. Concretely, we introduce bidirectional text-trajectory alignment, where MLLMs accept grounding-intended (text-to-trajectory) and captioning-intended (trajectory-to-text) instructions. This way, MLLMs can benefit from enhanced correspondence and better perceive object trajectories in videos. The mask generation from trajectories is achieved via a frame-level content integration (FCI) module and a unified mask decoder. The former adapts the MLLM-parsed trajectory-level token to frame-specific information. The latter unifies segmentation for all frames into a single structure, enabling the proposed framework to be simplified and end-to-end trainable. Extensive experiments on referring and reasoning video segmentation datasets demonstrate the effectiveness of TrajSeg, which outperforms all video reasoning segmentation methods on all metrics. The code will be publicly available at https://github.com/haodi19/TrajSeg.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation》的结构化深度总结：

### 1. 核心问题与整体含义
该论文关注的是**视频推理分割（Video Reasoning Segmentation）**任务，即根据复杂的人类指令（如“分割那个正在欺负另一只小猫的猫”）在视频中生成目标掩码。
*   **研究动机**：现有的视频推理分割方法大多直接从图像模型扩展而来，面临两大挑战：
    1.  **轨迹感知能力弱**：以往方法依赖单向且隐式的“文本到轨迹”对齐，在剧烈动态场景下难以准确捕捉物体运动。
    2.  **架构复杂且非端到端**：通常采用“分割+跟踪”的二阶段流水线，关键帧和非关键帧使用不同的模型处理，导致时空一致性差且训练/部署成本高。
*   **核心目标**：提出一个轨迹感知且统一的端到端框架 **TrajSeg**，以提升模型对视频动态的理解和分割的一致性。

### 2. 方法论
TrajSeg 框架由四个核心组件组成：MLLM、轨迹编码器、帧级内容集成（FCI）模块和统一掩码生成器。
*   **双向文本-轨迹对齐**：
    *   除了传统的**定位意图**（文本 $\rightarrow$ 轨迹），引入了**描述意图**（轨迹 $\rightarrow$ 文本）。
    *   **轨迹编码器**：通过 ROI-Align 提取物体在多帧中的特征，拼接后通过线性层映射为轨迹特征标记（token）。
    *   MLLM 被训练用于根据轨迹特征生成文本描述，这种双向任务迫使模型显式理解物体的动态属性。
*   **帧级内容集成（FCI）模块**：
    *   MLLM 输出的轨迹级标记富含语义但缺乏帧内空间细节。FCI 利用交叉注意力机制，将轨迹标记与特定帧的视觉特征融合，生成**帧级目标标记**。
*   **统一掩码生成器**：
    *   将关键帧（基于指令分割）和非关键帧（基于记忆跟踪）的分割统一在同一个解码器结构中。
    *   关键帧使用帧级目标标记作为提示；非关键帧使用包含前序帧信息的“记忆库”作为提示。这种设计实现了端到端训练，并保证了时空一致性。

### 3. 实验设计
*   **数据集**：
    *   **指代分割（RVOS）**：Ref-YouTube-VOS, Ref-DAVIS, MeViS（侧重运动描述）。
    *   **推理分割**：ReVOS（包含复杂推理指令）。
*   **Benchmark 与对比方法**：
    *   对比了传统的 RVOS 方法（如 MTTR, ReferFormer, ReferDINO, SSA）。
    *   对比了基于 MLLM 的推理分割方法（如 TrackGPT, VISA, VideoLISA, AL-Ref-SAM2）。
*   **评价指标**：Jaccard 指数（J）、F-measure（F）及其平均值（J&F）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 GPU** 进行训练（具体型号未标明，但考虑到 7B 模型和 DeepSpeed 框架，推测为 A100 或 H100 级别）。
*   **训练细节**：
    *   **预训练阶段**：在图像数据集上训练 10 个 epoch。
    *   **主训练阶段**：在视频数据集上混合训练 14 个 epoch。
    *   **参数配置**：使用 AdamW 优化器，学习率为 0.0003，Batch Size 为 80，采用了 LoRA 微调技术。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流视频分割基准上进行了测试，涵盖了从简单指代到复杂推理的多种场景。
*   **消融实验**：针对双向对齐、FCI 模块、关键帧数量（KF=1, 5, 10）以及时空鲁棒性进行了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅对比了同参数规模（7B）的模型，还与更大规模（13B）的模型进行了对比，证明了其架构的优越性。可视化分析（注意力图、长视频表现）进一步增强了结果的说服力。

### 6. 主要结论与发现
*   **性能领先**：TrajSeg 在所有测试基准上均达到了 SOTA 水平，尤其在 MeViS 和 ReVOS 等需要复杂动态理解的数据集上优势明显。
*   **双向对齐的价值**：引入“轨迹到文本”任务显著提升了模型在推理子集上的表现，证明了显式轨迹建模的重要性。
*   **统一架构的优势**：相比于二阶段方法，统一掩码生成器在减少参数量的同时，通过减少误差累积提升了时空稳定性。
*   **长视频鲁棒性**：TrajSeg 在长视频中的表现优于 VISA，显示出更强的长程轨迹解析能力。

### 7. 优点
*   **创新性对齐机制**：通过双向对齐解决了 MLLM 在视频任务中“感知模糊”的问题。
*   **架构简洁高效**：统一了分割与跟踪任务，实现了真正的端到端训练，降低了系统复杂性。
*   **即插即用**：FCI 模块有效地桥接了高层语义标记与底层空间特征。

### 8. 不足与局限
*   **采样限制**：受限于计算资源，模型在训练和推理时只能采样有限数量的帧（如 10 帧），这可能限制了其在极长视频或极高帧率场景下的精细表现。
*   **歧义处理**：在面对极其模糊或涉及多层嵌套逻辑的指令时，模型仍会出现定位错误（如 Failure Case 所示）。
*   **推理深度**：目前主要依赖静态提示对齐，尚未引入如强化学习（RLHF/GRPO）等更深层次的思维链推理技术。

（完）
