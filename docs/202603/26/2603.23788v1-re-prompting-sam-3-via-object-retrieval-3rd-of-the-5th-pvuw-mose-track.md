---
title: "Re-Prompting SAM 3 via Object Retrieval: 3rd of the 5th PVUW MOSE Track"
title_zh: 通过对象检索对 SAM 3 进行重新提示：第五届 PVUW MOSE 赛道第三名方案
authors: "Mingqi Gao, Sijie Li, Jungong Han"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23788v1"
tags: ["query:vot"]
score: 9.0
evidence: 基于SAM 3的视频目标分割与跟踪
tldr: "本研究针对PVUW 2026挑战赛中复杂的半监督视频对象分割任务，提出了一种基于SAM 3的自动重提示框架。针对目标消失重现、剧烈形变及同类干扰等难题，该方法利用SAM 3检测器识别候选目标，并通过DINOv3进行目标级匹配以检索可靠锚点，最后将多锚点注入追踪器进行传播。该方案在MOSEv2测试集中取得了51.17%的J&F得分，位列第三。"
selection_source: fresh_fetch
motivation: 旨在解决视频对象分割中目标频繁消失、剧烈形变以及同类干扰物导致的追踪失效问题。
method: 结合SAM 3检测器与DINOv3特征匹配，通过构建变换感知的特征池实现自动重提示与多锚点传播。
result: "在MOSEv2赛道测试集上实现了51.17%的J&F得分，最终排名第三。"
conclusion: 证明了通过目标检索进行自动重提示能显著提升SAM 3在复杂视频场景下的鲁棒性。
---

## 摘要
本技术报告探讨了 PVUW 2026 挑战赛中的 MOSEv2 赛道，该赛道旨在解决复杂的半监督视频对象分割问题。基于 SAM 3，我们开发了一个自动重新提示（re-prompting）框架，以提高在目标消失与重现、剧烈形变以及强同类别干扰项下的鲁棒性。我们的方法首先将 SAM 3 检测器应用于后续帧以识别同类别的候选对象，然后利用具有变换感知能力的目标特征池进行基于 DINOv3 的对象级匹配，从而检索可靠的目标锚点。这些锚点与首帧掩码一起被重新注入 SAM 3 追踪器，从而实现多锚点传播，而非仅仅依赖于初始提示。这一简单的方法直接助力解决了 MOSEv2 的若干核心挑战。我们的方案在测试集上达到了 51.17% 的 J&F 分数，在 MOSEv2 赛道中排名第三。

## Abstract
This technical report explores the MOSEv2 track of the PVUW 2026 Challenge, which targets complex semi-supervised video object segmentation. Built on SAM~3, we develop an automatic re-prompting framework to improve robustness under target disappearance and reappearance, severe transformation, and strong same-category distractors. Our method first applies the SAM~3 detector to later frames to identify same-category object candidates, and then performs DINOv3-based object-level matching with a transformation-aware target feature pool to retrieve reliable target anchors. These anchors are injected back into the SAM~3 tracker together with the first-frame mask, enabling multi-anchor propagation rather than relying solely on the initial prompt. This simple directly benefits several core challenges of MOSEv2. Our solution achieves a J&F of 51.17% on the test set, ranking 3rd in the MOSEv2 track.

---

## 论文详细总结（自动生成）

以下是对论文《Re-Prompting SAM 3 via Object Retrieval: 3rd of the 5th PVUW MOSE Track》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：半监督视频对象分割（VOS）要求在给定第一帧掩码的情况下，在后续视频帧中持续分割目标。
*   **核心问题**：现有的 VOS 方法（如 SAM 2/3）主要依赖第一帧作为唯一的视觉锚点进行内存传播。但在 MOSEv2 等复杂场景中，目标频繁消失又重现、经历剧烈形变，且存在大量同类别干扰物，导致追踪器容易发生“漂移”或彻底丢失目标。
*   **研究动机**：通过在视频后续帧中自动寻找并注入额外的可靠锚点（Anchor），将单锚点传播扩展为多锚点传播，以增强模型在长视频和复杂干扰下的鲁棒性。

### 2. 论文提出的方法论
该方法基于 **SAM 3** 框架，提出了一个两阶段的自动重提示（Re-Prompting）流程：
*   **阶段一：自动锚点挖掘（Anchor Mining）**
    1.  **候选提取**：利用 SAM 3 的检测器分支，根据第一帧的目标类别，在视频所有后续帧中识别并分割出同类别的候选对象。
    2.  **特征匹配**：使用 **DINOv3** 提取第一帧目标的特征，并构建一个“变换感知特征池”（包含目标的翻转、旋转版本）。
    3.  **对象检索**：计算候选对象与特征池的余弦相似度，选择相似度最高且在时间分布上具有多样性（抑制相邻帧冗余）的候选者作为额外锚点。
*   **阶段二：重提示追踪（Re-Prompting Tracking）**
    *   将第一帧的真实掩码与挖掘出的后续帧伪掩码（Pseudo-prompts）共同注入 SAM 3 追踪器。追踪器不再仅从起点出发，而是从多个分布在视频中的高置信度锚点开始传播。

### 3. 实验设计
*   **数据集**：主要使用 **MOSEv2** 数据集，该数据集以高频率遮挡、目标消失重现和严峻的同类干扰著称。
*   **Benchmark**：PVUW 2026 Challenge 的 MOSEv2 赛道测试集。
*   **对比方法**：与赛道排行榜上的其他顶尖方案（如 HITsz_Dragon, tobedone 等）进行定量对比；同时进行了“仅首帧传播”与“重提示传播”的定性对比。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：在 MOSEv2 训练集上微调了 10 个 Epoch，Batch Size 为 1。
*   **分辨率**：训练时输入分辨率为 1008，推理时为 1288。
*   **学习率**：视觉骨干网络为 $3 \times 10^{-6}$，其余参数为 $5 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：论文主要展示了在挑战赛测试集上的最终性能（J&F 得分）以及针对特定挑战场景（如目标重现）的可视化对比。
*   **充分性评价**：作为一篇技术报告，其核心在于验证竞赛方案的有效性。虽然缺乏详尽的消融实验（例如不同锚点数量对结果的影响），但通过与基准 SAM 3 的定性对比，直观地证明了重提示机制在解决目标丢失问题上的优越性。实验结果客观反映了该方法在处理复杂 VOS 任务中的竞争力。

### 6. 主要结论与发现
*   **多锚点优势**：自动挖掘并注入后续帧锚点能显著缓解长视频中的身份漂移问题。
*   **性能表现**：该方案在 MOSEv2 测试集上取得了 **51.17% 的 J&F 得分**，最终排名**第三**。
*   **鲁棒性提升**：结合 DINOv3 的对象级检索能有效区分目标与同类别干扰物，尤其是在目标消失后重新出现的情况下。

### 7. 优点
*   **即插即用**：该策略不需要对 SAM 3 的核心架构进行复杂修改，具有较强的通用性。
*   **自动化程度高**：无需人工干预即可在长视频中定位关键帧并提供提示。
*   **变换感知**：通过构建特征池处理目标的姿态和旋转变化，增强了匹配的准确度。

### 8. 不足与局限
*   **极端情况失效**：定性实验显示，当目标在视频末尾出现极端外观变化时，该方法仍可能无法正确找回目标。
*   **计算开销**：在所有帧上运行检测器和 DINOv3 特征提取会增加推理阶段的计算负担。
*   **特征表达限制**：目前的特征匹配仍基于预训练模型，对于某些特定目标的细粒度区分能力仍有提升空间。

（完）
