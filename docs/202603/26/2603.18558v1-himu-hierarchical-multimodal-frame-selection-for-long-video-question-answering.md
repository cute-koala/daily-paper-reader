---
title: "HiMu: Hierarchical Multimodal Frame Selection for Long Video Question Answering"
title_zh: HiMu：面向长视频问答的分层多模态帧选择
authors: "Dan Ben-Ami, Gabriele Serussi, Kobi Cohen, Chaim Baskin"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18558v1"
tags: ["query:vlm"]
score: 9.0
evidence: 使用大型视觉语言模型进行长视频问答的层级帧选择
tldr: 针对长视频问答中帧选择的效率与准确性权衡问题，本文提出HiMu框架。它通过LLM将复杂查询分解为层次化逻辑树，利用多模态专家（视觉、音频、OCR等）处理原子谓词，并结合模糊逻辑算子进行时序组合。该方法无需训练，在保持高精度的同时显著降低了计算开销，在多个基准测试上超越了现有的相似度匹配和智能体方法。
selection_source: fresh_fetch
motivation: 现有的长视频帧选择方法在快速相似度匹配（丢失结构信息）与高成本智能体推理（计算昂贵）之间存在难以调和的矛盾。
method: 利用LLM将查询分解为逻辑树，通过多模态专家提取信号，并使用模糊逻辑算子在时序上组合这些信号以生成连续的满意度曲线。
result: 在Video-MME等多个榜单上，HiMu以更少的帧数和约1/10的计算量，超越了现有的竞争选择器和复杂的智能体系统。
conclusion: HiMu证明了通过层次化逻辑分解和多模态信号组合，可以在不进行额外训练的情况下实现高效且精准的长视频理解。
---

## 摘要
长视频问答需要对扩展的时间上下文进行推理，这使得帧选择对于受限于有限上下文窗口的大型视觉语言模型（LVLMs）至关重要。现有方法面临着严峻的权衡：基于相似性的选择器速度快，但会将组合式查询压缩为单个稠密向量，从而丢失了子事件的顺序和跨模态绑定；基于智能体（agent-based）的方法通过迭代式 LVLM 推理来恢复这种结构，但成本极高。我们提出了 HiMu，这是一个无需训练的框架，旨在弥合这一差距。通过单次纯文本 LLM 调用，将查询分解为分层逻辑树，其叶节点是原子谓词，每个谓词都被路由到涵盖视觉（CLIP、开放词汇检测、OCR）和音频（ASR、CLAP）的轻量级专家模型。生成的信号经过归一化和时间平滑处理以对齐不同模态，并通过强制执行时间顺序和邻接关系的模糊逻辑算子自底向上进行组合，从而产生连续的满意度曲线。在 Video-MME、LongVideoBench 和 HERBench-Lite 上的评估表明，HiMu 提升了效率-准确率的帕累托前沿：在使用 Qwen3-VL 8B 且仅选取 16 帧的情况下，它的表现优于所有竞争选择器；而在使用 GPT-4o 时，它超越了运行在 32-512 帧的智能体系统，同时所需的 FLOPs 减少了约 10 倍。

## Abstract
Long-form video question answering requires reasoning over extended temporal contexts, making frame selection critical for large vision-language models (LVLMs) bound by finite context windows. Existing methods face a sharp trade-off: similarity-based selectors are fast but collapse compositional queries into a single dense vector, losing sub-event ordering and cross-modal bindings; agent-based methods recover this structure through iterative LVLM inference, but at prohibitive cost. We introduce HiMu, a training-free framework that bridges this gap. A single text-only LLM call decomposes the query into a hierarchical logic tree whose leaves are atomic predicates, each routed to a lightweight expert spanning vision (CLIP, open-vocabulary detection, OCR) and audio (ASR, CLAP). The resulting signals are normalized, temporally smoothed to align different modalities, and composed bottom-up through fuzzy-logic operators that enforce temporal sequencing and adjacency, producing a continuous satisfaction curve. Evaluations on Video-MME, LongVideoBench and HERBench-Lite show that HiMu advances the efficiency-accuracy Pareto front: at 16 frames with Qwen3-VL 8B it outperforms all competing selectors, and with GPT-4o it surpasses agentic systems operating at 32-512 frames while requiring roughly 10x fewer FLOPs.

---

## 论文详细总结（自动生成）

### 论文总结：HiMu —— 面向长视频问答的分层多模态帧选择

#### 1. 核心问题与研究动机
*   **核心问题**：长视频问答（VideoQA）受限于大型视觉语言模型（LVLM）有限的上下文窗口，必须进行“帧选择”。
*   **研究动机**：
    *   **现有方法的局限**：基于相似度的选择器（如 CLIP）速度快但缺乏逻辑推理能力，会将复杂查询压缩为单一向量，丢失时序和跨模态关联；而基于智能体（Agentic）的方法虽然推理深，但需要多次迭代调用 LVLM，计算成本极高。
    *   **HiMu 的目标**：在不增加显著计算开销的前提下，通过结构化分解实现复杂、多模态的帧选择，弥合“高效但浅层”与“准确但昂贵”之间的鸿沟。

#### 2. 方法论
HiMu 是一个**无需训练（Training-free）**的神经符号框架，其核心流程如下：
*   **神经符号查询分解**：利用单次纯文本 LLM 调用，将自然语言问题分解为**分层逻辑树**。
    *   **叶节点（原子谓词）**：路由至特定专家模型（如 `ovd` 识别物体、`asr` 识别语音、`clap` 识别环境音）。
    *   **内部节点（逻辑算子）**：定义谓词间的关系，包括 `And`（共时）、`Or`（析取）、`Seq`（时序顺序）和 `RightAfter`（紧邻时序）。
*   **多模态专家信号提取**：
    *   **视觉专家**：CLIP（语义）、YOLO-World（开放词汇检测）、docTR（OCR）。
    *   **音频专家**：Whisper（ASR 语音转文字）、CLAP（非语音音频）。
*   **信号处理与组合**：
    *   使用 **Median/MAD 归一化**和**高斯平滑**处理不同模态的原始分数，解决跨模态异步问题。
    *   利用**模糊逻辑算子（Fuzzy Logic）**自底向上组合信号，生成连续的“满意度曲线”。
*   **PASS 采样算法**：提出“峰值与扩展选择”（Peak-And-Spread Selection），在识别满意度峰值的同时，保留局部运动上下文，确保采样帧的代表性和多样性。

#### 3. 实验设计
*   **数据集**：
    *   **Video-MME**：涵盖短、中、长视频的全方位评估。
    *   **LongVideoBench**：侧重于长上下文和特定时刻检索。
    *   **HERBench-Lite**：侧重于多证据集成推理（纯视觉场景）。
*   **对比方法**：
    *   **相似度基准**：Uniform Sampling、BOLT、AKS、MDP³。
    *   **迭代/智能体基准**：T*、VSLS、VideoZoomer、VideoChat-A1、LVAgent。
*   **适配模型**：验证了在 Qwen3-VL、LLaVA-OV、InternVL、GPT-4o 和 Gemini-2.5-Flash 等多种 LVLM 上的通用性。

#### 4. 资源与算力
*   **硬件环境**：所有实验在 **8 × NVIDIA RTX Pro 6000 (48GB)** 或 **8 × A100 (80GB)** GPU 上完成。
*   **训练时长**：该方法**无需训练**，属于即插即用的推理增强框架。
*   **推理效率**：对于 10 分钟视频，HiMu 的端到端延迟约为 13.3 秒，但由于专家特征可缓存，后续查询的摊销延迟仅为 9.0 秒，计算量（FLOPs）比智能体方法低约 10 倍。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准测试上进行了广泛评估，涵盖了从 11 秒到 1 小时的视频。
*   **消融实验**：
    *   验证了逻辑树结构的必要性（对比“扁平融合”下降了 5.49%）。
    *   验证了各模态专家的贡献（ASR 和 CLIP 贡献最大）。
    *   测试了不同 LLM 解析器（如 Qwen, LLaVA, Gemini）对逻辑树生成的影响，证明了框架的鲁棒性。
*   **公平性**：在严格控制帧预算（如固定 16 帧）的情况下与 SOTA 方法对比，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **性能突破**：HiMu 重新定义了效率-准确率的帕累托前沿。在 16 帧配置下，其表现优于使用 64 帧的均匀采样。
*   **超越智能体**：配合 GPT-4o 时，HiMu 仅用 16 帧就超越了使用 32-512 帧的复杂智能体系统（如 VSLS 和 VideoChat-A1）。
*   **音频的重要性**：首次证明了在帧选择阶段引入音频专家（ASR/CLAP）能显著提升关键时刻的定位精度。

#### 7. 优点
*   **高效性**：单次 LLM 调用 + 轻量级专家，避免了昂贵的迭代式视觉推理。
*   **可解释性**：逻辑树和满意度曲线提供了清晰的决策路径，可以追溯每一帧被选中的具体原因（如哪个谓词触发了高分）。
*   **多模态融合**：真正实现了视觉、文字、语音、环境音的深度时序协同选择。
*   **即插即用**：无需针对特定模型微调，可直接提升现有 LVLM 的长视频处理能力。

#### 8. 不足与局限
*   **解析器依赖**：高度依赖 LLM 解析逻辑树的质量，若 LLM 无法正确理解复杂指令，会导致选择失效。
*   **冷启动延迟**：首次处理视频时，提取所有专家特征（OCR, ASR 等）仍需一定时间，虽然可摊销，但在单次查询场景下比简单相似度方法慢。
*   **语言限制**：ASR 专家的效果受限于底层语音识别模型的语言覆盖范围。

（完）
