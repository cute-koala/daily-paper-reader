---
title: Do VLMs Need Vision Transformers? Evaluating State Space Models as Vision Encoders
title_zh: VLM 是否需要 Vision Transformer？评估状态空间模型作为视觉编码器的性能
authors: "Shang-Jui Ray Kuo, Paola Cascante-Bonilla"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19209v1"
tags: ["query:vlm"]
score: 9.0
evidence: 评估SSM作为VLM预训练视觉编码器的效果
tldr: 本研究探讨了在视觉语言模型（VLM）中，状态空间模型（SSM）作为视觉编码器替代传统 Transformer 的潜力。通过在受控环境下系统评估 SSM 与 ViT 骨干网络，发现 SSM 在视觉问答和定位任务中表现优异，且在较小模型规模下仍具竞争力。研究还揭示了 ImageNet 准确率与 VLM 性能的不一致性，并提出了提升模型鲁棒性的稳定化策略，证明了 SSM 是构建高效 VLM 的强力选择。
selection_source: fresh_fetch
motivation: 探究状态空间模型（SSM）是否能作为视觉语言模型中视觉 Transformer（ViT）的高效替代方案。
method: 在统一的实验条件下，对比评估了 SSM 和 ViT 骨干网络在 VQA 和定位任务中的表现，并分析了密集任务微调的影响。
result: SSM 骨干网络在多项任务中表现出最强性能，且在模型规模更小的情况下依然保持竞争力，同时发现视觉预训练准确率并不总能转化为 VLM 性能。
conclusion: SSM 是视觉编码器的强力替代方案，结合所提出的稳定化策略，能有效提升 VLM 的鲁棒性和整体表现。
---

## 摘要
大规模视觉-语言模型（VLM）通常使用冻结的视觉主干网络，其图像特征通过轻量级连接器映射到大语言模型中。虽然基于 Transformer 的编码器是标准的视觉主干，但我们探究状态空间模型（SSM）视觉主干是否可以成为一种强有力的替代方案。我们在受控设置下系统地评估了用于 VLM 的 SSM 视觉主干。在匹配的 ImageNet-1K 初始化下，SSM 主干在视觉问答（VQA）以及定位/接地（grounding/localization）方面均实现了最强的综合性能。我们进一步通过检测或分割训练对 SSM 和 ViT 系列主干进行适配，发现密集任务微调通常能提升各系列模型的性能；在适配后，SSM 主干在模型规模显著减小的情况下仍保持竞争力。我们还观察到：(i) 更高的 ImageNet 准确率或更大的主干网络并不一定能转化为更好的 VLM 性能；(ii) 某些视觉主干在定位任务中表现不稳定。基于这些发现，我们提出了稳定化策略，提高了这两个主干系列的鲁棒性，并强调 SSM 主干是 VLM 中基于 Transformer 的视觉编码器的强力替代方案。

## Abstract
Large vision--language models (VLMs) often use a frozen vision backbone, whose image features are mapped into a large language model through a lightweight connector. While transformer-based encoders are the standard visual backbone, we ask whether state space model (SSM) vision backbones can be a strong alternative. We systematically evaluate SSM vision backbones for VLMs in a controlled setting. Under matched ImageNet-1K initialization, the SSM backbone achieves the strongest overall performance across both VQA and grounding/localization. We further adapt both SSM and ViT-family backbones with detection or segmentation training and find that dense-task tuning generally improves performance across families; after this adaptation, the SSM backbone remains competitive while operating at a substantially smaller model scale. We further observe that (i) higher ImageNet accuracy or larger backbones do not reliably translate into better VLM performance, and (ii) some visual backbones are unstable in localization. Based on these findings, we propose stabilization strategies that improve robustness for both backbone families and highlight SSM backbones as a strong alternative to transformer-based vision encoders in VLMs.

---

## 论文详细总结（自动生成）

这篇论文对视觉语言模型（VLM）中视觉编码器的架构选择进行了深入的受控研究，重点探讨了状态空间模型（SSM）替代传统 Vision Transformer（ViT）的可能性。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 VLM 领域，ViT 几乎是视觉主干网络的唯一标准。论文探讨了：**VLM 是否真的必须依赖 ViT？新兴的 SSM（如 VMamba）能否作为更高效、空间感知能力更强的替代方案？**
*   **研究动机**：
    *   ViT 的全局自注意力机制在处理高分辨率图像时计算成本极高。
    *   现有的 VLM 研究往往同时改变多个变量（预训练数据、分辨率、连接器等），导致难以孤立评估视觉主干架构本身的影响。
    *   SSM 在密集预测任务中表现出色，其 2D 扫描机制可能比 ViT 更好地保留空间细节。

### 2. 论文提出的方法论
*   **核心思想**：采用“受控主干网络替换”（Controlled Backbone Swaps）实验框架。在保持 LLaVA 式架构（视觉编码器 + 连接器 + LLM）和训练配方完全一致的前提下，仅更换视觉主干。
*   **关键技术细节**：
    *   **架构组件**：视觉编码器（冻结）、轻量级连接器（2层或3层 MLP）、大语言模型（Vicuna-7B）。
    *   **对比架构**：包括纯 ViT、层次化混合架构（MaxViT）、SSM-Transformer 混合架构（MambaVision）以及纯 SSM 架构（VMamba）。
    *   **特征对齐**：为了公平对比，通过选择层次化模型的特定阶段（Stage 3），确保输出的视觉标记（Tokens）数量与 ViT/16 一致（均为 196 个）。
    *   **稳定化策略**：针对发现的“定位崩溃”现象，提出了增加连接器容量（3层 MLP）和调整输入几何形状（统一为 512x512 正方形）的方案。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **VQA（视觉问答）**：VQA-v2, GQA, VizWiz, TextVQA, POPE, TallyQA。
    *   **Localization（定位/接地）**：RefCOCO, RefCOCO+, RefCOCOg, OCID-Ref。
*   **对比方法**：
    *   **匹配实验**：在 ImageNet-1K (IN1K) 预训练、224 分辨率下对比各家族模型。
    *   **密集任务适配**：对比经过检测（ViTDet, VMamba）或分割（DeiT, VMamba）微调后的主干网络。
*   **评估指标**：各基准测试的准确率、加权平均 VQA 分数、加权平均定位分数。

### 4. 资源与算力
*   **算力设备**：使用了 **4 × NVIDIA H200 GPU**。
*   **训练细节**：采用全分片数据并行（FSDP），混合精度（BF16）。
*   **训练数据**：使用 LLaVA-v1.5 的 665K 条多模态指令微调数据。
*   **训练时长**：每个模型训练 1 个 Epoch，全局 Batch Size 为 128。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了数十组实验，涵盖了 4 个架构家族、多种模型规模（Tiny/Small/Base/Large）、不同的预训练目标（分类 vs. 检测 vs. 分割）以及多种稳定化消融实验。
*   **充分性与公平性**：实验设计非常严谨。通过固定随机种子、优化器参数、Batch 顺序和 LLM 权重，确保了观察到的性能差异仅源于视觉主干及其适配方式。此外，还进行了线性探测（Linear Probing）和推理效率分析（Profiling），实验维度非常全面。

### 6. 主要结论与发现
*   **SSM 表现优异**：在同等条件下，基于 SSM 的 VMamba 在定位任务上显著优于 ViT，且在 VQA 任务上保持竞争力。
*   **ImageNet 准确率的误导性**：更高的 ImageNet 分类准确率并不等同于更好的 VLM 性能。大型分类模型往往会为了识别目标而舍弃空间细节，导致 VLM 性能下降。
*   **定位崩溃（Localization Collapse）**：发现某些检测适配的高分辨率模型在 VLM 中会出现定位能力骤降。这并非视觉特征缺失，而是“接口失效”（传输或利用瓶颈）。
*   **稳定化有效性**：通过增加 MLP 层数和使用正方形输入，可以有效修复定位崩溃，显著提升模型鲁棒性。
*   **规模效率**：SSM 主干在更小的参数规模下能匹配甚至超越大型 ViT 的定位表现。

### 7. 优点（亮点）
*   **系统性强**：首次在 VLM 语境下对 SSM 视觉编码器进行了严格的受控对比。
*   **诊断深入**：不仅报告了性能，还深入诊断了模型失效的原因（如分类目标过拟合、接口不稳定等）。
*   **实用性高**：提出的稳定化策略（MLP 深度、几何形状）对 VLM 开发者具有直接的指导意义。
*   **效率分析**：提供了详细的推理延迟和显存占用对比，证明了 SSM 在处理高分辨率输入时的扩展性优势。

### 8. 不足与局限
*   **预训练目标缺失**：目前 SSM 缺乏像 CLIP 或 SigLIP 那样的大规模对比学习预训练权重，这限制了其在当前主流 VLM 架构中的直接应用。
*   **LLM 覆盖有限**：实验主要基于 Vicuna-7B，尚未验证在更大规模或不同架构（如 Llama-3, Qwen）的 LLM 上的泛化性。
*   **SSM 变体有限**：主要测试了 VMamba 和 Vim，未涵盖所有最新的 SSM 视觉变体。

（完）
