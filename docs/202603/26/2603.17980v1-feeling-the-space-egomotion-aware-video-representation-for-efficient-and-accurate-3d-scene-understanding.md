---
title: "Feeling the Space: Egomotion-Aware Video Representation for Efficient and Accurate 3D Scene Understanding"
title_zh: 空间感知：用于高效准确 3D 场景理解的自身运动感知视频表示
authors: "Shuyao Shi, Kang G. Shin"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17980v1"
tags: ["query:vlm"]
score: 9.0
evidence: MLLM中的空间推理和物理对齐
tldr: 针对多模态大模型在3D场景理解中计算成本高且缺乏物理尺度感的问题，本文提出Motion-MLLM框架。该框架通过引入IMU捕获的自身运动数据，结合级联关键帧过滤和非对称跨模态融合技术，将视觉内容与物理运动轨迹关联。这使得模型能以极低的计算开销实现对绝对尺度和空间关系的精准推理，显著提升了3D场景理解的效率与准确性。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理3D场景时，要么依赖昂贵的3D表示，要么因缺乏物理接地而难以准确感知物体的绝对尺度和空间关系。
method: 提出Motion-MLLM框架，利用IMU数据进行关键帧过滤，并通过非对称跨模态融合模块将运动线索注入视觉表示。
result: 在多项3D场景理解任务中，该方法在保持甚至超越SOTA精度的同时，其成本效益比现有视频和显式3D数据方法分别高出1.40倍和1.63倍。
conclusion: 通过将自身运动信息与视频表征相结合，Motion-MLLM为高效且具备物理感知能力的3D空间推理提供了一种新颖且有效的解决方案。
---

## 摘要
近期的多模态大语言模型 (MLLMs) 在 3D 场景的空间推理方面展现出巨大潜力。然而，它们通常依赖于计算成本高昂的 3D 表示（如点云或重建的鸟瞰图 (BEV) 映射），或者缺乏物理基础来解决尺度和尺寸上的歧义。本文利用与视频同步采集的惯性测量单元 (IMU) 自身运动模态数据，显著增强了 MLLMs。具体而言，我们提出了一种名为 Motion-MLLM 的新型框架，引入了两个关键组件：(1) 级联运动-视觉关键帧过滤模块，利用 IMU 数据和视觉特征高效选择稀疏且具有代表性的关键帧集合；(2) 非对称跨模态融合模块，其中运动标记 (motion tokens) 作为中介，将自身运动线索和跨帧视觉上下文引导至视觉表示中。通过将视觉内容建立在物理自身运动轨迹的基础上，Motion-MLLM 能够推理整个场景的绝对尺度和空间关系。我们的广泛评估表明，Motion-MLLM 在与 3D 场景理解和空间推理相关的各项任务中取得了显著改进。与基于视频帧和显式 3D 数据的最先进 (SOTA) 方法相比，Motion-MLLM 在开销显著降低的情况下（即性价比分别提高 1.40 倍和 1.63 倍），表现出相似甚至更高的准确度。

## Abstract
Recent Multimodal Large Language Models (MLLMs) have shown high potential for spatial reasoning within 3D scenes. However, they typically rely on computationally expensive 3D representations like point clouds or reconstructed Bird's-Eye View (BEV) maps, or lack physical grounding to resolve ambiguities in scale and size. This paper significantly enhances MLLMs with egomotion modality data, captured by Inertial Measurement Units (IMUs) concurrently with the video. In particular, we propose a novel framework, called Motion-MLLM, introducing two key components: (1) a cascaded motion-visual keyframe filtering module that leverages both IMU data and visual features to efficiently select a sparse yet representative set of keyframes, and (2) an asymmetric cross-modal fusion module where motion tokens serve as intermediaries that channel egomotion cues and cross-frame visual context into the visual representation. By grounding visual content in physical egomotion trajectories, Motion-MLLM can reason about absolute scale and spatial relationships across the scene. Our extensive evaluation shows that Motion-MLLM makes significant improvements in various tasks related to 3D scene understanding and spatial reasoning. Compared to state-of-the-art (SOTA) methods based on video frames and explicit 3D data, Motion-MLLM exhibits similar or even higher accuracy with significantly less overhead (i.e., 1.40$\times$ and 1.63$\times$ higher cost-effectiveness, respectively).

---

## 论文详细总结（自动生成）

以下是对论文《Feeling the Space: Egomotion-Aware Video Representation for Efficient and Accurate 3D Scene Understanding》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLM）在3D空间推理方面存在局限。基于3D表示（如点云、BEV）的方法计算开销巨大；而仅基于2D视频的方法由于缺乏物理接地（Physical Grounding），难以准确分辨物体的绝对尺度、距离和大小。
*   **研究动机**：借鉴人类感知机制——人类不仅依靠视觉，还结合身体运动（自身运动）来理解空间。
*   **整体含义**：论文提出利用廉价且普及的惯性测量单元（IMU）数据作为“物理锚点”，为视频表征提供度量尺度，从而在不依赖昂贵3D传感器的情况下，实现高效且精准的3D场景理解。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建名为 **Motion-MLLM** 的框架，将同步采集的自身运动（Egomotion）数据与视频帧融合。
*   **关键技术细节**：
    1.  **级联运动-视觉关键帧过滤（Cascaded Filtering）**：
        *   **阶段1（运动门控）**：利用IMU积分计算位移和旋转，剔除相机静止或微动时的冗余帧。
        *   **阶段2（轻量级几何变化检测）**：通过SLAM前端的稀疏特征跟踪计算平均视差，识别几何结构变化。
        *   **阶段3（视觉标记分析）**：仅对通过前两阶段的帧提取视觉特征，计算与前一关键帧的余弦距离，确保视觉信息的多样性。
    2.  **非对称跨模态特征融合**：
        *   **运动编码器**：使用GRU处理关键帧之间变长的IMU数据段，生成运动标记（Motion Tokens）。
        *   **双层跨注意力融合**：第一层为双向注意力，让视觉和运动特征互补；第二层为单向注意力（视觉查询运动），使运动标记充当“桥梁”，沿着物理运动轨迹传递跨帧视觉上下文。

### 3. 实验设计
*   **数据集与场景**：
    *   **空间推理**：ScanQA、SQA3D（基于ScanNet室内场景）。
    *   **综合空间基准**：VSI-Bench（涵盖物体计数、绝对距离、房间大小等8个子任务）。
    *   **3D任务**：ScanRefer（视觉定位）、Scan2Cap（密集描述）。
*   **对比方法**：
    *   **2D输入模型**：Qwen2.5-VL、GPT-4v、Gemini-1.5 Pro、Spatial-MLLM等。
    *   **3D/2.5D输入模型**：3D-LLM、LL3DA、LLaVA-3D、Video-3D LLM等。
*   **数据处理**：由于现有基准缺乏原始IMU数据，作者通过B样条曲线拟合相机轨迹，合成了带有噪声的6轴IMU测量值。

### 4. 资源与算力
*   **硬件环境**：使用了 **8张 NVIDIA RTX 4000 Ada GPU** 的集群。
*   **训练时长**：完整训练过程大约耗时 **15小时**。
*   **模型规模**：基于 Qwen2.5-VL-3B 构建，总参数量约为 **4B**。
*   **训练策略**：采用两阶段训练（先冻结主干训练融合模块，再解冻LLM进行端到端微调）。

### 5. 实验数量与充分性
*   **实验规模**：在5个主流3D场景理解基准上进行了测试，涵盖了从问答到定位、描述的多种任务。
*   **消融实验**：针对级联过滤策略（对比均匀采样、全量过滤）和融合机制（对比拼接、单层注意力）进行了详细对比。
*   **充分性评价**：实验设计较为充分，不仅对比了精度，还引入了“成本效益（Cost-Effectiveness）”指标衡量推理延迟与准确率的平衡，客观地展示了模型在资源受限场景下的优势。

### 6. 主要结论与发现
*   **精度提升**：在VSI-Bench上比SOTA提升了9.6分；在ScanQA和SQA3D上显著优于所有2D基准，并达到了与显式3D输入模型相当的水平。
*   **尺度感知**：实验证明IMU数据能有效补偿单目视觉在绝对距离和物体尺寸判断上的缺陷。
*   **高效性**：Motion-MLLM 的性价比（Cost-Effectiveness）比2D SOTA高出1.40倍，比3D SOTA高出1.63倍。

### 7. 优点（亮点）
*   **低成本高回报**：利用极低成本的IMU传感器解决了MLLM在3D空间推理中的核心痛点（尺度歧义）。
*   **级联过滤机制**：通过三层递进式过滤，将昂贵的视觉特征提取操作减少了97%以上，极大地提升了处理长视频的效率。
*   **非对称融合设计**：巧妙地利用运动信息作为时空索引，增强了模型对跨帧空间关系的理解。

### 8. 不足与局限
*   **数据合成偏差**：实验主要依赖合成的IMU数据，虽然加入了噪声模拟，但在真实硬件上可能面临更复杂的传感器漂移和同步误差问题。
*   **场景局限性**：目前主要在室内场景（ScanNet系列）验证，在高速运动或开阔室外场景（如自动驾驶高速路段）的泛化能力尚待验证。
*   **依赖轨迹质量**：如果相机运动极其剧烈导致视觉模糊或IMU饱和，级联过滤和特征融合的鲁棒性可能会受到影响。

（完）
