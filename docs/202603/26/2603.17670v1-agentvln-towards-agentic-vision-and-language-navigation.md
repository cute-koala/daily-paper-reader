---
title: "AgentVLN: Towards Agentic Vision-and-Language Navigation"
title_zh: AgentVLN：迈向智能体视觉语言导航
authors: "Zihao Xin, Wentong Li, Yixuan Jiang, Ziyuan Huang, Bin Wang, Piji Li, Jianke Zhu, Jie Qin, Shengjun Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17670v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于具身导航和语义推理的视觉语言模型
tldr: AgentVLN是一个面向具身导航的高效框架，旨在解决视觉语言导航（VLN）中的空间感知受限和2D-3D表征不匹配问题。该框架采用“VLM作为大脑”的范式，通过解耦语义推理与感知规划，并引入跨空间表征映射、主动探索策略及查询驱动的感知思维链（QD-PCoT），显著提升了长程导航的准确性与鲁棒性，并支持边缘计算部署。
selection_source: fresh_fetch
motivation: 针对现有VLN系统在空间感知、2D-3D表征一致性及单目尺度模糊方面的局限性，寻求一种更高效且适用于边缘设备的具身导航方案。
method: 将VLN建模为部分可观测半马尔可夫决策过程，利用VLM进行高层推理，并通过跨空间映射、自纠错机制及感知思维链实现精准的路径规划。
result: 实验结果显示AgentVLN在长程VLN基准测试中一致优于现有的最先进方法，展现了卓越的导航性能和鲁棒性。
conclusion: 该研究为下一代具身导航模型提供了一个实用的轻量化部署范式，通过解耦推理与感知有效解决了非结构化环境中的复杂导航难题。
---

## 摘要
视觉语言导航 (VLN) 要求具身智能体在未知环境中将复杂的自然语言指令转化为长程导航任务。尽管视觉语言模型 (VLM) 提供了强大的 2D 语义理解能力，但当前的 VLN 系统仍受限于有限的空间感知、2D-3D 表示不匹配以及单目尺度歧义。在本文中，我们提出了 AgentVLN，这是一种新颖且高效的具身导航框架，可部署在边缘计算平台上。我们将 VLN 建模为部分可观测半马尔可夫决策过程 (POSMDP)，并引入了“VLM 作为大脑” (VLM-as-Brain) 的范式，通过即插即用的技能库将高层语义推理与感知和规划解耦。为了解决多层级表示的不一致性，我们设计了一种跨空间表示映射，将感知层的 3D 拓扑路标投影到图像平面，为 VLM 生成像素对齐的视觉提示。基于这一桥梁，我们集成了上下文感知的自我修正和主动探索策略，以从遮挡中恢复并抑制长轨迹上的误差累积。为了进一步解决非结构化环境中指令的空间歧义，我们提出了一种查询驱动的感知思维链 (QD-PCoT) 方案，使智能体具备主动寻求几何深度信息的元认知能力。最后，我们构建了 AgentVLN-Instruct，这是一个大规模指令微调数据集，具有基于目标可见性的动态阶段路由。大量实验表明，AgentVLN 在长程 VLN 基准测试中始终优于先前的最先进 (SOTA) 方法，为下一代具身导航模型的轻量化部署提供了一种实用的范式。代码：https://github.com/Allenxinn/AgentVLN。

## Abstract
Vision-and-Language Navigation (VLN) requires an embodied agent to ground complex natural-language instructions into long-horizon navigation in unseen environments. While Vision-Language Models (VLMs) offer strong 2D semantic understanding, current VLN systems remain constrained by limited spatial perception, 2D-3D representation mismatch, and monocular scale ambiguity. In this paper, we propose AgentVLN, a novel and efficient embodied navigation framework that can be deployed on edge computing platforms. We formulate VLN as a Partially Observable Semi-Markov Decision Process (POSMDP) and introduce a VLM-as-Brain paradigm that decouples high-level semantic reasoning from perception and planning via a plug-and-play skill library. To resolve multi-level representation inconsistency, we design a cross-space representation mapping that projects perception-layer 3D topological waypoints into the image plane, yielding pixel-aligned visual prompts for the VLM. Building on this bridge, we integrate a context-aware self-correction and active exploration strategy to recover from occlusions and suppress error accumulation over long trajectories. To further address the spatial ambiguity of instructions in unstructured environments, we propose a Query-Driven Perceptual Chain-of-Thought (QD-PCoT) scheme, enabling the agent with the metacognitive ability to actively seek geometric depth information. Finally, we construct AgentVLN-Instruct, a large-scale instruction-tuning dataset with dynamic stage routing conditioned on target visibility. Extensive experiments show that AgentVLN consistently outperforms prior state-of-the-art methods (SOTA) on long-horizon VLN benchmarks, offering a practical paradigm for lightweight deployment of next-generation embodied navigation models. Code: https://github.com/Allenxinn/AgentVLN.

---

## 论文详细总结（自动生成）

以下是对论文《AgentVLN: Towards Agentic Vision-and-Language Navigation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言导航（VLN）要求智能体在未知 3D 环境中根据自然语言指令进行长程导航。
*   **研究背景**：
    *   **空间感知受限**：现有的视觉语言模型（VLM）虽具备强 2D 语义理解，但缺乏对 3D 几何结构和度量尺度的直观感知。
    *   **表征不匹配**：2D 图像表征与 3D 物理轨迹之间存在断层，导致导航决策不精准。
    *   **部署困难**：主流 VLN 模型参数量巨大（通常 $\ge$ 7B），且依赖云端推理，难以满足物理智能体在边缘设备上的实时控制需求。
    *   **尺度歧义**：单目 RGB 观测缺乏深度信息，导致智能体在处理涉及空间方位词（如“在...后面”）的指令时容易出错。

### 2. 方法论
*   **核心思想**：提出“VLM 作为大脑”（VLM-as-Brain）的范式，将高层语义推理与底层感知规划解耦，并将 VLN 建模为部分可观测半马尔可夫决策过程（POSMDP）。
*   **关键技术细节**：
    *   **跨空间表示映射**：不依赖隐式的特征对齐，而是通过透视投影将感知层计算的 3D 拓扑路标显式地投影到 2D 图像平面，生成像素级对齐的“视觉提示”，供 VLM 直接决策。
    *   **查询驱动的感知思维链（QD-PCoT）**：当指令存在空间歧义时，智能体不会盲目预测，而是主动生成自然语言查询（如“椅子离我多远？”）并调用感知技能获取深度信息，通过多轮交互消除歧义。
    *   **上下文感知自纠错与主动探索**：在遇到遮挡或偏离轨迹时，智能体利用历史上下文生成细粒度的原子动作（左转、右转、前进一步）进行航向修正。
    *   **AgentVLN-Instruct 数据集**：构建了包含目标可见性动态路由的大规模指令微调数据集，模拟人类“先粗略导航，后精确对准”的寻路过程。

### 3. 实验设计
*   **数据集与场景**：在连续环境基准测试 **R2R-CE**（Room-to-Room）和 **RxR-CE**（Room-Across-Room）上进行评估。
*   **Benchmark 指标**：成功率（SR）、路径长度加权成功率（SPL）、导航误差（NE）和 Oracle 成功率（OS）。
*   **对比方法**：对比了包括 NavGPT、NaVid、StreamVLN、InternVLA、DualVLN 和 EfficientVLN 在内的多种 SOTA（最先进）单系统与双系统模型。
*   **真实世界验证**：在 Unitree Go2 四足机器人上部署，使用 Jetson 边缘计算平台进行室内外实地测试。

### 4. 资源与算力
*   **硬件资源**：实验在配备 **32 台 NVIDIA A100 GPU** 的计算集群上完成。
*   **模型配置**：采用 **Qwen2.5-VL-3B** 作为核心大脑。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，学习率为 $2 \times 10^{-5}$，采用余弦退火策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 VLN 基准上进行了全面对比。
    *   进行了**增量消融实验**，验证了 VLM-as-Brain、自纠错策略和 QD-PCoT 每一个模块的有效性。
    *   针对**时间上下文长度**（2 到 10 帧）进行了敏感度分析。
*   **充分性评价**：实验设计较为充分且客观。通过与参数量更大的模型（如 7B、8B 级别）对比，证明了该方法在轻量化（3B）前提下的优越性能。真实世界的部署进一步增强了实验的说服力。

### 6. 主要结论与发现
*   **性能领先**：AgentVLN 在 R2R-CE 上的成功率（SR）达到 67.2%，显著超过了参数量更大的 InternVLA（58.2%）和 DualVLN（64.3%）。
*   **效率卓越**：在 SPL 指标（反映路径最优性）上表现尤为突出，证明了其决策的高效性。
*   **边缘友好**：由于去除了冗余的 3D 编码器并采用了轻量化基座，模型可在 Jetson 等边缘设备上实现实时本地推理，无需依赖云端。
*   **元认知能力**：QD-PCoT 机制有效解决了单目视觉下的深度幻觉问题。

### 7. 优点（亮点）
*   **解耦设计**：通过即插即用的技能库，使模型无需重新训练即可适配不同的传感器（如 LiDAR 或深度相机）。
*   **显式投影**：巧妙地利用几何投影解决了 2D 语义与 3D 空间的不一致性，比隐式特征对齐更具解释性和泛化性。
*   **主动交互**：引入 QD-PCoT 让智能体具备了“不懂就问”的主动感知能力，提升了在复杂非结构化环境中的鲁棒性。

### 8. 不足与局限
*   **技能库依赖**：系统的表现高度依赖于底层感知算法（如 RTAB-Map）的质量，若底层 SLAM 失败，高层 VLM 也难以挽回。
*   **任务覆盖**：虽然在导航任务上表现优异，但对于更复杂的具身操纵任务（如抓取物体）的泛化能力尚未在本文中深入探讨。
*   **实时性上限**：尽管支持边缘部署，但在极低算力的嵌入式设备上，多轮对话（QD-PCoT）可能仍会带来一定的决策延迟。

（完）
