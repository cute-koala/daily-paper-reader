---
title: Rethinking Token-Level Policy Optimization for Multimodal Chain-of-Thought
title_zh: 重新思考多模态思维链的标记级策略优化
authors: "Yunheng Li, Hangyi Kuang, Hengrui Zhang, Jiangxia Cao, Zhaojie Liu, Qibin Hou, Ming-Ming Cheng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22847v1"
tags: ["query:vlm"]
score: 9.0
evidence: 多模态思维链与感知接地
tldr: 针对多模态思维链（CoT）推理中现有强化学习方法优化粒度过粗的问题，本文提出了PEPO算法。通过分析Token级别的动态特性，PEPO结合了基于隐藏状态相似度的感知先验与Token熵，利用平滑门控机制产生Token级优势。该方法无需额外监督即可无缝集成至现有RL框架，在几何推理、视觉定位等多个基准测试中显著提升了模型性能。
selection_source: fresh_fetch
motivation: 现有的多模态CoT强化学习方法通常采用粗粒度优化，忽视了不同Token在视觉感知和推理探索中的差异化作用。
method: 提出PEPO算法，通过隐藏状态相似度提取感知先验，并结合Token熵构建平滑门控机制，实现Token级别的策略优化。
result: 在几何推理、视觉定位和拼图解决等多种多模态任务上，PEPO相比GRPO等强基线模型取得了显著且稳健的性能提升。
conclusion: PEPO证明了在多模态推理中引入Token级细粒度感知与探索平衡，是提升大模型复杂推理能力的有效途径。
---

## 摘要
多模态思维链（CoT）推理要求大型视觉语言模型构建将感知落地与多步推理交织在一起的推理轨迹。然而，现有的带有可验证奖励的强化学习（RLVR）方法通常以粗粒度优化推理，统一对待 CoT，而没有区分它们不同程度的视觉落地。在这项工作中，我们对多模态推理轨迹进行了标记级分析，并表明成功的推理具有结构化标记动态的特征，反映了感知落地和探索性推理。基于这一分析，我们提出了感知-探索策略优化（PEPO），它从隐藏状态相似性中推导出感知先验，并通过平滑门控机制将其与标记熵整合，从而产生标记级优势。PEPO 与现有的 RLVR 框架（如 GRPO 和 DAPO）无缝集成，既不需要额外的监督，也不需要辅助分支。在各种多模态基准测试上的广泛实验表明，与强大的强化学习基准相比，该方法在几何推理、视觉落地、视觉谜题解决和少样本分类等方面均取得了持续且稳健的改进，同时保持了稳定的训练动态。代码：https://github.com/xzxxntxdy/PEPO

## Abstract
Multimodal Chain-of-Thought (CoT) reasoning requires large vision-language models to construct reasoning trajectories that interleave perceptual grounding with multi-step inference. However, existing Reinforcement Learning with Verifiable Rewards (RLVR) methods typically optimize reasoning at a coarse granularity, treating CoT uniformly without distinguishing their varying degrees of visual grounding. In this work, we conduct a token-level analysis of multimodal reasoning trajectories and show that successful reasoning is characterized by structured token dynamics reflecting both perceptual grounding and exploratory inference. Building upon this analysis, we propose Perception-Exploration Policy Optimization (PEPO), which derives a perception prior from hidden state similarity and integrates it with token entropy through a smooth gating mechanism to produce token-level advantages. PEPO integrates seamlessly with existing RLVR frameworks such as GRPO and DAPO, requiring neither additional supervision nor auxiliary branches. Extensive experiments across diverse multimodal benchmarks demonstrate consistent and robust improvements over strong RL baselines, spanning geometry reasoning, visual grounding, visual puzzle solving, and few-shot classification, while maintaining stable training dynamics. Code: https://github.com/xzxxntxdy/PEPO

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Token-Level Policy Optimization for Multimodal Chain-of-Thought》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大型视觉语言模型（LVLM）在多模态思维链（CoT）推理中，需要将视觉感知与多步逻辑推理交织。然而，现有的强化学习（如 GRPO）通常采用**序列级（Sequence-level）**的粗粒度监督，即对整个推理链条给予统一的奖励。
*   **核心矛盾**：这种粗粒度方法忽视了推理过程中不同 Token 的贡献差异。作者发现，成功的推理往往依赖于一小部分“视觉对齐”的 Token（锚定感知）和“高熵”的 Token（驱动探索）。
*   **整体含义**：本文旨在通过**标记级（Token-level）**的策略优化，精细化地分配信用（Credit Assignment），使模型在推理时能更好地平衡视觉感知与逻辑探索。

### 2. 方法论：PEPO (Perception-Exploration Policy Optimization)
*   **核心思想**：利用模型内部的隐藏状态相似度作为感知先验，结合输出概率的熵作为探索信号，动态调整每个 Token 在强化学习更新中的权重。
*   **关键技术细节**：
    1.  **感知建模（Perception Prior）**：计算每个文本 Token 的隐藏状态与所有视觉 Token 隐藏状态之间的余弦相似度，并跨层平均。相似度越高，代表该 Token 越依赖视觉信息。
    2.  **探索建模（Exploration Score）**：计算 Token 的信息熵。高熵 Token 通常对应推理的转折点或不确定区域，是模型探索不同路径的关键。
    3.  **平滑门控融合（Smooth Gating）**：将归一化后的感知分与探索分通过一个带有 `tanh` 激活函数的门控机制融合，生成 Token 权重 $w_t$。该机制确保感知占据主导地位，同时让熵在视觉相关的步骤中起到调节作用。
    4.  **Token 级优势计算**：将 GRPO 或 DAPO 原有的序列级优势（Advantage）乘以 $w_t$，从而在策略梯度更新时，对视觉落地好且具有探索价值的 Token 给予更大的更新步长。
*   **算法特点**：无需额外的辅助分支（如 Mask 预测），无需额外监督信号，计算开销极低。

### 3. 实验设计
*   **数据集与场景**：
    *   **几何与逻辑推理**：Geometry3K, MathVista, MathVerse, LogicVista。
    *   **视觉定位（Grounding）**：RefCOCO, LISA-Grounding。
    *   **视觉谜题**：PuzzleVQA, AlgoPuzzleVQA。
    *   **少样本分类**：FGVC Aircraft, Flower102。
    *   **大规模扩展性分析**：ViRL39K。
*   **基准模型（Backbones）**：Qwen2.5-VL-3B-Instruct, InternVL3-2B-Instruct。
*   **对比方法**：GRPO, DAPO, High-Entropy RL（仅基于熵的 RL）。

### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA A40 GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化器，全参数微调，bfloat16 精度。
*   **效率表现**：PEPO 引入的额外计算开销（$\rho$）在所有任务中均**低于 1%**，且由于 PEPO 倾向于生成更短、更精炼的回复，其实际吞吐量有时甚至高于基线 GRPO。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 大类任务、超过 10 个主流基准测试，并跨越了两种不同的模型架构。
*   **消融实验**：非常充分。作者分别验证了“仅感知”、“仅探索”以及“感知+探索”的效果，并对超参数 $\alpha$（门控强度）、相似度度量方式（L1/L2/余弦）、层选择策略等进行了详尽测试。
*   **客观性**：所有实验均采用可验证的规则奖励（Verifiable Rewards），避免了 LLM-as-a-judge 的主观偏差，确保了评估的公平性。

### 6. 主要结论与发现
*   **性能提升**：PEPO 在几何推理上比 GRPO 平均提升了 **3.5~3.7 个百分点**，在少样本分类和视觉定位任务上也表现出更强的泛化能力。
*   **感知与探索的互补性**：视觉相似度高的 Token 对应几何实体和空间属性，而高熵 Token 对应推理转折（如“因此”、“但是”）。两者结合能显著稳定训练动态。
*   **训练稳定性**：相比于纯熵驱动的方法（容易导致模型崩溃），PEPO 在训练过程中表现出更稳定的奖励增长和更合理的回复长度缩减。

### 7. 优点
*   **即插即用**：作为一种 Token 权重重分配策略，它可以无缝集成到 GRPO、DAPO 等任何基于优势函数的 RL 框架中。
*   **细粒度优化**：首次深入探讨了多模态 CoT 中 Token 级动态特性，解决了“信用分配不均”的问题。
*   **高效性**：在不增加参数量和显著计算成本的前提下，通过挖掘模型“原生”的隐藏状态信息实现了性能飞跃。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 2B-3B 规模的模型上，尚未在 7B 或更大规模（如 72B）的模型上验证其扩展性。
*   **任务覆盖**：虽然涵盖了推理和定位，但尚未在视频理解、长视频推理或工具调用（Tool-use）等更复杂的场景中进行测试。
*   **超参数敏感性**：门控系数 $\alpha$ 在不同任务间仍需微调（如几何任务设为 0.1，分类任务设为 0.02），缺乏一种完全自适应的参数设置方法。

（完）
