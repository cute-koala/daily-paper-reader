---
title: Cluster-Wise Spatio-Temporal Masking for Efficient Video-Language Pretraining
title_zh: 基于聚类的时空掩码用于高效视频-语言预训练
authors: "Weijun Zhuang, Yuqing Huang, Weikang Meng, Xin Li, Ming Liu, Xiaopeng Hong, Yaowei Wang, Wangmeng Zuo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22953v1"
tags: ["query:vlm"]
score: 9.0
evidence: 利用时空掩码进行高效的视频语言预训练
tldr: 针对大规模视频-语言预训练中计算成本高、高掩码率导致信息丢失及帧间相关性引起的时间信息泄露等问题，本文提出了一种名为 ClusterSTM 的聚类时空掩码策略。该方法通过帧内聚类将视觉标记划分为语义独立的簇，并保留每个簇中时间密度最高的标记，以在减少冗余的同时保留核心语义。结合视频-文本相关性重建目标，ClusterSTM 在多个基准测试中实现了高效且领先的性能。
selection_source: fresh_fetch
motivation: 现有的掩码视觉建模在处理视频时面临高掩码率下的信息丢失和帧间相关性导致的时间信息泄露挑战。
method: 提出 ClusterSTM 策略，通过帧内聚类划分语义簇并保留高时间密度的标记，同时引入视频-文本相关性重建目标。
result: 在视频-文本检索、视频问答和视频字幕生成等多个任务上均取得了优于现有高效视频-语言模型的效果，达到了新的 SOTA。
conclusion: ClusterSTM 通过创新的聚类掩码机制有效平衡了预训练效率与语义完整性，为高效多模态学习提供了新思路。
---

## 摘要
大规模视频-语言预训练在多模态任务中实现了强大的泛化能力，但通常会产生极高的计算成本。尽管掩码视觉建模（masked visual modeling）的最新进展有助于缓解这一问题，但它们仍面临两个根本性的局限：高掩码率下的严重视觉信息损失，以及由帧间相关性引起的时间信息泄漏。为了应对这些挑战，我们提出了 ClusterSTM，一种用于高效视频-语言预训练的基于聚类的时空掩码策略。ClusterSTM 首先进行帧内聚类，将视觉 token 划分为多个语义独立的聚类，然后通过保留每个聚类中具有最高时间密度的 token 来执行基于聚类的掩码。我们的掩码策略确保保留的 token 在捕捉整体视频内容的同时，表现出强大的时间相关性。此外，我们引入了一种视频-文本相关性重构目标，在传统的视觉重构之外对齐高层多模态语义。在多个基准测试上的广泛实验表明，ClusterSTM 在视频-文本检索、视频问答和视频字幕生成任务上取得了优异的性能，在高效视频-语言模型中树立了新的最先进水平。

## Abstract
Large-scale video-language pretraining enables strong generalization across multimodal tasks but often incurs prohibitive computational costs. Although recent advances in masked visual modeling help mitigate this issue, they still suffer from two fundamental limitations: severe visual information loss under high masking ratios and temporal information leakage caused by inter-frame correlations. To address these challenges, we propose ClusterSTM, a Cluster-Wise Spatio-Temporal Masking strategy for efficient video-language pretraining. ClusterSTM first performs intra-frame clustering to partition visual tokens into multiple semantically independent clusters, then conducts cluster-wise masking by retaining the token with the highest temporal density within each cluster. Our masking strategy ensure that the retained tokens capture holistic video content while exhibit strong temporal correlation. Additionally, we introduce a video-text relevance reconstruction objective that aligns high-level multimodal semantics beyond conventional visual reconstruction. Extensive experiments across multiple benchmarks demonstrate that ClusterSTM achieves superior performance on video-text retrieval, video question answering, and video captioning tasks, establishing a new state-of-the-art among efficient video-language models.

---

## 论文详细总结（自动生成）

### 论文总结：Cluster-Wise Spatio-Temporal Masking for Efficient Video-Language Pretraining

#### 1. 核心问题与研究动机
大规模视频-语言预训练（VLP）虽然泛化能力强，但计算成本极高。现有的掩码视觉建模（MVM）技术虽能提高效率，但存在两个痛点：
*   **视觉信息丢失严重：** 在高掩码率（如 90%）下，随机掩码容易丢弃关键的语义区域（如前景物体），导致模型难以理解整体场景。
*   **时间信息泄露：** 视频帧间具有高度相关性，随机掩码后的 token 很容易通过相邻帧的对应位置直接“推断”出来，导致重建任务变得过于简单，模型学不到深层表征。

#### 2. 方法论：ClusterSTM
为了解决上述问题，论文提出了 **ClusterSTM** 策略，包含以下核心技术：
*   **帧内聚类（Intra-Frame Clustering）：** 使用密度峰值聚类（DPC）算法将每帧的视觉 token 划分为多个语义独立的簇。这样可以确保每个簇代表不同的语义区域（如天空、人、草地）。
*   **基于时间密度的簇内掩码：** 
    *   计算每个 token 的**时间密度**：衡量该 token 与相邻帧中所有 token 的语义相似度总和。
    *   在每个聚类簇中，仅保留时间密度最高的那个 token，掩码掉其余 token。
    *   **核心逻辑：** 高时间密度的 token 意味着它在时间轴上具有更好的语义一致性，保留它们能有效捕捉视频动态并缓解信息泄露。
*   **视频-文本相关性重建（MRM）：** 
    *   不同于传统的像素或特征重建，该方法引入了**多模态重建目标**。
    *   利用教师模型（SigLIP）生成细粒度的“视频-文本相关性矩阵”，要求学生模型重建这一矩阵，从而在预训练阶段就强化跨模态对齐能力。

#### 3. 实验设计
*   **预训练数据集：** WebVid-2M（2.5M 视频-文本对）和 CC3M（3M 图像-文本对），总计约 5.5M 数据规模。
*   **下游任务与 Benchmark：**
    *   **视频-文本检索：** MSRVTT, DiDeMo, ActivityNet, MSVD。
    *   **视频问答（Video QA）：** TGIF (Action/Transition/Frame), MSRVTT-MC/QA, ActivityNet-QA, MSVD-QA。
    *   **视频字幕生成（Video Captioning）：** MSRVTT, MSVD。
*   **对比方法：** 包括 UMT, STM, Singularity, VIOLETv2, HiTeA 等高效模型，以及 InternVideo, CLIP4Clip 等大规模预训练模型。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA A100 GPU**。
*   **训练配置：** 在 5M 数据集上预训练 **10 个 epoch**。
*   **优化器：** AdamW，基础学习率为 1e-4。
*   **批大小（Batch Size）：** 视频 1024，图像 4096。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个检索数据集、7 个问答任务和 2 个字幕生成任务上进行了测试，涵盖了主流的视频理解维度。
*   **消融实验：** 针对掩码策略（帧间 vs 随机 vs 管道 vs 聚类）、掩码比例（75%-95%）、重建目标（像素 vs HOG vs 特征 vs 相关性）以及预训练损失函数进行了详尽的对比。
*   **评价：** 实验设计非常充分且客观。作者不仅对比了同等数据规模的模型，还展示了在更少数据下超越大规模模型（如 138M 数据的 VIOLET）的结果，证明了方法的效率。

#### 6. 主要结论与发现
*   **性能领先：** ClusterSTM 在所有下游任务中均达到了新的 SOTA（最先进水平），尤其在检索任务中，Recall@1 显著优于之前的 STM 和 UMT。
*   **掩码效率：** 证明了在 90% 的极高视频掩码率下，通过聚类保留关键 token 依然能保持极高的语义完整性。
*   **多模态重建的优越性：** 实验发现重建“视频-文本相关性”比单纯重建视觉特征对跨模态任务更有帮助。

#### 7. 优点
*   **创新的掩码机制：** 结合了空间聚类和时间密度，从根本上解决了视频数据冗余和信息泄露的矛盾。
*   **高效性：** 相比于 STM（掩码率 30%），ClusterSTM 使用 90% 的掩码率达到了更好的效果，极大地降低了计算开销。
*   **多模态对齐前置：** 将跨模态相关性引入重建目标，使模型在视觉编码阶段就具备了语言感知能力。

#### 8. 不足与局限
*   **教师模型依赖：** 掩码生成和相关性矩阵重建高度依赖于预训练好的教师模型（如 SigLIP），如果教师模型存在偏差，可能会限制学生模型的上限。
*   **聚类开销：** 虽然减少了 Transformer 的计算量，但在线进行 DPC 聚类和密度计算会带来额外的 CPU/GPU 预处理开销，文中未详细量化这部分时间成本。
*   **超参数敏感性：** 聚类数量 $N_c$ 和截止距离 $d_c$ 等参数可能需要针对不同类型的视频（如快节奏运动 vs 静态场景）进行微调。

（完）
