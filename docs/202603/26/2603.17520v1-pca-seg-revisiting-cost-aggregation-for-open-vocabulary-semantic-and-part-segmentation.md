---
title: "PCA-Seg: Revisiting Cost Aggregation for Open-Vocabulary Semantic and Part Segmentation"
title_zh: PCA-Seg：重新审视开放词汇语义与部分分割中的代价聚合
authors: "Jianjian Yin, Tao Chen, Yi Chen, Gensheng Pei, Xiangbo Shu, Yazhou Yao, Fumin Shen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17520v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于开放词汇分割的视觉语言模型
tldr: 本文针对开放词汇语义及部分分割（OSPS）任务，指出传统串行聚合方式会导致类别语义与空间上下文间的知识干扰。为此，提出了并行代价聚合框架PCA-Seg。该框架包含专家驱动感知学习（EPL）模块，利用多专家解析器和系数映射器自适应融合多维特征；同时引入特征正交解耦（FOD）策略以减少冗余。PCA-Seg在保持极低参数增量（0.35M）的同时，在八大基准测试上刷新了SOTA记录，显著提升了视觉-语言对齐效果。
selection_source: fresh_fetch
motivation: 现有方法在处理代价体时采用串行聚合结构，导致类别语义与空间上下文之间产生知识干扰。
method: 提出并行代价聚合（PCA-Seg）范式，结合专家驱动感知学习模块与特征正交解耦策略来高效整合语义与上下文信息。
result: 在八个基准测试中均取得SOTA性能，且每个并行块仅需额外增加0.35M参数。
conclusion: PCA-Seg通过并行结构和特征解耦有效缓解了知识干扰问题，为开放词汇分割任务提供了高效且鲁棒的解决方案。
---

## 摘要
视觉语言模型（VLM）的最新进展在开放词汇语义与部分分割（OSPS）领域引起了广泛关注。然而，现有方法通过空间聚合和类别聚合的串行结构从代价体（cost volumes）中提取图文对齐线索，导致类别级语义与空间上下文之间产生知识干扰。因此，本文提出了一种简单而有效的并行代价聚合（PCA-Seg）范式来缓解上述挑战，使模型能够从代价体中捕捉更丰富的视觉语言对齐信息。具体而言，我们设计了一个专家驱动的感知学习（EPL）模块，能够高效整合语义流和上下文流。该模块包含一个多专家解析器，用于从多个视角提取互补特征。此外，还设计了一个系数映射器，为每个特征自适应地学习像素级权重，从而将互补知识整合到统一且鲁棒的特征嵌入中。此外，我们提出了一种特征正交化解耦（FOD）策略，以减轻语义流和上下文流之间的冗余，使 EPL 模块能够从正交化特征中学习多样化的知识。在八个基准测试上的广泛实验表明，PCA-Seg 中的每个并行块仅增加 0.35M 参数，同时实现了最先进的 OSPS 性能。

## Abstract
Recent advances in vision-language models (VLMs) have garnered substantial attention in open-vocabulary semantic and part segmentation (OSPS). However, existing methods extract image-text alignment cues from cost volumes through a serial structure of spatial and class aggregations, leading to knowledge interference between class-level semantics and spatial context. Therefore, this paper proposes a simple yet effective parallel cost aggregation (PCA-Seg) paradigm to alleviate the above challenge, enabling the model to capture richer vision-language alignment information from cost volumes. Specifically, we design an expert-driven perceptual learning (EPL) module that efficiently integrates semantic and contextual streams. It incorporates a multi-expert parser to extract complementary features from multiple perspectives. In addition, a coefficient mapper is designed to adaptively learn pixel-specific weights for each feature, enabling the integration of complementary knowledge into a unified and robust feature embedding. Furthermore, we propose a feature orthogonalization decoupling (FOD) strategy to mitigate redundancy between the semantic and contextual streams, which allows the EPL module to learn diverse knowledge from orthogonalized features. Extensive experiments on eight benchmarks show that each parallel block in PCA-Seg adds merely 0.35M parameters while achieving state-of-the-art OSPS performance.

---

## 论文详细总结（自动生成）

### PCA-Seg: 重新审视开放词汇语义与部分分割中的代价聚合

#### 1. 核心问题与研究动机
*   **研究背景**：开放词汇语义与部分分割（OSPS）旨在识别和分割图像中任意类别的对象及其组件。现有主流方法（如 CAT-Seg, DeCLIP）通常采用**串行结构**来处理代价体（Cost Volume），即先后进行空间聚合（Spatial Aggregation）和类别聚合（Class Aggregation）。
*   **核心问题**：论文指出，这种串行结构会导致**知识干扰（Knowledge Interference）**。前一阶段的聚合偏差会传播并放大到后一阶段，导致类别语义与空间上下文信息的混淆（例如，将卡车区域误分类为跑道）。
*   **整体含义**：本文旨在通过将串行聚合改为**并行聚合**，并引入解耦机制，来提取更纯净、互补的图文对齐特征。

#### 2. 方法论
PCA-Seg 提出了并行代价聚合范式，核心组件包括：
*   **并行聚合结构**：将代价体同时输入空间聚合流（提取结构上下文）和类别聚合流（提取语义特征），实现两者的初步解耦。
*   **专家驱动感知学习（EPL）模块**：
    *   **多专家解析器（ME-Parser）**：使用多个并行的卷积块（专家）从不同视角解析拼接后的语义和上下文特征，捕捉多样化的互补知识。
    *   **系数映射器（Co-Mapper）**：一个轻量级网络，为每个专家的输出自适应地学习像素级权重系数，通过加权求和实现特征的高效融合。
*   **特征正交化解耦（FOD）策略**：
    *   为了确保并行流学习到的知识不冗余，引入了**正交化损失（$L_{od}$）**。
    *   该损失通过最小化语义特征和空间特征之间的余弦相似度平方，强制两者在表示空间上趋于正交，从而扩大模型探索的特征空间。
*   **训练目标**：总损失 $L = L_{sup} + \lambda L_{od}$，其中 $L_{sup}$ 为交叉熵监督损失，$\lambda$ 设为 0.01。

#### 3. 实验设计
*   **数据集**：
    *   **语义分割（OVSS）**：在 COCO-Stuff 上训练，在 ADE20K (A-847, A-150)、PASCAL-Context (PC-459, PC-59)、PASCAL VOC (PAS-20, PAS-20b) 等 6 个基准上评估。
    *   **部分分割（OVPS）**：在 Pascal-Part-116 和 ADE20K-Part-234 上评估。
*   **Benchmark 与对比方法**：对比了包括 LSeg, ZegFormer, OVSeg, CAT-Seg, DeCLIP, H-CLIP, PartCATSeg 等在内的十余种 SOTA 方法。
*   **评估指标**：采用 mIoU（平均交并比）和 h-IoU（见过的和未见过类别的调和平均值）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A6000 GPU**。
*   **训练时长/迭代**：
    *   OVSS 任务：训练 80k 次迭代。
    *   OVPS 任务：Pascal-Part-116 训练 30k 次，ADE20K-Part-234 训练 20k 次。
*   **参数规模**：每个并行块仅增加 **0.35M** 参数，相比整体模型（约 70.3M）增量极小。

#### 5. 实验数量与充分性
*   **实验规模**：在 **8 个主流基准数据集**上进行了测试，涵盖了从粗粒度语义到细粒度部分分割的多种场景。
*   **消融实验**：非常充分，包括：
    *   串行 vs 并行的直接对比。
    *   EPL 和 FOD 组件的有效性分析。
    *   专家数量 $Z$ 和损失权重 $\lambda$ 的参数敏感性分析。
    *   特征冗余度（CCA 分析）和知识耦合度的定量可视化。
*   **客观性**：通过在相同 Backbone（ViT-B/16, ViT-L/14）下与最新方法对比，证明了性能提升并非单纯来自模型规模扩大。

#### 6. 主要结论与发现
*   **性能领先**：PCA-Seg 在所有 8 个基准测试中均达到了 SOTA 性能。在 A-150 数据集上，比之前的 SOTA 提升了约 1.8% mIoU。
*   **并行优越性**：实验证明并行结构能有效缓解知识干扰，FOD 策略能将特征冗余度从串行结构的 88% 以上降低到接近 0%。
*   **高效性**：在仅增加极少量参数和可接受的计算开销（显存增加 0.96G，单图推理延迟增加 0.08s）的前提下，获得了显著的性能增益。

#### 7. 优点
*   **架构简洁**：通过简单的并行化改造解决了串行结构根深蒂固的干扰问题。
*   **解耦彻底**：引入正交化约束（FOD）是一个亮点，从数学层面保证了特征的多样性。
*   **通用性强**：一套架构同时适用于语义分割和更细粒度的部分分割任务。
*   **参数效率极高**：0.35M 的参数增量对于现代大模型几乎可以忽略不计。

#### 8. 不足与局限
*   **计算开销略增**：虽然参数量增加很少，但由于并行分支的存在，FLOPs（增加 34.4G）和显存占用有所上升，对于显存极度受限的设备可能存在压力。
*   **超参数依赖**：FOD 损失的权重 $\lambda$ 和专家数量 $Z$ 需要针对不同任务进行微调以达到最优效果。
*   **应用限制**：论文主要聚焦于代价体的聚合优化，对于视觉语言模型本身的跨模态对齐瓶颈（如 CLIP 原始特征的局限性）未做根本性改变。

（完）
