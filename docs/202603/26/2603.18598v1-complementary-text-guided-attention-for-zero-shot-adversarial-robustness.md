---
title: Complementary Text-Guided Attention for Zero-Shot Adversarial Robustness
title_zh: 互补文本引导注意力用于零样本对抗鲁棒性
authors: "Lu Yu, Haiyang Zhang, Changsheng Xu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18598v1"
tags: ["query:vlm"]
score: 9.0
evidence: CLIP模型的对抗鲁棒性
tldr: 针对视觉语言模型（如CLIP）在零样本场景下易受对抗攻击的问题，本文发现对抗扰动会导致文本引导注意力偏移。为此提出了TGA-ZSR框架，通过局部注意力细化和全局注意力约束来增强鲁棒性。针对其可能关注无关特征的局限，进一步提出Comp-TGA，结合类别提示和反向非类别提示的互补注意力，显著提升了模型在16个数据集上的零样本对抗鲁棒性。
selection_source: fresh_fetch
motivation: 视觉语言模型在零样本任务中表现优异但易受对抗攻击，且对抗扰动会显著干扰文本引导的注意力分布。
method: 提出了结合局部细化与全局约束的TGA-ZSR，以及利用类别与非类别提示互补注意力的Comp-TGA方法。
result: "在16个数据集上的实验表明，TGA-ZSR和Comp-TGA分别将零样本对抗鲁棒准确率提升了9.58%和11.95%。"
conclusion: 通过优化和互补文本引导注意力机制，可以有效增强预训练视觉语言模型的零样本对抗鲁棒性。
---

## 摘要
由于其出色的零样本能力，预训练视觉语言模型（如 CLIP）在各个领域引起了广泛关注并得到了广泛应用。尽管如此，研究发现 CLIP 容易受到对抗样本的影响。通过实验分析，我们观察到一种现象，即对抗扰动会导致文本引导注意力的偏移。基于这一观察，我们提出了一种简单而有效的策略：用于零样本鲁棒性的文本引导注意力（TGA-ZSR）。该框架包含两个组件：局部注意力细化模块和全局注意力约束模块。我们的目标是保持 CLIP 模型的泛化能力并增强其对抗鲁棒性。此外，全局注意力约束模块利用干净样本从目标模型和原始模型中获取文本引导注意力。其目标是在增强整体鲁棒性的同时，保持模型在干净样本上的性能。然而，我们观察到该方法偶尔会关注无关或伪特征，这可能导致性能欠佳，并在某些场景下削弱其鲁棒性。为了克服这一局限性，我们进一步提出了一种名为互补文本引导注意力（Comp-TGA）的新方法。该方法整合了两种类型的前景注意力：由类别提示引导的注意力和由非类别提示驱动的反向注意力。这些互补的注意力机制使模型能够捕捉到更全面、更准确的前景表示。实验验证表明，在 16 个数据集上，TGA-ZSR 和 Comp-TGA 在零样本鲁棒准确率方面分别比当前最先进的技术提高了 9.58% 和 11.95%。

## Abstract
Due to the impressive zero-shot capabilities, pre-trained vision-language models (e.g., CLIP), have attracted widespread attention and adoption across various domains. Nonetheless, CLIP has been observed to be susceptible to adversarial examples. Through experimental analysis, we have observed a phenomenon wherein adversarial perturbations induce shifts in text-guided attention. Building upon this observation, we propose a simple yet effective strategy: Text-Guided Attention for Zero-Shot Robustness (TGA-ZSR). This framework incorporates two components: Local Attention Refinement Module and Global Attention Constraint Module. Our goal is to maintain the generalization of the CLIP model and enhance its adversarial robustness. Additionally, the Global Attention Constraint Module acquires text-guided attention from both the target and original models using clean examples. Its objective is to maintain model performance on clean samples while enhancing overall robustness. However, we observe that the method occasionally focuses on irrelevant or spurious features, which can lead to suboptimal performance and undermine its robustness in certain scenarios. To overcome this limitation, we further propose a novel approach called Complementary Text-Guided Attention (Comp-TGA). This method integrates two types of foreground attention: attention guided by the class prompt and reversed attention driven by the non-class prompt. These complementary attention mechanisms allow the model to capture a more comprehensive and accurate representation of the foreground. The experiments validate that TGA-ZSR and Comp-TGA yield 9.58% and 11.95% improvements respectively, in zero-shot robust accuracy over the current state-of-the-art techniques across 16 datasets.

---

## 论文详细总结（自动生成）

这篇论文发表于 2026 年的 *IEEE Transactions on Pattern Analysis and Machine Intelligence (TPAMI)*，探讨了视觉语言模型（VLM）在零样本（Zero-Shot）场景下的对抗鲁棒性问题。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：预训练视觉语言模型（如 CLIP）在零样本学习中表现卓越，但极易受到对抗样本（Adversarial Examples）的攻击。
*   **核心发现**：作者通过实验观察到一种“文本引导注意力偏移”现象——对抗扰动虽然在视觉上微小，但会导致模型在处理图像时，其注意力从目标物体偏移到背景或其他无关区域。
*   **研究动机**：利用文本引导的注意力机制来解释对抗攻击，并以此为约束手段，在不牺牲模型泛化能力（干净样本准确率）的前提下，增强其对抗鲁棒性。

### 2. 方法论
论文先后提出了两个框架：**TGA-ZSR** 及其改进版 **Comp-TGA**。

*   **TGA-ZSR 框架**：
    *   **局部注意力细化模块 (LARM)**：将目标模型在对抗样本上的注意力图，与原始模型在干净样本上的注意力图进行对齐（L2 损失），强制模型在受攻击时仍关注正确区域。
    *   **全局注意力约束模块 (GACM)**：约束目标模型在干净样本上的注意力行为与原始模型保持一致，以防止对抗训练导致模型在常规任务上性能下降。
*   **Comp-TGA 方法（核心改进）**：
    *   **核心思想**：发现单一的类别提示（Class Prompt）有时会关注到错误特征。
    *   **技术细节**：引入“互补”概念。利用类别提示（如 "This is a photo of a [class]"）获取正向注意力，同时利用非类别提示（如 "This is not a photo of a [class]"）获取背景注意力。
    *   **融合公式**：$A_{comp} = A_{class} \odot (1 - A_{non-class})$。通过这种方式，模型能更精确地锁定前景目标，过滤背景噪声。
*   **训练流程**：在 Tiny-ImageNet 上进行对抗微调，损失函数结合了交叉熵损失（LCE）、LARM 损失和 GACM 损失。

### 3. 实验设计
*   **数据集**：在 **Tiny-ImageNet** 上进行微调，并在 **15 个其他数据集**上进行零样本测试（包括 CIFAR-10/100, ImageNet, STL-10, Food101, OxfordPets 等），共计 16 个数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **TeCoA** (ICLR 2023)、**PMG-AFT** (CVPR 2024)、**FARE** (ICML 2024)、**LAAT** (CVPR 2024) 和 **TTC** (CVPR 2025) 等最先进方法。
    *   评估了多种攻击类型：**PGD** (100步)、**CW** 攻击、**AutoAttack** (标准强力攻击) 以及针对性的**自适应攻击 (Adaptive Attack)**。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA RTX 3090** GPU。
*   **训练时长**：单次训练周期约为 **3 到 4 小时**。
*   **显存占用**：Comp-TGA 的训练显存占用约为 **16,293 MB**，相比 SOTA 方法 PMG-AFT（18,449 MB）更具效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 16 个数据集的零样本迁移实验，进行了 4 种不同强度的对抗攻击测试（$\epsilon$ 从 1/255 到 8/255）。
*   **消融实验**：详细分析了 LARM、GACM 模块的作用，对比了视觉注意力（Grad-CAM）与文本引导注意力的差异，并研究了不同提示词（Prompt）对结果的影响。
*   **客观性**：作者不仅报告了鲁棒准确率（Robust Accuracy），还报告了干净样本准确率（Clean Accuracy）和综合指标（Overall Accuracy），并引入了自适应攻击来验证模型是否通过“梯度掩盖”等欺骗手段获得虚假鲁棒性。实验设计全面且公平。

### 6. 主要结论与发现
*   **性能提升**：Comp-TGA 在 16 个数据集上的平均零样本鲁棒准确率比 PMG-AFT 提升了 **11.95%**。
*   **平衡性**：该方法在增强鲁棒性的同时，较好地保持了模型在未受攻击样本上的识别能力，解决了对抗训练中常见的“鲁棒性-泛化性”权衡难题。
*   **解释性**：证明了文本引导注意力是理解和防御对抗攻击的有效工具。

### 7. 优点
*   **可解释性强**：通过可视化注意力图的变化，直观地展示了对抗攻击如何失效以及防御如何生效。
*   **互补机制创新**：利用“非类别提示”来辅助定位前景，是一个简单但非常有效的逻辑改进。
*   **效率较高**：在提升性能的同时，训练时间和显存消耗优于部分同类 SOTA 方法。

### 8. 不足与局限
*   **极端攻击下的局限**：在面对极强攻击（如 AutoAttack）时，虽然表现优于基准，但绝对准确率仍有较大提升空间。
*   **注意力机制单一**：目前采用的是简单的点积注意力，对于极其复杂的场景（多目标重叠等），这种线性映射可能不足以捕捉所有语义细节。
*   **自适应攻击的深度**：虽然论文尝试了自适应攻击，但作者也承认，针对特定防御结构的攻击手段仍有进一步演进的可能，防御的绝对安全性需持续验证。

（完）
