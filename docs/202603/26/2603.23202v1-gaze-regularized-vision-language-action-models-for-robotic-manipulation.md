---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 用于机器人操作的视线正则化视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于机器人操作的视觉-语言-动作 (VLA) 模型
tldr: 本研究针对视觉-语言-动作（VLA）模型在精细机器人操作任务中缺乏主动视觉注意力分配的问题，提出了一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，以此约束Transformer的内部注意力，使模型在不改变架构或增加推理开销的情况下，学习到人类的感知偏置。实验表明，该方法显著提升了操作任务的成功率、收敛速度和鲁棒性，同时增强了模型的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细操作时缺乏有效的视觉注意力分配机制，而人类注视数据蕴含了丰富的任务意图和规划信息。
method: 提出一种注视正则化框架，通过KL散度将人类注视模式引入Transformer的注意力机制，引导模型关注任务相关的关键特征。
result: "在机器人操作基准测试中实现了4-12%的性能提升，并表现出更快的训练收敛速度以及对光照和噪声的更强鲁棒性。"
conclusion: 引入人类感知先验能显著加速机器人学习过程，在提升任务性能的同时增强了系统的可解释性和部署效率。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了进展，但由于当前模型缺乏主动视觉注意力分配机制，机器人操作在处理细粒度任务时仍面临困难。人类视线自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们提出了一种视线正则化训练框架，在不改变架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。我们的方法将时间聚合的视线热图转换为 patch 级分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，建立了对任务相关特征的归纳偏置。当集成到现有的 VLA 架构中时，我们的方法在各项操作基准测试中实现了 4-12% 的提升。视线正则化模型可以用更少的训练步数达到同等性能，并在光照变化和传感器噪声下保持鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，增强了对机器人系统的信任。此外，我们的框架不需要眼动追踪设备，并可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提高任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

### 论文总结：用于机器人操作的视线正则化视觉-语言-动作模型 (Gaze-Regularized VLA)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型在处理精细机器人操作时面临“被动感知”的局限。模型通常将所有视觉信息视为整体，试图同时学习“看哪里”和“如何做”，这导致训练效率低下、收敛缓慢，且在复杂环境下缺乏鲁棒性。
相比之下，人类在操作时会通过**主动视觉注意力（注视/Gaze）**来分配感知资源，注视点预示了任务意图和动作规划。本论文的核心动机是：**将人类的视觉注意力先验引入 VLA 模型的训练中，使其从被动观察者转变为主动感知者**，从而提升模型在复杂操作任务中的性能和可解释性。

#### 2. 核心方法论
论文提出了一种**注视正则化（Gaze-Regularization）训练框架**，其核心思想是在训练阶段约束模型的内部注意力机制，使其模拟人类的注视模式。

*   **注视先验生成**：由于现有机器人数据集缺乏人类眼动数据，作者使用预训练的注视预测模型（如 GLC 网络）在视频剪辑上生成合成注视热图（Heatmaps）。
*   **时间聚合**：为了捕捉人类的预测性注视（即在动作发生前看向目标），对连续帧的热图进行加权平均聚合，生成更稳定的注视分布。
*   **补丁级分布转换**：将连续的热图投影到与 Transformer 视觉 Token 相同的网格上，转换为离散的概率分布 $G_{i,t}$。
*   **注意力正则化**：从 VLA 模型的 Transformer 最后一层提取视觉-语言交叉注意力分布 $S_{i,t}$。通过最小化两者之间的 **KL 散度（Kullback-Leibler Divergence）** 来施加正则化：
    $$L_{total} = L_{action} + \lambda \cdot D_{KL}(G_{i,t} \| S_{i,t})$$
*   **推理阶段**：该方法仅在训练时使用，推理时不需要注视预测模型或眼动设备，保持了部署的轻量化和实时性。

#### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **LIBERO Suite**：包括 LIBERO-Spatial（空间位置）、LIBERO-Object（细粒度物体识别）、LIBERO-Goal（顺序推理）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim**：用于验证跨域泛化能力（如 Transfer Cube, Peg Insertion）。
    *   **真实世界实验**：在 Aloha 机器人上进行抓取、放置等任务。
*   **对比方法**：
    *   **Baseline**：不带正则化的标准 VLA 模型（如 Pi-0, OpenVLA）。
    *   **消融变体**：不同正则化强度（$\lambda$）、不同的注视预测模型（DeepGaze vs. GLC）、均匀分布注意力等。
*   **鲁棒性测试**：在光照变化、传感器噪声、指令扰动（更换动词）和视角缺失（关闭一个摄像头）的情况下进行测试。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   实验记录了不同训练步数（10k, 20k, 30k 步）下的性能表现，显示出正则化模型在 20k 步时即可达到基准模型 30k 步以上的性能，证明了极高的样本效率。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境（LIBERO, ALOHA）和真实世界，测试了超过 10 种不同的空间配置和任务类型。
*   **充分性**：实验设计非常全面。不仅验证了成功率的提升（4-12%），还通过消融实验确定了 $\lambda=0.001$（软约束）为最优值。
*   **客观性**：作者在两种不同的 VLA 架构（Pi-0 和 OpenVLA）上验证了该方法的有效性，证明了其作为通用模块的普适性。同时，通过 Top-k 重叠率定量分析了注意力对齐的程度，而非仅凭视觉直观判断。

#### 6. 主要结论与发现
*   **性能提升**：在 LIBERO-Spatial 上，成功率从 85.9% 提升至 95.5%；在多任务 LIBERO-10 上提升了 11.8%。
*   **收敛加速**：正则化模型能以更少的训练步数达到更高的性能，显著提高了训练效率。
*   **鲁棒性增强**：在光照变化和相机噪声下，正则化模型表现出更强的稳定性，因为它学会了关注语义上关键的区域（如物体边缘），而非背景噪声。
*   **可解释性**：可视化结果显示，正则化后的模型注意力点与人类策略高度一致，增强了人类对机器人决策的信任。

#### 7. 优点
*   **架构无关性**：作为一种辅助损失函数，它可以无缝集成到任何基于 Transformer 的 VLA 模型中。
*   **零推理开销**：训练完成后，推理过程不需要额外的计算资源或传感器输入。
*   **数据驱动的先验**：巧妙地利用预训练注视模型解决了机器人领域缺乏眼动标注数据的难题。
*   **显著的鲁棒性**：在视觉环境恶化时，注意力聚焦机制表现出了极佳的抗干扰能力。

#### 8. 不足与局限
*   **合成注视的偏差**：虽然验证了合成注视的有效性，但合成数据与真实人类在复杂操作中的细微注视差异仍可能限制性能上限。
*   **任务依赖性**：注视预测模型本身可能在某些极端未见场景下失效，从而提供错误的引导信号。
*   **实时性与预处理**：虽然推理无开销，但训练前需要对大规模数据集进行注视热图的预计算，增加了数据准备阶段的负担。
*   **真实世界规模**：虽然进行了真实机器人实验，但样本量和任务复杂度相比仿真环境仍较小，需进一步在大规模真实场景中验证。

（完）
