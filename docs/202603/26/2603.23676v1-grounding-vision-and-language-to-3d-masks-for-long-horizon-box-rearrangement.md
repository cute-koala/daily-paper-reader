---
title: Grounding Vision and Language to 3D Masks for Long-Horizon Box Rearrangement
title_zh: 将视觉与语言对齐至 3D 掩码以实现长程箱体重排
authors: "Ashish Malik, Caleb Lowe, Aayam Shrestha, Stefan Lee, Fuxin Li, Alan Fern"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23676v1"
tags: ["query:vlm"]
score: 9.0
evidence: 将视觉和语言对齐到3D掩码以进行规划
tldr: "针对长程3D物体重新排列任务，本文提出了RAMP-3D模型。该模型克服了传统符号规划器和2D视觉语言模型在处理复杂3D几何及多物体推理时的局限。通过将自然语言指令和RGB-D观测映射为成对的3D分割掩码（抓取物体和目标区域），RAMP-3D实现了反应式的多步取放规划。在包含多达30个物体的仓库环境实验中，该方法以79.5%的成功率显著优于现有基准。"
selection_source: fresh_fetch
motivation: 现有的符号规划器和2D视觉语言模型在处理具有复杂3D几何约束和大量物体的长程规划任务时表现不佳。
method: 提出RAMP-3D框架，通过预测“抓取物体”和“目标区域”的成对3D掩码，将长程规划转化为序列化的反应式动作生成。
result: "在11种仓库重排任务变体中，RAMP-3D达到了79.5%的成功率，在处理多物体和复杂指令方面显著优于2D VLM基准。"
conclusion: 基于3D掩码的反应式策略是替代传统符号规划流水线、实现长程3D任务规划的一种极具潜力的新方案。
---

## 摘要
我们研究了在 3D 环境中，仅利用视觉观测从不完全指定的自然语言目标出发进行长程规划，重点关注多步 3D 箱体重排任务。现有方法通常依赖于符号规划器（其状态与目标的关联对齐较为脆弱），或者依赖于从 2D 视觉语言模型 (VLM) 直接生成动作序列。这两种方法在处理大量物体、丰富的 3D 几何形状以及隐式语义约束的推理时都面临困难。3D VLM 的最新进展展示了自然语言指代物与 3D 分割掩码之间的强对齐能力，这表明了实现更通用规划能力的潜力。我们扩展了现有的 3D 对齐模型，并提出了反应式动作掩码规划器 (RAMP-3D)，该规划器将长程规划建模为成对 3D 掩码的序列反应式预测：一个“物体掩码”指示抓取对象，一个“目标区域掩码”指定放置位置。该系统处理 RGB-D 观测和自然语言任务规范，以反应式地生成用于 3D 箱体重排的多步取放动作。我们在包含 1-30 个箱体和多种自然语言约束的仓库式环境中，针对 11 种任务变体进行了实验。RAMP-3D 在长程重排任务上实现了 79.5% 的成功率，并显著优于基于 2D VLM 的基准模型，证明了基于掩码的反应式策略是长程规划中替代符号流水线的一种极具前景的选择。

## Abstract
We study long-horizon planning in 3D environments from under-specified natural-language goals using only visual observations, focusing on multi-step 3D box rearrangement tasks. Existing approaches typically rely on symbolic planners with brittle relational grounding of states and goals, or on direct action-sequence generation from 2D vision-language models (VLMs). Both approaches struggle with reasoning over many objects, rich 3D geometry, and implicit semantic constraints. Recent advances in 3D VLMs demonstrate strong grounding of natural-language referents to 3D segmentation masks, suggesting the potential for more general planning capabilities. We extend existing 3D grounding models and propose Reactive Action Mask Planner (RAMP-3D), which formulates long-horizon planning as sequential reactive prediction of paired 3D masks: a "which-object" mask indicating what to pick and a "which-target-region" mask specifying where to place it. The resulting system processes RGB-D observations and natural-language task specifications to reactively generate multi-step pick-and-place actions for 3D box rearrangement. We conduct experiments across 11 task variants in warehouse-style environments with 1-30 boxes and diverse natural-language constraints. RAMP-3D achieves 79.5% success rate on long-horizon rearrangement tasks and significantly outperforms 2D VLM-based baselines, establishing mask-based reactive policies as a promising alternative to symbolic pipelines for long-horizon planning.

---

## 论文详细总结（自动生成）

### 论文详细总结：将视觉与语言对齐至 3D 掩码以实现长程箱体重排 (RAMP-3D)

#### 1. 核心问题与整体含义（研究动机和背景）
在仓库等复杂 3D 环境中，机器人需要根据模糊的自然语言指令（如“按颜色分组并堆叠，高度不超过两层”）进行长程物体整理。
*   **现有挑战**：
    *   **符号规划器**：依赖手工设计的谓词和状态表示，在处理复杂的 3D 几何关系和噪声感知时非常脆弱。
    *   **2D 视觉语言模型 (VLM)**：缺乏对 3D 空间、遮挡和体积关系的深度理解，难以处理长程重排任务。
*   **核心动机**：利用 3D VLM 强大的“语言-3D 掩码”对齐能力，跳过显式的符号层，直接将视觉和语言输入映射为 3D 空间中的动作掩码，从而实现更具鲁棒性的反应式规划。

#### 2. 论文提出的方法论：RAMP-3D
RAMP-3D 将长程规划表述为序列化的 3D 掩码预测任务。
*   **核心思想**：在每个步骤，模型接收多视图 RGB-D 图像和自然语言指令，输出两个 3D 分割掩码：**“抓取哪个物体” (which-object)** 和 **“放置到哪个目标区域” (which-target-region)**。
*   **关键技术细节**：
    *   **基础架构**：基于 UniVLG。使用 Swin Transformer 提取 2D 特征，通过深度图反投影至 3D 空间并进行体素化融合，使用 RoBERTa 编码文本。
    *   **配对一致性损失 (Pair-consistency Loss)**：引入学习到的配对特征（Pickup/Putdown Embeddings），通过对比学习确保预测的抓取物体与放置目标在逻辑上是匹配的。
    *   **动作表示**：动作直接在 3D 掩码空间定义。预测的掩码随后被投影回物体 ID 或几何坐标，供底层控制器执行。
    *   **终止预测**：增加了一个轻量级的“完成” (Done) 分支，用于判断任务何时结束。
*   **算法流程**：观测刷新 -> 3D 特征融合 -> Transformer 解码器生成查询 -> 预测抓取/放置掩码对 -> 执行动作 -> 循环直至预测“完成”。

#### 3. 实验设计
*   **场景与数据集**：在 IsaacSim 仿真环境中构建了一个 12m x 12m 的仓库，包含多达 30 个箱体、托盘、货架和干扰物。
*   **任务变体 (Benchmark)**：设计了 **11 种任务类型**，涵盖高度限制、颜色优先级、空间排序、避开堆叠、可访问性约束等。
*   **对比方法**：
    *   **2D-pointer**：一个强基准，结合了 GPT-5.1（负责高层逻辑规划）和 Molmo-72B（负责在 2D 顶视图中进行坐标点选）。
*   **评估模式**：
    *   **Snap-to-target**：理想化放置（对齐到预设网格）。
    *   **Free-form**：完全根据预测掩码的质心进行放置（更接近真实物理情况）。

#### 4. 资源与算力
*   **模型组件**：使用了 Swin Transformer (视觉)、RoBERTa-base (文本) 和 GPT-oss-120b (用于生成训练数据的语言改写)。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到训练集包含约 9.5k 个场景样本，测试集包含 2.2k 个样本。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单步预测准确率测试（2.2k 实例）。
    *   进行了长程规划完整执行测试（200 个随机场景，覆盖 11 种任务）。
    *   进行了掩码质量 (IoU) 评估。
    *   **消融实验**：验证了辅助预测任务（如识别空闲位置、已放置物体等）和语言泛化性（针对未见过的指令模板）的影响。
*   **充分性评价**：实验设计较为充分，涵盖了从感知（掩码 IoU）到决策（单步有效性）再到执行（长程成功率）的全流程。对比 2D 基准的方法选择也具有代表性，客观展示了 3D 建模的必要性。

#### 6. 主要结论与发现
*   **性能优异**：RAMP-3D 在长程重排任务中达到了 **79.5%** 的成功率，远超 2D 基准（2D 基准在单步有效性上仅为 41.4%）。
*   **3D 优势**：2D 模型在处理非物体特征（如托盘上的空位）时表现极差，而 3D 掩码能更好地捕捉空间几何结构。
*   **复杂度敏感**：随着箱体数量增加（从 10 个增加到 30 个），成功率有所下降，主要原因是长程误差累积和更复杂的遮挡关系。
*   **语言泛化**：通过 LLM 数据增强，模型对未见过的语言表达方式具有较强的鲁棒性。

#### 7. 优点
*   **端到端掩码规划**：避免了脆弱的符号逻辑层，使系统能直接处理原始视觉输入。
*   **几何感知力强**：通过 3D 掩码直接编码了物理可行性和空间占用，天然支持堆叠和避障推理。
*   **任务多样性**：成功处理了包含多种常识约束（如“保持可访问性”）的复杂长程任务。

#### 8. 不足与局限
*   **缺乏前瞻性 (Lookahead)**：模型是纯反应式的，在处理需要全局最优解或具有复杂依赖关系的非对称任务时，可能会陷入局部最优。
*   **仿真与现实差距**：实验完全在仿真中进行，未考虑真实机器人的运动学约束、碰撞检测及感知噪声。
*   **计算开销**：3D 体素化和多视图融合计算量较大，论文未讨论实时性表现。
*   **场景复杂度限制**：虽然物体数量多，但物体种类（箱体）相对单一，对更广泛语义类别的泛化能力有待验证。

（完）
