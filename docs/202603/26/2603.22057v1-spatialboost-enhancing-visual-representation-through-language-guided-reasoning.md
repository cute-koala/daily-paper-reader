---
title: "SpatialBoost: Enhancing Visual Representation through Language-Guided Reasoning"
title_zh: SpatialBoost：通过语言引导推理增强视觉表征
authors: "Byungwoo Jeon, Dongyoung Kim, Huiwon Jang, Insoo Kim, Jinwoo Shin"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22057v1"
tags: ["query:vlm"]
score: 9.0
evidence: 通过语言引导推理增强视觉编码器的空间意识
tldr: 针对预训练视觉编码器在2D训练下缺乏3D空间感知的问题，本文提出SpatialBoost框架。该方法通过将图像的3D空间信息转化为语言描述，并利用大语言模型（LLM）的多轮思维链（CoT）推理过程，将层级化的空间知识注入视觉编码器。实验表明，该框架显著提升了DINOv3等模型在ADE20K等视觉任务上的表现，为增强视觉表征提供了新路径。
selection_source: fresh_fetch
motivation: 现有的视觉编码器因主要在2D数据上训练而难以捕捉现实世界的3D空间关系，限制了其在复杂场景下的应用。
method: 采用SpatialBoost框架，利用LLM和多轮思维链推理将密集的3D空间知识以语言形式注入视觉编码器。
result: "在ADE20K基准测试中，SpatialBoost将DINOv3的mIoU提升了3.8%，达到了59.7的领先水平。"
conclusion: 结合语言引导的推理和空间知识注入，可以显著增强视觉编码器的空间意识和下游任务性能。
---

## 摘要
尽管大规模预训练图像表征模型（即视觉编码器）在各种视觉任务中取得了显著成功，但它们主要是在二维（2D）图像数据上进行训练的，因此往往无法捕捉现实世界中物体与背景之间的三维（3D）空间关系，这限制了它们在许多下游应用中的有效性。为了解决这一问题，我们提出了 SpatialBoost，这是一个可扩展的框架，通过注入以语言描述表达的 3D 空间知识，来增强现有预训练视觉编码器的空间感知能力。其核心思想是将 2D 图像中密集的 3D 空间信息转换为语言表达，然后通过大语言模型（LLM）将这些空间知识注入到视觉编码器中。为此，我们采用了多轮思维链（CoT）推理过程，逐步整合密集的空间知识并构建层次化的空间理解。为了验证有效性，我们将 SpatialBoost 应用于 DINOv3 等最先进的视觉编码器，并在广泛的基准测试上评估其性能提升，这些测试涵盖了 3D 感知和通用视觉能力。例如，SpatialBoost 将 DINOv3 在 ADE20K 上的性能从 55.9 mIoU 提升至 59.7 mIoU，相比预训练的 DINOv3 提升了 3.8%，达到了最先进的性能水平。

## Abstract
Despite the remarkable success of large-scale pre-trained image representation models (i.e., vision encoders) across various vision tasks, they are predominantly trained on 2D image data and therefore often fail to capture 3D spatial relationships between objects and backgrounds in the real world, constraining their effectiveness in many downstream applications. To address this, we propose SpatialBoost, a scalable framework that enhances the spatial awareness of existing pre-trained vision encoders by injecting 3D spatial knowledge expressed in linguistic descriptions. The core idea involves converting dense 3D spatial information from 2D images into linguistic expressions, which is then used to inject such spatial knowledge into vision encoders through a Large Language Model (LLM). To this end, we adopt a multi-turn Chain-of-Thought (CoT) reasoning process that progressively incorporates dense spatial knowledge and builds hierarchical spatial understanding. To validate effectiveness, we adapt SpatialBoost to state-of-the-art vision encoders such as DINOv3, and evaluate its performance gains on a wide range of benchmarks requiring both 3D perception and general vision abilities. For instance, SpatialBoost improves DINOv3 performance from 55.9 to 59.7 mIoU on ADE20K, achieving state-of-the-art performance with 3.8% gain over the pre-trained DINOv3.

---

## 论文详细总结（自动生成）

以下是对论文《SpatialBoost: Enhancing Visual Representation through Language-Guided Reasoning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉编码器（如 DINOv2/v3, SigLIP）虽然在语义理解上表现卓越，但由于主要在 2D 图像上进行预训练，普遍缺乏对现实世界 **3D 空间关系**（如物体深度、相对位置、几何结构）的感知能力。
*   **研究动机**：缺乏空间意识限制了视觉模型在机器人控制、3D 场景理解等需要精确几何感知的下游任务中的表现。传统的 3D 学习方法依赖昂贵的多视图或雷达数据，难以大规模扩展。本文旨在探索如何利用**语言引导的推理**作为一种可扩展的监督信号，将 3D 空间知识注入现有的 2D 视觉编码器。

### 2. 方法论
SpatialBoost 框架通过大语言模型（LLM）将密集的空间知识转化为语言描述，并采用三阶段训练流程：
*   **核心思想**：将图像的 3D 几何信息（深度、分割、包围盒）转化为多轮对话形式的“思维链（CoT）”推理过程，通过 LLM 的解码过程引导视觉编码器学习空间特征。
*   **关键技术细节**：
    *   **多轮空间推理数据集**：构建了涵盖三个层级的 QA 对：
        1.  **像素级**：查询特定坐标的深度值或比较点对深度。
        2.  **物体级**：识别物体的 3D 中心坐标及物体间的相对空间关系（如左/右、前/后）。
        3.  **场景级**：预测物体间的精确 3D 距离。
    *   **双通道注意力机制（Dual-channel Attention）**：在微调阶段，为视觉编码器的每个注意力层引入一个额外的可学习通道 $Attn^+$，通过可学习因子 $\alpha$ 进行融合：$Attn_{final}(x) = \alpha \cdot Attn(x) + (1 - \alpha) \cdot Attn^+(x)$。这确保了在注入新空间知识的同时，不破坏原有的语义表征能力。
*   **训练流程**：
    1.  **特征对齐**：训练投影层（Projector）将视觉特征映射到 LLM 空间。
    2.  **视觉指令微调**：使 LLM 具备处理多视图图像的能力。
    3.  **编码器微调**：冻结 LLM，仅更新视觉编码器的双通道参数，利用空间推理数据进行训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练数据**：100K 张来自 SA1B 的单视图图像，200K 组来自 Ego4D 和 ScanNet 等的多视图/视频序列。
    *   **评估基准**：
        *   **密集预测**：NYUd、KITTI（深度估计）；ADE20K、Pascal VOC（语义分割）。
        *   **3D 场景理解**：Lexicon3D（包含 ScanQA、SQA3D、ScanRefer 等任务）。
        *   **机器人控制**：CortexBench（包含 Adroit, MetaWorld, DMControl, Trifinger 域）。
        *   **通用视觉**：ImageNet-1K（分类）、Oxford/Paris（检索）。
*   **对比方法**：包括纯视觉模型（V-JEPAv2）、视觉-语言对齐模型（OpenCLIP, SigLIPv2）、最先进的自监督模型（DINOv2, DINOv3）以及其他空间增强方法（TIPS, PE-Core）。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA Tesla A100 GPU**。
*   **模型配置**：LLM 后端采用 **Qwen-2.0-7B**，投影层为 2 层 MLP。
*   **训练时长**：每个阶段通常训练 1 个 epoch。具体总时长未明确标注，但从 300K 样本量和 A100 算力推断，微调过程具有较高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 大类任务、超过 15 个主流基准数据集上进行了测试，涵盖了从底层几何到高层推理的完整频谱。
*   **消融实验**：针对 LLM 监督的有效性、推理层级的顺序（正向 vs 逆向）、单/多视图数据的贡献、以及双通道注意力的保忘效果进行了详尽对比。
*   **客观性**：实验不仅关注空间任务的提升，还评估了模型在 ImageNet 分类等通用任务上的表现，证明了该方法没有发生“灾难性遗忘”，实验设计客观且公平。

### 6. 主要结论与发现
*   **空间能力显著增强**：SpatialBoost 显著提升了所有基准模型的空间感知。例如，DINOv3 在 ADE20K 上的 mIoU 从 55.9 提升至 59.7（+3.8%）；在 3D 场景理解任务 SQA3D 上提升了 3.5%。
*   **通用能力不降反升**：令人意外的是，注入空间知识后，DINOv3 在 ImageNet-1K 上的线性探测准确率从 88.4% 提升至 90.2%，说明空间意识有助于更稳健的特征提取。
*   **机器人任务增益大**：在机器人控制任务中，平均成功率提升显著（如 DINOv3 提升了 8%），证明了 3D 表征对具身智能的重要性。

### 7. 优点
*   **创新的知识注入方式**：巧妙地将难以直接学习的 3D 几何关系转化为 LLM 擅长的结构化语言推理任务。
*   **架构设计的平衡性**：双通道注意力机制有效解决了微调过程中的表征坍缩问题，实现了空间知识与通用语义的共存。
*   **可扩展性**：该框架不依赖特定架构，可灵活应用于 CLIP、DINO 等多种主流视觉编码器。

### 8. 不足与局限
*   **伪标签依赖**：训练数据中的空间信息是由其他专家模型（如 Depth-pro, SAM）生成的，这意味着 SpatialBoost 的上限受限于这些基础模型的准确度，可能存在偏差传播。
*   **数据生成成本**：虽然不需要人工标注，但构建包含 3D 重建、分割和 GPT-4o 描述的复杂流水线仍需大量的预处理计算资源。
*   **实时性限制**：虽然微调后的编码器推理速度不变，但训练过程中引入 LLM 增加了训练阶段的复杂性。

（完）
