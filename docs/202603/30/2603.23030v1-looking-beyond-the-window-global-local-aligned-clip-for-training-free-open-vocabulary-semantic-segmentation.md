---
title: "Looking Beyond the Window: Global-Local Aligned CLIP for Training-free Open-Vocabulary Semantic Segmentation"
title_zh: 视窗之外：用于无需训练的开放词汇语义分割的全域-局部对齐 CLIP
authors: "ByeongCheol Lee, Hyun Seok Seong, Sangeek Hyun, Gilhan Park, WonJun Moon, Jae-Pil Heo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23030v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于开放词汇语义分割的全域-局部对齐CLIP
tldr: 针对免训练开放词汇语义分割中滑动窗口推理导致的窗口间语义不一致问题，本文提出GLA-CLIP框架。该框架通过扩展键值对以引入全局上下文，并利用代理锚点解决窗口偏差，同时引入动态归一化以优化小目标分割。GLA-CLIP能有效提升现有方法的性能，在多个基准测试中表现优异，为开放词汇分割提供了更广阔的感受野。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的滑动窗口推理策略因各窗口独立处理而导致语义不一致，且存在查询特征难以关注到窗口外信息的偏差。
method: 提出GLA-CLIP框架，通过跨窗口信息交换、构建代理锚点统一语义参考以及采用动态归一化方案来增强全局感知和多尺度适应性。
result: 实验证明GLA-CLIP能显著提升免训练开放词汇语义分割的性能，并可作为插件增强现有方法的感受野。
conclusion: GLA-CLIP通过全局-局部对齐有效解决了滑动窗口推理的局限性，为免训练语义分割提供了一种高效的全局上下文建模方案。
---

## 摘要
在最近的无需训练的开放词汇语义分割方法中，通常采用滑动窗口推理策略，以克服 CLIP 在处理高分辨率图像时的局限性。然而，这种方法引入了新的挑战：每个窗口都是独立处理的，导致窗口之间存在语义差异。为了解决这一问题，我们提出了全域-局部对齐 CLIP (GLA-CLIP)，这是一个促进跨窗口全面信息交换的框架。GLA-CLIP 不再将注意力限制在单个窗口内的 token，而是扩展了键值 (key-value) token，以纳入来自所有窗口的上下文线索。尽管如此，我们观察到一种窗口偏差：窗口外的 token 被关注的可能性较低，因为查询 (query) 特征是通过窗口内部 patch 之间的交互产生的，从而缺乏其局部上下文之外的语义基础。为了缓解这一问题，我们引入了一个代理锚点 (proxy anchor)，它通过聚合来自所有窗口且与给定查询高度相似的 token 构建而成，为衡量窗口内和窗口外 patch 之间的相似性提供了统一的语义参考。此外，我们提出了一种动态归一化方案，通过动态缩放和阈值化注意力图，根据物体尺度调整注意力强度，以应对小物体场景。此外，GLA-CLIP 可以集成到现有方法中并扩大其感受野。大量实验验证了 GLA-CLIP 在提升无需训练的开放词汇语义分割性能方面的有效性。代码可在 https://github.com/2btlFe/GLA-CLIP 获取。

## Abstract
A sliding-window inference strategy is commonly adopted in recent training-free open-vocabulary semantic segmentation methods to overcome limitation of the CLIP in processing high-resolution images. However, this approach introduces a new challenge: each window is processed independently, leading to semantic discrepancy across windows. To address this issue, we propose Global-Local Aligned CLIP~(GLA-CLIP), a framework that facilitates comprehensive information exchange across windows. Rather than limiting attention to tokens within individual windows, GLA-CLIP extends key-value tokens to incorporate contextual cues from all windows. Nevertheless, we observe a window bias: outer-window tokens are less likely to be attended, since query features are produced through interactions within the inner window patches, thereby lacking semantic grounding beyond their local context. To mitigate this, we introduce a proxy anchor, constructed by aggregating tokens highly similar to the given query from all windows, which provides a unified semantic reference for measuring similarity across both inner- and outer-window patches. Furthermore, we propose a dynamic normalization scheme that adjusts attention strength according to object scale by dynamically scaling and thresholding the attention map to cope with small-object scenarios. Moreover, GLA-CLIP can be equipped on existing methods and broad their receptive field. Extensive experiments validate the effectiveness of GLA-CLIP in enhancing training-free open-vocabulary semantic segmentation performance. Code is available at https://github.com/2btlFe/GLA-CLIP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Looking Beyond the Window: Global-Local Aligned CLIP for Training-free Open-Vocabulary Semantic Segmentation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：开放词汇语义分割（OVSS）旨在识别训练类别之外的物体。由于 CLIP 模型预训练分辨率较低（如 224x224），现有免训练方法普遍采用**滑动窗口（Sliding-window）**策略处理高分辨率图像。
*   **核心问题**：滑动窗口策略将图像分割成独立的小块处理，导致模型缺乏全局上下文信息。这引发了两个主要问题：
    1.  **语义不一致性**：相邻窗口对同一物体可能给出不同的预测，产生明显的边界断裂和网格伪影。
    2.  **局部偏差**：查询（Query）特征仅在局部窗口内生成，导致其对窗口外的相关信息（Key-Value）关注度不足。

### 2. 方法论：GLA-CLIP 框架
论文提出了 **Global-Local Aligned CLIP (GLA-CLIP)**，通过以下三个关键技术实现全局与局部的对齐：
*   **键值扩展（Key-Value Extension, KVE）**：
    *   不再局限于当前窗口，而是将所有滑动窗口的特征拼接，构建全局键（$K_{global}$）和全局值（$V_{global}$）。
    *   允许每个窗口的 Query 检索全图范围内的上下文线索。
*   **代理锚点（Proxy Anchor）**：
    *   为了克服 Query 的局部偏差，通过迭代聚合全图中与当前 Query 相似度高的 token，生成一个“代理锚点”作为新的 Query。
    *   该锚点代表了物体的全局语义，使得注意力分配在窗口内外更加均衡。
*   **动态归一化（Dynamic Normalization）**：
    *   针对不同尺度的物体，动态调整注意力图的缩放（Scaling）和阈值（Thresholding）。
    *   利用高置信度 token 的数量来估计物体尺度：小物体增加缩放系数以突出关键特征，大物体则放宽限制以吸收更多上下文。

### 3. 实验设计
*   **数据集**：在 8 个主流基准测试上进行了评估，包括 Pascal VOC (20/21)、Pascal Context (59/60)、COCO-Object、COCO-Stuff、ADE20K 和 Cityscapes。
*   **Benchmark 与对比方法**：
    *   **基准**：以 ProxyCLIP、ClearCLIP、SCLIP 等先进的免训练方法为 Baseline。
    *   **对比对象**：包括 GroupViT、TCL、CLIP-DINOiser、NACLIP、CASS、ResCLIP 等十余种训练或免训练方法。
*   **评估指标**：主要使用平均交并比（mIoU），并引入了**边界错误率（BER）**来量化窗口边界的预测不一致性。

### 4. 资源与算力
*   **硬件设备**：实验在一台 **NVIDIA Titan RTX GPU** 上完成。
*   **训练时长**：由于是**免训练（Training-free）**方法，该框架不需要训练阶段，仅涉及推理过程。
*   **推理开销**：论文提到由于引入了全局键值扩展，计算复杂度从 $O(LN^2D)$ 增加到 $O(L^2N^2D)$（L 为窗口数），但在实际测试中（如 Pascal Context59），内存占用和延迟增加在可接受范围内（约 1.9GB 显存）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个不同领域的数据集上验证了性能。
    *   进行了详尽的**消融实验**，验证了 KVE、代理锚点和动态归一化各自的贡献。
    *   进行了**超参数敏感性分析**（如阈值 $\rho$、迭代步数 $T$ 等）。
    *   验证了在不同 Backbone（ViT-B/32, ViT-L/14）和不同视觉基础模型（DINO v2/v3）上的**泛化性**。
    *   扩展到了**遥感图像（Remote Sensing）**等跨域场景。
*   **客观性与公平性**：作者在相同的滑动窗口设置（分辨率、步长等）下对比了多种 Baseline，并展示了在不使用数据集特定调优（Dataset-specific tuning）的情况下依然领先，体现了实验的客观性。

### 6. 主要结论与发现
*   **全局信息至关重要**：通过简单的键值扩展和代理锚点机制，可以显著消除滑动窗口带来的网格伪影。
*   **性能提升明显**：GLA-CLIP 在多个数据集上刷新了免训练 OVSS 的 SOTA 记录，平均 mIoU 提升约 1.6%~1.7%。
*   **即插即用**：该框架可以作为插件集成到现有的免训练方法（如 ClearCLIP, SCLIP）中，并普遍提升它们的表现。
*   **尺度自适应**：动态归一化有效解决了小物体在全局注意力中被噪声淹没的问题。

### 7. 优点（亮点）
*   **首创性**：首次识别并系统性地解决了免训练 OVSS 中滑动窗口导致的跨窗口语义不一致问题。
*   **无需训练**：在不增加任何训练成本的前提下，通过改进推理逻辑大幅提升了分割质量。
*   **鲁棒性**：动态归一化机制减少了对特定数据集超参数手动调优的依赖。
*   **可视化效果**：定性分析显示，该方法生成的分割图边缘更平滑，物体完整性更好。

### 8. 不足与局限
*   **计算复杂度**：随着图像分辨率增加或滑动窗口数量 $L$ 增多，全局注意力计算的内存和时间开销呈平方级增长，处理超大分辨率图像时可能存在瓶颈。
*   **超参依赖**：虽然提出了动态归一化，但代理锚点构建中的相似度阈值 $\rho$ 等参数仍可能需要根据不同的特征提取器（Backbone）进行微调。
*   **噪声风险**：在极度复杂的场景下，全局键值扩展可能会引入过多的背景噪声，干扰极小物体的识别。

（完）
