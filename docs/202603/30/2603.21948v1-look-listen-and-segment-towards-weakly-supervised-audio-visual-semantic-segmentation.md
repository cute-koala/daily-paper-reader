---
title: "Look, Listen and Segment: Towards Weakly Supervised Audio-visual Semantic Segmentation"
title_zh: 边看、边听、边分割：迈向弱监督音视频语义分割
authors: "Chengzhi Li, Heyan Huang, Ping Jian, Yanghao Zhou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21948v1"
tags: ["query:vlm"]
score: 8.0
evidence: 弱监督视听语义分割与跨模态对齐
tldr: 针对音视频语义分割（AVSS）中逐帧像素级标注成本高昂的问题，本文提出了弱监督音视频语义分割（WSAVSS）任务，仅利用视频级标签生成发声对象的语义掩码。研究提出了渐进式跨模态语义对齐（PCAS）框架，包含“先看后听”和“先听后分割”两个模块，通过视觉语义提示增强音频理解，并利用对比学习实现音频类别与图像区域的映射。实验证明该方法在弱监督设置下达到领先水平，且能与全监督方法竞争。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21948v1/fig-001.webp\", \"caption\": \"Table 4. The ablation results of the Listening-before-Seg mentation module.\", \"page\": 4, \"index\": 1, \"width\": 462, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2603.21948v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of PCAS: The audio encoder processes audio frame-wise with visual prompts from the visual encoder. CMC aligns global semantics; CMPC and CMCC refine finegrained alignment via cross-modal similarity. The decoder then outputs masks using CAM-based pseudo-labels.\", \"page\": 2, \"index\": 2, \"width\": 502, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2603.21948v1/fig-003.webp\", \"caption\": \"Fig. 2. CMPC loss computation. ViT, AST*, 𝑃𝑎, 𝑃𝑝 denote the visual encoder, audio encoder with visual prompt, and projection layers for audio semantic tokens and visual patch tokens. Patches, AUX Patches, Patch Labels are ViT patch outputs, intermediate auxiliary patches, and positive/negative token-wise contrast labels.\", \"page\": 2, \"index\": 3, \"width\": 444, \"height\": 271}]"
motivation: 旨在解决音视频语义分割中像素级标注成本过高的问题，探索仅使用视频级标签实现发声物体分割的可能性。
method: 提出PCAS框架，通过视觉语义提示增强音频特征，并利用渐进式对比对齐将音频类别映射到对应的图像像素区域。
result: 在AVS和AVSS数据集上，该方法在弱监督设置下取得了当前最佳性能，甚至在某些指标上可与全监督基准媲美。
conclusion: 证明了通过跨模态对齐和语义提示，可以在无需像素级标注的情况下有效实现音视频语义分割。
---

## 摘要
音视频语义分割（AVSS）在像素级对齐音频和视频，但需要昂贵的逐帧标注。我们引入了弱监督音视频语义分割（WSAVSS），它仅使用视频级标签来生成发声物体的逐帧语义掩码。我们将 WSAVSS 分解为看、听和分割，并提出了用于语义的渐进式跨模态对齐（PCAS），包含两个模块：“先看后听”和“先听后分割”。PCAS 构建了一个分类任务，利用视频标签训练音视频编码器，注入视觉语义提示以增强帧级音频理解，然后应用渐进式对比对齐，在没有掩码标注的情况下将音频类别映射到图像区域。实验表明，PCAS 在 AVS 弱监督方法中达到了最先进的性能，并且在 AVSS 上与全监督基准相比仍具竞争力，验证了其有效性。

## Abstract
Audio-Visual Semantic Segmentation (AVSS) aligns audio and video at the pixel level but requires costly per-frame annotations. We introduce Weakly Supervised Audio-Visual Semantic Segmentation (WSAVSS), which uses only video-level labels to generate per-frame semantic masks of sounding objects. We decompose WSAVSS into looking, listening, and segmentation, and propose Progressive Cross-modal Alignment for Semantics (PCAS) with two modules: *Looking-before-Listening* and *Listening-before-Segmentation*. PCAS builds a classification task to train the audio-visual encoder using video labels, injects visual semantic prompts to enhance frame-level audio understanding, and then applies progressive contrastive alignment to map audio categories to image regions without mask annotations. Experiments show PCAS achieves state-of-the-art performance among weakly supervised methods on AVS and remains competitive with fully supervised baselines on AVSS, validating its effectiveness.