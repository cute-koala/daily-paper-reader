---
title: Domain-Invariant Prompt Learning for Vision-Language Models
title_zh: 视觉-语言模型的领域不变提示学习
authors: "Arsham Gholamzadeh Khoee, Yinan Yu, Robert Feldt"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28555v1"
tags: ["query:vlm"]
score: 9.0
evidence: 基于CLIP的视觉语言模型的域不变提示学习
tldr: 针对视觉语言模型（如CLIP）在下游任务中面临的领域偏移问题，本文提出了DiCoOp。虽然现有的CoOp通过学习上下文向量实现了高效适配，但缺乏处理未知分布的能力。DiCoOp通过引入对抗训练机制，强制模型学习领域不变的提示词，在保持分类判别力的同时增强了泛化性能。实验证明，DiCoOp在多种视觉领域的泛化任务中均优于CoOp。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28555v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of Domain-invariant Context Optimization (DiCoOp). Domain First Prompting (DFP) is illustrated, where the first half of the prompt is dedicated to domain information, and the remaining half is dedicated to class information. During domain-related optimization, the class tokens remain frozen, and vice versa.\", \"page\": 3, \"index\": 1, \"width\": 826, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2603.28555v1/fig-002.webp\", \"caption\": \"Figure 2: Results of few-shot learning on the PACS dataset using the leave-one-domain-out technique. Each plot is defined by the domain name that is left out during prompt learning, and testing is performed on that same domain.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 673}, {\"url\": \"assets/figures/arxiv/2603.28555v1/fig-003.webp\", \"caption\": \"Table 1: Accuracy (%) on Mini-DomainNet for domain generalization using a leave-one-domainout approach. Each column shows results on the domain that has been left out, comparing domain generalization performance to the baseline. Bold numbers indicate the best accuracy in each column.\", \"page\": 5, \"index\": 3, \"width\": 779, \"height\": 240}]"
motivation: 现有的软提示学习方法（如CoOp）在面对跨领域的未知分布时，缺乏显式的机制来处理领域偏移问题。
method: 提出DiCoOp方法，通过对抗训练优化上下文向量，使其在保留分类能力的同时学习到领域不变的特征。
result: 实验结果显示，DiCoOp在多个不同视觉领域的泛化任务中表现一致优于传统的CoOp方法。
conclusion: DiCoOp通过学习领域不变的提示词，显著提升了视觉语言模型在领域泛化场景下的鲁棒性和性能。
---

## 摘要
CLIP 等大型预训练视觉-语言模型通过在共享特征空间中对齐图像和文本，彻底改变了计算机视觉，并通过提示实现了鲁棒的零样本迁移。软提示（Soft-prompting），如上下文优化（CoOp），通过学习一组上下文向量，有效地使这些模型适应下游识别任务。然而，CoOp 缺乏处理未见分布中领域偏移的显式机制。为了解决这一问题，我们提出了领域不变上下文优化（DiCoOp），这是 CoOp 的一种针对领域泛化进行优化的扩展。通过采用对抗训练方法，DiCoOp 强制模型学习领域不变的提示，同时保持分类的判别能力。实验结果表明，在不同视觉领域的领域泛化任务中，DiCoOp 的表现始终优于 CoOp。

## Abstract
Large pre-trained vision-language models like CLIP have transformed computer vision by aligning images and text in a shared feature space, enabling robust zero-shot transfer via prompting. Soft-prompting, such as Context Optimization (CoOp), effectively adapts these models for downstream recognition tasks by learning a set of context vectors. However, CoOp lacks explicit mechanisms for handling domain shifts across unseen distributions. To address this, we propose Domain-invariant Context Optimization (DiCoOp), an extension of CoOp optimized for domain generalization. By employing an adversarial training approach, DiCoOp forces the model to learn domain-invariant prompts while preserving discriminative power for classification. Experimental results show that DiCoOp consistently surpasses CoOp in domain generalization tasks across diverse visual domains.

---

## 论文详细总结（自动生成）

这是一份关于论文《Domain-Invariant Prompt Learning for Vision-Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：预训练视觉-语言模型（如 CLIP）虽然在零样本迁移上表现优异，但现有的软提示微调方法（如 CoOp）在面对**领域偏移（Domain Shift）**时表现脆弱。CoOp 学习到的上下文向量往往会过拟合源域的分布，导致其在未见的测试域（领域泛化，DG）上性能下降。
*   **研究动机**：作者旨在设计一种提示学习机制，使模型既能保持强大的类别区分能力，又能对输入数据的领域特征保持“不敏感”（领域不变性），从而提升模型在未知领域上的鲁棒性。

### 2. 核心方法论
*   **核心思想**：借鉴领域对抗神经网络（DANN）的思想，将**对抗训练**引入提示学习。通过引入梯度反转层（GRL），在优化类别分类损失的同时，强制最大化领域分类的误差，从而诱导提示向量学习到领域无关的特征。
*   **关键技术细节**：
    *   **双重损失函数**：训练目标为 $L = L_{cls} - \lambda L_{dom}$。其中 $L_{cls}$ 负责准确分类，而 $L_{dom}$ 通过 GRL 进行梯度反转，使提示向量无法区分来源领域。
    *   **三种提示策略**：
        1.  **SCP（共享上下文提示）**：所有提示标记共同参与类别和领域任务。
        2.  **DFP（领域优先提示）**：将提示分为两半，前半部分负责领域信息，后半部分负责类别信息。在优化领域任务时冻结类别标记，反之亦然。
        3.  **CFP（类别优先提示）**：结构与 DFP 相反，类别标记在前，领域标记在后。
*   **算法流程**：每个训练样本进行两次前向传播：一次是“类别传递”（Class Pass）用于最小化分类损失；一次是“领域传递”（Domain Pass）通过 GRL 传递梯度，用于增强领域不变性。

### 3. 实验设计
*   **数据集**：
    *   **PACS**：包含 Photo、Art Painting、Cartoon、Sketch 四个领域，共 7 个类别。
    *   **Mini-DomainNet**：包含 Clipart、Painting、Sketch、Real 四个领域，共 126 个类别。
*   **Benchmark（基准）**：主要对比对象是 **CoOp**（经典的软提示学习方法）。
*   **实验设置**：采用“留一域验证”（Leave-one-domain-out）策略，即在三个域上训练，在剩下的一个未见域上测试。涵盖了 1-shot 到 16-shot 的多种少样本设置。
*   **骨干网络**：ResNet-50（用于 PACS）和 ViT-B/16（用于 Mini-DomainNet）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及具体的训练时长。考虑到其基于 CLIP 且仅微调提示向量（Soft Prompts），通常这类实验在单张消费级 GPU（如 RTX 3090 或 A100）上即可在数小时内完成。

### 5. 实验数量与充分性
*   **实验规模**：作者在两个主流领域泛化数据集上进行了测试，涵盖了从极少样本（1-shot）到较多样本（16-shot）的完整区间。
*   **消融实验**：对比了三种不同的提示结构（SCP, DFP, CFP），验证了“显式分离领域与类别标记”的有效性。
*   **充分性评价**：实验设计较为标准，遵循了领域泛化领域的常规评测流程。对比实验虽然主要集中在 CoOp 上，但通过多领域、多骨干网络、多样本量的组合，客观地证明了 DiCoOp 的改进是稳健的。

### 6. 主要结论与发现
*   **DiCoOp 优于 CoOp**：在所有测试领域和样本设置下，DiCoOp 的准确率均高于基准 CoOp。
*   **结构化提示更有效**：DFP 和 CFP（分离式提示）的表现优于 SCP（完全共享提示），说明显式地划分领域和类别标记有助于模型更好地解耦和处理领域变化。
*   **跨架构鲁棒性**：无论是在 CNN（ResNet）还是 Transformer（ViT）架构下，该方法均表现出一致的增益。

### 7. 优点与亮点
*   **简单高效**：将成熟的对抗训练思想巧妙地迁移到提示空间，无需修改 CLIP 的核心架构，仅通过调整提示向量的优化方式实现泛化。
*   **解耦设计**：提出的 DFP/CFP 策略为提示词的内部结构设计提供了新思路，证明了提示词内部也存在语义分工。
*   **即插即用**：作为 CoOp 的扩展，它可以很容易地集成到其他基于提示学习的视觉任务中。

### 8. 不足与局限
*   **对比基准较少**：实验主要对比了 CoOp，缺乏与更先进的变体（如 CoCoOp, ProGrad 等）或其他领域泛化 SOTA 方法的直接对比。
*   **超参数敏感性**：文中未详细讨论对抗损失权重 $\lambda$ 对结果的影响，这在对抗训练中通常是一个关键参数。
*   **任务局限**：目前仅在图像分类任务上进行了验证，尚未探讨在目标检测或分割等更复杂任务中的表现。
*   **领域标签依赖**：训练阶段仍需要明确的源域标签（Domain Labels），在某些完全无监督或领域标签模糊的场景下可能受限。

（完）
