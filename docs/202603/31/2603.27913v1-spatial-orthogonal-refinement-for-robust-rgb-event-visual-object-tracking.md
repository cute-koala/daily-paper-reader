---
title: Spatial Orthogonal Refinement for Robust RGB-Event Visual Object Tracking
title_zh: 空间正交细化用于鲁棒的 RGB-事件视觉目标跟踪
authors: "Dexing Huang, Shiao Wang, Fan Zhang, Xiao Wang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27913v1"
tags: ["query:vot"]
score: 10.0
evidence: 鲁棒的RGB-事件视觉目标跟踪
tldr: 针对高速运动场景下RGB传感器易产生运动模糊的问题，本文提出SOR-Track框架。该框架利用事件相机的高时间分辨率特性，通过空间正交细化（SOR）模块提取事件流中的定向几何先验。利用动态引导的正交方向滤波器捕捉运动一致的结构响应，并以此修正退化的RGB特征，实现了物理意义明确的多模态特征对齐。在FE108基准测试中，该方法在模糊和弱光条件下表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27913v1/fig-001.webp\", \"caption\": \"Fig. 2. Qualitative comparison of SOR-Track against the baseline CEUTrack on the FE108 dataset. In this HDR scenario, the baseline (red) drifts to the overexposed background, while our SOR-Track (blue) stays robust.\", \"page\": 5, \"index\": 1, \"width\": 529, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2603.27913v1/fig-002.webp\", \"caption\": \"Fig. 1. An overview of our proposed Spatial Orthogonal Refinement for a robust RGB-Event visual object tracking framework.\", \"page\": 3, \"index\": 2, \"width\": 1076, \"height\": 391}]"
motivation: 现有的RGB-Event融合方法多采用黑盒策略，未能充分利用事件流中蕴含的定向几何先验来修复RGB图像的运动模糊。
method: 提出空间正交细化（SOR）模块，通过动态运动引导的正交滤波器提取事件结构，并利用非对称结构调制机制修正RGB纹理。
result: 在大规模FE108基准测试上，SOR-Track在运动模糊和低光照等挑战性场景下均显著优于现有的融合跟踪算法。
conclusion: 该研究为多模态特征对齐和纹理修复提供了一种基于物理原理且简洁高效的解决方案，提升了视觉目标跟踪的鲁棒性。
---

## 摘要
在高速运动场景下，鲁棒的视觉目标跟踪（VOT）仍然具有挑战性，因为传统的 RGB 传感器会遭受严重的运动模糊和性能退化。事件相机具有微秒级的时间分辨率和高动态范围，能够提供互补的结构线索，从而有可能弥补这些局限性。然而，现有的 RGB-事件融合方法通常将事件数据视为密集的强度表示，并采用黑盒融合策略，未能显式地利用事件流中固有的方向几何先验来纠正退化的 RGB 特征。为了解决这一局限性，我们提出了 SOR-Track，这是一个基于空间正交细化（SOR）的精简框架，用于鲁棒的 RGB-事件跟踪。核心 SOR 模块采用了一组正交方向滤波器，这些滤波器由局部运动方向动态引导，以从事件流中提取清晰且运动一致的结构响应。这些响应作为几何锚点，通过非对称结构调制机制来调制和细化混叠的 RGB 纹理，从而显式地弥合两种模态之间的结构差异。在大规模 FE108 基准测试上的广泛实验表明，SOR-Track 一致优于现有的基于融合的跟踪器，特别是在运动模糊和低光照条件下。尽管该方法简单，但它为多模态特征对齐和纹理纠正提供了一种有原则且基于物理的方法。本文的源代码将在 https://github.com/Event-AHU/OpenEvTracking 上发布。

## Abstract
Robust visual object tracking (VOT) remains challenging in high-speed motion scenarios, where conventional RGB sensors suffer from severe motion blur and performance degradation. Event cameras, with microsecond temporal resolution and high dynamic range, provide complementary structural cues that can potentially compensate for these limitations. However, existing RGB-Event fusion methods typically treat event data as dense intensity representations and adopt black-box fusion strategies, failing to explicitly leverage the directional geometric priors inherently encoded in event streams to rectify degraded RGB features. To address this limitation, we propose SOR-Track, a streamlined framework for robust RGB-Event tracking based on Spatial Orthogonal Refinement (SOR). The core SOR module employs a set of orthogonal directional filters that are dynamically guided by local motion orientations to extract sharp and motion-consistent structural responses from event streams. These responses serve as geometric anchors to modulate and refine aliased RGB textures through an asymmetric structural modulation mechanism, thereby explicitly bridging structural discrepancies between two modalities. Extensive experiments on the large-scale FE108 benchmark demonstrate that SOR-Track consistently outperforms existing fusion-based trackers, particularly under motion blur and low-light conditions. Despite its simplicity, the proposed method offers a principled and physics-grounded approach to multi-modal feature alignment and texture rectification. The source code of this paper will be released on https://github.com/Event-AHU/OpenEvTracking

---

## 论文详细总结（自动生成）

### 论文总结：Spatial Orthogonal Refinement for Robust RGB-Event Visual Object Tracking (SOR-Track)

#### 1. 核心问题与研究动机
*   **核心问题**：在高速运动或极端光照（如低光、高动态范围 HDR）场景下，传统 RGB 相机因曝光时间长易产生**运动模糊**，导致目标纹理退化。
*   **研究动机**：虽然事件相机（Event Camera）具有微秒级响应和高动态范围，能提供清晰的结构线索，但现有融合方法多将事件数据视为普通的强度图，采用“黑盒”融合策略（如简单的拼接或注意力机制），未能显式利用事件流中蕴含的**方向性几何先验**来修复退化的 RGB 特征。

#### 2. 方法论
SOR-Track 采用了“**先细化后跟踪**”（Refine-then-Track）的范式，核心组件包括：
*   **细粒度特征编码主干（Granular Feature Stem）**：为了防止步长卷积（Strided Convolution）像低通滤波器一样抹除事件流中的高频结构，该方法引入了 **Space-to-Depth** 变换，在初始阶段保留像素级的结构信息。
*   **空间正交细化模块（SOR）**：
    *   **正交方向模块（ODM）**：受生物视觉皮层 Gabor 响应启发，利用一组可操纵的 Gabor 滤波器。这些滤波器由局部运动方向动态引导，生成 $K$ 个在正交空间内均匀分布的方向核。
    *   **事件引导的特征校正**：将事件流视为“结构蓝图”，通过 Gabor 滤波器提取多方向响应。利用事件响应生成空间感知调制掩码（Modulation Mask），对模糊的 RGB 特征进行非对称调制，从而“重新读取”并强化与事件边缘对齐的 RGB 纹理。
*   **损失函数**：结合了用于中心点分类的 Focal Loss，以及用于边界框回归的 $L_1$ Loss 和 GIoU Loss。

#### 3. 实验设计
*   **数据集**：主要在 **FE108** 大规模基准测试集上进行评估。该数据集包含 108 个高质量序列，涵盖高速运动、低光照和复杂背景等挑战性场景。
*   **评估指标**：成功率（AUC/RSR）、精确度（PR/RPR）、归一化精确度（NPR）以及重叠精度（OP50, OP75）。
*   **对比方法**：
    *   **纯 RGB 跟踪器**：SiamRPN++, ATOM, DiMP, PrDiMP。
    *   **RGB-Event 融合跟踪器**：ATOM+Event, DiMP+Event, PrDiMP+Event，以及基于 Transformer 的 SOTA 模型 **CEUTrack**。

#### 4. 资源与算力
*   **硬件配置**：使用了 **2 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：总 Batch Size 为 32，共训练 50 个 Epoch。
*   **优化器**：AdamW，初始学习率为 $1 \times 10^{-4}$（Backbone 学习率缩放 0.1 倍），在第 40 个 Epoch 时衰减。
*   **数据处理**：搜索区域大小为 $256 \times 256$，模板大小为 $128 \times 128$。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了定量对比实验、定性可视化分析（特征能量图对比）以及针对关键组件（如 StemNet）的消融实验。
*   **充分性与客观性**：
    *   对比了从传统相关滤波演进到最新 Transformer 架构的多种模型，实验覆盖面较广。
    *   消融实验证明了细粒度特征提取对保留事件高频信息的重要性（AUC 提升 3.93%）。
    *   通过可视化特征图直观展示了 SOR 模块如何抑制背景噪声并集中特征能量，增强了实验的说服力。

#### 6. 主要结论与发现
*   **性能领先**：SOR-Track 在 FE108 数据集上全面超越了 CEUTrack，尤其在代表高精度定位的 **OP75 指标上实现了 3.83% 的绝对增益**。
*   **鲁棒性强**：在 HDR 和过度曝光场景下，当传统跟踪器因背景边缘干扰而漂移时，SOR-Track 凭借事件驱动的定向锚点能保持稳定跟踪。
*   **物理一致性**：显式利用几何先验比隐式的黑盒融合更能有效解决模态间的结构不一致问题。

#### 7. 优点与亮点
*   **物理启发式设计**：不同于纯数据驱动的方法，引入 Gabor 滤波器和正交方向建模具有明确的物理意义和生物学解释。
*   **高精度定位**：通过空间细化显著提升了边界框回归的精度（OP75 的提升尤为明显）。
*   **简洁高效**：在不引入极其复杂的时空对齐算法的情况下，通过空间维度的特征校正实现了显著的性能提升。

#### 8. 不足与局限
*   **方向数固定**：目前 ODM 模块使用固定数量（$K=4$）的方向核，对于极度复杂的非线性运动，可能需要更自适应的方向选择机制。
*   **时空建模简化**：该框架侧重于空间细化（Spatial Refinement），对事件流长程时序信息的挖掘相对较少。
*   **应用限制**：虽然在 FE108 上表现优异，但在缺乏显著边缘特征的极度平滑物体上，事件相机的增益可能会受限。

（完）
