---
title: "Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens"
title_zh: Le MuMo JEPA：基于可学习融合 Token 的多模态自监督表示学习
authors: "Ciem Cornelissen, Sam Leroux, Pieter Simoens"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24327v1"
tags: ["query:vlm"]
score: 9.0
evidence: 多模态自监督表示学习架构
tldr: 本研究提出Le MuMo JEPA框架，旨在解决自监督学习局限于单模态的问题。通过在共享Transformer中引入可学习的融合标记作为潜在瓶颈，并结合剪枝融合策略，该方法能高效融合RGB与LiDAR或热成像数据。在Waymo和nuScenes等数据集的测试中，它在目标检测和深度估计任务上展现出卓越的性能与计算效率平衡，显著降低了训练成本。
selection_source: fresh_fetch
motivation: 现有的自监督学习方法多关注单一模态，未能充分利用异构传感器（如RGB与LiDAR）提供的互补结构信息。
method: 提出一种利用可学习融合标记作为共享Transformer内潜在瓶颈的框架，并采用剪枝策略强制跨模态信息整合。
result: 在Waymo、nuScenes和FLIR基准测试中，该方法在下游检测和深度估计任务上取得了最强的性能与效率权衡。
conclusion: Le MuMo JEPA证明了通过高效的融合机制，可以在显著降低计算和内存开销的同时，提升多模态自监督表征的质量。
---

## 摘要
自监督学习已成为一种无需人工标注即可学习视觉表示的强大范式，但大多数方法仍针对单一模态运行，因此错失了异构传感器提供的互补结构。我们提出了 Le MuMo JEPA，这是一个从 RGB 图像及其对齐的伴随模态中学习统一表示的自监督框架。在我们的自动驾驶实验中，第二种模态是与相机对齐的 LiDAR 深度；我们还在 Teledyne FLIR ADAS 基准测试上评估了 RGB-热成像训练和迁移效果。我们的方法通过学习融合 Token（fusion tokens）将 LeJEPA 扩展到多模态设置，这些 Token 在共享 Transformer 内部的模态特定 patch stem 之间充当潜瓶颈（latent bottleneck）。我们的默认模型采用了一种剪枝融合策略：在初始跨模态注意力层之后，丢弃模态特定的 Token，迫使跨模态信息进入共享的融合 Token 网格，将其作为高效的潜瓶颈，随后对联合多模态 CLS 嵌入应用草图各向同性高斯正则化（SIGReg）。在 Waymo 数据集上，Le MuMo JEPA 在从零开始训练的多模态基准模型中，在下游 patch 探测任务上表现出最强的性能-效率权衡，提升了 CenterNet 检测和稠密深度性能，同时在分割任务上保持了竞争力。在 nuScenes 的从零训练中，Le MuMo JEPA 依然是最强的模型，并且在 FLIR 上取得了最佳结果，尤其是在经过 Waymo 初始化微调之后。在我们的研究中，它还以显著更低的计算量、内存占用和预估训练时间，保持了最佳的整体准确率-效率平衡。

## Abstract
Self-supervised learning has emerged as a powerful paradigm for learning visual representations without manual annotations, yet most methods still operate on a single modality and therefore miss the complementary structure available from heterogeneous sensors. We present Le MuMo JEPA, a self-supervised framework that learns unified representations from RGB images and aligned companion modalities. In our driving experiments, the second modality is camera-aligned LiDAR depth; we also evaluate RGB-thermal training and transfer on the Teledyne FLIR ADAS benchmark. Our approach extends LeJEPA to the multi-modal setting by learning fusion tokens that act as a latent bottleneck between modality-specific patch stems inside a shared transformer. Our default model employs a pruned fusion strategy: after an initial cross-modal attention layer, modality-specific tokens are dropped, forcing cross-modal information into the shared fusion-token grid as an efficient latent bottleneck before Sketched Isotropic Gaussian Regularization (SIGReg) is applied to the joint multimodal CLS embedding. On Waymo, Le MuMo JEPA gives the strongest performance-efficiency trade-off on downstream patch probes among the from-scratch multimodal baselines, improving CenterNet detection and dense depth while remaining competitive on segmentation. Under from-scratch training on nuScenes, Le MuMo JEPA remains the strongest model, and it also gives the best FLIR results, especially after Waymo-initialized fine-tuning. It also retains the best overall accuracy-efficiency balance in our study at substantially lower compute, memory, and estimated training time.

---

## 论文详细总结（自动生成）

以下是对论文《Le MuMo JEPA: Multi-Modal Self-Supervised Representation Learning with Learnable Fusion Tokens》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的自监督学习（SSL）方法大多局限于单模态（如仅 RGB），忽略了自动驾驶等场景中异构传感器（如 LiDAR 深度、热成像）提供的互补信息。
*   **核心挑战**：如何在不依赖昂贵人工标注的情况下，有效地对齐和融合结构差异巨大的多模态信号，同时避免全注意力机制带来的高昂计算成本。
*   **研究目标**：将 LeJEPA（基于潜空间预测的自监督架构）扩展到多模态领域，学习一种既能捕捉几何结构又能捕捉纹理特征的统一表示。

### 2. 方法论
*   **核心思想**：引入**可学习融合 Token（Learnable Fusion Tokens）**作为“潜瓶颈（Latent Bottleneck）”，在共享的 Transformer 架构中介导跨模态信息的交换。
*   **关键技术细节**：
    *   **输入处理**：将 RGB 和伴随模态（如 LiDAR 投影的深度图）统一为 2D Patch 格式，并添加模态特定的嵌入（Embedding）。
    *   **剪枝融合策略（FT-Pruned）**：在 Transformer 的第 0 层，融合 Token 通过跨模态注意力收集对应位置的 RGB 和深度信息。随后，**丢弃所有原始模态 Token**，仅保留融合 Token 和 CLS Token 进入后续层（1-11 层）。这显著降低了计算复杂度（注意力开销减少约 9 倍）。
    *   **正则化机制（SIGReg）**：采用草图各向同性高斯正则化（SIGReg），将联合多模态的 CLS 嵌入推向标准正态分布 $N(0, I)$，从而防止表示崩溃，且无需负采样或动量教师网络。
    *   **训练目标**：结合不变性损失（Invariance Loss）和 SIGReg 损失，通过预测不同增强视图下的联合嵌入来学习特征。

### 3. 实验设计
*   **数据集与场景**：
    *   **Waymo Open Dataset**：主要驾驶基准，用于 RGB+LiDAR 深度训练。
    *   **nuScenes**：用于验证从零训练（From-scratch）的泛化能力。
    *   **Teledyne FLIR ADAS**：用于 RGB+热成像（Thermal）的训练及迁移实验。
*   **评估任务（Benchmark）**：使用冻结权重的 Patch 探测器（Frozen Patch Probes）评估 3D 目标检测（CenterNet 风格）、稠密深度估计和语义分割。
*   **对比方法**：
    *   单模态基准：LeJEPA (RGB/LiDAR)、DINOv3。
    *   多模态基准：MultiMAE（基于重构）、ImageBind（基于对比学习）。
    *   融合消融：早期融合（Early Fusion）、后期融合（Late Fusion）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **AMD EPYC 9275F 24核处理器和 NVIDIA H200 GPU** 的系统上运行。
*   **算力消耗**：
    *   默认模型（ViT-Small/16）在 Waymo 上的预估训练时间为 **55.09 分钟**，总 FLOPs 为 86.0 PFLOPs，峰值显存（VRAM）占用为 **12.11 GB**。
    *   相比之下，未剪枝的持久化路由版本（FT-Persistent）耗时高达 170.63 分钟，显存占用 36.08 GB。
*   **训练设置**：Waymo 和 nuScenes 采用 5 个 SSL 轮次（Epochs），FLIR 采用 20 个轮次。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流自动驾驶/感知数据集，进行了跨模态（深度、热成像）的验证。
*   **消融实验**：对比了剪枝与非剪枝、不同损失函数（SIGReg vs VICReg）、单次前向与三次前向（3-pass）等多种变体。
*   **公平性**：所有基准模型均在相同的硬件、数据子集和训练预算下**从零开始训练**，而非直接使用预训练权重，确保了架构和目标函数比较的客观性。

### 6. 主要结论与发现
*   **性能领先**：Le MuMo JEPA 在 Waymo 和 nuScenes 的所有空间指标（检测、深度、分割）上均优于单模态和其他多模态基准。
*   **效率卓越**：通过 Token 剪枝，该方法在保持高精度的同时，实现了极佳的计算效率，是目前性能-效率权衡（Trade-off）最优的方案。
*   **迁移能力**：在 Waymo 上预训练的模型在 FLIR 热成像数据集上表现出极强的零样本迁移和微调潜力，显著优于 ImageBind 和 MultiMAE。

### 7. 优点（亮点）
*   **高效的潜瓶颈设计**：通过在早期层剪枝模态 Token，解决了多模态 Transformer 计算量随 Token 数量平方增长的问题。
*   **无需标签的对齐**：利用 SIGReg 将不同模态拉向同一个数据无关的目标几何空间，避免了对比学习对负样本的依赖。
*   **统一性**：一套架构即可处理 RGB-深度和 RGB-热成像，展现了极强的模态通用性。

### 8. 不足与局限
*   **信息损失**：将 LiDAR 投影为 2D 深度图虽然简化了融合，但不可避免地丢失了部分原生 3D 空间结构。
*   **对齐依赖**：模型假设输入模态在空间上是严格对齐的，对于传感器校准误差或非重叠视野的鲁棒性尚未验证。
*   **模型规模限制**：实验主要集中在 ViT-Small 规模，尚未在大规模模型（如 ViT-Huge）上验证融合 Token 的扩展性。

（完）
