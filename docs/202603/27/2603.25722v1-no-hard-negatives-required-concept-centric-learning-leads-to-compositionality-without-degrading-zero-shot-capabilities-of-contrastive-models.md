---
title: "No Hard Negatives Required: Concept Centric Learning Leads to Compositionality without Degrading Zero-shot Capabilities of Contrastive Models"
title_zh: 无需难负样本：以概念为中心的学习在不降低对比模型零样本能力的情况下实现组合性
authors: "Hai X. Pham, David T. Hoffmann, Ricardo Guerrero, Brais Martinez"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25722v1"
tags: ["query:vlm"]
score: 9.0
evidence: 改进对比视觉语言模型的组合性
tldr: 针对对比视觉语言模型在组合性表示方面的局限，本文提出了一种无需硬负样本的概念中心学习方法。研究发现长文本描述和全局池化是导致组合性缺失的主因。通过使用NLP工具提取短概念标题并引入无参数跨模态注意力池化，该方法在提升组合性基准表现的同时，保持甚至增强了零样本和检索能力，且不增加推理成本。
selection_source: fresh_fetch
motivation: 现有的提升组合性方法过度依赖硬负样本，容易导致模型泛化能力下降及零样本性能受损。
method: 通过NLP工具提取短概念标题进行对齐，并引入无参数跨模态注意力池化以获取概念中心视觉嵌入。
result: 在标准组合性基准上达到SOTA水平，同时维持或提升了零样本分类和检索性能。
conclusion: 概念中心学习是解决视觉语言模型组合性问题的有效途径，且无需牺牲基础视觉语言能力。
---

## 摘要
对比视觉语言 (V&L) 模型仍然是各种应用的热门选择。然而，一些局限性也随之显现，最显著的是 V&L 模型学习组合表示的能力有限。先前的方法通常通过生成自定义训练数据以获取难负样本 (hard negative samples) 来解决这一局限。难负样本已被证明可以提高组合性任务的性能，但通常仅针对单一基准测试，缺乏泛化性，并且会导致零样本或检索性能等基础 V&L 能力的大幅下降，从而使其变得不切实际。在这项工作中，我们采用了一种不同的方法。我们确定了限制 V&L 组合性性能的两个根本原因：1) 长的训练字幕不需要组合表示；2) 文本和图像编码器中的最终全局池化导致学习绑定 (binding) 所需信息的彻底丢失。作为补救措施，我们提出了两个简单的解决方案：1) 我们使用标准的 NLP 软件获取以概念为中心的短字幕片段，并将其与图像对齐；2) 我们引入了一种无参数的跨模态注意力池化 (cross-modal attention-pooling)，以从图像编码器中获取以概念为中心的视觉嵌入。通过这两项改进和简单的辅助对比损失，我们在标准组合性基准测试上获得了 SOTA 性能，同时保持或提高了强大的零样本和检索能力。这是在不增加推理成本的情况下实现的。我们在 https://github.com/SamsungLabs/concept_centric_clip 发布了本工作的代码。

## Abstract
Contrastive vision-language (V&L) models remain a popular choice for various applications. However, several limitations have emerged, most notably the limited ability of V&L models to learn compositional representations. Prior methods often addressed this limitation by generating custom training data to obtain hard negative samples. Hard negatives have been shown to improve performance on compositionality tasks, but are often specific to a single benchmark, do not generalize, and can cause substantial degradation of basic V&L capabilities such as zero-shot or retrieval performance, rendering them impractical. In this work we follow a different approach. We identify two root causes that limit compositionality performance of V&Ls: 1) Long training captions do not require a compositional representation; and 2) The final global pooling in the text and image encoders lead to a complete loss of the necessary information to learn binding in the first place. As a remedy, we propose two simple solutions: 1) We obtain short concept centric caption parts using standard NLP software and align those with the image; and 2) We introduce a parameter-free cross-modal attention-pooling to obtain concept centric visual embeddings from the image encoder. With these two changes and simple auxiliary contrastive losses, we obtain SOTA performance on standard compositionality benchmarks, while maintaining or improving strong zero-shot and retrieval capabilities. This is achieved without increasing inference cost. We release the code for this work at https://github.com/SamsungLabs/concept_centric_clip.

---

## 论文详细总结（自动生成）

以下是对论文《No Hard Negatives Required: Concept Centric Learning Leads to Compositionality without Degrading Zero-shot Capabilities of Contrastive Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：对比视觉语言模型（如 CLIP、SigLIP）虽然在零样本分类和检索上表现优异，但在**组合性（Compositionality）**方面存在严重缺陷。它们往往表现得像“词袋模型”（Bag-of-Words），无法准确理解属性与名词的绑定（如区分“红色的沙发”与“沙发上的红色衣服”）或物体间的关系。
*   **核心问题**：现有提升组合性的方法大多依赖于生成“硬负样本”（Hard Negatives），这会导致模型在特定基准上过拟合，且往往会显著损害模型的通用零样本能力和检索性能。
*   **研究动机**：探索一种无需合成负样本、通过改进训练目标来从原始数据中更有效地学习组合表示的方法。

### 2. 方法论
论文提出了 **C²LIP (Concept-centric CLIP)**，其核心思想是解决长标题带来的“词袋快捷路径”问题以及全局池化导致的信息丢失。
*   **概念提取**：利用标准 NLP 工具（如 spaCy）从原始标题中提取名词短语（Noun-Phrases），将其视为独立的“概念”。
*   **名词短语对比损失 ($L_{npc}$)**：将图像的全局嵌入与标题中提取的所有名词短语嵌入进行对齐。由于短语较短，模型无法通过词袋捷径获胜，必须学习属性与名词的绑定。
*   **跨模态注意力池化损失 ($L_{xac}$)**：
    *   引入一个**无参数**的跨模态注意力机制。
    *   使用文本概念嵌入作为 Query，去查询图像编码器的 Patch Tokens（Key 和 Value）。
    *   这种方式在全局池化之前强制进行视觉与文本概念的细粒度对齐，从而在特征层级保留绑定信息。
*   **推理一致性**：该方法仅在训练时增加辅助损失，推理过程与原始 SigLIP 完全相同，不增加任何计算开销。

### 3. 实验设计
*   **数据集**：在 CC3M 数据集（使用 DreamLIP 提供的增强标题版本）上进行微调。
*   **评测基准 (Benchmarks)**：
    *   **组合性**：SugarCrepe、SugarCrepe++（涵盖物体、属性、关系的替换/交换/添加）。
    *   **零样本分类**：ImageNet-1K。
    *   **检索**：Flickr30K、MSCOCO、DOCCI（细粒度）、ImageInWords (IIW)。
*   **对比方法**：包括原始 CLIP/SigLIP、组合性增强模型（NegCLIP、CE-CLIP、DAC、CLIC 等）、细粒度模型（FG-CLIP、DreamLIP）以及大型模型（SigLIP-2、BLIP）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A40 GPU**。
*   **训练配置**：基于 ViT-B/16 骨干网络，微调 5 个 Epoch，有效 Batch Size 为 768，学习率为 1e-5。
*   **实现**：基于 OpenCLIP 框架实现。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的定量对比，涵盖了 10 多个不同的基准测试。
*   **消融实验**：详细验证了 $L_{npc}$ 和 $L_{xac}$ 各自的贡献，证明了两者结合的优越性。
*   **定性分析**：通过注意力图（Attention Map）可视化，展示了模型在训练后能更准确地关注到标题对应的视觉区域。
*   **下游任务**：将 C²LIP 作为视觉编码器接入 LLaVA 框架进行指令微调，验证了其组合性能力可以迁移至多模态大模型（VLM）。
*   **评价**：实验设计全面且客观，通过与多种 SOTA 方法在多个维度（而非单一指标）上的对比，证明了该方法的稳健性。

### 6. 主要结论与发现
*   **SOTA 性能**：C²LIP 在 SugarCrepe++ 等组合性基准上达到了领先水平，尤其在属性绑定任务上表现突出。
*   **能力保持**：与依赖硬负样本的方法不同，C²LIP 在提升组合性的同时，维持了极高的检索性能，甚至在某些检索任务上超过了原始模型。
*   **根本原因验证**：实验证明，通过短概念对齐和池化前的跨模态交互，可以有效克服对比学习模型对词袋表示的依赖。

### 7. 优点
*   **无需硬负样本**：避免了复杂的负样本生成流程和潜在的数据偏差。
*   **高效且无损**：训练开销小，推理成本零增加，非常适合作为基础模型的后处理微调手段。
*   **泛化性强**：在 SugarCrepe++（更难、更难“刷榜”的基准）上的表现远超同类方法，证明了其学习到的是真实的组合能力而非模式匹配。

### 8. 不足与局限
*   **分类性能轻微下降**：在 ImageNet 上的准确率有约 2.9% 的下降。作者认为这是因为模型从“物体中心”转向了“场景中心”表示，而 ImageNet 极其强调单一显著物体。
*   **依赖 NLP 解析**：名词短语的提取依赖于 spaCy 等工具的准确性，对于语法极不规范的互联网标题可能存在鲁棒性问题。
*   **扩展性待验证**：主要实验集中在 ViT-B 规模，对于超大规模参数模型（如 ViT-G）的效果有待进一步验证。

（完）
