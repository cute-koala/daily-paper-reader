---
title: "DyGeoVLN: Infusing Dynamic Geometry Foundation Model into Vision-Language Navigation"
title_zh: DyGeoVLN：将动态几何基础模型注入视觉语言导航
authors: "Xiangchen Liu, Hanghan Zheng, Jeil Jeong, Minsung Yoon, Lin Zhao, Zhide Zhong, Haoang Li, Sung-Eui Yoon"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21269v1"
tags: ["query:vlm"]
score: 9.0
evidence: 结合动态几何的视觉语言导航
tldr: 针对视觉语言导航（VLN）在动态现实场景中泛化能力不足的问题，本文提出DyGeoVLN框架。该框架通过将动态几何基础模型融入VLN，利用跨分支特征融合实现显式的3D空间表示和视觉语义推理。此外，引入了无位姿、自适应分辨率的令牌剪枝策略，有效降低了长程导航的推理成本。实验证明该方法在多个基准测试中达到SOTA，并具有极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLN方法多基于静态场景假设，难以应对动态且复杂的现实环境。
method: 提出DyGeoVLN框架，通过融合动态几何基础模型增强3D空间感知，并采用自适应令牌剪枝策略优化长程导航效率。
result: 在多个主流基准测试上取得了最先进的性能，并在真实世界环境中表现出显著的鲁棒性。
conclusion: 将动态几何感知与高效的令牌管理相结合，是提升视觉语言导航在动态场景下性能和效率的有效途径。
---

## 摘要
视觉语言导航（VLN）要求智能体理解视觉观测和语言指令，以便在未知环境中进行导航。大多数现有方法依赖于静态场景假设，难以泛化到动态的现实场景中。为了应对这一挑战，我们提出了 DyGeoVLN，一个动态几何感知的 VLN 框架。我们的方法通过跨分支特征融合将动态几何基础模型注入 VLN 框架，从而实现显式的 3D 空间表示和视觉语义推理。为了在长时程、动态导航中高效压缩历史 token 信息，我们进一步引入了一种新颖的无位姿且自适应分辨率的 token 剪枝策略。该策略可以去除时空冗余 token，以降低推理成本。大量的实验表明，我们的方法在多个基准测试上达到了最先进的性能，并在现实环境中表现出强大的鲁棒性。

## Abstract
Vision-language Navigation (VLN) requires an agent to understand visual observations and language instructions to navigate in unseen environments. Most existing approaches rely on static scene assumptions and struggle to generalize in dynamic, real-world scenarios. To address this challenge, we propose DyGeoVLN, a dynamic geometry-aware VLN framework. Our method infuses a dynamic geometry foundation model into the VLN framework through cross-branch feature fusion to enable explicit 3D spatial representation and visual-semantic reasoning. To efficiently compress historical token information in long-horizon, dynamic navigation, we further introduce a novel pose-free and adaptive-resolution token-pruning strategy. This strategy can remove spatio-temporal redundant tokens to reduce inference cost. Extensive experiments demonstrate that our approach achieves state-of-the-art performance on multiple benchmarks and exhibits strong robustness in real-world environments.

---

## 论文详细总结（自动生成）

这篇论文提出了 **DyGeoVLN**，旨在通过引入动态几何基础模型来增强视觉语言导航（VLN）智能体在复杂动态环境中的 3D 空间推理能力和长程导航效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 VLN 方法主要依赖于 2D 图像-文本预训练模型，缺乏对 3D 物理世界的全局一致性空间推理能力。此外，在处理包含动态物体（如行人）的现实场景时，现有的几何模型表现会大幅下降。
*   **研究动机**：
    *   **动态环境挑战**：静态场景假设不适用于现实世界，动态物体的存在会干扰几何重建和定位。
    *   **Token 爆炸问题**：长程导航会积累海量视觉 Token，导致计算开销巨大，现有剪枝方法往往依赖模拟器提供的完美位姿和深度，难以在真实硬件上部署。

### 2. 方法论
DyGeoVLN 框架由三个核心组件构成：
*   **动态几何基础模型 (DGFM)**：
    *   **深度引导的局部点图**：利用单目深度估计（Depth Anything v2）将 2D 像素投影为 3D 坐标。
    *   **多级 3D 嵌入**：通过点图 Transformer 提取分层几何特征。
    *   **动态感知融合**：采用“零均值卷积”模块将 3D 几何特征注入视觉骨干网络，在保留预训练语义先验的同时引入显式空间线索。
    *   **分层潜变量解码**：专门的解码器分别预测相机位姿、局部点云和全局几何 Token。
*   **跨分支特征融合架构**：
    *   **双分支设计**：一个分支提取 2D 语义 Token（基于 Qwen2-VL），另一个分支提取 3D 空间几何 Token。
    *   **交叉注意力融合**：通过多头交叉注意力机制将空间结构信息整合进语义 Token 中。
*   **自适应分辨率与占用感知 Token 剪枝**：
    *   **无位姿依赖**：利用 DGFM 自身推断的位姿和点云进行体素化，无需外部传感器定位。
    *   **自适应分辨率**：根据深度调整体素大小（近处精细，远处粗糙）。
    *   **重要性补偿**：引入 Token 补全策略，确保稀疏区域保留足够的语义和几何显著 Token。

### 3. 实验设计
*   **数据集与基准**：
    *   **动态基准**：**HA-VLN**（人类感知 VLN），包含多行人交互和社交建模。
    *   **静态基准**：**R2R-CE**（连续环境中的房间到房间导航）。
    *   **自建数据集**：**DyHM3D**，包含 50,000 条动态行人轨迹的 3D 数据集，用于训练 DGFM。
*   **对比方法**：包括 NaVid, Uni-NaVid, NaVILA, StreamVLN, g3D-LF 等最先进的（SOTA）模型。
*   **评估指标**：成功率 (SR)、路径长度加权成功率 (SPL)、导航误差 (NE)、碰撞率 (CR/TCR) 等。
*   **真实世界部署**：使用 Unitree Go1 四足机器人，在走廊、大厅、拥挤房间等场景进行实测。

### 4. 资源与算力
*   **硬件设备**：
    *   **机器人端**：Unitree Go1 机器人，配备 Intel RealSense D435i 相机和 NVIDIA Jetson Orin Nano。
    *   **服务端**：策略部署在本地服务器上，通过 Wi-Fi 与机器人通信。
*   **算力说明**：文中**未明确列出**具体的训练总时长、使用的 GPU 型号及数量（如 A100/H100 的具体规模）。但提到了模型基于 Qwen2-VL 和 π3 的权重进行初始化，并使用了 DAgger 算法进行微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流仿真基准上进行了详尽测试。
    *   **消融实验**：针对跨分支融合、动态空间注入、Token 剪枝等核心组件做了 4 组对比实验，验证了各部分的贡献。
    *   **真实世界实验**：在 4 种不同难度的场景下，每种模型进行了 20 次测试。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实、从静态到动态的维度。对比对象包含了最新的 VLM 导航模型，评估指标客观且符合机器人导航的标准。

### 6. 主要结论与发现
*   **性能领先**：在 HA-VLN 动态基准上，SR 提升了约 13%，CR 降低了 11%；在 R2R-CE 静态基准上，仅凭单目 RGB 输入就超越了许多使用全景 RGB-D 的方法。
*   **动态鲁棒性**：DGFM 能够有效处理行人移动带来的几何畸变，使智能体在拥挤环境中保持更低的碰撞率。
*   **效率优化**：提出的剪枝策略在不损失精度的情况下显著降低了长程任务的计算负担。

### 7. 优点与亮点
*   **动态几何感知**：通过自建 DyHM3D 数据集和 DGFM 模型，解决了 VLN 在动态场景下几何表示失效的痛点。
*   **系统解耦与轻量化**：Token 剪枝策略不依赖外部定位系统（Pose-free），极大降低了对硬件传感器的要求，增强了现实部署的可行性。
*   **端到端融合**：巧妙地将 3D 几何特征作为残差注入 MLLM，平衡了语义理解与空间推理。

### 8. 不足与局限
*   **实时性延迟**：真实世界部署依赖本地服务器通过 Wi-Fi 传输视频流和指令，这在网络不稳定的环境下可能导致控制延迟。
*   **传感器局限**：虽然模型支持单目 RGB，但在极端光照或完全无纹理的墙面环境下，单目深度估计的准确性可能会下降，进而影响几何 Token 的质量。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。

（完）
