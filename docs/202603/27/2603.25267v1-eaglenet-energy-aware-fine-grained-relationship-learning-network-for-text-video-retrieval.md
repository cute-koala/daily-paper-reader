---
title: "EagleNet: Energy-Aware Fine-Grained Relationship Learning Network for Text-Video Retrieval"
title_zh: EagleNet：用于文本-视频检索的能量感知细粒度关系学习网络
authors: "Yuhan Chen, Pengwen Dai, Chuan Wang, Dayan Wu, Xiaochun Cao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25267v1"
tags: ["query:vlm"]
score: 9.0
evidence: 文本-视频检索的细粒度关系学习
tldr: EagleNet 针对文本-视频检索中现有方法忽略视频帧间上下文关系的问题，提出了能量感知细粒度关系学习网络。该网络通过 FRL 机制构建文本-帧图并学习其交互，结合 EAM 建模交互能量，并采用 Sigmoid 损失优化对齐。实验证明该方法在 MSRVTT 等多个数据集上达到了领先性能，有效提升了文本表达的准确性与上下文感知能力。
selection_source: fresh_fetch
motivation: 现有文本增强方法忽略了视频内部帧之间的丰富交互，导致生成的扩展文本无法捕捉帧的上下文信息，造成文本与视频语义失配。
method: 提出 FRL 机制构建文本-帧图以学习细粒度关系，并设计 EAM 建模交互能量，同时引入 Sigmoid 损失替代传统对比损失。
result: 在 MSRVTT、DiDeMo、MSVD 和 VATEX 等主流数据集上的广泛实验证明了 EagleNet 的优越性。
conclusion: EagleNet 通过建模细粒度关系和能量感知匹配，显著增强了文本-视频检索中的跨模态对齐效果。
---

## 摘要
由于大规模视觉-语言预训练模型的近期发展，文本-视频检索任务取得了显著进步。传统方法主要侧重于视频表示或跨模态对齐，而近期研究转向增强文本表达能力，以更好地匹配视频中的丰富语义。然而，这些方法仅利用文本与帧/视频之间的交互，忽略了视频内部帧之间的丰富交互，导致最终扩展的文本无法捕捉帧上下文信息，从而造成文本与视频之间的差异。针对这一问题，我们提出了能量感知细粒度关系学习网络（EagleNet），用于生成准确且具有上下文感知能力的增强文本嵌入。具体而言，所提出的细粒度关系学习机制（FRL）首先利用生成的候选文本和视频帧构建文本-帧图，随后学习文本与帧之间的关系，并最终将候选文本聚合为包含帧上下文信息的增强文本嵌入。为了进一步提升 FRL 中的细粒度关系学习，我们设计了能量感知匹配（EAM）来建模文本-帧交互的能量，从而准确捕捉真实文本-视频对的分布。此外，为了实现更有效的跨模态对齐和稳定的训练，我们采用 sigmoid 损失替换了传统的基于 softmax 的对比损失。在 MSRVTT、DiDeMo、MSVD 和 VATEX 数据集上的广泛实验证明了 EagleNet 的优越性。代码可在 https://github.com/draym28/EagleNet 获取。

## Abstract
Text-video retrieval tasks have seen significant improvements due to the recent development of large-scale vision-language pre-trained models. Traditional methods primarily focus on video representations or cross-modal alignment, while recent works shift toward enriching text expressiveness to better match the rich semantics in videos. However, these methods use only interactions between text and frames/video, and ignore rich interactions among the internal frames within a video, so the final expanded text cannot capture frame contextual information, leading to disparities between text and video. In response, we introduce Energy-Aware Fine-Grained Relationship Learning Network (EagleNet) to generate accurate and context-aware enriched text embeddings. Specifically, the proposed Fine-Grained Relationship Learning mechanism (FRL) first constructs a text-frame graph by the generated text candidates and frames, then learns relationships among texts and frames, which are finally used to aggregate text candidates into an enriched text embedding that incorporates frame contextual information. To further improve fine-grained relationship learning in FRL, we design Energy-Aware Matching (EAM) to model the energy of text-frame interactions and thus accurately capture the distribution of real text-video pairs. Moreover, for more effective cross-modal alignment and stable training, we replace the conventional softmax-based contrastive loss with the sigmoid loss. Extensive experiments have demonstrated the superiority of EagleNet across MSRVTT, DiDeMo, MSVD, and VATEX. Codes are available at https://github.com/draym28/EagleNet.

---

## 论文详细总结（自动生成）

### EagleNet: 用于文本-视频检索的能量感知细粒度关系学习网络论文总结

#### 1. 核心问题与整体含义
文本-视频检索（TVR）的核心挑战在于跨模态语义对齐。尽管近期研究通过增强文本表达能力（如生成候选文本）来匹配视频语义，但现有方法往往只关注“文本-帧”或“文本-视频”的直接交互，而**忽略了视频内部帧与帧之间的上下文关系**。这导致生成的增强文本无法捕捉视频的动态时序逻辑，造成文本与视频语义的脱节。EagleNet 的提出旨在通过建模细粒度的文本-帧及帧-帧关系，生成更具上下文感知能力的增强文本嵌入。

#### 2. 方法论
EagleNet 主要由三个核心组件构成：
*   **细粒度关系学习（FRL）**：
    *   **文本建模**：利用随机文本建模策略生成多个候选文本。
    *   **图构建**：构建一个包含文本节点和视频帧节点的异构图，定义了三种关系：文本-文本、帧-帧、文本-帧。
    *   **关系学习**：采用关系图注意力网络（RGAT）学习节点间的交互。通过建模帧与帧之间的关系，使模型能捕捉视频的时序上下文。
    *   **聚合**：利用学习到的文本-帧权重，将候选文本聚合为最终的增强文本嵌入 $t_{gen}$。
*   **能量感知匹配（EAM）**：
    *   **核心思想**：引入能量模型（EBM）来建模真实文本-视频对的分布。
    *   **技术细节**：通过玻尔兹曼分布定义能量函数（如双线性函数或 MLP），为匹配对分配低能量，不匹配对分配高能量。
    *   **训练过程**：使用马尔可夫链蒙特卡罗（MCMC）采样生成负样本（假数据），通过最小化负对数似然损失来优化细粒度的文本-帧交互。
*   **Sigmoid 损失函数**：
    *   用 Sigmoid 损失替换传统的 Softmax 对比损失。Sigmoid 损失独立处理每个正负对，对批次大小不敏感，且更适合 TVR 中“一对多”或“多对一”的语义匹配场景。

#### 3. 实验设计
*   **数据集**：在四个主流基准数据集上进行了验证：**MSRVTT**（1k-A 分割）、**DiDeMo**、**MSVD** 和 **VATEX**。
*   **Benchmark 与指标**：使用 Recall@1/5/10、中值排名（MdR）、平均排名（MnR）和 Rsum 作为评价指标。
*   **对比方法**：对比了包括非 CLIP 方法（如 CE, MMT, T2vlad）和基于 CLIP 的最先进方法（如 CLIP4Clip, XPool, TS2Net, CLIP-VIP, TMASS, TV-ProxyNet, Video-ColBERT 等）。

#### 4. 资源与算力
*   **硬件**：训练阶段使用了 **2 台 NVIDIA A100 GPU**；推理阶段使用单台 A100。
*   **训练参数**：训练 5 个 epoch，批次大小（Batch Size）为 64。
*   **时长与效率**：文中提供了详细的训练和测试耗时对比。例如在 MSRVTT 上，EagleNet 的训练时间约为 5207 秒/epoch，测试总时长为 141.7 秒。虽然比基础 CLIP4Clip 慢，但在同类增强模型中具有竞争力。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个数据集上进行了全方位的性能对比，并针对 ViT-B/32 和 ViT-B/16 两种骨干网络分别给出了结果。
*   **消融实验**：非常充分。涵盖了组件有效性（FRL/EAM/Sigmoid）、能量函数选择（CosSim/Bilinear/MLP）、能量池化方式（Max/Min/Avg）、图编码器类型（GAT vs RGAT）以及损失系数敏感度分析。
*   **客观性**：作者重新实现了多个 SOTA 模型（标注为 †）以解决原始代码中的数据泄露问题，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能领先**：EagleNet 在所有四个数据集上均取得了 SOTA 性能，尤其在 R@1 指标上提升显著（如 MSRVTT 提升了 2.0%）。
*   **上下文的重要性**：实验证明，引入帧与帧之间的关系（f2f edges）能显著提升检索准确性，证明了视频上下文对文本增强的关键作用。
*   **EAM 的增益**：能量模型能有效辅助 FRL 学习更精准的细粒度交互，且由于 EAM 仅在训练阶段使用，不会增加推理负担。
*   **Sigmoid 损失的优越性**：相比 Softmax，Sigmoid 损失在多匹配场景下表现更稳定。

#### 7. 优点
*   **创新性建模**：首次将能量模型（EBM）引入文本-视频检索任务，并结合图神经网络捕捉视频内部时序关系。
*   **推理高效**：复杂的能量感知模块仅在训练时参与，保持了推理阶段的相对高效。
*   **鲁棒性强**：通过重新实现对比模型并解决数据泄露问题，其实验结果具有较高的可信度。
*   **细粒度对齐**：通过 FRL 机制有效缓解了视频描述过于简短导致的语义缺失问题。

#### 8. 不足与局限
*   **计算开销**：虽然推理较快，但训练阶段涉及 MCMC 采样和多层 RGAT，计算复杂度高于基础对比模型。
*   **超参数依赖**：尽管敏感度分析显示模型较稳健，但 MCMC 的步数、噪声方差等参数仍需根据不同数据集进行微调。
*   **模型复杂度**：相比于简单的池化方法，EagleNet 的架构较为复杂，增加了实现和维护的难度。

（完）
