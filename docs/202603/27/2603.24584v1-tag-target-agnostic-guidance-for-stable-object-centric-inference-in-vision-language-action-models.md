---
title: "TAG: Target-Agnostic Guidance for Stable Object-Centric Inference in Vision-Language-Action Models"
title_zh: TAG：视觉-语言-动作模型中稳定以对象为中心推理的目标无关引导
authors: "Jiaying Zhou, Zhihao Zhan, Ruifeng Zhai, Qinhan Lyu, Hao Liu, Keze Wang, Liang Lin, Guangrun Wang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24584v1"
tags: ["query:vlm"]
score: 9.0
evidence: 解决视觉-语言-动作模型中的实例级定位失败问题
tldr: 针对视觉-语言-动作（VLA）模型在杂乱场景中容易出现目标定位偏差的问题，本文提出了TAG（目标无关引导）机制。TAG受分类器自由引导（CFG）启发，通过对比原始观测与物体擦除后的观测，利用残差信号增强决策中的物体特征。该方法无需修改模型架构，在LIBERO等基准测试中显著提升了机器人在复杂环境下的操作鲁棒性，减少了误触和偏离目标的现象。
selection_source: fresh_fetch
motivation: VLA模型在杂乱环境中常因实例级定位失败导致抓取偏离或误触无关物体。
method: 提出TAG机制，通过对比原始图像与物体擦除图像的策略预测差异，生成残差信号来引导模型关注目标物体。
result: 在LIBERO、LIBERO-Plus和VLABench等标准基准上，TAG显著提高了模型在杂乱场景下的鲁棒性并减少了执行错误。
conclusion: TAG是一种简单且无需改变架构的推理增强方法，能有效缓解VLA模型在复杂环境中的外观偏差和干扰项影响。
---

## 摘要
视觉-语言-动作（VLA）策略在将语言指令和视觉观测映射到机器人动作方面取得了显著进展，但在包含干扰项的杂乱场景中，其可靠性会有所下降。通过分析失败案例，我们发现许多错误并非源于不可行的运动，而是源于实例级的定位（grounding）失败：策略通常会生成一个看似合理的抓取轨迹，但最终落点略微偏离目标，甚至落在错误的对象实例上。为了解决这一问题，我们提出了 TAG（Target-Agnostic Guidance，目标无关引导），这是一种简单的推理阶段引导机制，能够显式地减少 VLA 策略中由干扰项和外观引起的偏差。受无分类器引导（CFG）的启发，TAG 对比了原始观测和对象擦除观测下的策略预测，并将其差异作为残差引导信号，从而在决策过程中增强对象证据的影响。TAG 不需要修改策略架构，并且可以以极小的训练和推理改动集成到现有的 VLA 策略中。我们在包括 LIBERO、LIBERO-Plus 和 VLABench 在内的标准操作基准测试上评估了 TAG，结果表明它一致地提高了在杂乱环境下的鲁棒性，并减少了“差一点命中”和“错误对象”的执行情况。

## Abstract
Vision--Language--Action (VLA) policies have shown strong progress in mapping language instructions and visual observations to robotic actions, yet their reliability degrades in cluttered scenes with distractors. By analyzing failure cases, we find that many errors do not arise from infeasible motions, but from instance-level grounding failures: the policy often produces a plausible grasp trajectory that lands slightly off-target or even on the wrong object instance. To address this issue, we propose TAG (Target-Agnostic Guidance), a simple inference-time guidance mechanism that explicitly reduces distractor- and appearance-induced bias in VLA policies. Inspired by classifier-free guidance (CFG), TAG contrasts policy predictions under the original observation and an object-erased observation, and uses their difference as a residual steering signal that strengthens the influence of object evidence in the decision process. TAG does not require modifying the policy architecture and can be integrated with existing VLA policies with minimal training and inference changes. We evaluate TAG on standard manipulation benchmarks, including LIBERO, LIBERO-Plus, and VLABench, where it consistently improves robustness under clutter and reduces near-miss and wrong-object executions.

---

## 论文详细总结（自动生成）

### TAG: 视觉-语言-动作模型中稳定以对象为中心推理的目标无关引导

#### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型在处理杂乱场景时，经常出现“实例级定位失败”。具体表现为：机器人生成的抓取轨迹在物理上是可行的，但往往会略微偏离目标，或者抓错物体。
*   **核心问题**：背景干扰项和数据中的外观统计偏差误导了模型的注意力。论文旨在通过显式地抑制背景和干扰项的偏差，增强模型对目标物体的关注度，从而提高在复杂环境下的操作鲁棒性。

#### 2. 方法论
*   **核心思想**：借鉴生成模型中的“无分类器引导（Classifier-Free Guidance, CFG）”，提出 **TAG（Target-Agnostic Guidance）**。通过对比“包含目标的原始观测”和“不含目标的对冲观测”下的预测差异，提取出纯粹由目标驱动的动作残差，并进行放大。
*   **关键技术细节**：
    *   **引导公式**：$v_{TAG} = v(I_{uncond}) + w \cdot [v(I_{cond}) - v(I_{uncond})]$。其中 $w$ 是引导权重，$I_{uncond}$ 是目标无关的基准输入。
    *   **训练校准**：在训练阶段，以 10% 的概率随机将原始图像替换为“目标擦除”后的图像，使模型学会处理无目标情况下的先验。
    *   **反事实合成流水线**：利用 Qwen3-VL 解析指令获取目标描述，结合 Grounding DINO 和 SAM 2 生成时空掩码，最后使用 MiniMaxRemover 进行图像修复（Inpainting），自动生成擦除目标的训练数据。
    *   **推理策略**：推理时通常使用首帧提取的纯背景图（TAG-bg）或全黑图（TAG-black）作为无条件分支，以保持时空稳定性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：标准机器人操作基准，评估基础任务能力。
    *   **LIBERO-Plus**：引入视觉（光照、背景、相机位姿）和空间扰动的鲁棒性测试集。
    *   **VLABench (Track 1)**：包含高度相似干扰项（如在一堆扑克牌或麻将中选择特定一张）的精细操作任务。
*   **对比方法**：对比了 $\pi_0$、$\pi_{0.5}$、OpenVLA、Octo、CoT-VLA、SpatialVLA 等多种主流 VLA 模型。
*   **评估指标**：成功率（Success Rate, SR）和过程得分（Process Score, PS）。

#### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX PRO 6000 GPU** 进行训练和测试。
*   **训练细节**：每个模型训练 30,000 步，LIBERO 批大小为 32，VLABench 为 24。采用余弦衰减学习率（峰值 $2.5 \times 10^{-5}$）和 EMA（衰减率 0.999）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大仿真基准测试和真实世界机器人（Franka Research 3）实验。
*   **消融实验**：针对训练时的去条件策略（黑图 vs 擦除图）、推理时的擦除方法（实时掩码 vs 静态背景）、以及引导权重 $w$ 的取值进行了详尽测试。
*   **充分性**：实验设计非常充分且客观。不仅在多个模拟器上验证了性能提升，还通过注意力图可视化直观展示了 TAG 如何纠正模型注意力，并验证了其在真实世界相机位姿变动下的泛化性。

#### 6. 主要结论与发现
*   **性能提升**：TAG 在所有基准上均显著优于基座模型。在 VLABench 的精细选择任务中，成功率从 29.4% 提升至 55.41%。
*   **长程任务优势**：在 Libero-Long 任务中，TAG 显著减少了复合误差，成功率从 89.6% 提升至 97.0%。
*   **稳定性发现**：推理时使用静态背景或黑图的效果远好于实时动态擦除，因为后者会引入时域不一致性和视觉伪影。
*   **权重敏感度**：引导权重 $w$ 在 1.25 左右达到最优，过大的权重会扭曲动作分布。

#### 7. 优点
*   **架构无关性**：作为一种推理阶段的引导机制，TAG 不需要修改 VLA 模型的核心架构，具有很强的通用性。
*   **即插即用**：可以轻松集成到现有的基于扩散（Diffusion）或流匹配（Flow-matching）的策略中。
*   **自动化程度高**：开发了一套完整的自动化流水线来生成反事实训练数据，降低了人工标注成本。

#### 8. 不足与局限
*   **推理开销**：由于需要双分支预测（条件与无条件），推理时的计算量翻倍，可能影响实时控制频率。
*   **背景依赖**：TAG-bg 模式依赖于能够获取或合成干净的背景图，在极度动态或背景剧烈变化的场景下可能受限。
*   **数据合成质量**：引导效果高度依赖于训练阶段目标擦除和修复的质量，若修复产生严重伪影，可能会引入新的偏差。

（完）
