---
title: "SlotVTG: Object-Centric Adapter for Generalizable Video Temporal Grounding"
title_zh: SlotVTG：用于可泛化视频时序定位的以对象为中心适配器
authors: "Jiwook Han, Geo Ahn, Youngrae Kim, Jinwoo Choi"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25733v1"
tags: ["query:vlm"]
score: 9.0
evidence: 用于视频时序基准和以物体为中心推理的 MLLM
tldr: 针对多模态大模型在视频时序定位（VTG）中因过度拟合数据集捷径而导致泛化性差的问题，本文提出SlotVTG框架。该框架通过轻量级的槽位适配器将视觉特征分解为以物体为中心的抽象槽位，并利用自监督视觉模型的先验知识引导语义一致性。实验表明，该方法在保持域内性能的同时，显著提升了跨域泛化能力且计算开销极小。
selection_source: fresh_fetch
motivation: 多模态大模型在VTG任务微调时容易学习数据集特定的捷径而非真实的视觉内容，导致其在域外场景的泛化能力不足。
method: 提出一种轻量级槽位适配器，通过槽位注意力机制将视觉Token分解为抽象槽位，并结合自监督视觉先验进行重构以实现物体中心化推理。
result: 在标准VTG基准上的实验证明，该方法在显著提升跨域泛化鲁棒性的同时，维持了优秀的域内性能。
conclusion: SlotVTG通过引入物体中心学习，有效地引导模型进行基于输入的视觉推理，为提升视频理解模型的泛化性提供了一种低成本且高效的方案。
---

## 摘要
多模态大语言模型 (MLLM) 在视频时序定位 (VTG) 任务上表现出了强大的性能。然而，它们粗粒度的识别能力不足以进行细粒度的时序理解，使得特定任务的微调变得不可或缺。这种微调会导致模型记忆特定于数据集的捷径，而不是忠实地定位到实际的视觉内容，从而导致较差的域外 (OOD) 泛化能力。以对象为中心的学习通过将场景分解为实体级表示提供了一种有前景的补救方案，但现有方法需要从头开始重新运行整个多阶段训练流水线。我们提出了 SlotVTG，这是一个以极小成本引导 MLLM 进行以对象为中心、基于输入的视觉推理的框架。SlotVTG 引入了一个轻量级的槽位适配器，通过槽位注意力将视觉标记分解为抽象槽位并重构原始序列，其中来自自监督视觉模型的对象性先验促进了语义连贯的槽位形成。在标准 VTG 基准测试上的跨域评估表明，我们的方法在保持具有竞争力的域内 (ID) 性能的同时，以极小的开销显著提高了 OOD 鲁棒性。

## Abstract
Multimodal Large Language Models (MLLMs) have shown strong performance on Video Temporal Grounding (VTG). However, their coarse recognition capabilities are insufficient for fine-grained temporal understanding, making task-specific fine-tuning indispensable. This fine-tuning causes models to memorize dataset-specific shortcuts rather than faithfully grounding in the actual visual content, leading to poor Out-of-Domain (OOD) generalization. Object-centric learning offers a promising remedy by decomposing scenes into entity-level representations, but existing approaches require re-running the entire multi-stage training pipeline from scratch. We propose SlotVTG, a framework that steers MLLMs toward object-centric, input-grounded visual reasoning at minimal cost. SlotVTG introduces a lightweight slot adapter that decomposes visual tokens into abstract slots via slot attention and reconstructs the original sequence, where objectness priors from a self-supervised vision model encourage semantically coherent slot formation. Cross-domain evaluation on standard VTG benchmarks demonstrates that our approach significantly improves OOD robustness while maintaining competitive In-Domain (ID) performance with minimal overhead.

---

## 论文详细总结（自动生成）

以下是对论文《SlotVTG: Object-Centric Adapter for Generalizable Video Temporal Grounding》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：视频时序定位（VTG）要求模型根据自然语言查询在未修剪视频中精确定位起止时间。虽然多模态大语言模型（MLLM）表现强劲，但必须经过特定任务微调才能实现细粒度理解。
*   **核心问题**：微调过程会导致模型产生“捷径学习”（Shortcut Learning）现象。模型倾向于记忆数据集特定的偏见（如时间位置分布、文本模式），而非真正理解视觉内容。这导致模型在训练域内（In-Domain, ID）表现优异，但在面对未见过的域外（Out-of-Domain, OOD）数据时性能大幅下降。
*   **研究动机**：引入“以物体为中心”（Object-Centric）的学习机制，将场景分解为离散的实体表示，从而提取领域无关的视觉特征，增强模型的泛化能力。

### 2. 方法论
SlotVTG 提出了一种参数高效的框架，核心组件包括：
*   **轻量级槽位适配器（Slot Adapter）**：
    *   **下采样投影**：将视觉 Token 投影到低维瓶颈空间。
    *   **槽位注意力（Slot Attention）**：利用一组可学习的槽位（Slots）通过迭代竞争机制对视觉 Token 进行聚类，每个槽位负责捕捉视频中的特定实体或语义区域。
    *   **Token 重构**：通过交叉注意力机制，利用原始 Token 作为查询从槽位中检索信息，重构出与输入长度一致的序列，以便 LLM 解码器处理。
    *   **早期层插入**：将适配器插入 LLM 的前几层，因为研究发现早期层主要负责跨帧视觉交互。
*   **槽位对齐损失（Slot Alignment Loss, SA Loss）**：
    *   利用预训练的自监督模型（DINOv2）提取视觉特征的相似度矩阵作为“物体性先验”。
    *   通过余弦相似度损失，引导槽位注意力的权重分布与 DINOv2 的特征结构对齐，确保槽位形成语义连贯的实体表示。
*   **训练策略**：冻结视觉编码器，仅联合训练 Slot Adapter 和 LoRA 参数。

### 3. 实验设计
*   **数据集**：使用了三个主流 VTG 基准：**Charades-STA**、**QVHighlights** 和 **ActivityNet Captions**。
*   **评估协议**：采用跨域评估（例如在 Charades-STA 上训练，在另外两个数据集上测试 OOD 性能）。
*   **对比方法**：
    *   **零样本 MLLM**：如 HawkEye, TimeSuite, UniTime, VideoMind。
    *   **DETR 类专用模型**：如 EaTR, CG-DETR。
    *   **微调类 MLLM**：如 Chrono-BLIP, Chrono-Qwen。
*   **指标**：R1@0.3/0.5/0.7 和 mIoU。

### 4. 资源与算力
*   **硬件**：使用了 8 张 NVIDIA RTX 3090 或 4090 GPU。
*   **模型规模**：基于 Qwen2.5-VL-3B 和 7B 版本。
*   **参数量**：训练参数极少。3B 模型仅 7.6M（占比 0.25%），7B 模型仅 23.3M（占比 0.33%）。
*   **训练细节**：训练 5 个 epoch，Batch size 为 32，学习率为 5e-5。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的跨域对比实验，涵盖了不同规模的底座模型（3B/7B）。
*   **消融实验**：非常详尽，包括适配器类型对比、SA Loss 权重影响、插入层位置、槽位数量、瓶颈维度、重构方式等 6 组核心消融。
*   **诊断性实验**：设计了**噪声扰动实验**（证明模型是否关注真实视觉内容）、**视觉相似度分析**以及 **MMD（最大均值差异）分析**。
*   **评价**：实验设计科学、客观，通过多种诊断手段从本质上解释了为什么 SlotVTG 能提升泛化性，而非仅仅堆砌指标。

### 6. 主要结论与发现
*   **泛化性显著提升**：在跨域场景下，SlotVTG 相比基准模型在 R1@0.5 指标上提升了约 2.4% 到 4.3%，且在 ID 场景下保持了竞争力。
*   **缩小领域间隙**：通过 MMD 分析发现，以物体为中心的表示将源域和目标域之间的分布差异缩小了约 50%。
*   **克服捷径学习**：噪声扰动实验证明，SlotVTG 在 OOD 数据上依然高度依赖目标时刻的视觉内容，而普通微调模型在 OOD 场景下几乎忽略视觉输入。

### 7. 优点
*   **低成本高收益**：作为适配器，它不需要重训整个视觉-语言对齐流水线，参数量极小。
*   **即插即用**：可以轻松集成到现有的 MLLM 架构中。
*   **可解释性强**：通过槽位可视化直观展示了模型对人、物体和背景的解耦能力。
*   **鲁棒性**：有效地利用了自监督模型的先验知识，解决了小规模 VTG 数据集易过拟合的问题。

### 8. 不足与局限
*   **计算开销**：虽然参数量小，但 Slot Attention 的迭代计算和交叉注意力会引入额外的推理延迟（文中未详细量化）。
*   **超参数敏感性**：SA Loss 的权重 $\lambda$ 对性能有影响，过大的先验引导可能会损害 ID 性能。
*   **场景局限**：虽然在三个数据集上验证了泛化性，但对于极其复杂的长视频或极端多目标场景，固定数量的槽位（如 Ns=4）可能不足以捕捉所有关键实体。

（完）
