---
title: "ADaFuSE: Adaptive Diffusion-generated Image and Text Fusion for Interactive Text-to-Image Retrieval"
title_zh: ADaFuSE：用于交互式文本到图像检索的自适应扩散生成图像与文本融合
authors: "Zhuocheng Zhang, Xingwu Zhang, Kangheng Liang, Guanxuan Li, Richard Mccreadie, Zijun Long"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21886v1"
tags: ["query:vlm"]
score: 9.0
evidence: 利用扩散模型进行交互式文本到图像检索
tldr: 针对交互式文本图像检索（I-TIR）中扩散模型生成的图像与文本融合时存在的噪声干扰问题，本文提出了ADaFuSE。该模型通过轻量级的双分支融合机制（自适应门控和语义感知专家混合），动态平衡模态可靠性并捕捉细粒度跨模态特征。实验证明，ADaFuSE在不修改骨干编码器的情况下，显著提升了检索性能和鲁棒性，成为微调方法的有效替代方案。
selection_source: fresh_fetch
motivation: 现有的I-TIR框架采用简单的嵌入相加进行多模态融合，容易引入扩散模型产生的生成噪声，导致检索性能下降。
method: 提出一种轻量级的双分支融合模型，结合自适应门控分支动态平衡模态权重，以及语义感知专家混合分支捕捉跨模态细节。
result: "在四个标准基准测试中达到SOTA性能，Hits@10提升高达3.49%，且对噪声和长查询具有更强的鲁棒性。"
conclusion: 生成式增强结合合理的融合机制，为交互式检索提供了一种简单且泛化性强的微调替代方案。
---

## 摘要
交互式文本到图像检索（I-TIR）的最新进展利用扩散模型来弥合文本信息需求与待搜索图像之间的模态差距，从而提高了检索效率。然而，现有框架通常通过简单的嵌入相加来融合用户反馈的多模态视图。在这项工作中，我们证明了这种静态且无差别的融合会不加区分地引入扩散模型产生的生成噪声，导致多达 55.62% 的样本出现性能下降。我们进一步提出了 ADaFuSE（具有语义感知专家的自适应扩散-文本融合），这是一种轻量级融合模型，旨在为扩散增强的 I-TIR 对齐并校准多模态视图，且无需修改骨干编码器即可插入现有框架。具体而言，我们引入了一种双分支融合机制，利用自适应门控分支动态平衡模态可靠性，并结合语义感知混合专家分支来捕捉细粒度的跨模态细微差别。通过在四个标准 I-TIR 基准上的全面评估，ADaFuSE 达到了最先进的性能，在仅增加 5.29% 参数的情况下，Hits@10 指标比 DAR 高出多达 3.49%，同时对噪声和较长的交互式查询表现出更强的鲁棒性。这些结果表明，生成式增强结合原则性融合，为交互式检索提供了一种简单且具有泛化性的微调替代方案。

## Abstract
Recent advances in interactive text-to-image retrieval (I-TIR) use diffusion models to bridge the modality gap between the textual information need and the images to be searched, resulting in increased effectiveness. However, existing frameworks fuse multi-modal views of user feedback by simple embedding addition. In this work, we show that this static and undifferentiated fusion indiscriminately incorporates generative noise produced by the diffusion model, leading to performance degradation for up to 55.62% samples. We further propose ADaFuSE (Adaptive Diffusion-Text Fusion with Semantic-aware Experts), a lightweight fusion model designed to align and calibrate multi-modal views for diffusion-augmented I-TIR, which can be plugged into existing frameworks without modifying the backbone encoder. Specifically, we introduce a dual-branch fusion mechanism that employs an adaptive gating branch to dynamically balance modality reliability, alongside a semantic-aware mixture-of-experts branch to capture fine-grained cross-modal nuances. Via thorough evaluation over four standard I-TIR benchmarks, ADaFuSE achieves state-of-the-art performance, surpassing DAR by up to 3.49% in Hits@10 with only a 5.29% parameter increase, while exhibiting stronger robustness to noisy and longer interactive queries. These results show that generative augmentation coupled with principled fusion provides a simple, generalizable alternative to fine-tuning for interactive retrieval.

---

## 论文详细总结（自动生成）

这篇论文提出了 **ADaFuSE**，旨在解决交互式文本到图像检索（I-TIR）中，利用扩散模型生成的增强图像与原始文本查询在融合时存在的噪声干扰问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：在交互式检索中，用户通过多轮对话反馈来寻找目标图像。最近的研究（如 DAR）利用扩散模型根据对话上下文生成“视觉代理”图像，以弥合文本与图像之间的模态差距。
*   **核心问题**：现有的扩散增强方法通常采用**静态加权求和**（Static Additive Fusion）来融合文本和生成图像的嵌入。
*   **研究动机**：作者发现这种静态融合存在两大缺陷：
    1.  **忽略了实例差异**：不同查询对视觉/文本信息的依赖程度应是动态的。
    2.  **引入生成噪声**：扩散模型具有随机性，生成的图像可能包含与用户意图不符的“幻觉”噪声。实验显示，静态融合会导致超过 50% 的样本出现性能下降（降级率高）。

### 2. 论文提出的方法论：ADaFuSE
ADaFuSE 是一个轻量级的融合模块，核心思想是**动态校准多模态表示**，其架构包含两个协调分支：
*   **查询编码与投影**：使用预训练的 BLIP 编码器提取文本和生成图像的特征，随后通过非线性投影头将其映射到更高维的任务特定潜空间，以恢复被压缩的细粒度语义。
*   **自适应门控分支（Adaptive Gating Branch）**：
    *   输入文本和图像的联合上下文。
    *   通过 MLP 和 Sigmoid 函数预测一个动态权重 $\lambda$，用于平衡文本特征和生成图像特征的比例。
    *   **作用**：当生成图像质量低或与文本冲突时，自动降低图像权重，增强系统的鲁棒性。
*   **语义感知混合专家分支（MoE Branch）**：
    *   引入 $K$ 个轻量级专家网络和一个路由网络。
    *   路由网络根据当前上下文决定各专家的权重，生成补偿性特征（Residual Term）。
    *   **作用**：捕捉线性加权无法表达的高阶跨模态细微差别（如“款式相同但袖子更短”这种复杂的组合语义）。
*   **即插即用**：该模型作为现有 I-TIR 流程的中间件，无需微调骨干编码器（如 CLIP/BLIP）。

### 3. 实验设计
*   **数据集**：
    *   **VisDial**：标准视觉对话数据集（验证集）。
    *   **ChatIR 衍生基准**：包括 ChatGPT_BLIP2、Human_BLIP2 和 Flan-Alpaca-XXL_BLIP2，涵盖了从机器生成到人类真实对话的不同分布。
*   **Benchmark 与对比方法**：
    *   **ChatIR**：仅使用文本查询的强基线。
    *   **DAR**：目前最先进的扩散增强 I-TIR 方法（采用静态融合）。
*   **评价指标**：Hits@10（前10名检索结果中包含目标图像的比例）。

### 4. 资源与算力
*   **参数量**：论文提到 ADaFuSE 非常轻量，仅比基线模型（DAR）增加了 **5.29%** 的参数。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长。但由于其不微调骨干网络且仅包含简单的投影层和 MoE 模块，推测其训练开销较低。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个标准基准上进行了测试，涵盖了多轮交互（0-10轮）的性能演变。
*   **分析深度**：
    *   进行了**降级率分析**，量化了模型抑制噪声的能力。
    *   进行了**权重可视化实验**，证明了门控机制确实能根据语义一致性调整模态权重。
*   **充分性评价**：实验设计较为充分，不仅对比了 SOTA 方法，还通过跨数据集验证了模型的泛化能力。对比实验在相同的骨干网络（BLIP）下进行，保证了公平性。

### 6. 主要结论与发现
*   **性能提升**：ADaFuSE 在所有基准和所有交互轮次中均优于 DAR，Hits@10 提升最高达 **3.49%**。
*   **鲁棒性增强**：相比 DAR 超过 50% 的性能降级率，ADaFuSE 显著降低了降级率，并将误导性图像导致的平均排名下降从 7500 位降低到了约 20 位。
*   **长查询优势**：在对话轮次增加、文本信息变得冗长且可能存在冲突时，ADaFuSE 的领先优势更加明显。

### 7. 优点
*   **针对性强**：精准识别并解决了扩散增强检索中的“生成噪声”痛点。
*   **架构优雅**：双分支设计兼顾了宏观的模态平衡（门控）和微观的语义补偿（MoE）。
*   **高效易用**：轻量级、即插即用，无需昂贵的骨干网络微调，适合实际部署。

### 8. 不足与局限
*   **依赖生成质量**：虽然能抑制噪声，但如果扩散模型生成的图像质量极差，该模块提供的增益也会受限。
*   **专家数超参数**：MoE 中专家数量 $K$ 的选择以及路由策略的复杂性可能需要针对不同领域进行调优。
*   **硬件细节缺失**：缺乏具体的训练环境和耗时数据，不便于复现者评估计算成本。

（完）
