---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：一种用于世界与动作建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:world-model"]
score: 9.0
evidence: 用于规划和动作的世界模型
tldr: 现有机器人策略常依赖大型预训练视觉语言模型作为动作骨干，参数量大且效率低。本文提出WorldDiT，一种统一的扩散变换器架构，无需大型VLM即可同时生成连续动作块并预测未来相机帧的RGB补丁。在四个LIBERO模拟套件上，WorldDiT以sub-billion参数达到总参数与平均成功率的帕累托前沿。该工作为未来缩放研究提供了强大的基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有机器人策略依赖大型VLM动作骨干，参数量大且效率低，需要更轻量高效的统一架构。
method: 提出WorldDiT扩散变换器，训练时同时生成连续动作块和预测未来相机帧的归一化RGB补丁目标。
result: 在LIBERO四个套件上，WorldDiT位于报告所有套件的方法的参数量与成功率帕累托前沿。
conclusion: WorldDiT作为sub-billion参数基线，为未来机器人策略的缩放研究提供了有力支撑。
---

## 摘要
许多最近的机器人策略通过使用大型预训练视觉-语言模型（VLM）作为动作骨干来追求更强的控制力。我们提出了WorldDiT，这是一种统一的扩散变换器架构，它将动作生成与视觉世界建模相结合，无需大型预训练VLM动作骨干即可实现强性能。在训练过程中，单个扩散变换器生成连续的动作块，并预测来自未来相机帧的归一化RGB块目标。在四个LIBERO模拟套件中，WorldDiT处于报告的总模型参数与平均成功率之间的帕累托前沿（针对报告所有四个套件的方法）。这些结果为未来的扩展研究提供了一个强大的亚十亿参数基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# 论文《WorldDiT: A Unified Diffusion Architecture for World and Action Modeling》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前许多机器人策略（robot policies）倾向于依赖大型预训练视觉-语言模型（VLM）作为动作骨干（action backbone），这种做法虽然带来了强大的感知和语言理解能力，但使得模型规模与架构贡献难以分离。当策略包含数十亿参数时，无法判断强控制性能是来自动作设计、预训练骨干还是两者的组合。
- **核心问题**：能否在不使用大型预训练VLM作为动作骨干的前提下，通过一种统一的扩散变换器架构，将连续动作生成与辅助的未来视觉预测目标结合，从而保持强大的控制性能？
- **整体含义**：论文旨在探索一种更轻量、更紧凑的机器人策略范式，为未来的扩展研究（scaling studies）提供亚十亿参数（sub-billion）的强基线，并推动世界模型与动作模型的统一建模。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出WorldDiT，一种统一的扩散变换器（Diffusion Transformer, DiT）架构。训练时，共享的DiT骨干同时学习生成连续动作块和预测未来相机帧的归一化RGB补丁（normalized RGB patch）。推理时仅使用动作路径，移除RGB补丁预测模块，实现动作生成与部署的分离。
- **关键技术细节**：
  - **多模态编码**：使用冻结的MAE图像编码器、CLIP文本编码器、可训练的机器人状态编码器，通过一个共享的Perceiver Resampler将视觉和语言特征转换为条件令牌。
  - **输入窗口**：一个10步的窗口包含3步观测上下文（C=3）、7步动作目标（H=7）和1个未来归一化RGB补丁目标（来自第i+H帧的主相机和腕相机）。
  - **RGB补丁目标**：将224×224的CLIP预处理帧划分为16×16的补丁，每个补丁向量为768维，归一化后，每张相机保留64个均匀间隔的补丁，两个相机共128个目标令牌。
  - **训练目标**：基于流匹配（Flow Matching）损失。对每个动作目标或RGB补丁目标，定义噪声路径 x_τ = (1-τ)ε + τ y，损失为预测速度与真实速度的L2误差。总损失为动作损失与RGB补丁损失的加权和：L_total = w_action * L_action_flow + w_rgb * L_rgb_flow，其中w_action=0.1, w_rgb=0.001。
  - **推理流程**：从高斯噪声初始化7步动作令牌，通过20步欧拉积分（τ=0→1）生成动作块。执行前3个动作后，更新观测窗口并重新规划，同时使用时间集成（temporal ensembling）处理重叠动作。
  - **骨干网络**：深度4，隐藏层维度1024，16个注意力头，4个寄存器令牌。

## 3. 实验设计：使用的数据集、Benchmark、对比方法

- **数据集与Benchmark**：
  - 使用LIBERO操作基准测试（LIBERO manipulation benchmark），包含四个下游套件：LIBERO Spatial、LIBERO Object、LIBERO Goal、LIBERO Long（即liber_10）。还有一个大规模多任务集liber_90用于预训练。
  - 评估指标：每个套件的任务成功率（%）以及四个套件的平均成功率。
- **对比方法**：
  - 分为两类：使用大型预训练VLM动作骨干的方法（如ACoT VLA、MMaDA VLA、VLANeXt、VLA Adapter、OpenVLA OFT、π0.5、MemoryVLA、Discrete Diffusion VLA、VLA 0、π0、GR00T N1、UD VLA、π0 FAST、CoT VLA、WorldVLA、SpatialVLA、OpenVLA等）。
  - 不使用大型预训练VLM动作骨干的方法（如DreamVLA、FlowVLA、DiT Policy、Octo、Diffusion Policy、Seer等）。
  - WorldDiT被归入“不使用大型预训练VLM动作骨干”类别。

## 4. 资源与算力

- **训练资源**：
  - 预训练：在8张RTX Pro 6000 GPU上，使用per-GPU批大小40，梯度累积2，有效批大小80，学习率1e-4（余弦调度），单次预训练30 epochs。
  - 微调：每个下游套件独立微调，使用8张GPU，per-GPU批大小32，梯度累积2，有效批大小512，学习率1e-4，采用bf16混合精度。
- **总参数量**：WorldDiT总共399.084百万参数（包括推理时所需的冻结模块），可训练参数为135.107百万。
- **说明**：论文未明确给出总训练时长（小时或天数），但训练在一个节点上完成，算力相对有限。

## 5. 实验数量与充分性

- **实验数量**：
  - 在四个LIBERO套件上各评估500个模拟回合（共2000个回合），其中每个套件包含300个回合用于阶段性检查点选择（非完全独立测试集）。
  - 对比了24种方法（包含WorldDiT自身），但未进行消融实验（如移除RGB补丁预测目标、改变损失权重等）。
- **充分性与公平性**：
  - 实验覆盖了四个难度不同的套件，但仅测试了单一WorldDiT配置（固定C=3, H=7, 深度4等）。
  - 由于其他方法的权重和代码未完全公开，论文使用其他报告的结果进行比较，可能存在评估协议不一致的风险（例如不同方法的测试回合数、随机种子等未统一）。WorldDiT的得分包含部分检查点选择数据，因此不是完全无偏的测试估计。
  - 未在多机器人平台或真实世界场景中验证，仅限于LIBERO模拟器。

## 6. 论文的主要结论与发现

- WorldDiT以399M总参数在四个LIBERO套件上取得平均94.9%的成功率（Spatial 98.0%、Object 97.0%、Goal 92.8%、Long 91.8%），处于报告的总参数–平均成功率帕累托前沿。
- 所有报告平均成功率的、且成功率高于WorldDiT的方法，参数量均更大（如ACoT VLA 98.5%但参数量更大）；所有参数量小于或等于WorldDiT的方法，成功率均更低。
- 这表明：在sub-billion参数规模下，统一世界-动作建模可以取得与大型VLM骨干相当甚至更优的基准性能，为未来缩放研究提供了紧凑基线。

## 7. 优点：方法或实验设计上的亮点

- **架构创新**：提出统一的扩散变换器，同时建模动作和未来视觉，推理时仅需动作路径，实现了高效的轻量级策略。
- **无大型VLM依赖**：避免了依赖数十亿参数的大规模预训练VLM，降低了计算和部署成本。
- **训练效率**：使用流匹配损失，训练稳定；辅助视觉预测任务可能提供额外的监督信号，提升动作生成质量。
- **参数效率**：在亚十亿参数规模下达到SOTA级别的性能，展示了紧凑模型的潜力。
- **结果清晰**：提供了与24种方法的详细对比，并绘制了参数-成功率帕累托曲线，便于理解模型规模与性能的权衡。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 仅在LIBERO模拟器上评估，未涉及真实机器人、多样化的物体或动态环境。
  - 仅测试一种模型配置（深度4，隐藏层1024等），缺乏消融实验（如改变窗口长度、损失权重、补丁数量等）。
  - 未与其他不使用大型VLM的方法（如DreamVLA等）进行严格的统一协议对比。
- **偏差风险**：
  - 使用的评估数据包含部分检查点选择数据（300/500），可能高估真实性能。
  - 对比方法的结果来自不同论文，可能因固定种子、回合数、评估协议差异而导致不公平比较。
- **应用限制**：
  - 模型针对特定任务（LIBERO桌面操作），泛化性未验证。
  - 推理时使用20步流积分，实时性未讨论（可能无法满足高频控制需求）。
  - 辅助视觉预测目标仅在训练时使用，其具体贡献（如是否改善了动作质量）未通过消融量化。
- **其他限制**：
  - 论文未提供代码或模型权重，限制了可复现性。
  - 未讨论不同数据规模、不同任务组合下的行为。

（完）
