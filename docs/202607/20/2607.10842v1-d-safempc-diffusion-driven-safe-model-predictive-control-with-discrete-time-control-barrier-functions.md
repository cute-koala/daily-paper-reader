---
title: "D-SafeMPC: Diffusion-Driven Safe Model Predictive Control with Discrete-Time Control Barrier Functions"
title_zh: D-SafeMPC：基于扩散驱动的安全模型预测控制与离散时间控制障碍函数
authors: "Erdi Sayar, Ersin Daş, Joel W. Burdick, Alois Knoll, Erdal Kayacan"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10842v1"
tags: ["query:world-model"]
score: 9.0
evidence: 扩散模型作为世界模型用于MPC规划
tldr: 扩散模型在机器人规划中难以保证安全与动力学约束，现有混合方法因轨迹初始化不佳导致MPC不稳定。D-SafeMPC利用控制障碍函数和Lyapunov函数引导反向扩散过程，并通过迭代投影在去噪各步用MPC精化轨迹。在Franka机械臂四个仿真场景和实物实验中，该方法显著提升安全性、任务成功率和规划效率，优于现有基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1779, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10842v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10842v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 804, \"height\": 957, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10842v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1830, \"height\": 490, \"label\": \"Table\"}]"
motivation: 扩散模型在机器人规划中无法强制满足安全与动力学约束，导致输出不可行或危险。
method: D-SafeMPC在反向扩散中引入控制障碍函数与Lyapunov函数引导采样，并联合迭代投影MPC逐去噪步精化轨迹。
result: 在Franka机械臂的静态与动态障碍物场景中，D-SafeMPC在安全性、任务成功率和效率上超过基线。
conclusion: D-SafeMPC有效融合扩散模型与MPC，为安全关键机器人控制提供稳定可行的规划方案。
---

## 摘要
扩散模型在机器人规划中的一个关键限制是它们无法固有地强制执行安全性或动力学约束，这常常导致物理上不可行或不安全的输出。采用模型预测控制（MPC）来解决这个问题的混合方法可能不稳定，因为扩散模型的不良轨迹初始化阻止MPC收敛到安全且可行的解。为了克服这些挑战，我们提出了D-SafeMPC，它增强了扩散与控制之间的交互。我们的方法使用控制障碍函数（CBFs）和控制李雅普诺夫函数（CLFs）来引导反向扩散过程，并采用迭代投影方案，其中MPC在每个去噪步骤中细化轨迹。这引导采样朝向安全、目标导向的区域，并提供可靠的MPC热启动。在Franka机械臂的四个场景（一个静态障碍物和三个动态障碍物设置）上的仿真以及在真实Franka机器人上的sim-to-real实验中，D-SafeMPC在安全性、任务成功率和规划效率上优于最先进的基线。为了促进可重复性，我们的源代码和实验配置位于https://github.com/erdiphd/D-SafeMPC

## Abstract
A key limitation on the use of diffusion models in robotic planning is their inability to inherently enforce safety or dynamical constraints, which often results in physically infeasible or unsafe outputs. Hybrid approaches that employ model predictive control (MPC) to address this problem can be unstable, as poor trajectory initializations from the diffusion model prevent the MPC from converging to a safe and feasible solution. To overcome these challenges, we propose D-SafeMPC, which enhances the interaction between diffusion and control. Our method guides the reverse diffusion process with control barrier functions (CBFs) and control Lyapunov functions (CLFs) and employs an iterative-projection scheme where an MPC refines the trajectory at each denoising step. This steers sampling toward safe, goal-directed regions and provides reliable MPC warm starts. In simulations on a Franka manipulator across four scenarios (one static-obstacle and three dynamic-obstacle settings) and in a sim-to-real experiment on a physical Franka robot, D-SafeMPC improves safety, task success rates, and planning efficiency over state-of-the-art baselines. To facilitate reproducibility, our source code and experimental configurations are available in a repository at https://github.com/erdiphd/D-SafeMPC

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

扩散模型在机器人轨迹规划中表现出色，能够从演示中学习复杂的多模态行为并生成高质量的轨迹。然而，其**核心缺陷**在于缺乏内在机制来强制执行显式的约束，如系统动力学、障碍物避碰和安全裕度。这导致生成的轨迹经常违反物理约束，无法保证**硬安全**。

为了解决这一问题，现有混合方法（如 DPCC）尝试用模型预测控制（MPC）对扩散生成的轨迹进行后验证或投影，使其满足安全约束。但这类方法的**关键瓶颈**是：如果扩散模型初始生成的轨迹质量很差（例如极度不安全或动力学上不可行），MPC 可能在其有限的计算预算内**无法收敛**到安全、可行的解，从而导致规划失败或系统不稳定。

因此，本文的核心动机是**解决安全约束与扩散生成的矛盾**，提出一个既利用扩散模型的生成多样性，又能保证轨迹严格满足安全和动力学约束的鲁棒框架。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：D-SafeMPC 通过结合**软指导（Soft Guidance）**和**硬投影（Hard Projection）**，在扩散过程的每一步迭代地生成安全、可行的轨迹。具体来说，在反向扩散的每一步使用**离散时间控制障碍函数（CBF）**和**控制李雅普诺夫函数（CLF）**的梯度来引导采样，使其偏向安全、目标导向的区域（软指导）；随后，由**模型预测控制（MPC）**将该引导后的轨迹投影到严格的安全和动力学可行集上（硬投影）。这个迭代过程持续进行，直到最后一步。

**关键技术细节**：

1.  **CBF/CLF 梯度指导（Soft Guidance）**：
    -   通过贝叶斯公式，将安全集的条件分布分解为学习到的去噪模型 $p(\tilde{\tau}_{i-1}|\tilde{\tau}_i, i, y)$ 和安全分布 $p(C|\tilde{\tau}_{i-1})$。
    -   利用一阶泰勒展开，将安全分布的对数概率近似为梯度项，进而将条件分布近似为一个均值为偏移的高斯分布。
    -   **核心公式**（式 (27)）：引导后的均值 $\hat{\mu}$ 由原始均值 $\mu_\theta$ 加上 CBF 的梯度 $\nabla_u \psi_h$ 和减去 CLF 的梯度 $\nabla_u \Phi_V$ 构成。
    -   **作用**：CBF 梯度将轨迹“推离”障碍物，CLF 梯度将轨迹“拉向”目标，从而在概率上生成更安全的轨迹，为后续 MPC 提供高质量的热启动。

2.  **MPC 硬约束投影（Hard Projection）**：
    -   将经过指导和重采样后的轨迹 $\tilde{\tau}_{i-1}$ 作为初始化（热启动）和目标（在代价函数中鼓励靠近该轨迹）输入给 MPC。
    -   MPC 求解一个有限时域优化问题（式 (28)），其约束包括：严格的系统动力学、**离散时间 CBF 约束**（确保安全性）和状态/控制输入边界。
    -   **作用**：这个步骤强制执行了硬约束，将软指导得到的“概率上安全”的轨迹转化为“**严格可行**”的轨迹。

3.  **算法流程（Algorithm 1）**：
    -   **初始化**：从高斯噪声 $\tau_N$ 开始。
    -   **循环（i = N 到 1）**：
        -   **预测**：计算去噪均值 $\mu_\theta$。
        -   **指导**：添加 CBF/CLF 梯度，得到引导后均值 $\hat{\mu}$。
        -   **采样**：从调整后的分布中采样获得 $\tilde{\tau}_{i-1}$。
        -   **投影**：如果 $i \leq N/2$（即经过初始噪声阶段），则调用 MPC-Project 函数，将 $\tilde{\tau}_{i-1}$ 投影到可行集，得到 $\tau_{i-1}$。
        -   **反馈**：将投影后的 $\tau_{i-1}$ 作为下一轮去噪的输入。

### 3. 实验设计：使用的数据集、场景、基准及对比方法

-   **数据集**：使用 **D3IL** 数据集中的 Franka Emika Panda 7-DoF 机械臂演示轨迹。该数据集包含在无障碍物环境中的多种成功演示。
-   **实验场景**：共 **4 个**仿真场景（1 个静态障碍物 + 3 个动态障碍物环境）和 1 个 sim-to-real 实验。障碍物运动被建模为遵循圆形轨迹（动态情形）或固定点（静态情形）的 PD 控制点质量。
-   **基准（Benchmarks）**：与 **13 个**基线方法进行对比，涵盖不同设计理念：
    -   **纯扩散模型**：`Diffuser`
    -   **仅有软指导**：`CoBL-Diffusion` (CoBL), `Guidance`
    -   **扩散+MPC**：`DPCC` 的多种变体（DPCC-C, DPCC-R, DPCC-T 及其 Tightened 版本）
    -   **后处理**：`Post-Processing`（生成后再用 MPC 修正）
    -   **无模型**：`Model-Free`
-   **评价指标**：
    -   **目标到达率（Goal Reached Rate）**：成功到达目标且无碰撞的概率。
    -   **安全合规目标到达率（Safety-Compliant Goal Reached Rate）**：在上述基础上，还要求全程满足所有安全约束。

### 4. 资源与算力

**文中没有明确说明**所使用的 GPU 型号、数量、训练时长或推理时间等的具体信息。只提供了一个 **图 3** 展示了各个规划器在静态环境下生成一个控制动作所需的**平均时间（秒）**，用于比较计算效率。D-SafeMPC 的平均计算时间优于其他 MPC 类方法。

### 5. 实验数量与充分性

-   **数量**：每个规划器在 **10 个随机种子**下进行测试，每个种子有 **2 个变体**，共计 **20 次**评估。表格 I 报告了平均值和标准差，提供了统计显著性。
-   **充分性**：
    -   **场景覆盖**：涵盖了静态、动态（3种不同配置）障碍物场景，并加入了 sim-to-real 验证，具有一定的全面性。
    -   **对比公平性**：与多种类型（纯扩散、软指导、硬约束）的基线方法进行了广泛对比。
    -   **潜在不足**：动态障碍物仅限于**圆形轨迹**，未涉及更复杂、不可预测的障碍物行为（如行人、其他机器人）。缺少关于噪声、传感器不确定性等鲁棒性测试。没有专门的消融实验来独立分析 CBF 指导、CLF 指导和 MPC 投影各自的效果。

### 6. 论文的主要结论与发现

1.  **D-SafeMPC 在动态环境中表现最佳**：在所有三个动态障碍物场景中，D-SafeMPC 的**安全合规目标到达率**和**目标到达率**均达到或接近最高值，并且标准差较小，证明其鲁棒性强。
2.  **在静态环境中具有竞争力**：在静态场景中，虽然 DPCC-C 指标略高，但 D-SafeMPC 的表现依然很强，成功率和安全成功率均达到 80%。
3.  **计算效率优于其他 MPC 方法**：相比 DPCC 变体和 Post-Processing，D-SafeMPC 的计算时间显著更低（图 3），归功于 CBF/CLF 指导为 MPC 提供了更好的初始化，使其收敛更快。
4.  **CBF/CLF 指导是核心**：有效解决了单纯依赖 MPC 投影方法（如 DPCC）在不良初始轨迹下可能失败的问题，是提升系统稳定性和效率的关键。

### 7. 优点：方法或实验设计上的亮点

-   **软硬结合的创新思路**：D-SafeMPC 巧妙地结合了扩散模型的柔性和 CBF/CLF 的软指导与 MPC 的硬约束，从概率引导和确定性修正两个层面保障安全，设计逻辑严谨。
-   **解决稳定性问题**：通过 CBF/CLF 提供高质量热启动，有效解决了现有扩散+MPC 范式在面对劣质初始轨迹时的收敛失败问题，提升了混合框架的鲁棒性。
-   **无需重新训练**：CBF/CLF 梯度指导在推理时在线进行，无需修改或重新训练扩散模型，使其能灵活适应不同的测试环境。
-   **Sim-to-Real 验证**：进行了实物机器人实验，证明了该方法在真实世界中的有效性，增加了工作的可信度和应用价值。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

-   **可扩展性问题**：如作者在结论中所提，该方法可能难以扩展到**高维状态空间**（如视觉输入），因为 MPC 的优化计算量会变得非常昂贵。
-   **误差递归传播**：早期时间步的定位或模型不确定会递归传播。由于扩散模型每一步都基于当前状态生成轨迹，前序步骤的微小误差可能在后续去噪过程中被放大，导致策略不稳定。
-   **依赖障碍物预测**：该方法假设障碍物的未来轨迹在规划窗口内是已知的。对于高度随机或不可预测的动态障碍物，该方法可能失效。
-   **实验场景局限性**：动态障碍物仅模拟了简单的圆周运动，未测试更复杂的人类或车辆交互场景。评估指标也仅限于任务成功和碰撞，未涉及机器人能耗、轨迹平滑度等。
-   **缺乏消融分析**：论文虽然在表格中对比了多个变体，但没有专门设计实验来**量化**CBF指导和CLF指导的独立贡献，以及MPC投影的边际价值。

（完）
