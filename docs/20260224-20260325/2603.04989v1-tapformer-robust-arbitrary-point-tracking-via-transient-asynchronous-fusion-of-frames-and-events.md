---
title: "TAPFormer: Robust Arbitrary Point Tracking via Transient Asynchronous Fusion of Frames and Events"
title_zh: TAPFormer：通过帧与事件的瞬态异步融合实现鲁棒的任意点追踪
authors: "Jiaxiong Liu, Zhen Tan, Jinpu Zhang, Yi Zhou, Hui Shen, Xieyuanli Chen, Dewen Hu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04989v1"
tags: ["query:vot"]
score: 9.0
evidence: 基于Transformer的任意点跟踪
tldr: 针对任意点追踪（TAP）中RGB帧与事件流融合存在的时空对齐及鲁棒性问题，本文提出TAPFormer。该框架利用Transformer进行异步时序一致性融合，通过瞬态异步融合（TAF）机制衔接低频帧与高频事件，并结合跨模态局部加权融合（CLWF）提升在极端光照和模糊下的稳定性。实验表明，该方法在多个基准测试中达到SOTA，显著降低了平均像素误差。
selection_source: fresh_fetch
motivation: 现有的帧-事件融合追踪方法多依赖同步或非自适应融合，导致在模态失效或低频采样时出现时空错位和性能下降。
method: 提出一种基于Transformer的框架，包含用于建模帧间连续演化的瞬态异步融合机制（TAF）和根据模态可靠性自适应调整空间注意力的局部加权融合模块（CLWF）。
result: "在真实世界数据集上，该方法将平均像素误差降低了28.2%，并在标准点追踪基准测试中持续保持领先性能。"
conclusion: TAPFormer通过有效的异步多模态融合，显著提升了任意点追踪在复杂动态环境下的鲁棒性和精确度。
---

## 摘要
追踪任意点（TAP）是计算机视觉中一项基础且具有挑战性的任务，需要高精度和长期的运动推理。最近结合 RGB 帧和事件流的尝试已显示出潜力，但它们通常依赖于同步或非自适应融合，导致时间错位，且在单一模态失效时性能严重下降。我们提出了 TAPFormer，这是一个基于 Transformer 的框架，通过对帧和事件进行异步时间一致性融合，实现鲁棒且高频的任意点追踪。我们的核心创新是瞬态异步融合（TAF）机制，该机制通过连续的事件更新显式建模离散帧之间的时间演化，弥合了低帧率图像与高频率事件之间的差距。此外，跨模态局部加权融合（CLWF）模块根据模态可靠性自适应调整空间注意力，即使在模糊或低光照条件下也能产生稳定且具有辨别力的特征。为了在现实条件下评估我们的方法，我们构建了一个在多种光照和运动条件下采集的新型真实世界帧-事件 TAP 数据集。我们的方法优于现有的点追踪器，在阈值内平均像素误差方面实现了 28.2% 的提升。此外，在标准点追踪基准测试中，我们的追踪器始终保持最佳性能。项目网站：tapformer.github.io

## Abstract
Tracking any point (TAP) is a fundamental yet challenging task in computer vision, requiring high precision and long-term motion reasoning. Recent attempts to combine RGB frames and event streams have shown promise, yet they typically rely on synchronous or non-adaptive fusion, leading to temporal misalignment and severe degradation when one modality fails. We introduce TAPFormer, a transformer-based framework that performs asynchronous temporal-consistent fusion of frames and events for robust and high-frequency arbitrary point tracking. Our key innovation is a Transient Asynchronous Fusion (TAF) mechanism, which explicitly models the temporal evolution between discrete frames through continuous event updates, bridging the gap between low-rate frames and high-rate events. In addition, a Cross-modal Locally Weighted Fusion (CLWF) module adaptively adjusts spatial attention according to modality reliability, yielding stable and discriminative features even under blur or low light. To evaluate our approach under realistic conditions, we construct a novel real-world frame-event TAP dataset under diverse illumination and motion conditions. Our method outperforms existing point trackers, achieving a 28.2% improvement in average pixel error within threshold. Moreover, on standard point tracking benchmarks, our tracker consistently achieves the best performance. Project website: tapformer.github.io

---

## 论文详细总结（自动生成）

### TAPFormer：通过帧与事件的瞬态异步融合实现鲁棒的任意点追踪

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对的是**任意点追踪（Tracking Any Point, TAP）**任务，即在视频序列中长效、精准地追踪任意像素点的轨迹。
*   **研究动机**：传统的基于帧（RGB）的方法在高速运动（导致模糊）、极端光照（过曝或低光）或低帧率下容易失效；而纯事件相机（Event Camera）虽然具有高动态范围和微秒级响应，但缺乏纹理信息，在静止或慢速运动下信号稀疏。
*   **核心挑战**：如何有效融合低频、高纹理的 RGB 帧与高频、异步的事件流，并解决两者之间的**频率不匹配**和**时间错位**问题。

#### 2. 论文提出的方法论
TAPFormer 采用基于 Transformer 的架构，核心思想是将场景建模为连续演化的潜在表示。
*   **瞬态异步融合（Transient Asynchronous Fusion, TAF）**：
    *   不再强行将事件下采样以匹配帧率，而是以事件频率为基准。
    *   当图像帧到达时，初始化融合状态；在两帧之间，利用轻量级的交叉注意力（Cross-Attention）更新器，根据新到达的事件流持续更新特征表示。
*   **跨模态局部加权融合（Cross-modal Locally Weighted Fusion, CLWF）**：
    *   引入自适应局部注意力机制，让事件 Token 在空间邻域内查询图像 Token。
    *   根据模态的可靠性（如图像模糊时降低其权重）自动调整注意力分配，生成空间相干且具有辨别力的特征。
*   **迭代轨迹优化**：
    *   利用多尺度融合特征金字塔，通过 Transformer 优化器迭代预测点的坐标和可见性（遮挡状态）。得益于特征的稳定性，该方法仅需 3 次迭代即可收敛（优于 CoTracker3 的 6 次）。

#### 3. 实验设计
*   **数据集**：
    *   **FE-FastKub（合成）**：基于 Kubric 引擎生成，包含 10,953 个样本，提供 48Hz 的高频轨迹真值和模拟事件。
    *   **InivTAP & DrivTAP（真实）**：论文贡献了首个真实世界帧-事件 TAP 基准。包含 13 个复杂序列（如夜间驾驶、高速运动、过曝环境），共 20,450 个手动标注点。
    *   **标准基准**：在 EDS 和 EC 数据集上进行了特征点追踪评估。
*   **对比方法**：
    *   **帧基方法**：PIPs++、CoTracker3、Chrono。
    *   **事件基方法**：ETAP、MATE、E2Vid 转换后接追踪器。
    *   **融合方法**：FETAP、DeepEvT、EKLT。
*   **评价指标**：平均 Jaccard (AJ)、遮挡准确率 (OA)、像素误差阈值内的可见点时长 ($\delta_{vis}^{avg}$)、特征寿命 (FA/EFA)。

#### 4. 资源与算力
*   **训练环境**：使用了 **4 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：采用 AdamW 优化器，初始学习率为 $5 \times 10^{-4}$，每个样本包含 96 个时间步。
*   **推理性能**：在 RTX 3090 笔记本 GPU 上进行测试，推理速度与主流追踪器相当（约 20ms/step），甚至优于部分单模态基线。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据到真实复杂场景的全面测试。
*   **消融实验**：非常充分，分别验证了 TAF 机制、CLWF 模块、多尺度特征、时间注意力模块以及训练数据集对性能的影响。
*   **鲁棒性分析**：专门设计了**帧率敏感性实验**，证明在图像帧率从 75FPS 降至 9FPS 时，该方法性能仅下降 6.5%，而纯帧方法（CoTracker3）下降了 75.3%。
*   **客观性**：通过 PCA 特征可视化展示了融合特征在类内紧凑性和类间区分度上的优势，实验设计科学、对比公平。

#### 6. 论文的主要结论与发现
*   **性能领先**：在真实世界数据集上，TAPFormer 的平均像素误差改善了 28.2%，AJ 指标显著超过现有 SOTA。
*   **异步融合的优越性**：显式建模帧间的时间演化比简单的同步堆叠更能捕捉高速动态。
*   **模态互补**：实验证明，事件流在图像模糊时提供了关键的运动补偿，而图像在事件稀疏（慢速运动）时提供了必要的纹理支撑。

#### 7. 优点
*   **创新机制**：TAF 机制有效解决了异步传感器之间的时间对齐难题，实现了超越帧率的高频追踪。
*   **数据集贡献**：填补了真实世界多模态 TAP 数据集的空白，对后续研究具有重要价值。
*   **高鲁棒性**：在极端光照和低帧率下的表现极具竞争力，具有很强的工业应用潜力（如自动驾驶、无人机）。

#### 8. 不足与局限
*   **硬件依赖**：该方法依赖于同步采集的帧和事件数据，对于普通相机用户无法直接使用。
*   **计算开销**：虽然迭代次数减少，但引入事件处理和跨模态注意力机制增加了单次迭代的计算复杂度。
*   **泛化风险**：虽然在多个数据集表现良好，但在完全未见的极端噪声环境（如极高噪声的事件流）下的表现仍需进一步验证。

（完）
