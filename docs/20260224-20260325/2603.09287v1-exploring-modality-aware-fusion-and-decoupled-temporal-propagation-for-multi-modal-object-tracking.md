---
title: Exploring Modality-Aware Fusion and Decoupled Temporal Propagation for Multi-Modal Object Tracking
title_zh: 探索多模态目标跟踪中的模态感知融合与解耦时序传播
authors: "Shilei Wang, Pujian Lai, Dong Gao, Jifeng Ning, Gong Cheng"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09287v1"
tags: ["query:vot"]
score: 9.0
evidence: 多模态目标跟踪的模态感知融合
tldr: 针对多模态目标跟踪中融合策略单一及时间信息纠缠的问题，本文提出MDTrack框架。该框架引入模态感知融合机制，利用混合专家模型（MoE）为不同模态分配专用专家并动态选择最优融合路径；同时采用解耦的时间传播策略，通过独立的状态空间模型（SSM）分别处理RGB与其他模态的时间信息，并结合交叉注意力机制实现信息协同。实验表明，MDTrack在五个主流基准上均达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有跟踪器通常采用统一融合策略且时间信息传播存在纠缠，忽视了不同模态间的固有差异和时间特征的独立性。
method: 提出模态感知融合（利用MoE动态选择专家）与解耦时间传播（采用双SSM结构独立存储状态并结合交叉注意力实现协同）。
result: MDTrack在红外、事件、深度等五项多模态跟踪基准测试中均取得了最先进的性能表现。
conclusion: 通过模态特异性融合与解耦的时间建模，MDTrack有效提升了多模态目标跟踪的判别力与鲁棒性。
---

## 摘要
大多数现有的多模态跟踪器采用统一的融合策略，忽略了模态之间的固有差异。此外，它们通过混合 token 传播时序信息，导致时序表示纠缠且判别性较差。为了解决这些局限性，我们提出了 MDTrack，这是一个用于多模态目标跟踪的模态感知融合与解耦时序传播的新型框架。具体而言，对于模态感知融合，我们为每种模态（包括红外、事件、深度和 RGB）分配专门的专家，以处理各自的表示。混合专家（Mixture of Experts）中的门控机制根据输入特征动态选择最优专家，从而实现自适应且特定于模态的融合。对于解耦时序传播，我们引入了两个独立的结构化状态空间模型（State Space Model）结构，以独立存储和更新 RGB 和 X 模态流的隐藏状态，从而有效捕捉它们各自的时序信息。为了确保两种时序表示之间的协同作用，我们在两个 SSM 的输入特征之间加入了一组交叉注意力模块，促进了隐式信息交换。随后，通过另一组交叉注意力模块将增强后的时序特征集成到骨干网络中，提升了 MDTrack 利用时序信息的能力。大量实验证明了所提方法的有效性。MDTrack S 和 MDTrack U 在五个多模态跟踪基准测试中均达到了最先进的性能。

## Abstract
Most existing multimodal trackers adopt uniform fusion strategies, overlooking the inherent differences between modalities. Moreover, they propagate temporal information through mixed tokens, leading to entangled and less discriminative temporal representations. To address these limitations, we propose MDTrack, a novel framework for modality aware fusion and decoupled temporal propagation in multimodal object tracking. Specifically, for modality aware fusion, we allocate dedicated experts to each modality, including infrared, event, depth, and RGB, to process their respective representations. The gating mechanism within the Mixture of Experts dynamically selects the optimal experts based on the input features, enabling adaptive and modality specific fusion. For decoupled temporal propagation, we introduce two separate State Space Model structures to independently store and update the hidden states of the RGB and X modal streams, effectively capturing their distinct temporal information. To ensure synergy between the two temporal representations, we incorporate a set of cross attention modules between the input features of the two SSMs, facilitating implicit information exchange. The resulting temporally enriched features are then integrated into the backbone through another set of cross attention modules, enhancing MDTrack's ability to leverage temporal information. Extensive experiments demonstrate the effectiveness of our proposed method. Both MDTrack S and MDTrack U achieve state of the art performance across five multimodal tracking benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Exploring Modality-Aware Fusion and Decoupled Temporal Propagation for Multi-Modal Object Tracking》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的多模态目标跟踪器（如结合红外、深度或事件相机）主要存在两个瓶颈：
    1.  **融合策略单一**：通常对所有模态采用统一的融合模块，忽略了不同传感器信号（如红外热成像与深度几何结构）之间的固有差异。
    2.  **时序传播纠缠**：在处理视频序列时，往往将不同模态的特征 token 混合传播，导致异构的时序动态（如 RGB 的纹理变化与事件相机的极性变化）相互干扰，降低了表示的判别力。
*   **研究动机**：提出一种能够感知模态差异并实现时序信息解耦的统一框架，以提升在光照变化、运动模糊和遮挡等挑战场景下的跟踪鲁棒性。

### 2. 方法论：MDTrack 框架
MDTrack 核心思想在于“专人办专事”和“并行演化”，主要包含以下关键技术：
*   **模态感知融合（Modality-Aware Fusion, MoE）**：
    *   引入**混合专家模型（Mixture of Experts）**结构，为 RGB、红外（IR）、事件（Event）和深度（Depth）分别设立专用专家模块。
    *   通过**门控机制（Router）**根据输入特征动态计算权重，选择最合适的专家进行特征处理，实现自适应的跨模态集成。
*   **解耦时序传播（Decoupled Temporal Propagation）**：
    *   采用两个独立的**状态空间模型（SSM/Mamba）**结构，分别维护 RGB 流和 X 模态流（非 RGB 模态）的隐藏状态 $h$。
    *   **双向交互机制**：在进入 SSM 之前，通过交叉注意力（Cross-Attention）让两个流进行隐式信息交换；在 SSM 处理后，再次通过交叉注意力将时序增强后的特征注入骨干网络（HiViT）。
*   **算法流程**：输入模板和搜索区域 -> Patch Embedding -> 骨干网络特征提取（伴随解耦时序更新）-> MoE 模态感知融合 -> 预测头输出目标位置。

### 3. 实验设计
*   **数据集与场景**：涵盖了三种主流的多模态跟踪任务：
    *   **RGBT（红外）**：LasHeR（1224条序列）、RGBT234。
    *   **RGBD（深度）**：DepthTrack、VOT-RGBD2022。
    *   **RGBE（事件）**：VisEvent（820条序列）。
*   **Benchmark 与对比方法**：
    *   对比了包括 ViPT、STTrack、SUTrack、Un-Track、SDSTrack 等在内的最新 SOTA 方法。
    *   评估指标包括成功率（AUC/Success）、精度（Precision/MPR）、F-score 和 EAO 等。

### 4. 资源与算力
*   **硬件环境**：使用 **4 张 NVIDIA RTX 4090 GPU** 进行训练。
*   **训练时长**：
    *   **模态特定训练（MDTrack-S）**：每个任务训练 20 个 epoch。
    *   **统一模态训练（MDTrack-U）**：合并所有数据集训练 30 个 epoch。
    *   每个 epoch 包含 60,000 个样本对，Batch Size 为 16。
*   **推理效率**：在 RTX 4090 上推理速度约为 **25 FPS**，达到了实时处理的基本要求。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个大型主流基准数据集上进行了全面测试，实验覆盖面广。
*   **消融实验**：
    *   验证了“解耦时序模块”与“模态感知融合模块”的各自贡献，结果显示两者结合时性能提升最显著（平均增益 +2.1%）。
    *   对比了基于 Token 的时序传播、单 SSM 混合传播与双 SSM 解耦传播的效果，证明了解耦设计的优越性。
*   **客观性**：采用了模态特定（Specific）和统一（Unified）两种训练范式进行对比，证明了该架构在不同训练策略下的稳健性，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：MDTrack 在所有五个基准测试中均取得了第一或第二的成绩。例如在 LasHeR 上达到了 61.4% 的 AUC，显著超过了之前的 SOTA。
*   **解耦的必要性**：实验发现，简单地混合不同模态的时序 token 会导致性能受限，而通过独立 SSM 建模能更好地保留各模态独特的动态特征。
*   **MoE 的有效性**：相比于统一融合，MoE 能够根据输入模态动态调整融合权重，有效抑制了冗余信息并增强了互补特征。

### 7. 优点与亮点
*   **架构创新**：首次将 Mamba（SSM）的线性复杂度优势与 MoE 的自适应能力结合到多模态跟踪领域。
*   **通用性强**：一个框架（MDTrack-U）即可同时处理红外、深度、事件三种异构模态，无需针对每种模态修改网络结构。
*   **时序建模高效**：利用 SSM 实现了长程时序依赖的建模，且保持了比 Transformer 更低的计算复杂度。

### 8. 不足与局限
*   **参数量增加**：虽然 MoE 在推理时只激活部分专家，但模型总参数量（存储需求）会随着专家数量的增加而上升。
*   **推理延迟**：尽管达到了 25 FPS，但相比于一些极轻量级的单模态跟踪器，在嵌入式设备上的部署仍可能面临压力。
*   **极端场景依赖**：论文未详细讨论当某一模态完全失效（如传感器损坏或极端噪声）时，MoE 门控机制的容错极限。

（完）
