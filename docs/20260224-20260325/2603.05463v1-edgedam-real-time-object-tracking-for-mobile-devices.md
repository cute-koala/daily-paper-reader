---
title: "EdgeDAM: Real-time Object Tracking for Mobile Devices"
title_zh: EdgeDAM：面向移动设备的实时目标跟踪
authors: "Syed Muhammad Raza, Syed Murtaza Hussain Abidi, Khawar Islam, Muhammad Ibrahim, Ajmal Saeed Mian"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05463v1"
tags: ["query:vot"]
score: 10.0
evidence: 移动设备的实时单目标跟踪
tldr: EdgeDAM是一个专为移动设备设计的轻量级实时单目标跟踪框架，旨在解决边缘设备上跟踪器在面对遮挡和干扰物时易漂移且计算开销大的问题。该框架引入了双缓冲干扰感知存储器（DAM）来保存目标假设并显式惩罚干扰物，并结合置信度驱动的切换与Held-Box稳定机制来应对遮挡。实验表明，EdgeDAM在保持移动端实时性能（iPhone 15上25 FPS）的同时，显著提升了在复杂场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的干扰感知跟踪器计算开销过大难以在边缘设备部署，而轻量级跟踪器在遇到相似干扰物时极易发生漂移。
method: 提出EdgeDAM框架，通过双缓冲存储器管理目标与干扰物信息，并利用置信度驱动的切换机制和Held-Box策略在遮挡期间稳定跟踪。
result: "在DiDi等五个基准测试中表现优异，DiDi准确率达88.2%，且在iPhone 15上实现了25 FPS的实时运行速度。"
conclusion: EdgeDAM成功在资源受限的硬件上实现了高鲁棒性与实时性的平衡，为移动端目标跟踪提供了有效的解决方案。
---

## 摘要
边缘设备上的单目标跟踪（SOT）是一项关键的计算机视觉任务，要求在遮挡、干扰物干扰和快速运动的情况下，在视频帧中实现准确且连续的目标定位。然而，近期最先进的干扰物感知记忆机制主要建立在基于分割的跟踪器之上，并依赖于掩码预测和注意力驱动的记忆更新，这引入了巨大的计算开销，限制了在资源受限硬件上的实时部署；与此同时，轻量级跟踪器虽然保持了高吞吐量，但在出现视觉相似的干扰物时容易发生漂移。为了应对这些挑战，我们提出了 EdgeDAM，这是一个轻量级的检测引导跟踪框架，它在严格的边缘约束下为边界框跟踪重新构建了干扰物感知记忆。EdgeDAM 引入了两个关键策略：(1) 双缓冲干扰物感知记忆（DAM），它集成了用于保留时间一致目标假设的近期感知记忆（Recent-Aware Memory）和用于显式存储难负样本候选并在恢复期间惩罚其重新选择的干扰物解析记忆（Distractor-Resolving Memory）；(2) 置信度驱动切换与持框稳定（Held-Box Stabilization），其中跟踪器可靠性和时间一致性准则在遮挡期间自适应地激活检测和记忆引导的重识别，而持框机制则临时冻结并扩大估计范围以抑制干扰物污染。在包括专注于干扰物的 DiDi 数据集在内的五个基准测试上进行的广泛实验表明，该方法在遮挡和快速运动下提高了鲁棒性，同时保持了移动设备上的实时性能，在 DiDi 上达到了 88.2% 的准确率，并在 iPhone 15 上达到了 25 FPS。代码将会发布。

## Abstract
Single-object tracking (SOT) on edge devices is a critical computer vision task, requiring accurate and continuous target localization across video frames under occlusion, distractor interference, and fast motion. However, recent state-of-the-art distractor-aware memory mechanisms are largely built on segmentation-based trackers and rely on mask prediction and attention-driven memory updates, which introduce substantial computational overhead and limit real-time deployment on resource-constrained hardware; meanwhile, lightweight trackers sustain high throughput but are prone to drift when visually similar distractors appear. To address these challenges, we propose EdgeDAM, a lightweight detection-guided tracking framework that reformulates distractor-aware memory for bounding-box tracking under strict edge constraints. EdgeDAM introduces two key strategies: (1) Dual-Buffer Distractor-Aware Memory (DAM), which integrates a Recent-Aware Memory to preserve temporally consistent target hypotheses and a Distractor-Resolving Memory to explicitly store hard negative candidates and penalize their re-selection during recovery; and (2) Confidence-Driven Switching with Held-Box Stabilization, where tracker reliability and temporal consistency criteria adaptively activate detection and memory-guided re-identification during occlusion, while a held-box mechanism temporarily freezes and expands the estimate to suppress distractor contamination. Extensive experiments on five benchmarks, including the distractor-focused DiDi dataset, demonstrate improved robustness under occlusion and fast motion while maintaining real-time performance on mobile devices, achieving 88.2% accuracy on DiDi and 25 FPS on an iPhone 15. Code will be released.

---

## 论文详细总结（自动生成）

### EdgeDAM：面向移动设备的实时目标跟踪论文总结

#### 1. 研究动机与核心问题
*   **核心问题**：在移动和边缘设备上实现**实时且鲁棒**的单目标跟踪（SOT）。
*   **研究背景**：
    *   **现有SOTA方法的局限**：如 SAM2.1++ 等基于记忆机制的跟踪器虽然能很好地处理遮挡和干扰物，但依赖于复杂的分割掩码（Mask）传播和全局注意力机制，导致计算开销巨大（仅 2-8 FPS），无法在移动端运行。
    *   **轻量级跟踪器的不足**：如 EdgeTAM 或 OSTrack 虽然速度快，但在面对视觉相似的干扰物（Distractors）时极易发生身份切换或漂移。
*   **动机**：填补“高鲁棒性记忆机制”与“边缘端实时性”之间的空白，证明在边界框（Bounding-box）层面进行干扰物感知推理，无需密集注意力也能实现高性能跟踪。

#### 2. 方法论：核心思想与关键技术
EdgeDAM 采用“检测引导跟踪”的架构，结合了 YOLO 检测器、CSRT 相关滤波跟踪器和创新的双缓冲记忆模块。
*   **双缓冲干扰物感知记忆 (DAM)**：
    *   **近期感知记忆 (RAM)**：存储经过几何验证（IoU 和面积一致性门控）的近期目标状态，确保短期内的时间一致性。
    *   **干扰物解析记忆 (DRM)**：存储稳定的外观锚点（Anchor），用于长效恢复。它通过外观稳定性测试（余弦相似度）从 RAM 晋升而来。
*   **轻量级描述符**：放弃高维深度特征，使用 16x16 的灰度补丁和 HSV 颜色直方图拼接而成的紧凑向量，极大地降低了内存和计算需求。
*   **置信度驱动的切换机制**：
    *   实时监控 CSRT 的峰值旁瓣比（PSR）。当 PSR 低于阈值或出现多个重叠检测（暗示遮挡）时，系统自动切换到 DAM 引导的重识别模式。
*   **持框稳定 (Held-Box Stabilization)**：
    *   在遮挡期间，系统不盲目更新位置，而是冻结并根据运动估计（光流法）平滑扩大边界框，以覆盖可能的区域并抑制干扰物污染。

#### 3. 实验设计
*   **数据集**：
    *   **DiDi**：专门针对干扰物设计的基准测试。
    *   **VOT2020/2022**：评估综合跟踪性能（EAO、准确度、鲁棒性）。
    *   **LaSOT / LaSOText**：评估长时跟踪和类别无关的泛化能力。
    *   **GOT-10k**：评估在未见类别上的开放世界泛化能力。
*   **对比方法**：对比了包括 SAM2.1++、SAMURAI、EdgeTAM、OSTrack、MixFormer、ODTrack 等在内的十余种 SOTA 跟踪器。

#### 4. 资源与算力
*   **推理端**：在 **iPhone 15 Pro Max** 上通过 CoreML 部署，实现了 **25 FPS** 的实时速度。
*   **模型规模**：使用 YOLOv11s 作为骨干网络，参数量仅为 **9.4M**，远小于 SAM2.1++ 的 224M。
*   **训练细节**：文中提到将 YOLOv11s 微调为单类别检测器，但**未明确说明具体的训练 GPU 型号、数量及总训练时长**。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流公开基准测试上进行了详尽测试，并额外使用了一个包含 35,000 帧的自定义马术场景数据集进行消融实验。
*   **消融实验**：涵盖了组件贡献分析（检测器、RAM、DRM、后处理）、记忆缓冲区容量（5-5 到 20-20 的对比）以及多干扰物密度下的恢复率分析。
*   **评价**：实验设计全面，涵盖了从短时到长时、从特定类别到类别无关的多种场景，对比基准线清晰，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：在 DiDi 数据集上，EdgeDAM 的准确率（88.2%）和鲁棒性（97.3%）显著超过了 SAM2.1++。
*   **效率极高**：在保持高精度的同时，其运行速度是同类记忆型跟踪器的 3-10 倍，且模型参数量压缩了约 90%。
*   **缓冲区策略**：实验发现 10-10 的缓冲区大小是最佳平衡点；过大的缓冲区（如 20-20）反而会因为 FIFO 机制引入过时/损坏的条目而导致性能下降。

#### 7. 优点与亮点
*   **架构解耦**：框架对检测器不敏感（Detector-agnostic），可灵活更换 YOLO 系列版本。
*   **无需分割掩码**：证明了仅靠边界框几何信息和简单外观特征也能实现极强的干扰物排除能力，避开了昂贵的 Mask 运算。
*   **边缘友好**：通过 ROI 裁剪、半精度推理和模型融合等手段，真正实现了移动端的闭环实时跟踪。

#### 8. 不足与局限
*   **输出限制**：仅支持边界框输出，无法像 SAM 系列那样提供精细的像素级分割掩码。
*   **记忆更新策略**：目前采用简单的 FIFO（先进先出）淘汰机制，在极长视频中可能会面临“记忆污染”问题，缺乏更智能的长期记忆筛选机制。
*   **应用限制**：虽然在单目标跟踪上表现优异，但尚未扩展到多目标跟踪（MOT）或 3D 场景，其在极度拥挤场景下的表现仍有待验证。

（完）
