---
title: "Uni-MDTrack: Learning Decoupled Memory and Dynamic States for Parameter-Efficient Visual Tracking in All Modality"
title_zh: Uni-MDTrack：学习解耦的记忆与动态状态，实现全模态下的参数高效视觉跟踪
authors: "Wenrui Cai, Zhenyi Lu, Yuzhe Li, Yongchao Feng, Jinqing Zhang, Qingjie Liu, Yunhong Wang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14452v1"
tags: ["query:vot"]
score: 10.0
evidence: 基于Transformer的单流视觉跟踪
tldr: "Uni-MDTrack 针对单流跟踪器在时空上下文利用不足及计算开销大的问题，提出了学习解耦记忆与动态状态的框架。通过 MCP 模块将丰富记忆压缩为提示词，并利用 DSF 模块捕捉连续动态，实现了全模态（RGB, RGB-D/T/E, RGB-L）下的高效跟踪。该方法仅需训练约 30% 的参数，在 10 个数据集上达到 SOTA，且具有良好的通用性。"
selection_source: fresh_fetch
motivation: 现有跟踪器在利用历史帧上下文时存在计算开销大、特征融合不充分以及忽视目标动态特性的局限。
method: 提出由 MCP 模块（压缩记忆为提示词进行深度交互）和 DSF 模块（捕捉并融合连续动态状态）组成的 Uni-MDTrack 框架。
result: "在涵盖五种模态的 10 个数据集上取得领先性能，且仅需训练 30% 的参数，展现出极高的参数效率和通用性。"
conclusion: Uni-MDTrack 通过解耦记忆与动态状态，为全模态下的参数高效视觉跟踪提供了一种高性能且可扩展的解决方案。
---

## 摘要
随着在帧间关系建模方面具有强大能力的基于 Transformer 的单流跟踪器的出现，最近的研究越来越多地关注如何引入时空上下文。然而，大多数现有方法依赖于有限数量的历史帧，这不仅导致上下文利用不足，还不可避免地增加了输入长度并带来了极高的计算开销。另一方面，查询外部记忆库的方法在检索到的时空特征与骨干网络（backbone）之间的融合方面存在不足。此外，使用离散的历史帧作为上下文忽略了目标丰富的动态特性。为了解决这些问题，我们提出了 Uni-MDTrack，它由两个核心组件组成：记忆感知压缩提示（MCP）模块和动态状态融合（DSF）模块。MCP 有效地将丰富的记忆特征压缩为记忆感知提示标记（prompt tokens），这些标记在整个骨干网络中与输入进行深度交互，在保持稳定计算负载的同时显著提升了性能。DSF 通过捕捉连续动态来补充离散记忆，从浅层到深层逐步引入更新的动态状态特征，同时也保持了高效率。Uni-MDTrack 还支持跨 RGB、RGB-D/T/E 和 RGB-语言模态的统一跟踪。实验表明，在 Uni-MDTrack 中，仅训练 MCP、DSF 和预测头，将可训练参数比例保持在 30% 左右，即可获得显著的性能提升，并在涵盖五种模态的 10 个数据集上达到了最先进的结果。此外，MCP 和 DSF 都表现出极佳的通用性，可作为即插即用组件提升各种基准跟踪器的性能，同时显著优于现有的参数高效训练方法。

## Abstract
With the advent of Transformer-based one-stream trackers that possess strong capability in inter-frame relation modeling, recent research has increasingly focused on how to introduce spatio-temporal context. However, most existing methods rely on a limited number of historical frames, which not only leads to insufficient utilization of the context, but also inevitably increases the length of input and incurs prohibitive computational overhead. Methods that query an external memory bank, on the other hand, suffer from inadequate fusion between the retrieved spatio-temporal features and the backbone. Moreover, using discrete historical frames as context overlooks the rich dynamics of the target. To address the issues, we propose Uni-MDTrack, which consists of two core components: Memory-Aware Compression Prompt (MCP) module and Dynamic State Fusion (DSF) module. MCP effectively compresses rich memory features into memory-aware prompt tokens, which deeply interact with the input throughout the entire backbone, significantly enhancing the performance while maintaining a stable computational load. DSF complements the discrete memory by capturing the continuous dynamic, progressively introducing the updated dynamic state features from shallow to deep layers, while also preserving high efficiency. Uni-MDTrack also supports unified tracking across RGB, RGB-D/T/E, and RGB-Language modalities. Experiments show that in Uni-MDTrack, training only the MCP, DSF, and prediction head, keeping the proportion of trainable parameters around 30%, yields substantial performance gains, achieves state-of-the-art results on 10 datasets spanning five modalities. Furthermore, both MCP and DSF exhibit excellent generality, functioning as plug-and-play components that can boost the performance of various baseline trackers, while significantly outperforming existing parameter-efficient training approaches.

---

## 论文详细总结（自动生成）

### 论文总结：Uni-MDTrack —— 全模态参数高效视觉跟踪的解耦记忆与动态状态学习

#### 1. 核心问题与整体含义（研究动机和背景）
现代视觉跟踪器（SOT）多采用基于 Transformer 的单流（one-stream）架构，虽然在帧间关系建模上表现出色，但在利用视频时空上下文方面仍存在局限：
*   **上下文利用不足与开销矛盾**：现有方法要么仅依赖少量历史帧（导致长时信息缺失），要么引入过多辅助帧（导致输入序列过长，计算开销剧增）。
*   **特征融合不充分**：外部记忆库方法往往在预测头之前才进行融合，缺乏与骨干网络（Backbone）特征的深度交互。
*   **忽视连续动态特性**：离散的历史帧无法有效捕捉目标在视频演化过程中的连续状态变化。
*   **训练成本高**：全参数微调大型模型耗时耗力。

**Uni-MDTrack** 旨在通过一种参数高效（PEFT）的框架，在不显著增加计算负担的前提下，为全模态跟踪引入丰富的记忆信息和连续的动态状态。

#### 2. 论文提出的方法论
Uni-MDTrack 提出了两个核心解耦模块，以插件形式注入冻结的骨干网络：

*   **记忆感知压缩提示模块 (MCP)**：
    *   **核心思想**：维护一个长时记忆库，通过动态查询（Dynamic Queries）将海量记忆特征压缩为固定数量（如16个）的“记忆感知提示标记”。
    *   **技术细节**：这些标记作为 Prompt 与当前帧输入拼接，在整个骨干网络中进行深度自注意力交互。引入 **ALiBi (Attention with Linear Biases)** 位置偏置，使模型能外推处理更长的记忆序列，并优先关注近期记忆。
*   **动态状态融合模块 (DSF)**：
    *   **核心思想**：利用状态空间模型（SSM，如 Mamba）捕捉目标的连续动态演化。
    *   **技术细节**：DSF 仅利用搜索区域特征更新状态，避免模板信息的干扰。该模块在骨干网络的多层（浅层到深层）进行注入，通过交叉注意力机制将动态特征融合进主干特征流。
*   **全模态统一架构**：
    *   采用统一的 Patch Embedding 层处理 RGB、深度（D）、热红外（T）、事件（E）及语言（L）描述。
    *   **参数高效训练**：冻结骨干网络，仅训练 MCP、DSF 和预测头，可训练参数量占比低于 30%。

#### 3. 实验设计
*   **数据集与场景**：涵盖 5 种模态的 10 多个主流数据集。
    *   **RGB**: LaSOT, TrackingNet, LaSOT_ext, GOT-10k, UAV123, NfS, OTB2015。
    *   **RGB-D**: DepthTrack。
    *   **RGB-T**: LasHeR。
    *   **RGB-E**: VisEvent。
    *   **RGB-L**: TNL2K。
*   **Benchmark 与对比方法**：
    *   对比了 SUTrack, FlexTrack, STTrack 等最新的全模态跟踪器。
    *   对比了 OSTrack, MixFormer, ARTrack 等 SOTA RGB 跟踪器。
    *   对比了 HIPTrack, LoRAT, SPMTrack 等参数高效微调方法。

#### 4. 资源与算力
*   **硬件**：使用 4 台 NVIDIA A100 GPU 进行训练。
*   **训练配置**：
    *   Uni-MDTrack-B（基础版）：Batch Size 为 64/GPU。
    *   Uni-MDTrack-L（大型版）：Batch Size 为 16/GPU。
    *   **时长**：共训练 50 个 Epoch，每个 Epoch 采样 100,000 个序列。
*   **推理效率**：MCP 保持了稳定的计算负载，DSF 具有线性复杂度，整体推理速度具有竞争力。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **多模态覆盖**：在 10 个不同模态的数据集上均取得了 SOTA 结果。
*   **消融实验**：针对 MCP 和 DSF 的有效性、Prompt 数量、DSF 模块数量、记忆库更新策略、位置偏置类型等进行了多组对比实验。
*   **通用性验证**：将 MCP 和 DSF 插入到不同的基准模型（如 DropTrack）中，均观察到显著性能提升。
*   **客观性**：实验遵循了标准的评估协议（如 GOT-10k 的严格训练限制），对比基准涵盖了 2022-2025 年间的最先进方法，实验设计公平、客观。

#### 6. 主要结论与发现
*   **解耦建模的优越性**：将长时记忆（MCP）与短时连续动态（DSF）解耦处理，比单一的时空建模更有效。
*   **参数效率极高**：仅微调不到 30% 的参数，性能却超过了许多全参数微调的模型。
*   **SSM 的新用途**：证明了 SSM 不仅可以作为骨干网络，作为 PEFT 的动态特征提取器同样表现卓越。
*   **全模态通用性**：单一模型即可在多种传感器组合下实现顶尖性能，无需为每种模态单独设计架构。

#### 7. 优点（亮点）
*   **深度融合**：解决了记忆特征与骨干特征融合不深的问题，通过 Prompt 实现了全层交互。
*   **计算友好**：通过压缩技术避免了因增加历史帧导致的计算量爆炸。
*   **即插即用**：MCP 和 DSF 具有很强的迁移能力，可作为通用插件提升现有跟踪器的性能。
*   **长时外推**：ALiBi 的引入解决了推理时记忆长度超过训练长度的泛化问题。

#### 8. 不足与局限
*   **极端场景失效**：定性分析显示，在极端遮挡、快速运动且背景极度复杂（如赛车、硬币堆）的情况下，模型仍会出现跟踪失败（见论文图6）。
*   **SSM 训练敏感性**：消融实验显示，增加过多的 DSF 模块可能导致 50 个 Epoch 的训练不足以使其收敛，说明 SSM 模块对超参数和训练时长有一定要求。
*   **依赖预训练权重**：模型性能在很大程度上仍依赖于强大的预训练骨干网络（如 HiViT）。

（完）
