---
title: "FocusTrack: One-Stage Focus-and-Suppress Framework for 3D Point Cloud Object Tracking"
title_zh: FocusTrack：用于三维点云目标跟踪的单阶段聚焦与抑制框架
authors: "Sifan Zhou, Jiahao Nie, Ziyu Zhao, Yichao Cao, Xiaobo Lu"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.24133v1"
tags: ["query:vot"]
score: 9.0
evidence: 基于深度学习的单阶段3D点云目标跟踪
tldr: FocusTrack是一个针对3D点云目标追踪的单阶段框架，旨在解决传统两阶段运动建模方法中存在的误差累积和计算效率低下的问题。该框架通过帧间运动建模（IMM）捕获全局运动模式，并结合聚焦-抑制注意力机制，在无需显式分割的情况下增强前景特征并抑制背景噪声。实验表明，该方法在多个主流基准测试上达到了SOTA性能，且推理速度高达105 FPS。
selection_source: fresh_fetch
motivation: 针对现有两阶段运动追踪方法中显式前景分割导致的误差累积及串行处理带来的计算瓶颈。
method: 提出包含帧间运动建模（IMM）和聚焦-抑制注意力机制的单阶段框架，实现运动与语义的统一建模。
result: 在KITTI、nuScenes和Waymo等数据集上刷新了SOTA记录，并实现了105 FPS的高速运行。
conclusion: 该研究证明了通过端到端单阶段架构统一运动与语义建模，能显著提升3D点云追踪的性能与效率。
---

## 摘要
在三维点云目标跟踪中，以运动为中心的方法因其在建模帧间运动方面的卓越性能而成为一种极具前景的途径。然而，现有的基于运动的两阶段方法存在根本性的局限性：(1) 由于在运动估计之前进行显式的前景分割，导致解耦优化产生的误差累积；(2) 顺序处理带来的计算瓶颈。为了应对这些挑战，我们提出了 FocusTrack，这是一种新颖的单阶段范式跟踪框架，通过两项核心创新实现了运动-语义协同建模：帧间运动建模 (IMM) 和聚焦与抑制注意力机制 (Focus-and-Suppress Attention)。IMM 模块采用时差孪生编码器来捕捉相邻帧之间的全局运动模式。聚焦与抑制注意力机制通过运动显著特征门控增强前景语义，并根据 IMM 提供的时序感知运动上下文抑制背景噪声，而无需显式分割。基于上述两项设计，FocusTrack 能够通过紧凑的单阶段流水线实现端到端训练。在 KITTI、nuScenes 和 Waymo 等著名 3D 跟踪基准测试上的大量实验表明，FocusTrack 在以 105 FPS 的高速运行的同时，达到了新的 SOTA 性能。

## Abstract
In 3D point cloud object tracking, the motion-centric methods have emerged as a promising avenue due to its superior performance in modeling inter-frame motion. However, existing two-stage motion-based approaches suffer from fundamental limitations: (1) error accumulation due to decoupled optimization caused by explicit foreground segmentation prior to motion estimation, and (2) computational bottlenecks from sequential processing. To address these challenges, we propose FocusTrack, a novel one-stage paradigms tracking framework that unifies motion-semantics co-modeling through two core innovations: Inter-frame Motion Modeling (IMM) and Focus-and-Suppress Attention. The IMM module employs a temp-oral-difference siamese encoder to capture global motion patterns between adjacent frames. The Focus-and-Suppress attention that enhance the foreground semantics via motion-salient feature gating and suppress the background noise based on the temporal-aware motion context from IMM without explicit segmentation. Based on above two designs, FocusTrack enables end-to-end training with compact one-stage pipeline. Extensive experiments on prominent 3D tracking benchmarks, such as KITTI, nuScenes, and Waymo, demonstrate that the FocusTrack achieves new SOTA performance while running at a high speed with 105 FPS.

---

## 论文详细总结（自动生成）

### FocusTrack：用于三维点云目标跟踪的单阶段聚焦与抑制框架

#### 1. 核心问题与研究动机
*   **核心问题**：现有的以运动为中心（Motion-centric）的3D单目标跟踪（SOT）方法多采用“先分割前景、后估计运动”的两阶段模式。这种模式存在两个主要缺陷：
    1.  **误差累积**：显式的前景分割与运动估计是解耦优化的，分割错误会直接导致运动预测失败。
    2.  **计算瓶颈**：两阶段的顺序处理限制了推理速度。
*   **研究背景**：在点云稀疏或遮挡严重的场景下，传统的基于外观匹配（Appearance matching）的方法效果较差，而运动建模方法更具鲁棒性。论文旨在通过单阶段框架统一运动与语义建模，在抑制背景噪声的同时增强前景特征。

#### 2. 方法论
FocusTrack 提出了一个单阶段的“聚焦与抑制”框架，核心组件包括：
*   **柱体特征嵌入（Pillar Feature Embedding）**：利用 PointPillars 将稀疏点云转换为密集的鸟瞰图（BEV）特征。
*   **帧间运动建模（IMM）模块**：
    *   受2D视频处理中“帧差法”启发，在特征空间捕捉全局运动差异。
    *   通过计算当前帧与前一帧的查询-键（Query-Key）乘积之差（即 $Q_t K_t^T - \alpha Q_{t-1} K_{t-1}^T$）来生成运动感知权重 $W_m$。
    *   引入可学习参数 $\alpha$ 动态调整前一帧的影响力。
*   **聚焦-抑制注意力机制（Focus-and-Suppress Attention）**：
    *   利用 IMM 生成的运动权重 $W_m$ 对空间语义进行加权。
    *   **聚焦**：增强具有显著运动特征的前景目标语义。
    *   **抑制**：在不进行显式分割的情况下，利用时序运动上下文抑制静态背景噪声。
    *   采用**线性注意力（Linear Attention）**将计算复杂度从 $O(N^2)$ 降低到 $O(N)$，提升效率。
*   **运动预测头**：直接从增强后的特征中回归目标的相对运动参数（$\Delta x, \Delta y, \Delta z, \Delta \theta$）。

#### 3. 实验设计
*   **数据集**：KITTI、nuScenes、Waymo Open Dataset (WOD)。
*   **评估指标**：成功率（Success）和精确度（Precision），采用单次通过评估（OPE）标准。
*   **对比方法**：
    *   **匹配类方法**：SC3D, P2B, BAT, STNet, CXTrack, MBPTrack 等。
    *   **运动类方法**：M2Track, M2Track++, P2P-voxel, VoxelTrack 等。
*   **泛化性测试**：在 KITTI 上预训练，直接在 nuScenes 和 WOD 上进行跨数据集测试。

#### 4. 资源与算力
*   **训练设备**：使用 NVIDIA RTX 4090 GPU，Batch Size 为 128。
*   **推理设备**：在单张 NVIDIA RTX 3090 GPU 上进行测试。
*   **运行速度**：达到了 105 FPS，远超许多现有的 SOTA 模型。
*   **训练细节**：使用 AdamW 优化器，初始学习率 1e-4，每 20 个 epoch 衰减 5 倍。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准数据集上进行了详尽测试，涵盖了从小型（KITTI）到大型（nuScenes, WOD）的数据规模。
*   **消融实验**：针对 IMM 模块、深度可分离卷积（DWC）、权重共享设计、下采样率以及可学习参数 $\alpha$ 进行了多组对比实验。
*   **客观性**：通过可视化注意力图（Heatmap）直观展示了背景抑制效果，并提供了成功与失败案例分析，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：FocusTrack 在所有测试数据集上均达到了新的 SOTA 水平。在 KITTI 上 Success/Precision 达到 71.3%/89.4%，在 nuScenes 上显著超过了之前的最佳方法 MBPTrack（提升约 4.2%）。
*   **效率极高**：单阶段架构和线性注意力的应用使其在保持高精度的同时，推理速度达到 105 FPS，满足实时性要求。
*   **运动引导有效性**：实验证明，利用帧间特征差异引导注意力机制，比单纯的外观匹配或解耦的分割-运动预测更有效。

#### 7. 优点与亮点
*   **架构创新**：将经典的“帧差法”思想巧妙地融入 Transformer 的注意力机制中，实现了运动与语义的端到端协同优化。
*   **无需显式分割**：避免了分割错误对后续跟踪的负面影响，简化了流水线。
*   **高效性**：在计算复杂度优化（线性注意力）和单阶段设计上表现出色，平衡了精度与速度。

#### 8. 不足与局限
*   **极端稀疏场景**：在点云极度稀疏的情况下（如失败案例所示），仅靠几何运动信息仍难以实现完美跟踪。
*   **多模态缺失**：目前仅依赖 LiDAR 数据，论文提到未来可引入 RGB 图像融合以增强语义细节。
*   **长时依赖**：目前主要关注相邻帧间的差异，对于长距离时序信息的利用仍有提升空间。

（完）
