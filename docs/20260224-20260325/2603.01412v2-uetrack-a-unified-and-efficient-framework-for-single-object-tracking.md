---
title: "UETrack: A Unified and Efficient Framework for Single Object Tracking"
title_zh: UETrack：一种统一且高效的单目标跟踪框架
authors: "Ben Kang, Jie Zhao, Xin Chen, Wanting Geng, Bin Zhang, Lu Zhang, Dong Wang, Huchuan Lu"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01412v2"
tags: ["query:vot"]
score: 10.0
evidence: 高效单目标跟踪框架
tldr: 针对现有单目标跟踪算法在多模态场景下复杂度高、难以在资源受限设备部署的问题，本文提出了UETrack框架。该框架通过基于Token池化的混合专家机制（MoE）增强多模态特征建模，并引入目标感知自适应蒸馏策略优化训练。UETrack支持RGB、深度、热成像、事件和语言等多种输入，在保持高精度的同时实现了极高的运行效率，为多模态高效跟踪提供了统一且实用的解决方案。
selection_source: fresh_fetch
motivation: 现有跟踪方法多局限于RGB输入，且现有的多模态方案设计过于复杂，难以满足资源受限环境下的实时部署需求。
method: 提出了基于Token池化的混合专家机制以增强特征聚合，并结合目标感知自适应蒸馏策略来减少冗余监督并提升性能。
result: "在12个基准测试和3个硬件平台上表现优异，其中UETrack-B在LaSOT上达到69.2% AUC，且在GPU上运行速度高达163 FPS。"
conclusion: UETrack是一个统一且高效的框架，成功平衡了多模态跟踪的精度与速度，具有极强的实用性和通用性。
---

## 摘要
随着现实世界需求的增长，高效跟踪受到了越来越多的关注。然而，大多数现有方法局限于 RGB 输入，在多模态场景中表现不佳。此外，当前的多模态跟踪方法通常采用复杂的设计，导致其过于沉重且速度较慢，难以在资源受限的环境中部署。为了解决这些局限性，我们提出了 UETrack，一个高效的单目标跟踪框架。UETrack 展示了极高的实用性和通用性，能够高效处理包括 RGB、深度（Depth）、热成像（Thermal）、事件（Event）和语言（Language）在内的多种模态，填补了高效多模态跟踪领域的空白。它引入了两个关键组件：一种基于令牌池化（Token-Pooling）的混合专家（Mixture-of-Experts）机制，通过特征聚合和专家专业化增强建模能力；以及一种目标感知的自适应蒸馏（Target-aware Adaptive Distillation）策略，根据样本特征选择性地进行蒸馏，从而减少冗余监督并提升性能。在 3 个硬件平台上的 12 个基准测试中进行的广泛实验表明，与以往的方法相比，UETrack 实现了更优的速度与精度权衡。例如，UETrack-B 在 LaSOT 上达到了 69.2% 的 AUC，并在 GPU/CPU/AGX 上分别以 163/56/60 FPS 的速度运行，展示了强大的实用性和通用性。代码可在 https://github.com/kangben258/UETrack 获取。

## Abstract
With growing real-world demands, efficient tracking has received increasing attention. However, most existing methods are limited to RGB inputs and struggle in multi-modal scenarios. Moreover, current multi-modal tracking approaches typically use complex designs, making them too heavy and slow for resource-constrained deployment. To tackle these limitations, we propose UETrack, an efficient framework for single object tracking. UETrack demonstrates high practicality and versatility, efficiently handling multiple modalities including RGB, Depth, Thermal, Event, and Language, and addresses the gap in efficient multi-modal tracking. It introduces two key components: a Token-Pooling-based Mixture-of-Experts mechanism that enhances modeling capacity through feature aggregation and expert specialization, and a Target-aware Adaptive Distillation strategy that selectively performs distillation based on sample characteristics, reducing redundant supervision and improving performance. Extensive experiments on 12 benchmarks across 3 hardware platforms show that UETrack achieves a superior speed-accuracy trade-off compared to previous methods. For instance, UETrack-B achieves 69.2% AUC on LaSOT and runs at 163/56/60 FPS on GPU/CPU/AGX, demonstrating strong practicality and versatility. Code is available at https://github.com/kangben258/UETrack.

---

## 论文详细总结（自动生成）

这是一份关于论文《UETrack: A Unified and Efficient Framework for Single Object Tracking》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的单目标跟踪（SOT）算法存在两个主要局限：
    1.  **模态局限性**：大多数高效跟踪器仅限于 RGB 输入，在复杂环境（如低光照、遮挡）下鲁棒性不足。
    2.  **计算开销大**：现有的多模态跟踪器通常设计复杂、模型沉重，难以在资源受限的嵌入式设备（如 Jetson AGX）上实现实时部署。
*   **研究动机**：开发一个**统一且高效**的框架，能够同时支持 RGB、深度（Depth）、热成像（Thermal）、事件（Event）和语言（Language）五种模态，并在保持高精度的同时实现极速推理。

### 2. 核心方法论
UETrack 采用了轻量化架构，并引入了两个创新组件：
*   **统一模态建模**：
    *   对于图像类辅助模态（D/T/E），将其与 RGB 图像拼接成 6 通道输入，通过 Patch Embedding 生成统一 Token。
    *   对于语言模态，利用预训练的 CLIP 提取文本 Token。
    *   所有模态的 Token 序列统一输入 Transformer 块处理，极大地减少了参数冗余。
*   **基于 Token 池化的混合专家机制 (TP-MoE)**：
    *   **核心思想**：取代传统 MoE 中耗时的离散门控（Gating）机制。
    *   **技术细节**：通过“局部聚合”增强上下文，利用“相似度驱动的软分配策略”实现特征与专家 Token 的加权聚合。这种设计支持全并行计算，消除了 Token 排序和专家间通信的延迟。
*   **目标感知自适应蒸馏 (TAD)**：
    *   **核心思想**：并非所有样本都适合蒸馏。如果教师模型在困难样本（如严重遮挡）上预测错误，强制蒸馏会引入噪声。
    *   **技术细节**：引入一个轻量级的 **Adaptive Net**（利用 Gumbel-Softmax），动态决定当前样本是否接受教师模型的分布和特征监督，从而过滤误导信号。

### 3. 实验设计
*   **数据集与场景**：涵盖了 5 种模态的 12 个主流基准测试：
    *   **RGB**: LaSOT, LaSOT_ext, TrackingNet, GOT-10k, VOT2021。
    *   **RGB-D**: VOT-RGBD22, DepthTrack。
    *   **RGB-T**: LasHeR, RGBT234。
    *   **RGB-E**: VisEvent。
    *   **RGB-L**: TNL2K, OTB99。
*   **对比方法**：对比了包括 MixFormerV2、HiT、AsymTrack 等高效跟踪器，以及 SUTrack、ViPT、SDSTrack 等多模态跟踪器。
*   **硬件平台**：在 GPU (2080Ti)、CPU (i9-14900KF) 和嵌入式平台 (Jetson AGX Xavier) 上分别测试了速度。

### 4. 资源与算力
*   **训练设备**：使用了 **2 台 80GB Tesla A800 GPU**。
*   **训练配置**：总 Batch Size 为 128，训练 500 个 Epoch，每个 Epoch 包含 100,000 个样本。
*   **优化器**：AdamW，初始学习率 Backbone 为 $1\times10^{-5}$，其余部分为 $1\times10^{-4}$。
*   **推理效率**：UETrack-B 在 GPU 上达到 163 FPS，在 AGX 上达到 60 FPS。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，不仅在 12 个数据集上验证了性能，还针对 TP-MoE 的专家数量、插入层位置、局部聚合的必要性以及 TAD 的各个组件进行了 **13 组消融实验**。
*   **客观性与公平性**：实验覆盖了从服务器级 GPU 到移动端 AGX 的多平台测试，并与最新的 SOTA 方法进行了对比，结果显示其在速度与精度的权衡（Speed-Accuracy Trade-off）上具有显著优势，实验设计客观且充分。

### 6. 主要结论与发现
*   **性能领先**：UETrack-B 在 LaSOT 上达到 69.2% AUC，超越了之前的实时跟踪器 AsymTrack (64.7%)。
*   **多模态通用性**：在 RGB-D、RGB-T 和 RGB-E 任务中，UETrack 在保持实时速度的同时，精度接近甚至超过了某些非实时的重型模型。
*   **效率提升**：相比于统一建模的基准 SUTrack-T，UETrack-B 在 AGX 上的运行速度快了 1.8 倍，且精度更高。

### 7. 优点与亮点
*   **极高的实用性**：是首个在嵌入式设备（AGX）上实现多模态（如 RGB-D/T/E）实时运行的高性能框架。
*   **创新的 TP-MoE**：巧妙地解决了传统 MoE 在视觉任务中的推理延迟问题，通过软分配实现了高效的专家协作。
*   **自适应蒸馏**：TAD 策略通过 Adaptive Net 智能过滤教师模型的错误指导，提升了轻量级学生的学习质量。

### 8. 不足与局限
*   **语言模态的微小差距**：在某些纯语言引导的指标上（如 TNL2K 的 AUC），UETrack-B 虽远快于教师模型，但精度略低于教师模型（58.0% vs 60.9%），说明轻量化对复杂语义理解仍有一定影响。
*   **模态融合深度**：虽然采用了统一 Token 处理，但对于极度异构的模态（如事件流与静态图像），简单的通道拼接是否为最优解仍有探讨空间。
*   **应用限制**：虽然在 AGX 上表现优异，但对于算力更低的单片机或超低功耗芯片，其 Transformer 架构可能仍存在内存带宽瓶颈。

（完）
