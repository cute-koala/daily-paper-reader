---
title: Sparse-Dense Mixture of Experts Adapter for Multi-Modal Tracking
title_zh: 用于多模态跟踪的稀疏-稠密专家混合适配器
authors: "Yabin Zhu, Jianqi Li, Chenglong Li, Jiaxiang Wang, Chengjie Gu, Jin Tang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13719v1"
tags: ["query:vot"]
score: 9.0
evidence: 使用MoE适配器的多模态跟踪
tldr: 针对多模态跟踪中跨模态异构性导致统一框架难以有效表征特征的问题，本文提出了一种稀疏-密集混合专家适配器（SDMoEA）框架。该框架通过SDMoE模块分别建模模态特定和共享信息，并引入基于Gram矩阵的语义对齐超图融合（GSAHF）模块来捕捉高阶相关性。实验证明，该方法在LasHeR、RGBT234等多个主流基准上均优于现有的参数高效微调（PEFT）方法，实现了高效且精准的多模态目标跟踪。
selection_source: fresh_fetch
motivation: 现有的参数高效微调方法在统一框架下难以有效处理多模态特征的异构性及高阶相关性建模问题。
method: 提出SDMoEA框架，利用稀疏与密集混合专家模块分别提取模态特定与共享特征，并结合语义对齐超图融合模块实现深层信息整合。
result: 在LasHeR、RGBT234、VTUAV等多个多模态跟踪基准测试中，该方法的性能均优于现有的PEFT方法。
conclusion: SDMoEA通过创新的混合专家适配器与超图融合机制，为多模态跟踪提供了一种高效且强大的参数微调方案。
---

## 摘要
参数高效微调 (PEFT) 技术（如提示和适配器）在多模态跟踪中得到了广泛应用，因为它们缓解了全模型微调带来的问题，包括时间效率低、资源消耗高、参数存储负担重以及灾难性遗忘。然而，由于跨模态异质性，大多数现有的基于 PEFT 的方法难以在具有共享参数的统一框架内有效表示多模态特征。为了解决这一问题，我们提出了一种新型的稀疏-稠密专家混合适配器 (SDMoEA) 框架，用于统一模型结构下基于 PEFT 的多模态跟踪。具体而言，我们设计了一个 SDMoE 模块作为多模态适配器，以高效地建模模态特定信息和共享信息。SDMoE 由稀疏 MoE 和稠密共享 MoE 组成：前者捕获模态特定信息，而后者建模共享的跨模态信息。此外，为了克服现有跟踪方法在多级多模态融合过程中建模高阶相关性的局限性，我们引入了基于 Gram 矩阵的语义对齐超图融合 (GSAHF) 模块。该模块首先利用 Gram 矩阵进行跨模态语义对齐，确保构建的超图能够准确反映模态间的语义相似性和高阶依赖关系。随后，对齐后的特征被集成到超图结构中，以利用其建模高阶关系的能力，从而实现多级多模态信息的深度融合。大量实验表明，与其它 PEFT 方法相比，所提方法在 LasHeR、RGBT234、VTUAV、VisEvent、COESOT、DepthTrack 和 VOT-RGBD2022 等多个多模态跟踪基准测试中均取得了优越的性能。

## Abstract
Parameter-efficient fine-tuning (PEFT) techniques, such as prompts and adapters, are widely used in multi-modal tracking because they alleviate issues of full-model fine-tuning, including time inefficiency, high resource consumption, parameter storage burden, and catastrophic forgetting. However, due to cross-modal heterogeneity, most existing PEFT-based methods struggle to effectively represent multi-modal features within a unified framework with shared parameters. To address this problem, we propose a novel Sparse-Dense Mixture of Experts Adapter (SDMoEA) framework for PEFT-based multi-modal tracking under a unified model structure. Specifically, we design an SDMoE module as the multi-modal adapter to model modality-specific and shared information efficiently. SDMoE consists of a sparse MoE and a dense-shared MoE: the former captures modality-specific information, while the latter models shared cross-modal information. Furthermore, to overcome limitations of existing tracking methods in modeling high-order correlations during multi-level multi-modal fusion, we introduce a Gram-based Semantic Alignment Hypergraph Fusion (GSAHF) module. It first employs Gram matrices for cross-modal semantic alignment, ensuring that the constructed hypergraph accurately reflects semantic similarity and high-order dependencies between modalities. The aligned features are then integrated into the hypergraph structure to exploit its ability to model high-order relationships, enabling deep fusion of multi-level multi-modal information. Extensive experiments demonstrate that the proposed method achieves superior performance compared with other PEFT approaches on several multi-modal tracking benchmarks, including LasHeR, RGBT234, VTUAV, VisEvent, COESOT, DepthTrack, and VOT-RGBD2022.

---

## 论文详细总结（自动生成）

### 论文总结：用于多模态跟踪的稀疏-稠密专家混合适配器 (SDMoEA)

#### 1. 核心问题与研究动机
*   **核心问题**：多模态目标跟踪（如 RGB+红外、RGB+事件、RGB+深度）面临**跨模态异构性**挑战。现有的参数高效微调（PEFT）方法（如 Prompt 或 Adapter）在统一框架下难以平衡“模态特定信息”与“模态共享信息”的表征，且往往忽略了多级特征融合中的**高阶相关性**。
*   **研究背景**：全参数微调（FFT）虽然性能强，但存在计算资源消耗大、存储负担重及灾难性遗忘等问题。因此，如何在保持参数高效的同时，实现多模态特征的深度融合与精准表征是当前的研究重点。

#### 2. 方法论
论文提出了 **SDMoEA** 框架，其核心由两个创新模块组成：
*   **稀疏-稠密混合专家适配器 (SDMoE)**：
    *   **稀疏 MoE (Sparse MoE)**：包含 $N$ 个专家，通过路由机制为每个 Token 选择 $K=1$ 个专家。主要用于捕捉不同模态的特定（Specific）特征，在不显著增加计算量的情况下扩展模型容量。
    *   **稠密共享 MoE (Dense-Shared MoE)**：采用创新的**串行-并行结构**。所有 Token 先经过共享的降维层，再进入多个并行的轻量级子网络（专家），最后通过共享的升维层。这种设计比传统的并行结构更节省资源，且能有效提取跨模态的共享（Shared）特征。
*   **基于 Gram 矩阵的语义对齐超图融合 (GSAHF)**：
    *   **语义对齐**：利用 Gram 矩阵计算模态内的基向量，将异构模态特征映射到统一语义空间，缩小模态间隙。
    *   **超图建模**：将对齐后的多级特征构建为超图（Hypergraph），利用超图卷积（HyperConv）建模特征之间复杂的高阶依赖关系，增强特征的鲁棒性。

#### 3. 实验设计
*   **数据集**：涵盖了三大主流多模态任务，共 7 个数据集：
    *   **RGB-T (红外)**：LasHeR, RGBT234。
    *   **RGB-E (事件)**：VisEvent, COESOT。
    *   **RGB-D (深度)**：DepthTrack, VOT-RGBD2022。
*   **Benchmark 与对比方法**：
    *   对比了最新的 PEFT 方法（如 ViPT, ProTrack, SDSTrack, XTrack, OneTracker 等）。
    *   对比了全微调（FFT）方法（如 SUTrack, FlexTrack, APTrack 等）。
    *   基准模型采用了 ODTrack (基于 ViT-B 和 ViT-L)。

#### 4. 资源与算力
*   **硬件**：使用 **2 台 NVIDIA A800 GPU** 进行训练。
*   **训练配置**：
    *   ViT-B 版本：Batch Size 为 8，训练 35 个 Epoch。
    *   ViT-L 版本：Batch Size 为 4，训练 40 个 Epoch。
*   **效率指标**：ViT-B 版本的参数量约为 118.28M，计算量为 189.47 GFLOPs，在 A800 上推理速度约为 **19.3 FPS**。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个大型数据集上进行了详尽的定量评估，并提供了 PR（精度）、SR（成功率）、AUC、EAO 等多维度指标。
*   **消融实验**：针对 SDMoE、GSAHF、多级融合等组件进行了 12 组对比实验（见表 V），并探讨了 MoE 插入层数、稀疏 vs 稠密专家的贡献。
*   **充分性评价**：实验设计非常充分，涵盖了消融分析、参数量/计算量分析、定性可视化（特征图对比）以及极端场景下的鲁棒性测试，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：SDMoEA 在所有测试的 PEFT 方法中均达到了 SOTA（最先进）水平。
*   **超越全微调**：在某些数据集（如 LasHeR）上，SDMoEA 的 PEFT 版本甚至优于部分全微调方法；而其全微调版本（SDMoEA*）则刷新了多个榜单的最高纪录。
*   **共享特征的重要性**：消融实验发现，相比于稀疏专家，**稠密共享专家**对性能的提升更为显著，说明挖掘模态间的共性是多模态任务的关键。

#### 7. 优点与亮点
*   **结构创新**：SDMoE 的“串行-并行”设计巧妙地解决了多专家带来的计算冗余问题。
*   **高阶建模**：引入超图理论处理多模态融合，比传统的简单拼接或注意力机制更能捕捉复杂的语义关联。
*   **通用性强**：一个统一的框架和参数配置即可适配 RGB-T/E/D 三种不同的模态组合，无需为每种任务单独训练模型。

#### 8. 不足与局限
*   **实时性挑战**：虽然是参数高效微调，但 19.3 FPS 的速度在某些高性能实时要求（如 30+ FPS）的嵌入式场景下仍显吃力。
*   **参数增量**：相比于极简的 Adapter（如仅增加 1-2M 参数），该方法引入了约 20M 的额外参数，虽然相对于 Backbone 较小，但在极度受限的存储环境下仍有优化空间。
*   **模态缺失处理**：论文主要讨论了双模态齐全的情况，未深入探讨在某一模态突然丢失（如红外传感器故障）时的模型退化表现。

（完）
