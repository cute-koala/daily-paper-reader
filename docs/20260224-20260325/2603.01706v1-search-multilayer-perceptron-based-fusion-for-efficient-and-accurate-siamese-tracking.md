---
title: Search Multilayer Perceptron-Based Fusion for Efficient and Accurate Siamese Tracking
title_zh: 基于搜索的多层感知机融合的高效准确孪生网络跟踪
authors: "Tianqi Shen, Huakao Lin, Ning An"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01706v1"
tags: ["query:vot"]
score: 9.0
evidence: 基于 MLP 融合的高效孪生跟踪
tldr: 针对Siamese视觉跟踪器在资源受限硬件上难以平衡像素级交互效率与准确性的问题，本文提出一种基于多层感知机（MLP）的融合模块。通过构建分层搜索空间并利用可微分神经架构搜索（DNAS）解耦通道宽度与深度的优化，自动构建出低复杂度的跟踪架构。该方法在多个通用及航拍基准上达到了最先进的性能，且在GPU和NPU上均能实现实时运行。
selection_source: fresh_fetch
motivation: 现有的卷积或Transformer融合机制在资源受限硬件上难以高效实现像素级交互，导致准确性与效率失衡。
method: 重新设计Siamese颈部网络，引入MLP融合模块，并利用改进的DNAS策略在分层搜索空间中自动优化通道宽度与深度。
result: 该跟踪器在四个通用和三个航拍基准测试中表现优异，同时在GPU和NPU上保持了出色的实时性能。
conclusion: 通过MLP融合与神经架构搜索的结合，成功实现了高精度与高效率并存的Siamese跟踪系统。
---

## 摘要
孪生视觉跟踪器近期通过基于卷积或 Transformer 架构的日益复杂的融合机制取得了显著进展。然而，这两类架构在资源受限的硬件上都难以高效地实现像素级交互，导致准确率与效率之间长期存在失衡。受此局限性启发，我们使用一种简单而有效的基于多层感知机（MLP）的融合模块重新设计了孪生网络颈部（neck），该模块能以极小的结构开销实现像素级交互。尽管如此，简单地堆叠 MLP 块会引入新的挑战：计算成本会随通道宽度呈二次方增长。为克服这一问题，我们构建了一个由精心设计的 MLP 模块组成的分层搜索空间，并引入了一种定制的松弛策略，使微分神经架构搜索（DNAS）能够将通道宽度优化与其他架构选择解耦。这种针对性的解耦能够自动平衡通道的宽度与深度，从而产生一种低复杂度的架构。实验结果表明，该跟踪器实现了最先进的准确率与效率权衡。它在四个通用和三个航空跟踪基准测试中表现优异，同时在资源受限的图形处理单元（GPU）和神经网络处理单元（NPU）上均保持了实时性能。

## Abstract
Siamese visual trackers have recently advanced through increasingly sophisticated fusion mechanisms built on convolutional or Transformer architectures. However, both struggle to deliver pixel-level interactions efficiently on resource-constrained hardware, leading to a persistent accuracy-efficiency imbalance. Motivated by this limitation, we redesign the Siamese neck with a simple yet effective Multilayer Perception (MLP)-based fusion module that enables pixel-level interaction with minimal structural overhead. Nevertheless, naively stacking MLP blocks introduces a new challenge: computational cost can scale quadratically with channel width. To overcome this, we construct a hierarchical search space of carefully designed MLP modules and introduce a customized relaxation strategy that enables differentiable neural architecture search (DNAS) to decouple channel-width optimization from other architectural choices. This targeted decoupling automatically balances channel width and depth, yielding a low-complexity architecture. The resulting tracker achieves state-of-the-art accuracy-efficiency trade-offs. It ranks among the top performers on four general-purpose and three aerial tracking benchmarks, while maintaining real-time performance on both resource-constrained Graphics Processing Units (GPUs) and Neural Processing Units (NPUs).

---

## 论文详细总结（自动生成）

### 论文总结：基于搜索的多层感知机融合的高效准确孪生网络跟踪 (SEAT)

#### 1. 核心问题与整体含义（研究动机和背景）
视觉目标跟踪领域中，孪生网络（Siamese Networks）是主流架构。然而，现有的特征融合机制（Neck部分）面临**准确性与效率的失衡**：
*   **CNN融合**：虽然在硬件上运行快，但属于补丁级（patch-level）交互，难以捕捉精细的像素级相似性。
*   **Transformer融合**：实现了像素级（pixel-level）交互，精度高，但在资源受限的嵌入式硬件（如NPU）上计算开销巨大，难以实时。
*   **研究动机**：利用多层感知机（MLP）的全连接特性实现高效的像素级融合，并通过神经架构搜索（NAS）解决MLP堆叠带来的计算量随通道数二次方增长的问题。

#### 2. 方法论：核心思想与关键技术
本文提出了 **SEAT** 跟踪器，其核心在于重新设计的 MLP 融合颈部网络及配套的搜索算法：
*   **粗略融合模块 (CFM)**：作为预融合层，通过矩阵乘法和点对点加法初步整合模板和搜索区域特征。理论证明其效果类似于 Transformer 中的交叉注意力机制，但计算更简洁。
*   **精细融合模块 (RFM)**：由多个 **Wave-MLP** 块组成，利用相位感知令牌混合（PATM）建模令牌间的全局依赖。
*   **分层搜索空间 (MCAS)**：
    *   将搜索空间划分为不同的“协调块”（Harmonization Blocks），每个块固定不同的通道数（如256, 320, 384）。
    *   **Harmony-Relaxation 策略**：引入两组松弛参数 $\alpha$（选择块内操作，如卷积核大小、膨胀率）和 $\beta$（选择块间连接路径，即优化通道宽度）。
*   **优化策略**：采用三阶段训练法（冻结权重预热、解冻权重预热、权重与架构参数交替优化），并针对 $\alpha$ 和 $\beta$ 使用不同的优化器（SGD 和 Adam）以实现解耦优化。

#### 3. 实验设计
*   **通用基准测试**：GOT-10k, OTB2015, VOT2019, NFS30。
*   **航拍/无人机基准测试**：UAV123 (含UAV10FPS, UAV20L), UAVDT, VISDRONE。
*   **对比方法**：
    *   轻量级GPU跟踪器：LightTrack, FEAR, E.T.Track。
    *   高性能跟踪器：TransT, AutoMatch。
    *   航拍专用跟踪器：SiamAPN++, HiFT, TCTrack。
*   **变体模型**：针对 GPU 优化的 **SEAT_LT**（使用 LightTrack 主干）和针对 NPU 优化的 **SEAT_AL**（使用 AlexNet 主干）。

#### 4. 资源与算力
*   **训练硬件**：使用了 **4 张 NVIDIA RTX 3090 GPU**。
*   **训练时长**：搜索阶段训练 48 个 epoch，搜索完成后重新训练（Retrain）48 个 epoch。
*   **部署测试**：在 **地平线 (Horizon) X3 芯片**（低功耗嵌入式 NPU 平台）上进行了实测。
*   **模型复杂度**：SEAT_LT 约为 12.28M 参数，6.01G FLOPs。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 7 个主流公开数据集，进行了大量的定量对比。
*   **消融实验**：对比了 CFM 与卷积/Transformer 预融合的效果，以及 RFM 中不同块类型的性能差异，验证了“由粗到精”融合框架的有效性。
*   **真实场景测试**：在无人机拍摄的行人、巴士等真实序列上进行了 CLE（中心位置误差）分析。
*   **客观性**：通过 DNAS 自动搜索架构，减少了人工调参的偏见；在不同硬件（GPU vs NPU）上分别测试，体现了方法的通用性。

#### 6. 主要结论与发现
*   **性能领先**：SEAT 在多个基准上达到了轻量级跟踪器的 SOTA 水平（如 OTB2015 成功率 69.7%，VOT2019 EAO 0.340）。
*   **硬件友好**：在嵌入式 NPU 上，SEAT_AL 达到了 20.41 FPS，显著优于基于 ViT 的跟踪器（如 TCTrack 仅 0.71 FPS）。
*   **架构发现**：通过 DNAS 发现，自动平衡通道宽度与深度比单纯增加深度更能提升 MLP 架构的效率。

#### 7. 优点（亮点）
*   **创新性**：首次将 MLP 架构作为 Siamese 跟踪器的核心融合机制，并证明了其在像素级交互上的潜力。
*   **搜索策略**：提出的 Harmony-Relaxation 策略成功解决了 DNAS 在处理离散通道数搜索时的困难，实现了通道宽度与操作类型的解耦优化。
*   **实用性强**：不仅关注学术指标，还针对边缘计算设备（NPU）进行了深度优化和实测。

#### 8. 不足与局限
*   **光照敏感性**：在 VOT2019 的属性分析中，SEAT 在“光照变化”属性下的表现略逊于部分竞争对手，这可能与 Wave-MLP 的振幅分支对像素强度敏感有关。
*   **搜索成本**：虽然推理高效，但 DNAS 的搜索过程（训练超网络）相比进化算法（ENAS）需要更多的显存和计算时间。
*   **主干依赖**：目前仍依赖预定义的轻量级主干（如 AlexNet），尚未实现主干与颈部的全端到端联合搜索。

（完）
