---
title: Tube MPC for Bilinear Koopman Models using Robust Control Contraction Metrics
title_zh: 基于鲁棒控制收缩度量的双线性库普曼模型的管式模型预测控制
authors: "Thomas de Jong, Mircea Lazar"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29538v1"
tags: ["query:world-model"]
score: 9.0
evidence: 针对学习Koopman模型的鲁棒管式MPC，直接对应基于学习世界模型的预测控制
tldr: 针对非线性系统数据驱动Koopman模型与真实动力学间存在失配的问题，本文提出一种鲁棒tube模型预测控制（MPC）框架。核心是推导双线性Koopman模型的离散时间鲁棒控制收缩度量（RCCM），并采用神经网络在稀疏采样状态下学习度量与反馈控制器，以可扩展方式合成证书。利用该证书构造带收紧约束的tube MPC，证明其递推可行、鲁棒约束满足且闭环系统对失配具有输入到状态稳定性（ISS）。通过利用双线性模型的线性参数变化结构，MPC问题对固定调度序列保持凸性；测地线计算经Chebyshev伪谱离散化转为序列二次规划。在非线性摆基准上验证了框架的有效性，为高维提升状态上的鲁棒数据驱动控制提供了可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.29538v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1882, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 562, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1755, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 850, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 854, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 858, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 692, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 916, \"height\": 883, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.29538v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 641, \"height\": 129, \"label\": \"Table\"}]"
motivation: Koopman模型与实际系统存在失配，需要鲁棒控制框架以保证闭环稳定性与约束满足。
method: 提出离散时间RCCM并神经网络参数化，构造tube MPC；利用双线性LPV结构使优化凸化，测地线经伪谱离散化转为QP。
result: 证明递推可行性、鲁棒约束满足及ISS稳定性，并在非线性摆基准上仿真验证。
conclusion: 将鲁棒收缩度量与数据驱动Koopman模型结合，为高维提升空间的鲁棒MPC提供了可扩展且可证明的方法。
---

## 摘要
本文提出了一种针对由数据辨识得到的双线性库普曼模型所表示的非线性系统的鲁棒管式模型预测控制（MPC）框架。我们为双线性库普曼模型推导了离散时间的鲁棒控制收缩度量（RCCMs），其验证了一种收缩性质，该性质显式地考虑了库普曼模型与真实动力学之间的失配。为了对通常高维的提升状态综合此类证书，我们提出了一种可扩展的学习方法，该方法在提升状态空间的稀疏采样子集上训练度量的神经网络参数化以及相关的反馈控制器。所得的证书用于构建具有收紧约束的鲁棒管式MPC方案，并针对该方案建立了递归可行性、鲁棒约束满足以及闭环系统相对于库普曼模型与真实动力学失配的输入到状态稳定性（ISS）。通过利用双线性库普曼模型的线性参数变化结构，对于固定的调度序列，MPC问题变为凸问题，而基于收缩反馈的测地线计算则通过切比雪夫伪谱离散化重构为一系列二次规划。该完整框架在具有状态相关输入增益的非线性摆基准上进行了验证。

## Abstract
This paper presents a robust tube model predictive control (MPC) framework for nonlinear systems represented by bilinear Koopman models identified from data. We derive discrete-time robust control contraction metrics (RCCMs) for bilinear Koopman models, which certify a contraction property that explicitly accounts for the mismatch between the Koopman model and the true dynamics. To synthesize such certificates for the typically high-dimensional lifted state, we propose a scalable learning approach that trains neural network parameterizations of the metric and of an associated feedback controller over a sparsely sampled subset of the lifted state space. The resulting certificates are used to construct a robust tube MPC scheme with tightened constraints, for which we establish recursive feasibility, robust constraint satisfaction and input-to-state stability (ISS) of the closed-loop system with respect to the mismatch between the Koopman model and the true dynamics. By exploiting the linear parameter-varying structure of bilinear Koopman models, the MPC problem becomes convex for a fixed scheduling sequence, while the geodesic computation underlying the contractive feedback is reformulated, via a Chebyshev pseudospectral discretization, as a sequence of quadratic programs. The complete framework is validated on a nonlinear pendulum benchmark with a state-dependent input gain.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：模型预测控制（MPC）是少数能系统化处理硬状态/输入约束的控制方法，但其实现高度依赖足够精确的预测模型。在很多实际应用中，基于第一性原理（first-principles）建模成本高昂或不切实际，因此从数据中直接学习预测模型成为自然选择。
- **Koopman 理论的吸引力**：Koopman 算子理论通过一组可观测量（observables）将非线性系统的状态提升到高维空间，使动力学在该空间中（近似）线性演化，从而可将线性系统理论工具应用于非线性系统。对于控制仿射非线性系统，双线性 Koopman 实现（bilinear Koopman realization）尤为重要——因为一般不存在带输入的精确线性提升实现，而双线性实现能以任意精度逼近控制仿射动力学。
- **核心难点——模型失配**：实际中，有限数据和有限维提升映射必然导致建模误差，因此 Koopman 模型应被视为不确定模型，需要鲁棒 MPC 设计。对于线性 Koopman 实现已有鲁棒管式 MPC 等方案；但对于双线性实现，能同时提供递归可行性、鲁棒约束满足和真实非线性系统闭环保证的鲁棒 MPC 方案仍然稀缺，部分原因在于提升状态的高维性使经典鲁棒不变集构造不可行。
- **现有收缩理论的不足**：现有的基于收缩的管式 MPC 方案主要针对连续时间动力学，依赖 SOS 规划或网格化 LMI 松弛，且离散时间公式只适用于低维状态。这些方法均不能直接移植到 Koopman 模型：提升状态维度高（排除 SOS 综合），离散时间设置又在连续时刻间耦合度量（使综合和在线测地线计算复杂化）。
- **本文目标**：针对数据驱动的双线性 Koopman 模型，开发一种直接在高维提升状态空间内表述的、基于离散时间鲁棒控制收缩度量（RCCM）的管式 MPC 框架，并建立递归可行性、鲁棒约束满足和输入到状态稳定性（ISS）保证。

## 2. 论文提出的方法论

### 2.1 整体框架架构

- 方法流程：**数据采集 → 双线性 Koopman 模型学习（提升函数 + 模型矩阵联合训练）→ 神经网络收缩度量与反馈控制器学习 → 鲁棒管式 MPC（含收紧约束与终端件）→ 在线测地线计算与收缩反馈矫正**。
- 完整闭环：测量真实状态 xₖ → 经训练提升函数 φ̄_θ* 映射为提升状态 zₖ → 求解 MPC 获得标称轨迹（ż*₀|ₖ, ū*₀|ₖ）→ 求解测地线 γ₀|ₖ(s) → 计算收缩反馈修正 κ(zₖ, ż*₀|ₖ) → 施加实际控制 uₖ = ū*₀|ₖ + κ。

### 2.2 双线性 Koopman 模型学习

- 提升状态定义为 zₖ := φ̄_θ(xₖ) = col(xₖ, f_θ(xₖ))，原始状态被显式包含在提升向量中。
- 有限维双线性 Koopman 模型：
  zₖ₊₁ = Azₖ + B₀uₖ + Σᵢ₌₁^nᵤ [uₖ]ᵢBᵢzₖ + wₖ
  其中 wₖ 表示真实提升动力学与双线性逼近之间的失配。
- **Problem 1**：联合训练提升函数 φ̄_θ（神经网络 f_θ）和模型矩阵 {A, B₀, …, B_nᵤ}，最小化 N 步多步预测归一化误差，并加入正则化项（权重范数 + 矩阵 Frobenius 范数）。训练使用 50000 个采样点，预测视界 N=30。

### 2.3 鲁棒控制收缩度量（RCCM）理论

- 研究对象：不确定非线性模型 xₖ₊₁ = f̂(xₖ, uₖ) + wₖ，其中 wₖ ∈ W 为有界建模误差（假设 0 ∈ W 且 W 紧致）。
- **鲁棒收缩性质**（核心不等式）：
  E_γₖ₊₁(x̄ₖ₊₁, xₖ₊₁) ≤ (1-α)E_γₖ(x̄ₖ, xₖ) + ν‖wₖ - w̄ₖ‖²₂
  其中 E 为黎曼能量（由度量 M 定义），α ∈ (0,1) 为收缩率，ν > 0 为扰动增益。
- **Lemma 1（欧氏误差界）**：由收缩性质可推出任意两条轨迹之间的欧氏距离满足：
  ‖xₖ₊ₙ - x̄ₖ₊ₙ‖₂ ≤ √(α₂/α₁)(1-α)^(N/2)‖xₖ - x̄ₖ‖₂ + √(ν/α₁)·(δ̄/(1-√(1-α)))
  即轨迹偏差由初始距离的 KL 函数项加扰动增益项界定——这正是管式 MPC 所需的"管"的宽度依据。
- **Theorem 1**：若 Assumption 1 成立（存在度量 W = M⁻¹、控制器 L、收缩率 α、扰动增益 ν 满足 LMI (16)），则系统满足鲁棒收缩性质。

### 2.4 针对双线性 Koopman 模型的收缩不等式

- 利用双线性结构，微分动力学变为：
  ∂cₖ₊₁/∂s = (A*(vₖ(s)) + B*(cₖ(s))K(cₖ(s)))·∂cₖ/∂s + wₖ - w̄ₖ
  其中 A*(v) = A + Σᵢ vᵢBᵢ，B*(c) = [B₀, B₁c, …, B_nᵤc]。
- **关键洞察**：对任意固定的状态 zₖ 和输入 uₖ，不等式 (28) 是关于度量 W(z) 和控制器 L(z) 取值的**凸 LMI**。但综合问题仍是无限维的（需对所有 z、u、w 成立）。

### 2.5 神经网络收缩度量学习（可扩展综合方案）

- **稀疏采样策略**：提升状态集 Z 定义为状态约束集在提升函数下的像，通常只占据提升空间 R^nz 的一小部分（低维流形），因此可在稀疏采样的子集上学习证书。具体做法是：生成覆盖 X、U、W 的虚拟数据集，对每个采样点计算提升状态，并额外生成**离流形扰动样本**（加小扰动 ϵ），以适应 MPC 优化初始条件偏离流形的需求。
- **Problem 2（度量学习）**：度量参数化为 W_θw(z) = L_θ(z)L_θ(z)ᵀ + γI（下三角矩阵 + 正定偏移，保证对称正定）；控制器 L_θℓ 同样用神经网络参数化。损失函数使用 softplus 函数惩罚 Mᵢⱼ 的负特征值（促进正定性），并加入条件数正则化项 L_cond（最小化条件数以缩小管宽度）。
- **算法 1**：离线神经网络收缩度量学习流程（生成虚拟数据 → 提升与扰动 → 模型传播 → 神经网络参数化 → 求解 Problem 2）。

### 2.6 鲁棒管式 MPC 方案

- **LPV 嵌入**：将零扰动标称动力学写为精确 LPV 形式 z̄ₖ₊₁ = A*z̄ₖ + B̄*(ρₖ)ūₖ，调度变量 ρₖ = z̄ₖ。由此得到多步预测的线性表达式 z̄ₖ = Φz̄₀|ₖ + Γ(ρₖ)ūₖ。
- **Problem 3（鲁棒 MPC）**：目标函数为标准二次阶段/终端代价；约束包括：动力学等式、初始条件约束 ‖z₀|ₖ - z*₁|ₖ₋₁‖₂ ≤ w̄（保证优化初始条件序列是真实不确定模型的合法轨迹，是递归可行性的关键）、收紧的输入/状态约束（h_u + c_uδ_max ≤ 0、h_z + c_zδ_max ≤ 0）、终端约束 h_T(z) ≤ 0。
- **管宽 δ_max** = √ν·δ̄/(1-√(1-α))（几何级数和），收紧常数 c_z、c_u 由约束函数梯度和度量界计算（公式 (42)）。
- **固定调度序列下 MPC 为凸问题**：对固定 ρₖ，Problem 3 是凸的（二次规划），可通过迭代更新调度序列（Algorithm 2 中 N_mpc 次迭代）求解。
- **测地线计算**：通过 Chebyshev 伪谱离散化将测地线优化问题（Problem 4）转化为序列二次规划（Problem 5，每次迭代为 QP），利用 Kronecker 积性质线性化。

### 2.7 理论保证

- **Theorem 2（鲁棒约束满足）**：若初始 z*₀|₀ = z₀ 且 Problem 3 可行，则闭环轨迹满足 z_fᵢ|ₖ ∈ Z、u_fᵢ|ₖ ∈ U。
- **Theorem 3（递归可行性）**：在 Assumption 1、2 下，若 k 时刻可行则 k+1 时刻可行（通过移位轨迹 + 终端控制器构造候选解）。
- **Theorem 4（ISS 稳定性）**：
  (i) 优化初始条件轨迹渐近稳定（lim ‖z*₀|ₖ - r_z‖ = 0）；
  (ii) 闭环系统 (2)-(44) 关于模型失配 eₖ := w⁰ₖ - wˢₖ 是 ISS 的，即存在 β̃ ∈ KL、γ̃ ∈ K∞ 使 ‖xₖ - rₓ‖₂ ≤ β̃(‖x₀ - rₓ‖₂, k) + γ̃(‖e[0,k-1]‖∞)。
- **ISS 的含义与强度**：这是比 ISpS（输入到状态实用稳定性）和集合收敛更强的性质——若失配渐近消失则状态渐近收敛到参考点；而许多既有管式 MPC 方案只能证明收敛到目标邻域。该结果也作为副产品为线性 Koopman 管式 MPC 提供了 ISS 保证。

## 3. 实验设计

- **Benchmark**：具有状态相关输入增益的非线性摆（水平力驱动而非直接力矩）：
  ẋ₁ = x₂
  ẋ₂ = -(g/l)sin(x₁) - d/(ml²)x₂ + cos(x₁)/(ml²)u
  参数：m=1, l=1, g=9.81, d=0.3；采样时间 Tₛ = 1/30s，四阶 Runge-Kutta 离散化。
  - 该 benchmark 的非线性输入耦合增加了系统辨识难度，适合验证双线性 Koopman 模型的建模能力。
- **模型辨识数据**：分段常数输入 + 随机抖动信号激励，共 Nₛ = 50000 个样本；输入范围约 ±20，状态覆盖 x₁ ∈ [-1,1]、x₂ ∈ [-2,2] 附近。
- **Koopman 模型训练（Problem 1）**：预测视界 N=30，40000 轮训练；最大建模误差 max‖wₖ‖₂ < 0.01，据此设定扰动集 W = {w : ‖w‖₂ ≤ 0.01}。
- **度量学习（Problem 2）**：虚拟数据集 N_M =
