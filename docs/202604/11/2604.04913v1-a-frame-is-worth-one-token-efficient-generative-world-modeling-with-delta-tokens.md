---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一令：基于增量令牌的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:profile-4"]
score: 10.0
evidence: 在视觉基础模型特征空间中使用增量标记的生成式世界模型
tldr: 本文针对视频世界建模中未来状态预测的多样性挑战，提出了一种高效的生成式世界模型框架。传统方法要么是确定性预测缺乏多样性，要么计算成本高昂。作者创新性地开发了DeltaTok标记器，将连续帧间的视觉基础模型特征差异编码为紧凑的“delta”标记，从而大幅降低了数据维度。基于此构建的DeltaWorld模型能够进行可处理的多假设训练，并行生成多个未来场景并选择最优结果进行监督。实验表明，该方法在密集预测任务上取得了更贴近真实世界的预测效果，同时模型参数减少了35倍以上，计算量降低了2000倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 现有生成式世界模型计算成本高且多样性有限，需要一种能高效预测多样化未来的方法。
method: 提出DeltaTok将连续帧间特征差异编码为delta token，并构建DeltaWorld模型进行高效多假设未来生成。
result: 在密集预测任务中，DeltaWorld比现有模型参数减少35倍、计算量降低2000倍，同时预测结果更贴近真实。
conclusion: DeltaTok与DeltaWorld通过创新的delta token表示和高效生成建模，显著提升了世界模型的预测多样性与计算效率。
---

## 摘要
预测多样化的未来状态是视频世界建模的核心挑战。判别式世界模型产生一个确定性预测，该预测隐式地平均了可能的未来，而现有的生成式世界模型在计算上仍然昂贵。最近的研究表明，在视觉基础模型的特征空间中预测未来，而非在为像素重建优化的潜在空间中预测，所需的世界模型参数量显著减少。然而，此类方法大多仍是判别式的。在本工作中，我们引入了 DeltaTok（一种分词器），它将连续帧之间的 VFM 特征差异编码为单个连续的“增量”令牌；以及 DeltaWorld（一种生成式世界模型），它对这些令牌进行操作以高效生成多样且合理的未来。增量令牌将视频从三维时空表示简化为一维时间序列，例如对于 512x512 的帧可实现 1,024 倍的令牌缩减。这种紧凑的表示使得可处理的多假设训练成为可能，即并行生成多个未来并仅监督最佳者。在推理时，这能在单次前向传递中产生多样化的预测。在密集预测任务上的实验表明，DeltaWorld 生成的未来预测与真实结果更吻合，同时参数量比现有生成式世界模型少 35 倍以上，且使用的 FLOPs 少 2,000 倍以上。代码与权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：预测未来世界的多样化状态对于自动驾驶等智能体至关重要。现有方法存在两大瓶颈：
    *   **判别式世界模型**（如 DINO-world）：输出单一确定性预测，在不确定性下会坍缩为条件均值，无法捕捉多样化的未来可能。
    *   **生成式世界模型**（如 Cosmos）：虽然能生成多样未来，但通常计算成本极高。它们通常在为像素重建优化的潜在空间中操作，需要多次前向传递（如扩散模型），且建模了大量时空冗余信息。
*   **核心目标**：开发一个**高效且能生成多样化未来**的世界模型。其关键在于利用视频帧间变化通常具有低维结构这一特性，将建模重点从完整的空间特征转移到“变化”本身。

## 2. 方法论
*   **核心思想**：将视频从三维（空间x时间）表示压缩为一维（仅时间）的“增量令牌”序列，并在此紧凑表示上进行高效的生成式建模。
*   **关键技术细节**：
    1.  **DeltaTok (增量令牌化器)**：
        *   输入：一个冻结的视觉基础模型提取的连续两帧特征图 `x_{t-1}` 和 `x_t`。
        *   过程：通过一个基于 Vision Transformer (ViT) 的编码器-解码器架构，将两帧之间的特征差异编码为一个单一的、连续的“增量令牌” `z_t`。
        *   解码：给定前一帧特征 `x_{t-1}` 和增量令牌 `z_t`，解码器可以重建出当前帧特征 `x_t`。
        *   优势：极大地压缩了表示。例如，对于512x512分辨率的帧，VFM会产生1024个空间令牌；而DeltaTok将其压缩为1个令牌，实现了1024倍的缩减。
    2.  **DeltaWorld (生成式世界模型)**：
        *   **架构基础**：基于判别式的 DINO-world 预测器架构进行改造。
        *   **输入/输出**：操作对象不再是空间特征图网格，而是由 DeltaTok 生成的增量令牌序列 `Z_{1:t}`。目标是预测下一个增量令牌 `z_{t+1}`。
        *   **生成机制 - Best-of-Many (BoM)训练**：
            *   训练时：从高斯分布中采样 K 个不同的噪声查询向量 `q_k`。每个查询向量通过预测器并行生成 K 个候选的未来增量令牌。仅对其中与真实未来最接近的那个候选进行监督和反向传播。
            *   推理时：不同的噪声查询会映射到不同的、合理的未来假设上，从而实现单次前向传递即可生成多个多样化样本。

## 3. 实验设计
*   **评估基准**：遵循密集预测基准 ，评估短期和中期预测精度。
*   **主要任务与数据集**：
    *   **语义分割**：VSPW (多种场景)、Cityscapes (驾驶场景)。
    *   **单目深度估计**：KITTI (驾驶场景)。
*   **对比方法**：
    *   **判别式基线**: DINO-world 。
    *   **生成式基线**: Cosmos-4B, Cosmos-12B 。
    *   **上下界**: “复制最后一帧”作为下界，“使用真实未来帧”作为上界。
*   **评估指标**: mIoU (分割)，RMSE (深度)。对于生成式模型，报告“最佳样本”(best-of-K)和“平均样本”(mean-of-K)的性能。

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量和总训练时长等硬件细节。文中提到了部分实验设置：
*   主要结果使用512x512输入分辨率训练了300K次迭代。
*   消融实验使用256x256输入分辨率训练了100K次迭代。
*   批量大小为1024。
*   在效率对比中提及使用了 DeepSpeed FLOPs Profiler 进行浮点运算量测算。

## 5. 实验数量与充分性
实验设计较为系统和充分，主要包括以下几组：
1.  **渐进式构建分析**: （表2）逐步展示了从判别式基线 -> BoM训练 -> Frame压缩 -> Delta压缩的性能、计算量和内存变化趋势，清晰地论证了每个改进步骤的必要性和贡献。
2.  **BoM超参数分析**: （图5）研究了训练时查询数量(K)对最终最佳/平均性能的影响，验证了方法的可扩展性。
3.  **主基准对比**: （表3）在三个不同数据集、两个预测时域上全面对比了所有基线方法，证明了DeltaWorld在性能和效率上的显著优势。
4.  **跨架构验证**: （附录C）将Delta Token思想应用于另一个判别式世界模型DINO-Foresight ，证明了其通用性。
5.  **定性分析**: （图6及附录E）提供了丰富的可视化样例，展示了生成的多样性以及预测质量。

总体而言，实验覆盖了定量性能、计算效率、消融分析和定性验证等多个维度，对比对象选择合理（包括SOTA判别式和生成式方法），评估协议遵循公开基准，具有较好的客观性和公平性。

## 6.主要结论与发现
1.  DeltaTok能够将视频高效地表示为一系列一维的增量令牌序列（例如实现1024倍的token缩减）。
2. DeltaWorld结合DeltaTok和BoM目标函数后成为一个高效的生成式世界模型。它能在单次前向传递中产生多样化的未来假设。
3. DeltaWorld在密集预测任务上取得了优于或媲美现有方法的性能表现。其最佳样本质量显著超过Cosmos等大型生成模型；其平均样本质量也与优化的判别式基线相当甚至更好。（表3）
4. DeltaWorld实现了巨大的效率提升：相比Cosmos等现有生成式世界模型参数量减少35倍以上（0.3B vs >12B），FLOPs降低2000倍以上。（图2）

##7 .优点
1.	创新性强: “一帧即一令”的核心思想非常新颖且直观有效,巧妙地将视频压缩问题转化为对时序变化的建模,大幅降低了问题复杂度.
2	效率极高:通过delta token化和BoM单次前向采样,实现了数量级的计算量下降,使高效的多假设未来生成为可能.
3	方法简洁有效:整体框架清晰,B o M目标函数简单但效果显著,避免了复杂的扩散或自回归采样过程.
4	实验扎实:从渐进构建到全面基准测试再到跨架构验证,实验设计完整且有说服力.

##8 .不足与局限
1	分布建模的理论保证不足:B o M目标缺乏像扩散模型那样明确的概率分布建模连接,其覆盖真实分布的能力受限于训练时探索的查询数量K.
2	误差累积风险:由于采用自回归方式在delta token空间进行多步展开(rollout),编码器和预测器的误差可能在解码重建特征图时逐级累积.
3	应用场景限制:目前工作聚焦于短中期(约0 .6秒)的视频预测,对于更长期的复杂动态模拟能力尚未验证.
4	依赖预训练的VFM:模型的性能依赖于D INOv3等视觉基础模型的表征质量.

(完)

好的，我们继续从“不足与局限”部分之后开始，对论文的其他方面进行总结。

## 9. 潜在影响与未来方向
1.  **高效世界建模的新范式**：论文提出的“增量令牌”思想为视频表示和生成式世界建模开辟了一条高效的新路径，可能启发后续研究在更紧凑的表示空间中进行时序动态建模。
2.  **推动具身智能应用**：计算效率的大幅提升使得在资源受限的边缘设备（如机器人、自动驾驶汽车）上部署能够预测多样化未来的世界模型成为可能，增强了智能体在开放环境中的决策鲁棒性。
3.  **与其他生成框架的结合**：DeltaWorld 的框架（DeltaTok + BoM）具有模块化特点。未来可以探索将 DeltaTok 与其他先进的生成模型（如 latent diffusion models）结合，或在 BoM 训练中引入更复杂的分布匹配目标。
4.  **扩展到更长期、更复杂的场景**：当前工作验证了短中期预测的有效性。一个自然的延伸是测试该方法在更长时序跨度、包含更多复杂交互（如多智能体）的场景下的性能，并研究如何缓解自回归展开中的误差累积问题。

## 10. 总结
本文针对生成式世界模型效率低下的核心问题，提出了一个简洁而强大的解决方案。其核心洞察在于：与其费力地重建每一帧完整的空间信息，不如专注于建模帧与帧之间高效的“变化”（Delta）。通过 **DeltaTok** 将高维时空特征压缩为一维增量令牌序列，并结合 **Best-of-Many (BoM)** 训练策略在令牌空间进行高效的多假设生成，最终形成了 **DeltaWorld** 模型。

该方法在多个密集预测基准测试中达到了领先的性能，同时实现了相比现有生成式方法数百至数千倍的计算效率提升。它成功地在“高效”与“生成多样化未来”这两个以往难以兼得的目标之间取得了突破性的平衡。尽管在理论完备性和长期预测方面存在局限，但这项工作无疑为下一代高效的世界模型树立了一个重要的里程碑。

（完）
