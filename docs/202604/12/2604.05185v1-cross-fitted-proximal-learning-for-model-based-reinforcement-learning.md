---
title: Cross-fitted Proximal Learning for Model-Based Reinforcement Learning
title_zh: 基于模型的强化学习中交叉拟合的近端学习
authors: "Nishanth Venkatesh, Andreas A. Malikopoulos"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05185v1"
tags: ["query:profile-4"]
score: 9.0
evidence: 具有潜因子的序列决策模型强化学习
tldr: 本文研究基于模型的离线强化学习在存在隐藏混杂的部分可观测环境中的统计估计问题。针对奖励-发射和观测-转移桥函数的条件矩限制估计难题，作者提出了一种K折交叉拟合的两阶段估计方法。该方法通过引入条件均值嵌入和条件密度作为干扰对象来表述桥学习问题，相比单一样本分割更高效地利用数据，同时保持了基于桥的识别策略。主要贡献在于开发了交叉拟合扩展程序并推导了其理论误差界。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05185v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of observation-transition recovery\", \"page\": 6, \"index\": 1, \"width\": 512, \"height\": 325}]"
motivation: 在存在隐藏混杂的离线部分可观测马尔可夫决策过程中，直接从观测数据学习的模型可能存在偏差，需要可靠的桥函数估计方法。
method: 提出了一种K折交叉拟合的两阶段桥函数估计器，将桥学习表述为带有条件均值嵌入和条件密度等干扰对象的条件矩限制问题。
result: 推导了交叉拟合估计器的oracle比较界，并将误差分解为由干扰项估计引起的第一阶段误差和由经验平均引起的第二阶段误差。
conclusion: 该方法在保持原有识别策略的同时，更高效地利用数据，并提供了误差分解的理论分析。
---

## 摘要
基于模型的强化学习因其显式地估计奖励和转移模型，并通过模拟推演支持规划，在序列决策中颇具吸引力。然而，在存在隐藏混杂的离线设置中，直接从观测数据学习的模型可能存在偏差。这一挑战在部分可观测系统中尤为突出，因为潜在因素可能共同影响动作、奖励和未来观测。近期研究表明，在此类混杂的部分可观测马尔可夫决策过程（POMDP）中，策略评估可以简化为估计满足条件矩限制（CMR）的奖励-发射和观测-转移桥函数。本文研究这些桥函数的统计估计问题。我们将桥学习表述为一个CMR问题，其干扰对象由条件均值嵌入和条件密度给出。随后，我们开发了现有两阶段桥估计器的$K$折交叉拟合扩展。所提出的方法保留了原有的基于桥的识别策略，同时比单次样本分割更有效地利用可用数据。我们还推导了交叉拟合估计器的oracle比较器界，并将所得误差分解为由干扰估计引起的第一阶段项和由经验平均引起的第二阶段项。

## Abstract
Model-based reinforcement learning is attractive for sequential decision-making because it explicitly estimates reward and transition models and then supports planning through simulated rollouts. In offline settings with hidden confounding, however, models learned directly from observational data may be biased. This challenge is especially pronounced in partially observable systems, where latent factors may jointly affect actions, rewards, and future observations. Recent work has shown that policy evaluation in such confounded partially observable Markov decision processes (POMDPs) can be reduced to estimating reward-emission and observation-transition bridge functions satisfying conditional moment restrictions (CMRs). In this paper, we study the statistical estimation of these bridge functions. We formulate bridge learning as a CMR problem with nuisance objects given by a conditional mean embedding and a conditional density. We then develop a $K$-fold cross-fitted extension of the existing two-stage bridge estimator. The proposed procedure preserves the original bridge-based identification strategy while using the available data more efficiently than a single sample split. We also derive an oracle-comparator bound for the cross-fitted estimator and decompose the resulting error into a Stage I term induced by nuisance estimation and a Stage II term induced by empirical averaging.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Cross-fitted Proximal Learning for Model-Based Reinforcement Learning》的论文进行结构化、深入且客观的总结。

---

### **论文总结：基于模型的强化学习中交叉拟合的近端学习**

#### **1. 核心问题与整体含义**
*   **研究动机**：在现实世界的离线强化学习（RL）场景中（如医疗记录、自动驾驶），数据通常来自一个**部分可观测且存在隐藏混杂**的系统。这意味着存在未观测到的潜在变量，同时影响智能体的决策（动作）和系统的演化（状态转移与奖励）。直接从这种“有偏”的观测数据中学习动态模型，会导致基于模型的规划产生错误结果。
*   **核心问题**：如何在存在隐藏混杂的**部分可观测马尔可夫决策过程（Confounded POMDP）** 中，从离线观测数据中无偏地估计用于策略评估的模型组件？近期研究指出，这可以归结为估计满足特定**条件矩限制（CMR）** 的“桥函数”。本文聚焦于这些桥函数的**统计估计方法本身**，旨在提升现有估计器的样本效率和理论清晰度。

#### **2. 方法论**
*   **核心思想**：将桥函数学习重新表述为一个标准的条件矩限制问题，并引入机器学习中常用的 **“交叉拟合”技术来改进现有的两阶段估计流程**。
*   **关键技术细节**：
    1.  **问题重构**：将奖励-发射桥函数和观测-转移桥函数的估计统一为一个通用框架：寻找函数 `b`，使得 `E[b(W, z) | C] = p(z | C)`。其中 `W` 和 `Z` 是观测变量，`C` 是条件集。这个等式是一个条件矩限制。
    2.  **核表示法**：使用再生核希尔伯特空间理论，将上述条件期望操作转化为内积形式：`E[b(W, z) | C=c] = <μ_{W|C}(c) ⊗ φ(z), b>`。这里 `μ_{W|C}`（条件均值嵌入）和 `p(z|C)`（条件密度）是未知的“干扰对象”。
    3.  **交叉拟合两阶段估计器**：
        *   **第一阶段（干扰项估计）**：将数据分为K折。对于每一折 `k`，使用其余 `K-1` 折的数据来估计该折对应的条件均值嵌入 `μ_{W|C}` 和条件密度 `p(z|C)`。
        *   **第二阶段（桥函数估计）**：对于每一折 `k`，使用第一阶段在该折上训练的干扰项估计量，在留出的第 `k` 折数据上计算一个经验风险（即CMR残差的平方和）。
        *   **聚合**：将所有K折的经验风险平均，并加上对桥函数的正则化项，最小化这个总目标以得到最终的桥函数估计量。
*   **与基线方法的区别**: 基线方法 ([8]) 使用一次性的样本分割（一部分数据用于干扰项估计，另一部分用于桥函数估计），而本文的交叉拟合方法让所有数据都既用于干扰项训练也用于桥函数评估，从而更高效地利用数据并减少对单次随机分割的依赖。

#### **3. 实验设计**
*   **实验场景**: 论文设计了一个简单的、低维的非线性高斯POMDP仿真环境。该系统包含一个隐藏的二值上下文因子。
*   **评估目标**: 专注于评估对第一阶段“观测-转移”桥函数的恢复效果。具体评估的是该桥函数所恢复的去混杂后观测转移的条件均值的均方误差。
*   **对比方法**: 
    *   **基准方法**: “一枪”样本分割法 (50%-50% split)，即原论文[8]的方法。
    *   **本文方法**: K折交叉拟合估计器 (文中未明确K值)。
*   Benchmark: 恢复出的去混杂观测转移的条件均值与真实值的均方误差。

#### **4. 资源与算力**
论文未明确说明实验所使用的具体硬件配置（如GPU型号、数量）、训练时长或计算资源消耗。考虑到其仿真实验相对轻量级（低维非线性系统），很可能是在标准学术计算环境中完成的。

#### **5. 实验数量与充分性**
*   **实验数量**: 
    *   主要进行了单一系列的实验：在不同样本量 (N=120,240,480,960,1920)下比较交叉拟合方法与一次性分割方法的性能。
    *   没有在不同环境结构、不同策略、不同核函数或超参数上进行广泛的消融研究或鲁棒性测试。
*   **充分性与客观性评价**: 
    *   ***有限但聚焦***: 实验旨在核心验证所提方法在提升样本效率方面的基本主张。在这个目标下是充分的。
    *   ***公平性***: 对比是在相同仿真环境和相同评估指标下进行的，基准方法是该方法旨在改进的直接前身[8]，因此对比是公平的。
    *   ***局限性***: 
        1.  缺乏在更复杂、更高维基准环境下的验证。
        2.  没有展示该方法如何最终提升下游任务（如策略优化）的性能。
        3.  实验规模较小，未能全面探索交叉拟合折数(K)的影响。

#### **6. 主要结论与发现**
1.  ****理论贡献****: 
    *   成功地将混杂POMDP中的桥学习问题形式化为一个带有特定干扰对象（条件均值嵌入、条件密度）的条件矩限制问题。
    *   提出了该问题的K折交叉拟合两阶段估计器。
    *   推导了该估计器的Oracle比较误差界 (`Lλ(ˆb_cf) - Lλ(b†_λ)`)，并将总误差清晰地分解为：
        a) **(Stage I)** :由干扰项(η)估计误差引起的项；
        b) **(Stage II)** :由经验平均过程引起的项。这为理解误差来源提供了更精细的理论视角。
2. ****实证发现****: 
    在设计的仿真环境中，交叉拟合方法在所有测试的样本量下都一致地优于一次性样本分割方法 (例如N=960时误差从0.0907降至0.0339)，验证了其在提升样本效率方面的有效性。

#### .7优点
1. ****方法论创新****: 
    将成熟的交叉拟合技术系统地引入到混杂POMDP的模型学习领域，
    提供了一种简单而有效的改进现有方法的途径，
    无需改变底层的识别策略.
2.
****理论清晰度****:
    通过Oracle界和明确的二阶段误差分解，
    增强了方法的统计可解释性，
    为后续更深入的理论分析(如有限样本界)
    奠定了基础.
3.
****实用性****:
    实现相对简单，
    易于集成到现有的基于桥函数的离线MBRL框架中.

#### .8不足与局限
1.
****有限的实证验证****:
    仅在单一、
    简单的合成环境中进行了验证.
    缺乏在标准RL基准或真实世界数据集上的测试，
    其在高维复杂问题中的可扩展性和有效性存疑.
2.
****未涉及下游任务****:
    论文止步于桥函数的准确率，
    没有展示学习到的模型如何最终改善策略评估或策略优化的性能，
    这是MBRL的终极目标.
3.
****未实现正交化/双稳健性****:
作者明确指出，
虽然双机器学习文献表明正交化可以进一步减少一阶段偏差对二阶段的影响，
但本文并未推导出适用于此问题的Neyman正交得分,
这被视为未来工作方向,
因此当前方法可能仍对一阶段干扰项的估计质量较为敏感.
4.
假设依赖性强:
整个框架依赖于关键的假设(如桥梁存在性和完备性),
这些假设在实践中难以验证,
若假设不成立,
则方法的理论基础会受到影响.

---
**(完)**

好的，我将从您提供的输出中断处继续补全。根据您的要求，我将补充论文中可能未充分展开或隐含的其他重要方面，并提供一个更全面的总结收尾。

---

#### **9. 理论贡献的深度剖析**
*   **误差分解的启发性**：论文将总误差分解为“干扰项估计误差”和“经验过程误差”，这一分解不仅具有理论美感，更具有实践指导意义。它清晰地指出性能瓶颈可能来自两个阶段：
    1.  **第一阶段是根本限制**：如果条件均值嵌入 `μ_{W|C}` 或条件密度 `p(z|C)` 难以准确估计（例如在高维、稀疏数据下），那么无论第二阶段如何改进，整体误差的下限都会受到制约。
    2.  **第二阶段是优化目标**：交叉拟合的核心作用在于减少因使用相同数据估计干扰项和桥函数而引入的“过拟合偏差”，从而更高效地逼近给定第一阶段估计质量下的最优解（Oracle解）。
*   **与经典双机器学习的联系与区别**：本文方法在精神上与计量经济学中的“双机器学习”高度相关，都采用交叉拟合和两阶段估计来缓解正则化偏差。然而，关键区别在于本文处理的是**条件矩限制（CMR）**问题，其干扰项是无限维的条件期望算子（`μ_{W|C}`），而非标准的回归或倾向得分函数。这要求使用核方法进行表示，并导致了不同的理论分析路径。

#### **10. 潜在影响与未来方向**
*   **对离线MBRL领域的影响**：本文为解决混杂POMDP中的模型学习问题提供了一个更稳健、样本效率更高的统计工具。它使得基于桥函数的去混杂方法向实用化迈进了一步，特别是在数据收集成本高昂的领域（如医疗、教育）。
*   **启发的未来研究方向**：
    1.  **正交化/双稳健扩展**：如作者所述，推导该特定CMR问题的Neyman正交得分是首要的理论延伸。这将使估计器对第一阶段的慢收敛速率（如非参数率）更加稳健。
    2.  **高维与深度学习集成**：当前框架依赖于核方法。一个自然的扩展是探索如何使用深度神经网络来参数化桥函数和干扰项，并利用现代优化工具进行训练，以处理高维观测空间。
    3.  **端到端策略优化**：将本方法学习到的无偏动态模型集成到完整的MBRL管道中，研究其在策略评估和改进中的最终性能提升，并与模型无关的离线RL方法进行对比。
    4.  **假设检验与敏感性分析**：开发工具来检验“桥函数存在性”等核心假设是否合理，或者当假设轻微违背时（近似可识别性），分析估计结果的敏感性。

#### **11. 总体评价**
本文是一篇扎实的方法论论文，其贡献主要体现在**理论框架的清晰化与统计工具的改进上**。它将一个复杂的强化学习问题优雅地转化为一个结构化的统计学习问题，并成功引入了交叉拟合这一成熟技术来提升估计效率。

*   **优势集中体现在理论与方法的简洁性上**：思路清晰、推导严谨、实现直观。所获得的Oracle误差界为理解问题本质提供了有价值的见解。
*   **主要局限性在于实证验证的广度与深度不足**：实验部分更像是一个“概念验证”（proof-of-concept），而非全面的评估。这在一定程度上削弱了该方法在复杂现实问题中效力的说服力。

总而言之，《Cross-fitted Proximal Learning for Model-Based Reinforcement Learning》为处理存在隐藏混杂的离线RL模型学习问题提供了一个重要的、经过理论增强的工具箱组件。虽然其实际效能有待在更具挑战性的基准上进一步验证，但其在连接统计机器学习与强化学习方面的思路具有明确的启发价值。

---
（完）
