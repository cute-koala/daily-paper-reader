---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一令：基于增量令牌的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:profile-4"]
score: 10.0
evidence: 使用增量令牌的高效生成式世界模型
tldr: 视频世界建模的核心挑战是高效生成多样的未来状态。现有判别式模型缺乏多样性，而生成式模型计算成本高。本文提出 DeltaTok 和 DeltaWorld：DeltaTok 将连续帧间的视觉基础模型特征差编码为单个“delta”令牌，极大压缩了视频表示；基于此的生成式世界模型 DeltaWorld 能通过可并行化的多假设训练高效生成多样未来。该方法在密集预测任务中取得了更真实的预测结果，同时参数量和计算量大幅降低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 视频世界建模中预测多样未来状态是一大挑战，现有方法要么确定性预测缺乏多样性，要么计算成本高昂。
method: 提出 DeltaTok 将连续帧间的视觉基础模型特征差编码为单个“delta”令牌，并构建生成式世界模型 DeltaWorld 在这些令牌上高效生成多样未来。
result: 在密集预测任务上，DeltaWorld 的预测更接近真实结果，且参数量减少35倍以上、计算量减少2000倍。
conclusion: DeltaWorld 通过创新的 Delta 令牌和生成式建模，在保持预测多样性的同时，实现了远超现有方法的参数和计算效率。
---

## 摘要
预测多样化的未来状态是视频世界建模中的一个核心挑战。判别式世界模型产生一个确定性预测，该预测隐式地平均了可能的未来，而现有的生成式世界模型在计算上仍然昂贵。最近的研究表明，在视觉基础模型的特征空间中预测未来，而不是在为像素重建优化的潜在空间中，所需的世界模型参数显著减少。然而，大多数此类方法仍然是判别式的。在这项工作中，我们引入了DeltaTok（一种分词器），它将连续帧之间的VFM特征差异编码为单个连续的“增量”令牌；以及DeltaWorld（一种生成式世界模型），它对这些令牌进行操作以高效地生成多样化的合理未来。增量令牌将视频从三维时空表示简化为一维时间序列，例如对于512x512的帧可产生1,024倍的令牌缩减。这种紧凑的表示使得可行的多假设训练成为可能，即并行生成多个未来并仅监督最佳的那个。在推理时，这能在单次前向传递中产生多样化的预测。在密集预测任务上的实验表明，DeltaWorld预测的未来更符合真实世界的结果，同时其参数量比现有生成式世界模型少35倍以上，使用的FLOPs少2,000倍以上。代码和权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：视频世界建模的核心挑战在于高效且准确地预测**多样化的未来状态**。这对于自动驾驶等需要安全规划的智能体至关重要。
*   **现有方法局限**：
    *   **判别式世界模型**（如DINO-world）：输出单一确定性预测，在不确定性下会趋向于条件均值，无法捕捉多样的未来可能性。
    *   **生成式世界模型**（如Cosmos）：能够生成多样未来，但通常计算成本高昂。原因包括：在像素重建优化的潜在空间中操作、需要多次前向传递（如扩散模型）、未能充分利用视频帧间的时空冗余。
*   **本文目标**：提出一种高效的生成式世界模型，能够在单次前向传递中生成多个合理的未来假设，同时大幅降低计算开销。

## 2. 方法论
*   **核心思想**：利用连续帧之间的变化通常是低维且结构化的这一特性，将视频表示从三维时空网格压缩为一维时间序列。
*   **关键技术细节**：
    1.  **DeltaTok（增量令牌分词器）**：
        *   输入：一个冻结的视觉基础模型（VFM，如DINOv3）提取的连续两帧特征图 `x_{t-1}` 和 `x_t`。
        *   编码器：一个基于Transformer的编码器，将两帧特征图压缩为单个连续的“增量令牌” `z_t`。该令牌编码了从 `x_{t-1}` 到 `x_t` 的变化。
        *   解码器：接收前一帧特征 `x_{t-1}` 和增量令牌 `z_t`，重构出当前帧特征 `\hat{x}_t`。
        *   训练目标：最小化重构特征与真实特征的均方误差（MSE）。
    2.  **DeltaWorld（生成式世界模型）**：
        *   构建于判别式DINO-world架构之上，但操作对象是DeltaTok生成的增量令牌序列 `Z_{1:t}`。
        *   采用“最佳多假设”（Best-of-Many, BoM）训练目标实现单次前向生成多样性：
            *   **训练时**：从高斯分布中采样K个噪声查询向量 `q_k`。每个查询向量通过预测器独立地预测下一个增量令牌 `\hat{z}_{t+1}^k`。仅对K个预测中与真实值最接近的那个进行反向传播监督。
            *   **推理时**：不同的噪声查询输入会映射到不同的未来假设，从而实现单次前向传递下的多样化采样。
        *   对于多步自回归推演，预测出的增量令牌被追加到上下文序列中，整个过程在紧凑的增量令牌空间中进行。

## 3. 实验设计
*   **评估基准**：遵循密集预测基准（dense forecasting benchmark），评估短期（~0.2秒）和中期（~0.6秒）预测精度。
*   **主要任务与数据集**：
    1.  **语义分割**: VSPW数据集（多种场景）、Cityscapes数据集（驾驶场景）。
    2.  **单目深度估计**: KITTI数据集（驾驶场景）。
*   **对比方法**:
    *   **判别式基线**: DINO-world (本文复现版)。
    *   **生成式基线**: Cosmos-4B, Cosmos-12B (基于像素重建潜在空间的生成式世界模型)。
    *   **上下界**: “复制上一帧”（Copy last）作为下界，“使用真实未来帧”（Present）作为上界。
*   **评估指标**: 
    *   对于生成式模型: 
        - ***最佳*** (`best-of-20`) : 20个样本中最接近真实值的那个样本的性能，反映模型产生至少一个准确未来的能力。
        - ***平均*** (`mean`) : 将20个样本的特征平均后再应用任务头得到的性能，反映预测的一致性和平均质量。

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量和总训练时长等硬件细节。实验配置信息如下：
*   **主要结果训练**: DINO-world和DeltaWorld均在512×512输入分辨率下训练了300K次迭代。BoM训练的K=256。
*   **消融实验训练**: 使用256×256输入分辨率训练100K次迭代。BoM训练的K=16。
*   **分词器训练**: DeltaTok分词器在各分辨率下单独训练50K次迭代。

## 5. 实验数量与充分性
论文进行了较为充分和系统的实验设计：
1.  **渐进式构建分析 (表2)** ：展示了从判别式基线 → BoM训练 → 全帧压缩 → Delta压缩 (DeltaWorld)的演变过程及其对性能、计算量和内存的影响。这是一个关键的消融研究。
2.  ***BoM样本缩放分析 (图5)*** ：研究了不同数量的训练查询(K)和评估查询对最终性能的影响，验证了方法的可扩展性。
3. ***主要基准对比 (表3)*** ：在三个不同数据集的两个任务上全面对比了现有方法，证明了DeltaWorld在性能和效率上的优势。
4. ***跨架构验证 (附录C)*** ：将delta令牌思想应用于另一个判别式世界模型DINO-Foresight上并取得良好效果，证明了其通用性。
5. ***定性可视化*** ：提供了多个数据集的样例来展示生成的多样性、短期/中期推演效果以及RGB重建结果。

总体而言，实验设计覆盖了消融分析、定量比较、定性展示和泛化验证等多个维度，较为全面客观地评估了所提方法的有效性。

##6 .论文的主要结论与发现
1. ****高效且有效的表示****: DeltaTok成功地将视频动态压缩为一系列一维增量令牌 ，实现了高达1024倍的令牌缩减(对于512×512帧)，同时保持了足够的信息以准确重构特征图 。
2 .****高效的多样化生成****: DeltaWorld结合BoM目标和delta表示 ，能够在单次前向传递中高效地采样多个合理的未来 。其最佳(`best`)样本的性能超越了现有的计算密集型生成式模型(Cosmos)。
3 .****显著的效率提升****: DeltaWorld以极小的参数量(比Cosmos少35倍以上)和计算量(FLOPs少2000倍以上)，取得了优于或媲美大型生成式和判别式基线的性能 。
4 .****恢复的平均性能****: DeltaWorld的平均(`mean`)预测质量恢复到了专为平均优化设计的判别式基线的水平 ，表明其生成的多样性是围绕合理模式展开的 ，而非随机噪声 。

##7 .优点
1 . ****创新性的表示学习****: “一帧即一令”的核心思想非常新颖 ，通过编码帧间变化而非完整内容 ，巧妙地利用了视频的时间冗余性 ，极大地简化了建模问题 。
2 . ****简洁高效的框架****: BoM目标提供了一种简单而有效的机制来实现单步多样化生成 ，避免了扩散模型中耗时的迭代去噪过程 。整个框架(DeltaTok + BoM)概念清晰 ，实现相对直接 。
3 . ****卓越的效率收益比****:论文最突出的贡献在于实现了数量级级别的参数和计算量削减 ，同时保持了甚至提升了预测性能 。这为在实际应用(如机器人 、自动驾驶)中部署实时 、多样化的世界模型铺平了道路 。
4 . ****详实的实验支撑****:从逐步构建到跨基准比较再到泛化验证的实验链条完整有力 ，充分支撑了论文的主张 。

##8 .不足与局限
1 . ****分布建模的理论保证不足****: BoM目标缺乏像扩散模型那样明确的概率分布建模连接 。模型的多样性覆盖受限于训练时探索的噪声查询数量K没有机制保证采样的未来分布能近似真实的未来概率分布 。这可能导致某些低概率但重要的模式未被捕捉 。
2 . ****误差累积风险****:由于采用自回归推演并在每一步都依赖前一部的重构或预测作为条件误差可能在多步推演过程中累积导致特征漂移或质量下降 。这是自回归模型的通病文中提及但未深入解决 。
3 . ****应用范围限制于VFM特征空间****:整个方法建立在预训练的VFM(如DINOv3)的特征空间之上 。虽然这带来了语义抽象的优势但其性能上限受限于该VFM的表达能力和领域适应性 。对于VFM难以很好表征的新颖场景或精细动作可能表现不佳 。
4 . ****未探索更长的上下文和多模态输出****:论文主要关注短中期预测(最多约0秒)。对于需要更长历史上下文或更长时间跨度的规划任务以及结合语言指令等多模态条件的扩展能力尚未验证 。

**(完)**

## 9. 潜在影响与未来方向
1.  **对世界建模领域的影响**：本文为生成式世界模型提供了一条高效的新路径，挑战了“高质量生成必须依赖复杂、高维潜在空间和迭代采样”的固有观念。其“增量令牌”的思想可能启发更多利用时间冗余性的序列建模方法。
2.  **对机器人学与自动驾驶的启示**：所展示的高效、多样化预测能力，使其非常适合集成到需要实时、多假设规划的智能体架构中，例如用于风险评估、轨迹规划或主动感知。
3.  **未来研究方向**：
    *   **概率框架的融合**：探索将BoM目标与更正式的概率模型（如条件变分自编码器CVAE或归一化流）结合，以提供更好的分布覆盖和似然估计。
    *   **长程预测与规划**：研究如何将delta令牌表示扩展到更长的时间尺度，并整合到基于模型的强化学习或分层规划框架中。
    *   **多模态与条件生成**：扩展模型以接受语言指令或其他传感器输入作为条件，实现目标导向的未来场景生成。
    *   **基础模型的协同进化**：探索DeltaWorld框架与更强大或领域特定的视觉基础模型（VFM）的结合，以及端到端联合优化的可能性。

## 10. 总结
本文提出的DeltaWorld是一个在概念和方法上均具创新性的工作。它通过引入“增量令牌”（DeltaTok）这一紧凑表示，将视频动态压缩为一维序列，并结合“最佳多假设”（BoM）训练目标，成功构建了一个能够在单次前向传递中高效生成多样化未来假设的世界模型。其实验结果令人信服地表明，该方法在保持甚至超越现有大型生成式模型（如Cosmos）预测质量的同时，实现了数个数量级的计算和参数效率提升。

尽管在概率建模的理论完备性和长程推演的稳健性方面存在局限，但其核心思想——利用帧间变化的低维特性来简化世界建模——为整个领域提供了宝贵的洞见。这项工作显著降低了高质量、多样化视频预测的计算门槛，为推动世界模型在实时交互系统中的实际应用迈出了关键一步。

（完）
