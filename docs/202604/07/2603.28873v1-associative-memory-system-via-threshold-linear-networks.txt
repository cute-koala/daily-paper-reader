Title: Associative Memory System via Threshold Linear Networks

URL Source: https://arxiv.org/pdf/2603.28873v1

Published Time: Wed, 01 Apr 2026 00:03:50 GMT

Number of Pages: 8

Markdown Content:
# Associative Memory System via Threshold Linear Networks 

Qin (Eric) He, Jing Shuang (Lisa) Li 

Abstract — Humans learn and form memories in stochastic environments. Auto-associative memory systems model these processes by storing patterns and later recovering them from corrupted versions. Here, memories are learned by associating each pattern with an attractor in a latent space. After learn-ing, when (possibly corrupted) patterns are presented to the system, latent dynamics facilitate retrieval of the appropriate uncorrupted pattern. In this work, we propose a novel online auto-associative memory system. In contrast to existing works, our system supports sequential memory formation and provides formal guarantees of robust memory retrieval via region-of-attraction analysis. We use a threshold-linear network as latent space dynamics in combination with an encoder, decoder, and controller. We show in simulation that the memory system successfully reconstructs patterns from corrupted inputs. 

I. I NTRODUCTION 

An auto-associative memory system stores patterns and later reconstructs them from partial or noisy versions of those same patterns (i.e., pattern completion [1]). For example, un-der poor lighting conditions, the human brain can reconstruct a familiar cat well enough for recognition. Computational neuroscientists and computer scientists often model such systems using a dynamical latent space. During learning, patterns are associated with fixed-point attractors in latent space through an encoder and decoder; during inference, the memory system retrieves the appropriate pattern by leverag-ing latent dynamics. Previous work on associative memory systems includes energy-based models such as Hopfield net-works [1], [2] and neural network-related models [3]. These frameworks provide principled mechanisms for associative memory, with applications in behavioral modeling [4] and machine learning [5]. However, classical Hopfield networks can have spurious attractors [6] , while neural networks lack formal guarantees for robust pattern retrieval. Moreover, most existing work requires full prior knowledge of stored patterns and does not support online learning as humans do. We propose a novel online auto-associative memory sys-tem with robustness guarantees. The system consists of a threshold-linear network (TLN) latent space (section II), a controller (section III-A), an encoder, and a decoder (section III-B). Figure 1 shows the full architecture. The encoder maps patterns to the latent space. Latent dynamics are mod-ulated by the controller for memory formation and retrieval. The decoder then maps latent states back to pattern space. During learning (Fig. 1A), we sequentially present the system with new patterns to memorize (i.e., online learning). When a new pattern is presented, the decoder output does  

> The authors are with the Department of Electrical Engineering and Computer Science, University of Michigan.

{ericheq@umich.edu, jslisali }@umich.edu 

not match this pattern; this mismatch triggers the controller and induces a transition to a new attractor. Once the latent dynamics converge to the new attractor, the encoder and decoder are updated to match the pattern to the attractor. During inference (Fig. 1B), noisy patterns are presented to the system sequentially. Each noisy pattern is mapped through the encoder to a target state. The controller then drives the latent dynamics to the target state. When this target state lies in the region of attraction (ROA) of the relevant attractor, the decoder is guaranteed to reconstruct the correct pattern from that attractor. We rigorously characterize the ROA of the latent dynamics and use this to provide robust-ness guarantees (section IV-B); this is, to our knowledge, the first such characterization for TLN dynamics. We then validate our analyses in simulation (section V). 

Notation: Let Sn ⊆ Rn×n denote the set of symmetric matrices. For M ∈ Sn, we use M ⪰ 0 and M ≻ 0 denote positive semidefinite and positive definite, respectively. Let 

1n ∈ Rn denote the all-ones vector and 0n ∈ Rn denote the all-zeros vector. We use I[·] to denote the indicator function, which takes the value 1 when its argument is true and 0 otherwise. We denote an equilibrium by xe. When the equilibrium is an attractor or a saddle point, we write it as 

x∗ or x†, respectively. II. T HRESHOLD LINEAR NETWORKS 

The proposed memory system uses threshold-linear net-work (TLN) dynamics as its latent space dynamics. TLNs have been used to study neural dynamics, such as firing-rate dynamics, are computationally tractable, and support fixed-point attractors. [7], [8]. In this section, we discuss TLNs and their equilibria. The standard TLN dynamics are: ˙x = −x + [ y]+, y := W x + θ (1) where x ∈ Rn is the state vector, W ∈ Sn is the connectivity matrix, and θ ∈ Rn is the bias. The threshold-nonlinearity (i.e., rectifier) is defined as [y]+ := max {y, 0}. We now introduce relevant definitions for TLN dynamics. 

Definition 1. The support of an equilibrium x e is the subset of active states, written as supp xe := { i | xei > 0 }.

Definition 2. Let [n] := {1, . . . , n} and σ ⊆ [n]. The cell 

associated with support σ is: 

Cσ :=

n

x ∈ Rn yi(x) ≥ 0 ∀i ∈ σ , yk(x) ≤ 0 ∀k /∈ σ

o

. (2) For i = 1, . . . , n − 1, we call C{i,i+1} a double-support cell. For i = 2, . . . , n − 1, we call C{i−1,i,i+1} a triple-support cell. 

> arXiv:2603.28873v1 [eess.SY] 30 Mar 2026 Learning Sequence Encoder TLN Switching Gate
> Last Learned

Learning Phase 

> Local Inhibition
> New Pattern
> Controller Correlated Noise Decoder
> Attractor Switch New Learned
> Before Learning During Learning After Learning Encoder (fixed) TLN Switching Gate
> Last Inferred

Inference Phase  

> Corrupted Pattern
> Controller
> Decoder (fixed)
> New Inferred
> Target State Neuronal LQR

A B     

> Before attractor switch After attractor switch
> 1. LQR drives to ROA of desired 2. Stop control and follow dynamics Fig. 1. Proposed auto-associative memory system. A. During learning, a new pattern is presented to the model, and a switching signal activates the controller to induce attractor switch and form new mappings (memories) in the encoder and decoder. B. Inference phase. The encoder maps a noisy pattern through the learned mappings to a target state. The feedback controller drives the latent dynamics toward the target state and is then turned off so the TLN settles into the corresponding attractor. The decoder then maps the attractor to the corresponding uncorrupted pattern.

Our memory system uses a sub-class of TLNs called 

chain-structured TLNs (CSTLNs) , which support multiple attractors by construction. In this paper, we will refer to a locally asymptotically stable equilibrium as an attractor 1.

Definition 3. A CSTLN is (1), with W and θ defined as 

Wkℓ =



0, k = ℓ, 

−1 + ε, |k − ℓ| = 1,

−1 − δ , |k − ℓ| > 1,

ε ∈ (0, 1), δ > 0,

θ = c 1n, c > 0.

(3) When parameters ε, δ , and c are appropriately chosen, the CSTLN is competitive and nondegenerate; then, each cell of the CSTLN contains at most one equilibrium [8]. We now present some basic results on the structure of these equilibria. 

Proposition 1. For CSTLNs, the support of an equilibrium xe coincides with the support of the cell containing it. Proof. From (1), we have xe = [ y(xe)] +. Therefore, 

xei > 0 ⇐⇒ yi(xe) > 0, xei = 0 ⇐⇒ yi(xe) ≤ 0. (4) By [8, Cor. 5.5], each cell of a CSTLN contains at most one equilibrium and no equilibrium lies on its boundary. For the remainder of the paper, we will denote both the support of an equilibrium and the support of the cell con-taining it by σ . Then, let ¯ σ = [ n] \ σ denote the off-support states. We can reorder states such that on-support states are followed by off-support states, i.e., ˜ x = Πx = [ xσ , x ¯σ ]⊤ where 

Π ∈ Rn×n is a permutation matrix. The dynamics restricted to a cell with support σ are: 

d ˜xdt = ˜Aσ ˜x +

θσ

0



, ˜Aσ =

−I|σ | +Wσ σ Wσ ¯σ

0 −In−| σ |



, (5)  

> 1In literature [8], this is also called a stable fixed point

where Wσ σ is obtained by keeping only the rows and columns of W in σ , Wσ ¯σ is obtained by keeping rows in σ

and columns in ¯ σ , and θσ is θ restricted to states in σ . Since each cell of the CSTLN contains at most one equilibrium, the equilibrium xe of permuted system(5) lies in the cell defined by σ if and only if the following conditions hold: 

yi(xe) > 0, ∀i ∈ σ , (6a) 

yk(xe) < 0, ∀k /∈ σ . (6b) CSTLNs contain two types of equilibria: attractors (which we use to store memories) and saddle points (which we use in robustness analysis). We now discuss this further. 

Definition 4. Let S ⊆ [n] := {1, 2, . . . , n}, and let j ∈ [n] \ S.The distance from j to S is dist ( j, S) := min s∈S | j − s|.

Lemma 1. For each integer i = 1, . . . , n − 1, double-support cell C {i,i+1} contains a unique attractor x ∗ where x∗ 

> k

=

 

> c
> 2−ε

, k = i and k = i + 1,

0, otherwise. (7) 

Proof. First, following [8, Lem. 5.2], we compute the equi-librium xe 

> σ

in each cell Cσ by restricting the dynamics to the subsystem on σ and setting the remaining states xe 

> ¯σ

to zero. Applying this to C{i,i+1} and using (5) yields: 

xe 

> σ

= ( I −Wσ σ )−1θσ = c

2 − ε 1|σ |, xe 

> ¯σ

= 0| ¯σ |. (8) Next, we show that the equilibrium lies in C{i,i+1}. From (8), the on-support states satisfy (6a), 

xe 

> σ

= [ yσ (xe)] +, xe 

> σ

= c

2 − ε > 0 ⇒ yσ (xe) > 0. (9) For the off-support states, consider k = i − 1 (the case k =

i + 2 is similar). Substituting (8) into yi−1(xe) gives 

yi−1(xe) = ( W x e + θ )i−1

= ( −1 + ε)xi + ( −1 − δ )xi+1 + c = −cδ

2 − ε < 0 (10) For any state k with dist (k, {i, i + 1}) > 1, yk(xe) becomes more negative. Therefore, yk(xe) < 0 for all k /∈ σ , so the off-support condition (6b) holds. Thus, xe ∈ C{i,i+1}. We then show that xe is an attractor x∗. For C{i,i+1}, the matrix −I|σ | +

Wσ σ in (5) becomes 

−I|σ | +Wσ σ =

 −1 −1 + ε

−1 + ε −1



, (11) with eigenvalues λ1 = −2 + ε and λ2 = −ε. Under the CSTLN parameter constraints in Definition 3, both eigen-values are negative. The off-support Jacobian −In−| σ | in (5) has eigenvalue −1 only. Therefore, the system Jacobian ˜ Aσ

for the dynamics in C{i,i+1} is Hurwitz, which implies the equilibrium xe is an attractor. 

Lemma 2 (Triple-support cell saddle) . For each integer i = 2, . . . , n − 1, triple-support cell C {i−1,i,i+1} has a unique hyperbolic saddle point x † where m := cε

∆ , n := c(δ + 2ε)

∆ , ∆ := δ + 4ε − 2ε2, (12) 

x† 

> k

=



m, k = i − 1 or k = i + 1,

n, k = i,

0, otherwise. 

(13) 

Proof. Similar to proof for Lemma 1. An equilibrium of the cell C{i−1,i,i+1} is given by 

xe 

> σ

= ( I −Wσ σ )−1θσ = [ m, n, m]⊤, xe 

> ¯σ

= 0. (14) We now show that xe 

> σ

lies in C{i−1,i,i+1}. Under CSTLN pa-rameter constraints in Definition 3, we have ∆ > 0, and hence 

m > 0 and n > 0. Therefore, on-support states satisfies (6a) 

xe 

> σ

= [ yσ (xe)] +, xe 

> σ

> 0 ⇒ yσ (xe) > 0. (15) For off-support states, consider k = i−2 (similar for k = i+2) 

yi−2(xe) = ( −1 + ε)xei−1 + ( −1 − δ )xei + ( −1 − δ )xei+1 + c

= c − (2 + δ − ε)m − (1 + δ )n = − c(δ 2 + 3δ ε + ε2)

∆ < 0.

For any state k with dist (k, {i − 1, i, i + 1}) > 1, yk(xe) be-comes more negative. Therefore, the off-condition (6b) holds. Hence, xe ∈ C{i−1,i,i+1}. Then, we show that xe is a hyperbolic saddle point x†. For C{i−1,i,i+1}, matrix −I|σ | + Wσ σ in (5) becomes 

−I|σ | +Wσ σ =

 −1 −1 + ε −1 − δ

−1 + ε −1 −1 + ε

−1 − δ −1 + ε −1

 , (16) with eigenvalues λ1 = δ and λ2,3 = −1 + b±

√b2+8a2 

> 2

< 0, where a = −1+ε and b = −1−δ . So (16) has one eigenvalue with positive real part and two with negative real parts. Off-support Jacobian −In−| σ | in (5) has eigenvalue −1. There-fore, the equilibrium xe is a hyperbolic saddle point. III. C ONTROLLER AND LEARNING RULES 

Our memory system consists of a CSTLN (described above) in addition to a decoder, encoder, and controller. We now describe these additional components. 

A. Controller and action trigger 

Controller action is triggered by a mismatch between input pattern Pt ∈ Rd and output pattern ˆPt ∈ Rd . To measure mismatch, let s(t) denote cosine similarity 2 between patterns 

Pt and ˆ Pt . When similarity drops below some threshold, we generate a trigger signal for the controller: 

γ(t) = g m sth − s(t) , g(x) = 11 + e−x (trigger) (17a) ˙q(t) = γ(t)( 1 − q(t)) 

τq

(latch) (17b) ˙T (t) = q(t) (timer) (17c) 

w(t) = g β  T (t) − H (timer threshold) (17d) ˙G(t) = (1 − G(t)) γ(t) ( 1 − q(t)) 

τr

− G(t) w(t)

τd

(pulse) (17e) where m is a positive constant chosen large enough to ensure fast mismatch detection, mismatch threshold sth is set near 0.9, τq, τr, τd are time constants typically selected in the range 2–10, and H sets the pulse duration and should be large enough to trigger a successful transition without causing persistent switching. The pulse G(t) is then clipped to [0, 1].Intuitively, G(t) rises when a new pattern is detected, remains active for duration H to trigger the controller, then decays after sufficient control input has been injected. We now discuss the separate controllers for learning and inference. 

1) Controller for learning: During learning, the controller injects input ulearn into the latent dynamics (1) as follows: ˙x(t) = −x(t) + W x (t) + θ + rG (t)ulearn (t)

> +

, (18a) 

ulearn (t) = Winh x(t) + ξ (x) (18b) where Winh x represents local inhibition ( Winh ∈ Rn×n 

> <0

), ξ (x)

represents correlated noise, and r is a positive scalar. In general, in the absence of inputs, the latent state will settle at an attractor. When the controller is triggered, Winh is updated. This reshapes the latent dynamics so that the state can be easily induced (with correlated noise) to another attractor. For example, to move the state from the attractor with support 

σ i = {i, i + 1} to the one with support σ i+1 = {i + 1, i + 2},we set 

[Winh ]σ iσ i =

 0 −cinh 

−cinh 0



, (19) where cinh ∈ R>0. Correlated noise is 

ξ (x) = κ tanh  a(t) k x, (20a) ˙a(t) = − 1

τ a(t) + 

r 2

τ ζ (t). (20b) where k(x) ∈ Rn is a spatial weighting profile across states, centered on the current support and decaying exponentially 

> 2The memory system can work equally well with other similarity mea-sures such as Euclidean distance.

away from it; a(t) is an Ornstein–Uhlenbeck (OU) process with time constant τ; ζ (t) is Gaussian white noise. Saturation 

κ tanh (a(t)) is used to avoid large perturbations. Parameters 

cinh , r, κ, and τ should be chosen so that the attractor transition occurs within duration H.

2) Controller for inference: During inference, the encoder maps a (possibly corrupted) input pattern to a target state 

xtar near the desired attractor. The controller injects state feedback input ufb into latent dynamics (1) as follows: ˙x(t) = −x(t) + [ W x (t) + θ ]+ + rG (t)ufb (t), (21a) 

ufb (t) = −K x(t) − xtar 

. (21b) We design a gain matrix K so that the closed-loop system (21) drives the state x to the target state xtar . To do so, we use an LQR controller derived from the linearization (about 

xtar ) of the closed-loop dynamics. ˙x − ˙xtar = A x − xtar 

 + rG (t)ufb , (22a) 

A = −I + DW , Dii = I{w⊤ 

> ixtar +θi>0}

. (22b) Here, A ∈ Rn×n is the state matrix of the local linearized model and D selects the positive components of the threshold nonlinearity. Let B(t) = rG (t), we introduce an additional auxiliary dynamical system to compute the optimal LQR gain: 

gon = [( h − hon )] +, goff = [( hoff − h)] +, h = W x tar + θ , (23a) ˙D = 1 

> τd

(α(I − D) Diag (gon ) − β D Diag (goff )) , (23b) ˙A = 1 

> τa

(−A + DW − I) , Acl = A − BK , (23c) ˙P = 1

> τp



A⊤ 

> cl

P + PA cl + Q + K⊤RK 



, (23d) ˙K = − 1

> τk



RK − B⊤P



, (23e) where hon is an activation threshold chosen slightly above 0 while hoff = 0. Constants α and β are selected so that D(t)

quickly converges to D in (22b). Time constants in (23) are chosen to be small (around 0 .01) so that D(t), A(t), and 

P(t) converge faster than K(t), which in turn converges to the optimal LQR solution. After duration H, the state reaches 

xtar and the controller is turned off as G(t) approaches zero. Then, the CSTLN returns to its autonomous dynamics (1). If the target state xtar lies in the ROA of the intended attractor, then CSTLN dynamics will converge to it, and the decoder will recover the correct pattern. Remark: We formulate the control mechanism as a dynamical system; this is a desirable property for biological plausibility, since biological neurons perform computations via dynamics. If biological plausibility is not a priority, one could equally use a standard, non-dynamical LQR setup. 

B. Encoder and decoder learning rules 

The encoder WE ∈ Rd×n maps input pattern P ∈ Rd to a target latent state via xtar = W ⊤ 

> E

P. The decoder WD ∈ Rn×d

maps latent state x to output pattern ˆ P via ˆ P = W ⊤ 

> D

x.During learning, when a new pattern P is presented, the abovementioned controller mechanism moves the latent state to a new attractor x∗. Once this occurs, we update WE and 

WD to associate x∗ with P . For the encoder, we only update the rows associated with support σ of x∗. Let F be the set of frozen rows (i.e., weights associated with previously learned patterns) and define U := σ \F. This gives the decomposition 

x∗ = W ⊤ 

> E

P = W ⊤

> E,F

| {z } 

> frozen rows

P + W ⊤

> E,U

| {z } 

> free rows

P. (24) We then choose the minimum-norm update for the free rows using the Moore–Penrose solution: 

W ⊤ 

> E,U

=

 x∗ −W ⊤ 

> E,F

PP⊤

∥P∥22

. (25) This ensures that after learning, new pattern P is associated with x∗ without disrupting previously learned associations. An analogous column update process applies to the decoder. The overall memory system including latent dynamics, con-troller, encoder, and decoder is presented in Algorithm 1. 

Algorithm 1 Proposed memory system 

Learning: for each new pattern P do: 

Compute cosine similarity s(t) and control trigger (17) Apply control (18) to drive latent state x to attractor x∗

Update encoder and decoder weights (25) 

Inference: for each corrupted input pattern P do: 

Compute cosine similarity s(t) and control trigger (17) Compute target state xtar = W ⊤ 

> E

P

Apply control (21) to drive the latent state x to xtar 

Deactivate control after duration H, x will settle to x∗

Compute the reconstructed pattern ˆ P = W ⊤ 

> D

x∗

IV. R EGION OF ATTRACTION ANALYSIS 

The robustness of our memory system depends on the region of attraction (ROA) of the TLN attractors.We now present ROA computations for TLNs. In general, computing ROA for high-dimensional nonlinear systems is difficult [9]. For TLNs, existing analyses require additional assumptions on network structure [10], [11]. In contrast, we now present (i) a Lyapunov-based approximation of ROAs for general TLNs and (ii) a geometric method for exact calculation of ROAs for CSTLNs. We then use these to provide robustness guarantees for the proposed memory system. 

A. ROA certification for TLNs via SDP 

A common way to compute an inner ROA approximation is to find a Lyapunov function; this can be formulated as a semidefinite program (SDP) [12]. This method has been applied to threshold nonlinearities, leveraging quadratic constraints (QCs) derived from slope-restricted sector bounds [13]. However, [13] uses a global slope restriction, which is too conservative for our application; we now introduce a local slope restriction similar to [14] and use this to find an appropriate Lyapunov function. We first shift coordinates so that the attractor is at the origin. Define z such that: 

z = x − x∗, y∗ := W x ∗ + θ . (26) The dynamics in the shifted coordinates are: ˙z = −z + [ W z + y∗]+ − [y∗]+ = −z + φ (z), (27) where φ (z) = [ W z + y∗]+ − [y∗]+. Consider the local ellip-soidal region D := { z | z⊤Ez ≤ α2 }, where E ∈ Sn, E ≻ 0, and α > 0. For componentwise analysis, let v := W z + y∗.Then, for all z ∈ D, each vi satisfies: 

vi ∈



y∗ 

> i

− α

q

w⊤ 

> i

E−1wi, y∗ 

> i

+ α

q

w⊤ 

> i

E−1wi



, (28) where w⊤ 

> i

is the i-th row of W . Let [v, v] denote the componentwise bounds obtained by stacking the intervals in (28). Equivalently, we can write φ (v) = [ v]+ − [y∗]+ for 

v ∈ [v, v]. Since the shifted system (27) has an equilibrium at 

z∗ = 0, evaluate (v, φ (v)) at z∗, we have: 

v∗ := W z ∗ + y∗ = y∗, (29a) 

φ (v∗) := [ W z ∗ + y∗]+ − [y∗]+ = 0. (29b) Since φ acts elementwise, for each i we have [φ (v)] i = [ vi]+ −

[y∗ 

> i

]+, each component [φ (v)] i falls into one of the two cases shown in Figure 2, depending on the sign of y∗ 

> i

.A B 

> Fig. 2. Local sector bound for shifted system.

Therefore, for v ∈ [v, v], φ (v) satisfies the tighter slope restriction ∆φ (v) 

> ∆v

∈ [Sα , Sβ ] around (v∗, φ (v∗)) . Following [14, Lemma 1], if λ ∈ Rn 

> ≥0

then 

 v − v∗

φ (v) − φ (v∗)

⊤

Ψ⊤M(λ )Ψ

 v − v∗

φ (v) − φ (v∗)



≥ 0, ∀v ∈ [v, v],

Ψ :=

 diag (Sβ ) −I

− diag (Sα ) I



, M(λ ) :=

 0 diag (λ )

diag (λ ) 0



.

With v = W z + y∗ and define a new matrix Mκ ,

Mκ :=

W 00 I

⊤

Ψ⊤M(λ ) Ψ

W 00 I



,

the corresponding quadratic constraint in the z-coordinates is 

 z

φ (z)

⊤

Mκ

 z

φ (z)



≥ 0. (30) After learning, for each attractor, we can solve an SDP to find a local ROA estimation and the maximum noise bound for its associated pattern, as given in the following theorem. 

Theorem 1 (SDP-based robustness certification) . Given E ∈

Sn with E ⪰ 0, and α > 0, define the local domain D =

{z ∈ Rn : z⊤Ez ≤ α2}. Define L := Y Σ, where W E = UΣY ⊤

is the compact singular value decomposition of encoder W E

(i.e., with zero singular values discarded). Assume that the nonlinearity φ (z) in (27) with A = −I and B = I satisfies local QC (30) over D. If there exist matrix P ≻ 0, diagonal matrices S 0 ≻ 0, S 1 ≻ 0, and positive scalars ε such that 

A⊤P + PA PB B⊤P 0



+ Mκ ≺

−εI 00 0



, (31a) 1

α2 E ⪯ P, (31b) 

L⊤PL ⪯ 1

r2 I, (31c) 

then E (P) = {z ∈ Rn : z⊤Pz ≤ 1} is a local ROA estimate of attractor z ∗ for system (27) . Furthermore, assume the input pattern to the memory system is a previously learned pattern corrupted with additive noise η. Then, as long as ∥η∥2 ≤ r, the encoder will map the corrupted input pattern to a target state which lies in E (P).Proof. V (z) = z⊤Pz is a Lyapunov function; pre- and post-multiplying (31a) by [z⊤, φ (z)⊤]⊤ and applying (30) yields ˙V (z) ≤ − ε∥z∥22. In addition, (31b) implies that the set {z :

z⊤Pz ≤ 1} is contained in {z : z⊤Ez ≤ α2} through S-procedure. Thus, E (P) is contained in the local domain where the QC holds and is therefore forward invariant. Consequently, trajectories that starting in E (P) converge to attractor z∗ = 0 by the Lyapunov stability theory [15]. Next, let z0 = W ⊤ 

> E

η be the perturbation in z coordinates. Requiring 

z0 ∈ E (P) for all ∥η∥2 ≤ r implies η⊤WE PW ⊤ 

> E

η ≤ 1, ∀η :

∥η∥2 ≤ r, or, equivalently, WE PW ⊤ 

> E

⪯ 1 

> r2

I. Since WE PW ⊤

> E

and L⊤PL have the same eigenvalues, this is equivalent to requiring (31c). 

B. ROA certification for CSTLNs via LP 

The SDP-based method yields hyperellipsoidal ROA ap-proximations for general TLNs. We now present a comple-mentary method for CSTLNs specifically, which leads to a linear program (LP). We first construct forward-invariant sets, then show that each set is partitioned by a separating hyperplane into two regions; each region belongs to a differ-ent attractor’s ROA. Each attractor’s complete ROA can be assempled from pieces across these sets. 

Theorem 2 (Local forward-invariant set) . For CSTLNs with n ≥ 3 and i = 2, . . . , n − 1, the following set 

R(i) 

> FI

:= { x ∈ Rn | yi(x) ≥ α, yi±1(x) ≤ β ,

yk(x) ≤ 0 ∀ k /∈ { i − 1, i, i + 1} } , α, β ≥ 0. (32) 

is forward-invariant if there exist α, β ∈ R≥0 such that 

α ≤ c + 2(−1 + ε)β , (33a) 

(−1 + ε)α + c ≤ β , (33b) 

α ≥ c

1 + δ . (33c) 

Proof. We show that if (33a)–(33c) hold, the vector field points inward on the boundary of R(i) 

> FI

, and therefore R(i)

> FI

is forward-invariant by Nagumo’s theorem [16]. Let w⊤ 

> i

be the i-th row of W . On the boundary yi(x) = w⊤ 

> i

x + c = α,differentiating yi and restricting to R(i) 

> FI

in (32) gives: ˙yi = w⊤ 

> i

˙x = − w⊤ 

> i

x + w⊤ 

> i

[y(x)] +, (34) ˙yi = c − α + ( −1 + ε)[ yi−1]+ + ( −1 + ε)[ yi+1]+. (35) Substituting yi±1(x) ≤ β into (35), (33a) implies that ˙ yi ≥ 0. Similarly, on the boundary yi±1 = β :˙yi−1 = c − β + ( −1 + ε)[ yi]+ + ( −1 − δ )[ yi+1]+, (36) ˙yi+1 = c − β + ( −1 + ε)[ yi]+ + ( −1 − δ )[ yi−1]+. (37) Substituting yi±1(x) ≤ 0 and yi(x) ≥ α into (36) and (37), it follows from (33b) that ˙ yi±1 ≤ 0. On the boundary yk = 0, we consider the following 2 cases. If dist ( j, {i − 1, i, i + 1}) ≥ 2: ˙yk = c + ( −1 − δ )([ yi−1]+ + [ yi]+ + [ yi+1]+). (38) If dist ( j, {i − 1, i, i + 1}) = 1, consider yi+2 (similarly yi−2), ˙yi+2 = c+( −1−δ )([ yi−1]+ +[ yi]+)+( −1+ε)[ yi+1]+. (39) Substituting yi±1(x) ≤ 0 and yi(x) ≥ α into (38) and (39), it follows from (33c) that ˙ yk ≤ 0 and ˙ yi+2 ≤ 0. Therefore, the vector field points inward on every boundary of R(i) 

> FI

. Since the TLN dynamics in (1) are globally Lipschitz and R(i) 

> FI

is a closed convex polyhedron, Nagumo’s theorem implies that 

R(i) 

> FI

is forward invariant [16], [17]. 

Proposition 2. For CSTLNs, conditions (33a) – (33c) are feasible only when plant parameter ε ∈ [ 12 , 1).

Next, we show that each forward-invariant set contains two attractors and one saddle point. 

Lemma 3. For CSTLNs with n ≥ 3 and integer i = 2, . . . , n −

1, if a forward-invariant set R(i) 

> FI

exists for some α, β

satisfying (33a) –(33c) , then R(i) 

> FI

contains the attractor in C{i−1,i}, the attractor in C {i,i+1}, and the saddle point in C{i−1,i,i+1}.Proof. We first show that the attractor x∗ ∈ C{i,i+1} lies in 

R(i) 

> FI

. By Proposition 2 and (33a)–(33b), we have 0 ≤ α ≤ αmax = c(1 − 2ε)

1 + 2ε2 − 4ε (40) By Lemma 1, y∗ 

> k

= x∗ 

> k

= 0 for all k /∈ { i, i + 1}, and y∗ 

> i−1

=

x∗ 

> i−1

= 0 ≤ β . Notice that, 

y∗ 

> i

= x∗ 

> i

= c

2 − ε ≥ αmax . (41) Further, from (41) and (33b), β ≥ c+( −1+ε)α ≥ c+( −1+

ε)x∗ 

> i

= c 

> 2−ε

, which implies 

y∗ 

> i+1

= x∗ 

> i+1

= c

2 − ε ≤ β . (42) Therefore, the attractor in C{i,i+1} satisfies (32) and thus lies in R(i) 

> FI

. Similar analysis holds for the attractor in C{i−1,i}.For the saddle point x† ∈ C{i−1,i,i+1}, Lemma 2 implies that 

y∗ 

> k

= x† 

> k

= 0 for all k /∈ { i − 1, i, i + 1}. Together with the constraints in Definition 3 and (41)–(42), we have 

x† 

> i

= y† 

> i

> c

2 − ε ≥ αmax , x† 

> i±1

= y† 

> i±1

< c

2 − ε ≤ β . (43) Therefore, the saddle point in C{i−1,i,i+1} lies in R(i) 

> FI

.Next, we identify a hyperplane that divides R(i) 

> FI

into two sub-regions, each lying in the ROA of one attractor. 

Lemma 4. For i = 2, . . . , n − 1, let A T 3 be the Jacobian of dynamics in the triple-support cell C {i−1,i,i+1}, and let w be a left eigenvector of A T associated with the positive eigenvalue 

δ . Define φ (x) := w⊤x. Then along any trajectory x (t),

˙φ (x) = 

(

δ φ (x), x(t) ∈ C{i−1,i,i+1},

−φ (x), x(t) ∈ C{i}. (44) 

Proof. (1) In each triple-support cell, let T = {i − 1, i, i + 1}

for i = 2, · · · , n − 1, and JT T := −I|T | + WT T , given explic-itly in (16). The dynamics in the triple-support cell with reordered states (as in (5)) is 

d ˜xdt = ˜AT ˜x +

θT

0



, ˜AT =

JT T WT ¯T

0 −In−| T |



. (45) Let ew⊤ be a left eigenvector of eAT corresponding to δ , and partition it as ew⊤ = [ w⊤ 

> u

v⊤ ] = [ ui−1, ui, ui+1, v⊤ ]. Therefore, 

w⊤ 

> u

JT T = δ w⊤ 

> u

, wu ∈ span {[1, 0, −1]⊤}. (46) Substituting ˜ w⊤ ˜AT = δ ˜w⊤ and ˜ w⊤

θT

0



= 0 into the follow-ing: ˙˜φ := ˜w⊤ ˙˜x = ˜w⊤



˜AT ˜x +

θT

0

 

= δ ˜w⊤ ˜x = δ ˜φ . (47) In the original coordinates, this becomes: ˙φ (t) = ˙˜φ (t) = δ ˜w⊤ ˜x(t) = δ ˜w⊤Πx(t), (48a) 

= δ (Π⊤ ˜w)⊤x(t) = δ w⊤x(t) = δ φ (t). (48b) In C{i} dynamics, the Jacobian is −I + D{i} eW , where D{i}

selects the row of eW corresponding to coordinate i and zeros out all others. Since ui = 0 in (46), we obtain ˜ w⊤D{i} eW = 0. Therefore, ˙˜φ = ˜w⊤ ˙˜x = ˜w⊤h 

−I + D{i} eW



˜x + [ 0, θi, 0, 0⊤

> n−3

]⊤i

, (49a) 

= − ˜w⊤ ˜x = − ˜φ . (49b) In the original coordinates, this becomes ˙ φ (x) = − φ (x).

Theorem 3. Let φ (x) := w⊤x as in (44) . Then, in each RFI ,1) {x : φ (x) > 0} ∩ RFI is an inner approximation of the ROA of the attractor in C {i−1,i}.2) {x : φ (x) < 0} ∩ RFI is an inner approximation of the ROA of the attractor in C {i,i+1}.3) {x : φ (x) = 0} ∩ RFI is an invariant hyperplane sepa-rating the two ROAs.         

> 3For notational simplicity, we drop the dependence of Tand left eigen-vector won i. In Theorem 3, we also drop the dependence of RFI on i.

Proof. (1) By Lemma 4, φ (x) = 0 implies ˙ φ (x) = 0. There-fore, the hyperplane {φ = 0} is invariant in RFI . Since the dynamics admit unique solutions, trajectories cannot cross this hyperplane. Additionally, [8, Lemma 2.1] shows that 

B = ∏ni=1[0, θi] is globally attracting for the competitive TLN. Thus, any trajectory starting in RFI eventually enters and remains in the following compact set: 

Ω = {x ∈ Rn : RFI ∩ B}. (50) Consider the following energy function on Ω:

V (x) := 12 x⊤(I −W )x − θ ⊤x (51) Since W is symmetric, we have: 

∇V (x) = x − 12 (W +W ⊤)x − θ = x − (W x + θ ) (52a) ˙V = ∇V (x)⊤ ˙x = ( x − y)⊤(−x + [ y]+) (52b) 

=

> n

# ∑

> i=1

(xi − yi) ( −xi + [ yi]+). (52c) Then, consider the following two cases: (a) If yi > 0, then (xi − yi)( −xi + [ yi]+) = −(xi − yi)2 ≤ 0. (b) If yi ≤ 0, then [yi]+ = 0 and (xi −yi)( −xi) = −x2 

> i

+yixi ≤

0, since xi ≥ 0 on Ω and yi ≤ 0. Therefore, ˙V (x) ≤ 0 for all x ∈ Ω. Since Ω is compact and forward invariant, LaSalle’s invariance principle implies that every trajectory with x(0) ∈ Ω converges to the largest invariant set in {x ∈ Ω : ˙ V (x) = 0}. By [8, Cor. 2.2], all equilibria of CSTLNs lie in B. Together with Lemma 3, this implies that Ω contains two attractors and one saddle point, and every trajectory converges to an equilibrium as 

t → ∞. By Lemma 2, the saddle in C{i−1,i,i+1} is hyperbolic. By [18, Sec. 5], the set of initial conditions converging to an unstable hyperbolic equilibrium is a lower-dimensional smooth manifold and therefore has Lebesgue measure zero. In Ω, this manifold is the hyperplane φ (x) = 0. Therefore, for almost every initial condition in Ω, trajectories with φ (x) > 0converge to the attractor in C{i−1,i}, while trajectories with 

φ (x) < 0 converge to the attractor in C{i,i+1}.A2 A1 A3 B1 B2 B3 Local Separatrix Attractor Saddle Point Forward Invariant Set Converge to attractor or saddle Forward Invariant Set Example PC 1 PC 2  

> Fig. 3. Illustrative example of a forward invariant set in 4D CSTLN projected onto 2D via PCA. The forward invariant set (contained by the red dashed line) contains two ROAs corresponding to two different attractors. Two ROAs are separated by the black dashed line.

Figure 3 shows a forward-invariant set for a 4-dimensional CSTLN with ε = 0.7, δ = 2.5, and c = 1. For visualization, the 4D space is projected into 2D via principal component analysis (PCA). In this example, regions B1 to B3 form the ROA of the attractor in C{0,1}, whereas regions A1 to A3form to the ROA of the attractor in C{1,2}. Notice that some attractors, such as the one in C{1,2}, have both left (from 

R(1) 

> FI

) and right (from R(2) 

> FI

) ROA polyhedra, which together form the total ROA estimate. With these approximations, we next show that Theorem 3 leads to a LP for computing a larger noise bound after the learning. 

Theorem 4 (LP-based robustness certification) . Let P (m) be a learned pattern associated with an attractor x ∗,m ∈ C{i,i+1}

for some i ∈ { 1, . . . , n − 1}. Let PL 

> m

and PR 

> m

denote the left and right ROA polyhedra for x ∗,m from R(i) 

> FI

and R(i+1) 

> FI

,respectively 4. For s ∈ { L, R}, write 

Psm = {x ∈ Rn : Asmx ≤ bsm},

where A sm ∈ RMsm×n and b sm ∈ RMsm collect the constraints defining the forward-invariant set in Theorem 2 together with the separating half-space from Theorem 3, written below: 

α j ≤ c + 2(−1 + ε)β j, (53a) 

(−1 + ε)α j + c ≤ β j, (53b) 

α j ≥ c

1 + δ , (53c) 

φ sm(x) ≤ 0, x ∈ Psm, if s = L, (53d) 

φ sm(x) ≥ 0, x ∈ Psm, if s = R, (53e) 

with j = i for s = L and j = i + 1 for s = R. Assume there exists r sm > 0 such that the following linear program is feasible: 

maximize 

> rsm

rsm (54a) 

subject to rsm ≤ bsm,ℓ − as⊤

> m,ℓ

W ⊤ 

> E

P(m)

∥as⊤

> m,ℓ

W ⊤ 

> E

∥2

, ℓ = 1, . . . , Msm, (54b) 

where a s⊤ 

> m,ℓ

is the ℓ-th row of A sm and b sm,ℓ is the ℓ-th entry of b sm. Furthermore, assume the input pattern to the memory system is the previously learned pattern P (m) corrupted with additive noise η. Then, as long as ∥η∥2 ≤ min (rRm, rLm), the encoder will map the corrupted input pattern to a target state xtar which lies in the ROA of x ∗,m (i.e., PL 

> m

∪ PR 

> m

). Proof. For s ∈ { L, R}, consider the attractor x∗,m and the polyhedron Psm = {x : Asmx ≤ bsm}. To guarantee xtar ∈ Psm,it suffices to enforce 

as⊤ 

> m,l

xtar ≤ bsm,l , l = 1, . . . , Msm.

Substituting xtar = W ⊤ 

> E

(P(m) +η) and using Cauchy–Schwarz gives, for any ∥η∥2 ≤ rsm,

as⊤

> m,ℓ

W ⊤ 

> E

P(m) + as⊤

> m,ℓ

W ⊤  

> E2

rsm ≤ bsm,ℓ , ℓ = 1, . . . , Msm.

which yields (54b). Therefore, xtar ∈ Psm.     

> 4If x∗,mis a boundary attractor, that is, in C{0,1}or C{n−1,n}, It has only one ROA polyhedral approximation.

Time TLN States Projected Trajectory Sequence Presented [2, 3, 4, 8, 0, 5, 2, 8] ALearning Inference Max Tolerable L2 Noise Attractor Successful Retrieval Ratio Noise Scale ( LP Bound) BCDEGuaranteed Retrieval Learning Inference Target State Firing Rate SDP LP Fig. 4. Simulation of 7-dimensional TLN-based memory system and noise robustness analysis on the MNIST dataset. A. Trajectories visualized using projection into 2D space. Dashed lines show trajectories during learning; solid lines show trajectories during inference. The background colors show the true ROA from simulations. B. TLN firing rates over time. The black dashed line marks the end of the second inference pattern (image with number 8). C. The system reconstructs the noisy input pattern. D. Comparison of input-noise robustness for the two methods over 108 encoders and decoders corresponding to different learning sequences randomly sampled from MNIST. E. Empirical simulations show that the system begins to fail to converge to the correct attractor when the noise level exceeds approximately twice the bound found by the LP method, where the bound is taken as the median value in panel D. 

V. S IMULATIONS 

In this section, we present an example of learning and inference, followed by a robustness analysis. Input patterns are drawn from the MNIST dataset [19]. We use a 7-dimensional CSTLN with ε = 0.9, δ = 2, and c = 1. Fig. 4A shows the trajectory of TLN during learning (dashed lines) and inference (solid lines). The small bend in the solid trajectory marks the moment when control is turned off at the target state and the system returns to the autonomous TLN dynamics. Background colors show the true ROA (computed through sampling). Figure 4B Shows the corresponding TLN firing rates over time, where each chunk corresponds to an attractor in the TLN. Fig. 4C shows the successful reconstruction of a noisy input pattern. Next, we evaluate robustness using the two methods described in Theorems 1 and 4. Figure 4D summarizes the results on 108 encoders and decoders corresponding to different learning sequences, each containing 6 patterns. As shown in Fig. 4D, the LP method in Theorem 4 outperforms the SDP method in Theorem 1 by yielding a larger l2-norm noise bound. Figure 4E further shows that, on the same learning examples, the empirical worst-case noise bound guaranteeing convergence to the correct attractor is approximately twice the bound (value is taken as the median in Figure 4D) found through LP method. Beyond this level, the TLN may converge to a wrong attractor and retrieve the wrong memory. VI. C ONCLUSION 

In this work, we presented a novel associative memory system with a TLN latent space, dynamical controller, linear encoder, and linear decoder. We provided ROA and robust-ness analyses for this system, and validate our results in simulation. Future work will explore the generalization of this model to hetero-associative memory and motor tasks. REFERENCES [1] J. J. Hopfield, “Neural networks and physical systems with emer-gent collective computational abilities,” Proceedings of the national academy of sciences , vol. 79, no. 8, pp. 2554–2558, 1982. [2] D. Krotov, “A new frontier for hopfield networks,” Nature Reviews Physics , vol. 5, no. 7, pp. 366–367, 2023. [3] H. Ramsauer, B. Sch¨ afl, J. Lehner, P. Seidl, M. Widrich, T. Adler, L. Gruber, M. Holzleitner, M. Pavlovi´ c, G. K. Sandve et al. , “Hopfield networks is all you need,” arXiv preprint arXiv:2008.02217 , 2020. [4] Q. He, D. N. Scott, M. J. Frank, C. B. Calderon, and M. R. Nassar, “Adaptive learning via bg-thalamo-cortical circuitry,” bioRxiv , pp. 2025–03, 2025. [5] D. Krotov, B. Hoover, P. Ram, and B. Pham, “Modern methods in associative memory,” arXiv preprint arXiv:2507.06211 , 2025. [6] J. A. Hertz, Introduction to the theory of neural computation . Crc Press, 2018. [7] R. Hahnloser and H. S. Seung, “Permitted and forbidden sets in symmetric threshold-linear networks,” Advances in neural information processing systems , vol. 13, 2000. [8] K. Morrison, A. Degeratu, V. Itskov, and C. Curto, “Diversity of emergent dynamics in competitive threshold-linear networks,” arXiv preprint arXiv:1605.04463 , 2016. [9] U. Topcu, A. Packard, and P. Seiler, “Local stability analysis using simulations and sum-of-squares programming,” Automatica , vol. 44, no. 10, pp. 2669–2675, 2008. [10] C. Lienkaemper, “Combinatorial geometry of neural codes, neural data analysis, and neural networks,” arXiv preprint arXiv:2209.07583 ,2022. [11] J. L. Alvarez, K. Morrison, and C. Curto, “Attractor-based models for sequences and pattern generation in neural circuits,” Neural Compu-tation , pp. 1–35, 2026. [12] S.-C. Liao, M. S. Hemati, and P. Seiler, “Quadratic constraints for local stability analysis of quadratic systems,” in 2022 IEEE 61st Conference on Decision and Control (CDC) . IEEE, 2022, pp. 7053–7058. [13] S. V. Noori, B. Hu, G. Dullerud, and P. Seiler, “A complete set of quadratic constraints for repeated relu and generalizations,” arXiv preprint arXiv:2407.06888 , 2024. [14] H. Yin, P. Seiler, and M. Arcak, “Stability analysis using quadratic constraints for systems with neural network controllers,” IEEE Trans-actions on Automatic Control , vol. 67, no. 4, pp. 1980–1987, 2021. [15] H. K. Khalil and J. W. Grizzle, Nonlinear systems . Prentice hall Upper Saddle River, NJ, 2002, vol. 3. [16] M. Nagumo, “ ¨ Uber die lage der integralkurven gew¨ ohnlicher differen-tialgleichungen,” Proceedings of the physico-mathematical society of Japan. 3rd Series , vol. 24, pp. 551–559, 1942. [17] F. Blanchini, “Set invariance in control,” Automatica , vol. 35, no. 11, pp. 1747–1767, 1999. [18] M. W. Hirsch, “Convergent activation dynamics in continuous time networks,” Neural networks , vol. 2, no. 5, pp. 331–349, 1989. [19] Y. LeCun, L. Bottou, Y. Bengio, and P. Haffner, “Gradient-based learning applied to document recognition,” Proceedings of the IEEE ,vol. 86, no. 11, pp. 2278–2324, 2002.