---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一令：基于增量令牌的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:q1"]
score: 10.0
evidence: 使用增量标记的高效生成式世界建模
tldr: 本文针对视频世界建模中高效生成多样化未来状态的挑战，提出了 DeltaTok 和 DeltaWorld。DeltaTok 将连续帧间的视觉基础模型特征差异编码为单个“delta”令牌，从而将视频从三维时空表示压缩为一维时间序列，大幅减少了令牌数量。基于此，DeltaWorld 作为一种生成式世界模型，能够进行高效的多假设训练与推理，在单次前向传播中即可产生多样化的未来预测。该方法在密集预测任务上取得了更接近真实世界的结果，同时参数量和计算量相比现有生成式模型显著降低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 视频世界建模中预测多样未来状态是一大挑战，现有生成式模型计算成本高，而判别式模型只能做出确定性预测。
method: 该方法引入 DeltaTok 将连续帧间的特征差异编码为单个“delta”令牌，并构建 DeltaWorld 模型在此令牌序列上进行高效的多假设生成式训练。
result: 实验表明，DeltaWorld 在密集预测任务中能生成更符合真实世界的多样化未来，其参数量比现有生成式模型少35倍以上，计算量减少2000倍。
conclusion: DeltaWorld 通过创新的 Delta 令牌和生成式建模，在保持预测多样性的同时，实现了参数和计算量的大幅降低，为高效的世界建模提供了新方案。
---

## 摘要
预测多样化的未来状态是视频世界建模的核心挑战。判别式世界模型产生一个确定性预测，该预测隐式地平均了可能的未来，而现有的生成式世界模型在计算上仍然昂贵。最近的研究表明，在视觉基础模型（VFM）的特征空间中预测未来，而不是在为像素重建优化的潜在空间中，所需的世界模型参数显著减少。然而，大多数此类方法仍然是判别式的。在这项工作中，我们引入了DeltaTok（一种分词器），它将连续帧之间的VFM特征差异编码为单个连续的“增量”令牌；以及DeltaWorld（一种生成式世界模型），它对这些令牌进行操作以高效生成多样化的合理未来。增量令牌将视频从三维时空表示简化为一维时间序列，例如对于512x512的帧可产生1,024倍的令牌缩减。这种紧凑的表示使得可行的多假设训练成为可能，即并行生成多个未来并仅监督最佳者。在推理时，这能在单次前向传递中产生多样化的预测。在密集预测任务上的实验表明，DeltaWorld预测的未来更符合真实世界结果，同时其参数量比现有生成式世界模型少35倍以上，使用的FLOPs少2,000倍。代码和权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：在视频世界建模中，预测未来状态对于自动驾驶等应用至关重要。未来具有不确定性，因此模型需要能够生成**多样化且合理**的未来假设。现有方法面临两大挑战：
    *   **判别式模型**（如DINO-world）只能输出单一的确定性预测，在不确定性下会趋向于条件均值，无法捕捉多样的未来模式。
    *   **生成式模型**（如Cosmos）虽然能产生多样结果，但计算成本高昂。其低效性源于：(i) 在像素级或为重建优化的潜在空间中进行建模；(ii) 需要多次前向传递（如扩散模型的迭代去噪）来生成单个样本；(iii) 未能充分利用连续帧间的时空冗余。
*   **整体目标**：提出一种**高效、紧凑的生成式世界模型**，能够在单次前向传递中生成多个多样化的未来预测，同时大幅降低参数量和计算开销。

## 2. 方法论
*   **核心思想**：将视频建模从三维时空网格压缩为一维时间序列。核心是仅对帧间的“变化”进行建模，而非每一帧的全部内容。
*   **关键技术细节**：
    1.  **DeltaTok（增量令牌分词器）**：
        *   输入：一个冻结的视觉基础模型（VFM，如DINOv3）提取的连续两帧特征图 `x_{t-1}` 和 `x_t`。
        *   编码器：一个基于ViT的编码器，将两个特征图压缩为单个连续的“增量令牌” `z_t`。该令牌编码了从 `x_{t-1}` 到 `x_t` 的变化。
        *   解码器：接收前一帧特征 `x_{t-1}` 和增量令牌 `z_t`，重构出当前帧特征 `\hat{x}_t`。
        *   优势：极大地压缩了表示（例如512×512帧从1024个空间令牌变为1个增量令牌），并利用了视频的时间冗余性。
    2.  **DeltaWorld（世界模型）**：
        *   架构：基于判别式世界模型DINO-world的预测器架构进行改造。
        *   **Best-of-Many (BoM)训练目标**：
            *   训练时：从高斯分布中采样K个噪声查询向量 `q_k`。每个查询向量通过预测器独立地生成一个未来的增量令牌候选 `\hat{z}_{t+1}^k`。
            *   损失计算：仅对K个候选中最接近真实未来增量令牌的那一个施加监督损失（平滑L1损失）。
            *   作用：鼓励模型将不同的噪声输入映射到不同的合理未来上，实现了单次前向传递内的多样化生成能力。
        *   推理与自回归展开：
            *   给定上下文增量令牌序列 `Z_{1:t}`，使用不同的噪声查询可并行生成多个未来的增量令牌序列。
            *   通过DeltaTok解码器可将预测的增量令牌序列逐步解码回空间特征图，供下游任务使用。

## 3. 实验设计
*   **评估基准与任务**：采用密集预测基准 ，评估短期（~0.2秒，直接预测）和中期（~0.6秒，三步自回归展开）预测精度。具体任务包括：
    1.  语义分割 (mIoU)：在VSPW和Cityscapes数据集上评估。
    2.  单目深度估计 (RMSE)：在KITTI数据集上评估。
*   **对比方法**：
    *   **判别式基线**: DINO-world （作者重新实现）。
    *   **生成式基线**: Cosmos-4B和Cosmos-12B （一种在像素重建潜在空间中运行的扩散模型）。
    *   **上下界**: “复制最后一帧”作为下界，“使用真实未来帧”作为上界。

## 4. 资源与算力
*   **硬件与训练规模**: 
    - GPU: NVIDIA H200 (8卡用于测量训练时间和内存)。
- VFM: DINOv3 ViT-B作为冻结的特征提取器。
- Tokenizer: ViT-B配置的编码器和解码器,单独训练50K步,批量大小1024.
- Predictor: ViT-B配置,主实验训练300K步,消融实验100K步,批量大小1024.
- BoM训练中的查询数K:主实验256,消融实验16.
*   **算力对比**: 
论文的核心贡献之一是效率提升。如表3所示:
DeltaWorld (0.3B参数)的总FLOPs为31 TFLOPs (用于生成20个样本)。
相比之下,Cosmos-4B和Cosmos-12B的总FLOPs分别为60,000 TFLOPs和64,000 TFLOPs(包含其7B参数的扩散解码器),即DeltaWorld的计算量减少了约2000倍。

##5.实验数量与充分性
论文设计了较为系统且充分的实验来验证方法的有效性:
*主要性能对比(表3):在三个数据集(VSPW、Cityscapes、KITTI)、两个时间尺度(短/中期)、两个评价指标(mIoU/RMSE)上全面对比了判别式和生成式基线,并报告了最佳样本(`best`)和平均样本(`mean`)的性能.
*渐进式构建分析(表2):通过三个步骤清晰地展示了从判别式基线到最终DeltaWorld的性能演变:(0)判别式,(1)+BoM训练,(2)+全帧压缩,(3)+增量压缩(DeltaWorld).这有效地说明了每个组件的作用及其带来的效率/精度权衡.
*消融研究:
-BoM样本缩放研究(图5):探究了训练时查询数(K_train)和评估时查询数(K_eval)对性能的影响.
-DeltaTokens在其他架构中的有效性(附录C):将DeltaTokens应用于另一个判别式世界模型DINO-Foresight,证明了其通用性.
-FLOPs分解分析(附录B表B):详细分析了各组件(DinoV3骨干网、DeltaTok编码/解码器、预测器)的计算开销占比.
*定性分析(图6及附录E):提供了多个可视化案例,展示了生成的多样化未来在分割、深度估计以及RGB重建上的效果.

总体而言,**实验设计是客观且公平的**.遵循了已有基准的评价协议;对于基线方法(Cosmos),按照其约束条件进行评估并统一用相同的VFM重新编码特征以确保公平比较;同时报告最佳(`best`)和平均(`mean`)性能,全面衡量了模型的多样性和准确性.

##6.主要结论与发现
1.**高效且有效的表示**: DeltaTok成功地将视频压缩为一维时间序列的“增量令牌”,实现了高达1024倍的令牌缩减,同时保持了足够的信息以准确重构特征.
2.**高效的生成能力**:结合BoM目标训练的DeltaWorld能够在单次前向传递中并行生成多个多样化的合理未来假设.
3.**显著的效率优势**:相比现有的强大生成式世界模型(Cosmos),DeltaWorld以超过35倍的更少参数和2000倍的更低计算量(FLOPS),取得了更优或相当的性能.DeltaWorld的最佳(`best`)样本在所有任务上都超越了Cosmos;其平均(`mean`)样本也与专为平均预测优化的判别式基线(DINO-world)表现相当甚至更好.
4.**性能提升归因**:作者认为性能提升部分归功于“delta公式”的自然先验——预测无变化即保留前一帧,这使得模型只需专注于学习随时间变化的部分.

##7.优点
1.**创新性的表示学习**:提出“delta token”概念,巧妙地将高维时空冗余信息压缩到一维变化序列中,是视频表示学习的一个简洁而有效的思路.
2.**巧妙的训练策略**:采用Best-of-Many(BoM)目标将判别式模型转化为高效的生成式模型,**避免了扩散模型中昂贵的迭代采样过程**,实现了单次前向传播的多样本生成.
3.**卓越的效率表现**:论文最突出的贡献在于实现了数量级的参数量和计算量削减(**35倍参数减少、2000倍FLOPS减少**)的同时保持甚至提升了性能,**为实际部署提供了可能**.详细的FLOPs分解也增强了说服力.
4.**系统性的验证**:通过渐进式的构建分析、充分的定量比较以及丰富的定性可视化,**全面地论证了方法的有效性和优势**.

##8.不足与局限
论文已在附录D中讨论了局限性:
1.**分布建模的理论保证不足**: BoM目标缺乏像扩散模型那样的显式分布建模目标.模型的多样性覆盖受限于训练时探索的噪声查询数量K,**没有机制保证采样的未来分布能近似真实的概率分布**.虽然实践中不同查询会产生不同结果,但这更像是一种隐式的动作条件化而非严格的概率建模.
2.**误差累积风险**:由于采用自回归方式基于前一重构帧来解码当前帧,**Tokenizer的重构误差可能在多步展开过程中累积**,导致特征漂移.DeltaWorld自身的预测误差也会加剧这一问题.(作者提到可采用顺序计算delta token等现有技术缓解).
3.**应用场景限制**:目前工作主要聚焦于密集视觉预测任务(分割、深度).虽然框架具有通用性,**但在需要精确物理模拟或长程规划的任务中的有效性尚未验证**.此外,VFM的选择(DINOv3)限制了特征的语义范围.

**(完)**

## 9. 潜在影响与未来方向
1.  **对世界建模领域的影响**：论文为生成式世界模型提供了一条**高效化**的新路径。它挑战了“高质量生成必须依赖复杂迭代采样（如扩散）”的固有观念，证明了通过精心设计的表示和训练目标，可以在单次前向传递中获得多样且高质量的预测。这可能会激励更多研究探索非扩散的高效生成范式。
2.  **对下游应用的推动**：极低的推理成本使得在自动驾驶、机器人等实时系统中部署生成式世界模型进行多假设规划成为可能。模型可以实时并行生成多个未来场景，供规划器评估风险或选择最优策略。
3.  **未来研究方向**：
    *   **理论改进**：探索如何为BoM目标引入更严格的分布匹配约束（如对抗性训练、基于能量的模型），以更好地覆盖真实未来分布。
    *   **长程预测与闭环控制**：将框架扩展到更长的时间范围，并集成到闭环的决策与控制循环中进行端到端评估。
    *   **多模态与通用化**：探索将Delta Token思想应用于更丰富的传感器模态（如LiDAR、雷达）或更通用的视觉基础模型，以构建更全面的世界模型。

## 10. 总结评价
本文是一项在视频世界建模领域兼具**创新性、实用性和简洁性**的优秀工作。其核心贡献在于提出了“Delta Token”这一高效的视频表示方法，并结合“Best-of-Many”训练策略，成功构建了一个参数和计算量极低、却能单次前向生成多样未来的生成式世界模型。

论文通过严谨的实验设计，在多个标准基准上证明了该方法在性能上媲美或超越现有方法，同时在效率上实现了数个数量级的提升。尽管存在理论保证不足和误差累积等局限性，但其开创性的思路和卓越的工程实现为高效生成建模树立了新的标杆，对推动世界模型在实际系统中的落地应用具有重要价值。

（完）
