---
title: Scaling Video Pretraining for Surgical Foundation Models
title_zh: 扩展用于手术基础模型的视频预训练
authors: "Sicheng Lu, Zikai Xiao, Jianhui Wei, Danyu Sun, Qi Lu, Keli Hu, Yang Feng, Jian Wu, Zongxin Yang, Zuozhu Liu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29966v1"
tags: ["query:q9"]
score: 9.0
evidence: 用于手术视频预训练的 SurgRec-JEPA 变体
tldr: 针对手术视频理解领域数据规模有限、评估不统一的问题，本研究提出了一个名为SurgRec的可扩展且可复现的预训练方案。该方法基于一个包含超过一万个手术视频的大型多源数据集，开发了统一的预训练流程和标准化基准。研究贡献在于提供了两种具体实现（SurgRec-MAE/JEPA），并在多个下游任务上验证了其优于现有基线的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29966v1/fig-001.webp\", \"caption\": \"Table 2. Main comparison on Acc@1 (%) against SSL baselines. SR and V-MAE denote SurgRec and VideoMAE, respectively. SR-MAE (w/o bal.) refers to SurgRecMAE trained without balanced sampling. Dataset names are abbreviated (e.g., Cat: Cataract, Colono: Colonoscopic).\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-002.webp\", \"caption\": \"Table 3. VLM Acc@1 (%). SurgRec-MAE is the supervised reference. P1: baseline prompt; P2: stricter output constraint. ∆ = P2−P1 (points). Dataset names are abbreviated as in Table 2.\", \"page\": 7, \"index\": 2, \"width\": 716, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-003.webp\", \"caption\": \"Table 1. Pretraining data scale comparison across recent medical foundation models.\", \"page\": 3, \"index\": 3, \"width\": 725, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed medical video pretraining and evaluation framework.\", \"page\": 3, \"index\": 4, \"width\": 581, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-005.webp\", \"caption\": \"Table 4. Generalization analysis on the 4-domain benchmark (Acc@1, %). Domain scores are macro-averaged; Overall Macro and Worst Domain denote the mean and minimum across domains, respectively.\", \"page\": 8, \"index\": 5, \"width\": 731, \"height\": 304}]"
motivation: 手术视频理解对于计算机辅助干预至关重要，但现有手术基础模型受限于数据规模、手术多样性和评估不一致性，且缺乏可复现的训练流程。
method: "提出了一个名为SurgRec的可扩展且可复现的预训练方案，包含SurgRec-MAE和SurgRec-JEPA两个变体，并基于一个包含10,535个视频的大型多源数据集，开发了统一的预训练流程和标准化评估基准。"
result: 在16个下游数据集上的广泛比较中，SurgRec始终优于自监督学习和视觉语言模型基线，而后者在细粒度时序识别任务上表现不可靠。
conclusion: 本研究为社区提供了一个可复现、可扩展的基础，以构建更通用的手术视频模型。
---

## 摘要
手术视频理解对于计算机辅助干预至关重要，然而现有的手术基础模型仍受限于数据规模有限、手术流程多样性不足以及评估标准不一致，通常缺乏可复现的训练流程。我们提出了SurgRec，一个用于手术视频理解的可扩展且可复现的预训练方案，并实例化为两个变体：SurgRec-MAE和SurgRec-JEPA。我们整理了一个包含10,535个视频和2.145亿帧的大型多源数据集，涵盖内窥镜、腹腔镜、白内障和机器人手术。基于此数据集，我们开发了一个具有平衡采样功能的统一预训练流程，并在16个下游数据集和四个临床领域中通过一致的数据划分标准化了一个可复现的基准测试。在与自监督学习基线和视觉语言模型的广泛比较中，SurgRec在下游数据集上始终表现出卓越的性能。相比之下，视觉语言模型在细粒度时序识别方面被证明不可靠，既存在性能差距也对提示措辞敏感。我们的工作为社区构建更通用的手术视频模型提供了一个可复现、可扩展的基础。所有代码、模型和数据都将公开发布。

## Abstract
Surgical video understanding is essential for computer-assisted interventions, yet existing surgical foundation models remain constrained by limited data scale, procedural diversity, and inconsistent evaluation, often lacking a reproducible training pipeline. We propose SurgRec, a scalable and reproducible pretraining recipe for surgical video understanding, instantiated with two variants: SurgRec-MAE and SurgRec-JEPA. We curate a large multi-source corpus of 10,535 videos and 214.5M frames spanning endoscopy, laparoscopy, cataract, and robotic surgery. Building on this corpus, we develop a unified pretraining pipeline with balanced sampling and standardize a reproducible benchmark across 16 downstream datasets and four clinical domains with consistent data splits. Across extensive comparisons against SSL baselines and vision-language models, SurgRec consistently achieves superior performance across downstream datasets. In contrast, VLMs prove unreliable for fine-grained temporal recognition, exhibiting both performance gaps and sensitivity to prompt phrasing. Our work provides a reproducible, scalable foundation for the community to build more general surgical video models. All code, models, and data will be publicly released.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对《Scaling Video Pretraining for Surgical Foundation Models》这篇论文进行结构化、深入且客观的总结。

---

# 论文总结：Scaling Video Pretraining for Surgical Foundation Models

## 1. 核心问题与整体含义
*   **研究动机**：手术视频理解是计算机辅助干预的核心，但现有手术基础模型面临三大瓶颈：
    1.  **数据规模有限**：缺乏大规模、多样化的预训练数据。
    2.  **评估不一致**：不同研究使用不同的数据集和划分方式，导致结果难以公平比较和复现。
    3.  **方法可靠性存疑**：视觉语言模型（VLMs）在细粒度手术时序理解任务上的有效性和稳定性尚未得到验证。
*   **整体含义**：本研究旨在为手术视频理解领域提供一个**可扩展、可复现的预训练方案（SurgRec）**，并通过构建大规模数据集、统一训练流程和标准化基准测试，系统性地探索数据规模、平衡采样以及不同预训练范式（SSL vs. VLM）对模型泛化能力的影响。

## 2. 方法论
*   **核心思想**：通过整合多源、大规模的手术视频数据，并采用平衡采样策略来缓解数据分布不均的问题，从而训练出泛化能力更强的通用手术视频表征模型。
*   **关键技术细节**：
    1.  **数据集构建 (SurgRec Corpus)**：
        *   来源：整合了32个公开数据集（2,790个视频）和公开可获取的教学资源（7,745个视频），总计10,535个视频、214.5M帧。
        *   领域覆盖：腹腔镜、内窥镜（胃肠）、白内障、机器人手术四大临床领域。
    2.  **平衡采样策略**：
        *   使用DINOv3提取所有视频片段的特征嵌入。
        *   应用分层K-means聚类（25k → 5k → 1k）。
        *   从每个最终簇的中心附近选取样本，确保从长尾分布中均匀采样，构建一个平衡的子集用于训练。
    3.  **统一预训练流程**：
        *   **混合批次策略**：每个训练批次由15%的“临床核心”（来自公开数据集）和85%的混合数据（70%平衡采样的无标签网络视频 + 30%临床核心）组成，最终全局比例约为60%无标签 vs. 40%临床数据。
        *   **模型变体**：基于两种主流自监督学习范式实例化了SurgRec方案：
            *   `SurgRec-MAE`：基于掩码重建（如VideoMAE）。
            *   `SurgRec-JEPA`：基于潜在预测（如V-JEPA）。
    4.  **标准化评估基准 (SurgBench)**：
        *   涵盖16个下游数据集，分属上述四大领域。
        *   制定了可复现的数据划分规则（优先使用官方/社区划分，否则采用7:2:1的视频级划分），并公开发布划分清单。

## 3. 实验设计
*   **Benchmark**: SurgBench。包含16个下游数据集，用于评估模型在四大手术领域的泛化能力。
*   **对比方法**：
    *   **自监督学习基线**: `VideoMAE`, `JEPA`, `DINOv3` (通用域)，以及`DINOv3-SurgeNetXL` (在临床子集上继续预训练的域适应版本)。
    *   **视觉语言模型基线**: `Qwen3-VL-8B`, `LLaVA-NeXT-7b`, `Qwen2.5-VL-7B` (进行零样本推理)。
    *   **消融实验**: `SurgRec-MAE (w/o bal.)` (不使用平衡采样的版本)。

## 4. 资源与算力
论文明确提到了算力配置：
*   **预训练**: 使用8块NVIDIA A100 GPU。`SurgRec-MAE`耗时约8天，`SurgRec-JEPA`耗时约15天。
*   **下游微调**: 在每个下游数据集上使用8块RTX 3090 GPU进行微调，耗时1至12小时不等。

## 5. 实验数量与充分性
实验设计较为充分和客观：
*   **主实验数量**:在16个独立的下游数据集上分别评估了所有基线方法和提出的方法 (`SurgRec-MAE/JEPA`)。
*   **对比维度全面性**:
    1.  **跨域泛化分析**:将16个数据集的结果按四大领域进行宏平均比较 (`Table4`)。
    2.  **VLM系统性评估**:不仅比较性能差距 (`Table3`)，还测试了提示词敏感性 (`P1 vs P2`) ，揭示了VLM的不稳定性。
    3.  **关键消融实验**:通过对比有无平衡采样的版本 (`Table4`)，明确分离了“数据规模”和“数据分布”各自的影响。
*   **公平性与客观性保障**:
    1.  所有对比方法均在完全相同的标准化测试集上进行评估。
    2.  对于SSL方法采用相同的微调协议；对于VLM采用零样本推理以确保公平比较其开箱即用能力。

## -6-.主要结论与发现
1.	**规模化与平衡采样至关重要**:大规模、多领域的预训练能显著提升模型在下游任务上的性能。然而，仅增加数据量而不进行平衡采样会导致严重的域间性能不均衡；引入平衡采样策略能有效提升最差领域的性能及整体宏平均精度 (+10个百分点)。
2	.**域内预训练的优越性**:提出的两种域内预训练变体 (`SurgRec-MAE/JEPA`)在绝大多数下游任务上均稳定优于通用域的SSL基线 (`VideoMAE/JEPA`)以及经过部分域适应的基线(`DINOv3-SurgeNetXL`) 。
3	.**VLMs在细粒度手术任务上不可靠**:尽管VLMs具有开放词汇能力，但在细粒度的手术时序识别任务上存在明显性能差距。此外其表现对提示词措辞敏感不稳定这限制了其在需要高可靠性的临床应用中的直接使用.
4	.**提供了一个可复现的基础框架:**通过公开发布大规模数据集(SurgRec Corpus)标准化的评测基准(SurgBench)以及详细的训练配方(SurgRec)为社区后续研究提供了坚实且可比较的基础.

## -7-.优点
1	.**系统性贡献突出:**不仅提出了新方法更贡献了高质量的数据集标准化的评测基准和完整的可复现流程这对推动整个领域发展具有重要价值.
2	.**分析深入透彻:**不仅展示了“是什么”(性能提升)还深入探究了“为什么”(通过消融实验分离了规模效应与分布效应)并对新兴的VLM范式进行了批判性验证.
3	.**工程严谨性高:**详细描述了数据处理流程(包括伦理审查)混合批次策略以及确保评测公平性的数据划分规则体现了高度的严谨性和可复现性.
4	.**洞察具有前瞻性:**明确指出当前基于网络教学视频的数据集与真实临床记录之间存在域鸿沟为未来工作指明了方向.

## -8-.不足与局限
1	.**数据源偏差:**尽管规模庞大但大部分网络爬取的教学视频内容偏向于“理想化”或“精华片段”缺乏真实手术中常见的并发症模糊烟雾遮挡等挑战场景这可能导致模型在实际复杂临床环境中的泛化能力被高估.
2	.**评测任务的局限性:**下游评测主要集中在分类识别任务(如阶段工具动作识别)尚未覆盖更复杂的任务如预测决策支持或生成式任务这些可能是未来手术AI系统的关键功能.
3	.**计算成本较高:**虽然使用了高效的SSL方法但预处理大规模数据和长达数周的A100集群预训练仍需要可观的算力资源可能对部分研究者构成门槛.
4	.**部分基准数据集较小或存在噪声:**作者承认某些评测子集规模较小或标注有噪声这可能影响个别结果指标的绝对可靠性.

(完)

好的，我将从您提供的“不足与局限”部分之后继续，补充论文的其他关键部分和整体评价。

---

## 9. 潜在影响与未来方向
1.  **对领域发展的影响**：
    *   **标准化推动**：通过提供SurgRec Corpus和SurgBench，为手术视频理解领域建立了一个事实上的“预训练-评测”标准流程，有望结束此前各自为政、难以比较的局面。
    *   **范式验证**：系统性地证明了在手术领域进行大规模、多源域内预训练的有效性，并揭示了当前通用VLMs在细粒度手术任务上的局限性，为后续研究资源投入提供了明确指引。
    *   **基础模型生态**：发布的预训练模型（`SurgRec-MAE/JEPA`）可作为强大的视觉编码器，供社区直接用于各种下游任务（如检测、分割）的微调或特征提取，降低研究门槛。

2.  **提出的未来研究方向**：
    *   **数据质量与真实性**：需要构建包含更多真实临床记录（含并发症、非理想情况）的大规模数据集，以弥合教学视频与真实手术之间的域鸿沟。
    *   **任务扩展**：将基准测试扩展到更复杂的任务，如手术工作流预测、决策支持、自动报告生成、3D重建等。
    *   **多模态融合**：探索整合除视觉外的其他模态数据（如机器人运动学数据、生理信号、外科医生注视点），以构建更全面的手术情境理解模型。
    *   **高效架构与训练**：研究更高效的视频编码架构和预训练目标，以降低计算成本，使资源有限的研究者也能受益。

## 10. 总结评价
本文是一项工程扎实、分析全面且对社区具有重要基础性贡献的研究。它没有追求单一指标的突破，而是致力于解决该领域长期存在的根本性问题——数据碎片化与评估不一致。其核心价值在于提供了一个**可扩展、可复现的解决方案框架（SurgRec）** 和一个用于公平比较的**标准化基准（SurgBench）**。

*   **贡献的层次性**：贡献体现在三个层面：（1）方法论层面，提出了结合大规模数据与平衡采样的预训练方案；（2）资源层面，构建并发布了大规模多源数据集；（3）评估层面，建立了统一的评测体系。这种多层次贡献极大地提升了工作的实用价值和影响力。
*   **批判性思维的体现**：论文不仅展示了所提方法的优势，还通过严谨的消融实验剖析了成功的关键因素（规模 vs. 分布），并对热门的VLM范式进行了客观评估，指出了其在特定任务上的局限性。这种批判性视角增强了结论的可信度。
*   **局限性的坦诚**：作者明确指出了工作的主要局限（如数据源偏差、任务范围），这非但没有削弱其价值，反而体现了研究的严谨性并为后续工作指明了清晰路径。

总而言之，《Scaling Video Pretraining for Surgical Foundation Models》是一篇具有里程碑意义的论文。它通过系统性的工程努力和深入的分析，为手术视频基础模型的研究奠定了新的基石，有望显著加速该领域向更可靠、可泛化且可比较的方向发展。

（完）
