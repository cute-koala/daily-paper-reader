---
title: "Mine-JEPA: In-Domain Self-Supervised Learning for Mine-Like Object Classification in Side-Scan Sonar"
title_zh: Mine-JEPA：用于侧扫声呐中类水雷物体分类的领域内自监督学习
authors: "Taeyoun Kwon, Youngwon Choi, Hyeonyu Kim, Myeongkyun Cho, Junhyeok Choi, Moon Hwan Kim"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00383v1"
tags: ["query:q2"]
score: 10.0
evidence: Mine-JEPA 使用联合嵌入预测架构进行自监督学习
tldr: 本研究针对侧扫声纳水雷分类任务中数据稀缺和领域差异大的挑战，提出了首个域内自监督学习流程Mine-JEPA。该方法仅使用1170张未标记声纳图像进行预训练，在二元和三元分类任务中均超越了基于海量自然图像预训练的DINOv3基础模型。结果表明，精心设计的域内自监督学习是数据稀缺声纳图像分析中更高效可行的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00383v1/fig-001.webp\", \"caption\": \"Figure 1. Performance comparison across model scales. Using only 1,170 sonar images for pretraining, Mine-JEPA outperforms DINOv3, pretrained on 1.7B images, at both backbone scales. ViT-Tiny remains competitive with 4× fewer parameters.\", \"page\": 1, \"index\": 1, \"width\": 462, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-002.webp\", \"caption\": \"Table 4. Ablation on initialization, augmentation, and data composition. Real∗ denotes natural-image augmentation (saturation/hue jitter, solarization, grayscale). Rows 1–2 compare against Random Init, rows 3–4 against DINOv3, and rows 5–8 show cumulative gains; each ∆ is relative to the preceding row.\", \"page\": 6, \"index\": 2, \"width\": 952, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Mine-JEPA pipeline. Stage 1 extracts unlabeled 96×96 patches from full SSS images using a sliding window. Stage 2 performs in-domain self-supervised pretraining with SSS-adapted multi-view augmentation and the SIGReg objective. Stage 3 evaluates the pretrained backbone with frozen or fine-tuned probes for downstream patch classification.\", \"page\": 3, \"index\": 3, \"width\": 1014, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-004.webp\", \"caption\": \"Figure 3. Representative samples from the public side-scan sonar dataset. Top: a full SSS image with annotated mine-like contacts (MILCO) and non-mine bottom objects (NOMBO). Bottom: 96×96 patches from the three classes used in our evaluation: MILCO, background (BG), and NOMBO.\", \"page\": 4, \"index\": 4, \"width\": 475, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-005.webp\", \"caption\": \"Table 1. 3-class patch classification on BG/MILCO/NOMBO. For each method, we report the best probe mode and the corresponding macro-F1, NOMBO F1, and accuracy. Probe modes: linear and mlp (frozen backbone), finetune and ft mlp (fine-tuned backbone).\", \"page\": 5, \"index\": 5, \"width\": 994, \"height\": 223}]"
motivation: 侧扫声纳水雷分类面临数据极度稀缺以及与自然图像存在巨大领域差异的挑战，而通用基础模型在此领域的应用尚未充分探索。
method: 该方法提出了首个用于侧扫声纳的域内自监督学习流程Mine-JEPA，使用基于正则化的自监督损失SIGReg，仅需1170张未标记声纳图像进行预训练。
result: 在二元分类任务中，Mine-JEPA的F1分数达到0.935，优于在17亿图像上预训练的DINOv3（0.922）；在三分类任务中（使用数据增强）也以0.820优于DINOv3（0.810）。
conclusion: 研究表明，在数据稀缺的侧扫声纳图像领域，精心设计的域内自监督学习是比依赖海量预训练的大型基础模型更可行的替代方案。
---

## 摘要
侧扫声呐（SSS）水雷分类是一个具有挑战性的海洋视觉问题，其特点是数据极度稀缺且与自然图像存在巨大的领域差异。尽管自监督学习（SSL）和通用视觉基础模型在通用视觉及多个专业领域表现出强大的性能，但它们在SSS中的应用在很大程度上仍未得到探索。我们提出了Mine-JEPA，这是首个用于SSS水雷分类的领域内自监督学习流程，它使用基于正则化的SSL损失函数SIGReg，仅用1,170张未标记的声呐图像进行预训练。在二分类（水雷与非水雷）设置中，Mine-JEPA取得了0.935的F1分数，优于经过微调的、在17亿张图像上预训练的基础模型DINOv3（0.922）。对于3类水雷状物体分类任务，Mine-JEPA通过合成数据增强达到了0.820的分数，同样优于经过微调的DINOv3（0.810）。我们进一步观察到，将领域内自监督学习应用于基础模型会导致性能下降10-13个百分点，这表明更强的预训练模型并不总能从额外的领域适应中受益。此外，采用紧凑型ViT-Tiny骨干网络的Mine-JEPA实现了有竞争力的性能，同时其参数量比DINOv3少4倍。这些结果表明，在数据稀缺的海洋声呐图像处理中，精心设计的领域内自监督学习是替代庞大基础模型的一个可行方案。

## Abstract
Side-scan sonar (SSS) mine classification is a challenging maritime vision problem characterized by extreme data scarcity and a large domain gap from natural images. While self-supervised learning (SSL) and general-purpose vision foundation models have shown strong performance in general vision and several specialized domains, their use in SSS remains largely unexplored. We present Mine-JEPA, the first in-domain SSL pipeline for SSS mine classification, using SIGReg, a regularization-based SSL loss, to pretrain on only 1,170 unlabeled sonar images. In the binary mine vs. non-mine setting, Mine-JEPA achieves an F1 score of 0.935, outperforming fine-tuned DINOv3 (0.922), a foundation model pretrained on 1.7B images. For 3-class mine-like object classification, Mine-JEPA reaches 0.820 with synthetic data augmentation, again outperforming fine-tuned DINOv3 (0.810). We further observe that applying in-domain SSL to foundation models degrades performance by 10--13 percentage points, suggesting that stronger pretrained models do not always benefit from additional domain adaptation. In addition, Mine-JEPA with a compact ViT-Tiny backbone achieves competitive performance while using 4x fewer parameters than DINOv3. These results suggest that carefully designed in-domain self-supervised learning is a viable alternative to much larger foundation models in data-scarce maritime sonar imagery.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Mine-JEPA: In-Domain Self-Supervised Learning for Mine-Like Object Classification in Side-Scan Sonar》的论文进行结构化、深入且客观的总结。

---

## 1. 核心问题与整体含义
*   **研究背景**：侧扫声呐（SSS）水雷分类是海洋安全领域的关键任务。传统方法依赖人工判读，效率低下。虽然深度学习在自然图像领域取得了巨大成功，但将其应用于SSS图像面临两大核心挑战：
    1.  **数据极度稀缺**：公开可用的标注声呐图像数量极少。
    2.  **巨大的领域差异**：SSS图像（基于声学回波的亮-影结构、海底纹理）与自然RGB图像在视觉统计特性上存在根本性差异。
*   **研究动机**：尽管自监督学习（SSL）和大型视觉基础模型（如DINOv3）在其他领域表现出色，但它们在SSS领域的应用尚未得到充分探索。一个自然的思路是进行“领域内”自监督学习，但现有SSL方法大多针对大规模数据设计，在小数据、强域差场景下的有效性存疑。本文旨在探索并验证一种适用于SSS水雷分类的、高效的小数据域内自监督学习方案。

## 2. 方法论
*   **核心思想**：提出 **Mine-JEPA** 流程，其核心是在极少量（1170张）未标记SSS图像上进行领域内自监督预训练，以获得优于在海量自然图像上预训练的大型基础模型的表征能力。
*   **关键技术细节**：
    *   **三阶段流程**：
        1.  **数据准备**：从全尺寸SSS图像中以滑动窗口提取96x96的图像块。
        2.  **域内SSL预训练**：使用经过改造的自监督目标函数和增强策略对Vision Transformer骨干网络进行预训练。
        3.  **下游评估**：将预训练好的骨干网络接入线性或MLP分类头，在标注数据上进行微调或冻结评估。
    *   **SSL目标函数 - SIGReg**: 采用LeJEPA框架中的Sketched Isotropic Gaussian Regularization损失。该损失结合了视图不变性损失和分布正则化损失，旨在将嵌入向量分布推向标准正态分布。其优势在于结构简单（无需动量编码器、负样本对或复杂的防坍缩启发式方法），非常适合小数据场景下的稳定优化。
        *   公式简化为：`L = (1 - λ) * Linv + λ * Lsig`，其中`Linv`促使同一图像的不同增强视图表征一致，`Lsig`通过随机切片计算拟合优度统计量来正则化整体嵌入分布。
    *   **针对SSS领域的三大关键适配**：
        1.  **SSS定制化数据增强**：移除了对自然图像有效但对声呐无效的增强（如色相/饱和度抖动、灰度化），增加了符合声呐扫描几何特性的增强（如垂直翻转、小角度旋转）。
        2.  **恰当的初始化策略**：使用ImageNet-1K预训练的权重初始化ViT骨干网络（而非随机初始化或DINOv3初始化），为模型提供基础的视觉先验，同时保留足够的适应空间。
        3.  **异构数据组合**：除了真实的RGB声呐块外，还加入了大量灰度合成声呐块进行混合预训练，以测试SSL方法吸收异构但相似数据的能力。

## 3. 实验设计
*   **数据集与Benchmark**: 
    *   使用公开的侧扫声呐水雷检测数据集 [24]。包含1170张图像，其中304张包含668个标注物体（437个类水雷接触物MILCO和231个非水雷海底物体NOMBO）。
    *   从所有1170张图中提取约15.3万个未标记96x96块用于SSL预训练。在下游任务中，使用标注区域提取的块进行分类评估。
    *   Benchmark任务：
        *   **三分类**: 背景(BG) / MILCO / NOMBO。
        *   **二分类**: MILCO vs. (BG+NOMBO)。
*   **对比方法**:
    *   **基线模型**: Random Init, ImageNet-1K (IN1K) Only, DINOv3 (ViT-S/16)。
    *   **其他SSL方法**: SimCLR, VICReg, BYOL (均在相同IN1K初始化下用相同数据进行预训练)。
    *   **消融对照**: 
        *   不同初始化源（Random, IN1K, DINOv3）。
        *   不同数据增强策略（自然图像增强 vs SSS定制增强）。
        *   不同数据构成（仅真实数据 vs “真实+合成”混合数据）。

## 4. 资源与算力
论文中未明确说明实验所使用的具体GPU型号、数量及单次训练的精确时长。仅提及了训练配置如批量大小(1024)、优化器(AdamW)、总轮数(100轮)等超参数。可以推断实验是在标准深度学习服务器上完成的。

## 5. 实验数量与充分性
*   **实验数量充足且系统化**: 
    1. 在主实验中对比了多种基线模型和主流SSL方法在三分类和二分类任务上的性能。
    2. 进行了详尽的消融研究以验证每个关键设计选择的有效性：
        *   初始化策略的影响。
        *   数据增强策略的影响（证明定制化增强至关重要）。
        *   添加合成数据的收益分析。
        *   不同SSL目标函数在异构数据处理上的鲁棒性比较。
    3. 测试了不同规模的骨干网络(ViT-Tiny vs ViT-S)。
    4.所有结果均报告了10次随机种子的均值±标准差，增强了统计可靠性。
*   **客观性与公平性**: 
    对比实验的设置相对公平——对比的SSL方法均使用相同的骨干网络(ViT-S/16)、相同的IN1K初始化、相同的数据集和相同的训练轮数。这确保了性能差异主要源于算法本身而非不公平的训练条件。

## 6.主要结论与发现
1. Mine-JEPA在仅使用1170张未标记声呐图像的条件下进行域内自监督学习后：
      -在三分类任务上达到0.820的macro-F1分数；在二分类任务上达到0.935的F1分数；均超越了在海量自然图像(17亿张)上预训练的DINOv3基础模型(分别为0.810和0.922)。
      -证明了“小规模精心设计的域内学习”可以超越“大规模通用预训练+微调”的模式。

2.**重要反直觉发现**: 
      -对强大的基础模型(DINOv3)直接应用额外的域内自监督学习会导致性能显著下降(10-13个百分点)。这表明当目标领域与源领域差异巨大且目标领域数据稀缺时，“过强”的通用表征可能难以被有效调整甚至会发生退化。

3.**轻量化可行性**: 
      -使用参数量仅为DINOv3四分之一的ViT-Tiny骨干网络的Mine-JEPA仍能取得有竞争力的性能(F1: Tiny-0.814 vs S-0.820)，证明了该方法在资源受限平台(如自主水下航行器AUV)上的部署潜力。

4.**技术要点有效性验证**: 
      -针对SSS的图像特性定制化的数据增强是成功的关键前提；错误的增强会严重损害性能。
      -正则化类SSL目标(SIGReg, VICReg)比对比式(SimCLR)或蒸馏式(BYOL)方法更能有效利用异构合成数据进行提升。

##7 .优点
*   `问题导向明确`:精准定位了SSS水雷分类中“小样本”和“大域差”的核心痛点.
* `方法论创新`:首次系统地将域内自监督学习引入该领域并构建完整流程(Mine-JEPA).
* `设计精巧务实`:没有盲目追求复杂架构而是选择了适合小数据的简单稳定损失(SIGReg)，并进行了细致入微的领域适配(增广、初始化).
* `分析深入全面`:不仅展示了最终效果还通过大量消融实验揭示了各组件的作用以及“基础模型+额外域适应可能有害”这一重要现象.
* `结果具有启发性`:挑战了“更大规模预训练总是更好”的传统观念为资源受限的边缘计算场景提供了高效替代方案.

##8 .不足与局限
* `数据集规模有限`:所有结论基于单一公开数据集仅含1170张原始图测试集规模较小限制了结论的一般性和鲁棒性.
* `任务范围较窄`:仅聚焦于小块(patch-level)级别的分类未扩展到更实用的滑动窗口检测或端到端的目标检测框架.
* `泛化能力待验证`:未在不同型号声纳设备不同海洋环境条件或其他类型水下目标上进行广泛测试.
* `合成数据的普适性`:虽然证明了加入合成数据的益处但这些合成数据的生成方式及其对其他任务的适用性未做深入探讨.
* `理论解释不足`:对于为何强大的DINOv3会在额外域适应后性能下降的现象主要停留在经验观察层面缺乏更深入的理论分析.

---

**(完)**

好的，我将从您上次输出的中断处继续补全总结。根据您提供的文本，上一次输出已包含至“不足与局限”部分，并以“（完）”结束。因此，我将从论文的后续部分（通常是结论、未来工作或讨论）开始补充，并确保内容不重复。

---

## 9. 结论与未来工作展望
*   **核心结论重申**：论文成功证明了 **Mine-JEPA** 作为一种轻量级、数据高效的域内自监督学习框架的有效性。它通过在极少量未标记侧扫声呐图像上进行预训练，获得了超越大规模通用视觉基础模型（DINOv3）的下游分类性能。这一发现挑战了“更大规模预训练总是更好”的固有观念，并为数据稀缺、领域特殊的应用场景提供了新的范式。
*   **关键洞见总结**：
    1.  **领域适配至关重要**：在跨域任务中，简单的“预训练-微调”流水线可能不是最优的。针对目标领域的特性（如SSS图像的亮-影结构）进行定制化的数据增强和恰当的模型初始化是成功的关键。
    2.  **简单性与稳定性优势**：在小数据场景下，结构简单、优化稳定的自监督目标函数（如SIGReg）比需要复杂技巧（如动量编码器、大批量负样本）的方法更具优势。
    3.  **基础模型的局限性**：当源领域与目标领域差异巨大时，过于强大的通用表征可能难以通过有限的领域数据进行有效调整，甚至可能导致性能退化。这提示我们需要更谨慎地评估和使用大型基础模型。
*   **未来工作方向**：
    *   **扩展到更复杂的任务**：将Mine-JEPA框架应用于像素级分割、目标检测或实例分割等更贴近实际部署需求的任务。
    *   **增强泛化能力**：在更多样化的声呐数据集（不同传感器、不同海况、不同地理区域）上验证方法的鲁棒性。
    *   **探索更优的合成数据融合策略**：研究如何生成质量更高、多样性更强的合成声呐数据，并设计更有效的算法来融合真实与合成数据。
    *   **理论深化**：深入研究为何强大的预训练模型在小规模域内适应时会发生性能下降的内在机理，为模型选择和适应策略提供理论指导。
    *   **部署优化**：进一步压缩模型（如知识蒸馏、量化），并集成到自主水下航行器的实时处理流水线中。

## 10. 总体评价
该论文是一项扎实且富有洞察力的研究工作。它没有追求前沿算法的简单套用，而是从一个实际应用的核心痛点出发，通过系统性的实验设计和细致的工程适配，找到了一个在当前约束条件下的高效解决方案。其最重要的贡献不仅在于提出了一个性能优异的流程（Mine-JEPA），更在于揭示了在特定条件下“小规模域内学习”可以战胜“大规模通用预训练”这一反直觉现象，为类似的小样本、强域差视觉任务提供了宝贵的经验参考和方法论启示。尽管受限于数据集规模和任务范围，但其研究思路和实证结论具有较高的参考价值。

---

**(完)**
