---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一令：基于增量令牌的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:q8"]
score: 10.0
evidence: 利用高维视频的增量标记进行生成式世界建模
tldr: 视频世界建模旨在预测多样的未来状态，但现有生成式模型计算昂贵，判别式模型则缺乏多样性。本文提出 DeltaTok 和 DeltaWorld：DeltaTok 将连续帧间的视觉基础模型特征差异编码为单个“delta”令牌，极大压缩了时空表示；基于此，DeltaWorld 作为生成式世界模型进行高效的多假设训练与推理。该方法在保持预测多样性的同时，参数量减少超过35倍、计算量减少2000倍，显著提升了效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 视频世界建模中预测多样未来状态是一个核心挑战，现有生成式模型计算成本高昂，而判别式模型则缺乏多样性。
method: 该方法引入 DeltaTok 编码器将连续帧间的特征差异压缩为单个“delta”令牌，并构建 DeltaWorld 模型在此令牌序列上进行高效的多假设生成式训练。
result: 在密集预测任务上，DeltaWorld 的预测更贴近真实结果，且参数量减少超过35倍、计算量减少2000倍。
conclusion: DeltaWorld 通过创新的 Delta 令牌和生成式建模，在保持预测多样性的同时，实现了远超现有方法的效率。
---

## 摘要
预测多样化的未来状态是视频世界建模中的一个核心挑战。判别式世界模型产生一个确定性预测，该预测隐式地平均了可能的未来，而现有的生成式世界模型在计算上仍然昂贵。最近的研究表明，在视觉基础模型的特征空间中预测未来，而非在为像素重建优化的潜在空间中，所需的世界模型参数量显著减少。然而，大多数此类方法仍然是判别式的。在这项工作中，我们引入了DeltaTok（一种分词器），它将连续帧之间的VFM特征差异编码为单个连续的“增量”令牌；以及DeltaWorld（一种生成式世界模型），它基于这些令牌运行以高效生成多样化的合理未来。增量令牌将视频从三维时空表示简化为一维时间序列，例如对于512x512的帧可产生1,024倍的令牌缩减。这种紧凑的表示使得可行的多假设训练成为可能，即并行生成多个未来并仅监督最佳的一个。在推理时，这能在单次前向传递中产生多样化的预测。在密集预测任务上的实验表明，DeltaWorld预测的未来与现实世界结果更吻合，同时其参数量比现有生成式世界模型少35倍以上，使用的FLOPs少2,000倍。代码与权重：https://deltatok.github.io

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：视频世界建模的核心挑战是高效且准确地预测**多样化的未来状态**。这对于自动驾驶等需要安全规划的智能体至关重要。
*   **现有问题**：
    *   **判别式模型**（如DINO-world）：输出单一确定性预测，在不确定性下会坍缩为条件均值，无法捕捉多样的未来可能性。
    *   **生成式模型**（如Cosmos）：虽然能生成多样未来，但计算成本极高。原因包括：1) 在像素重建优化的潜在空间中操作；2) 需要多次前向传递（如扩散模型）生成一个假设；3) 未充分利用连续帧间的时空冗余性。
*   **整体目标**：提出一种高效的生成式世界模型，能够在单次前向传递中生成多个合理的未来假设，同时大幅降低计算开销。

## 2. 方法论
*   **核心思想**：将视频建模从三维（空间x空间x时间）压缩为一维（时间）序列。关键在于仅编码和预测帧与帧之间的“变化”，而非完整的场景内容。
*   **关键技术细节**：
    1.  **DeltaTok (增量令牌分词器)**：
        *   输入：由冻结的视觉基础模型（VFM，如DINOv3）提取的连续两帧特征图 `x_{t-1}` 和 `x_t`。
        *   过程：通过一个基于Transformer的编码器-解码器架构，将两帧特征图的差异压缩为单个连续的“增量令牌” `z_t`。
        *   输出：解码器利用前一帧特征 `x_{t-1}` 和增量令牌 `z_t`，重建当前帧特征 `\hat{x}_t`。训练目标是最小化重建损失（MSE）。
        *   优势：极大压缩表示。例如，对于512x512分辨率的帧（对应32x32=1024个空间令牌），每帧仅用1个令牌表示，实现了1024倍的压缩。
    2.  **DeltaWorld (生成式世界模型)**：
        *   **架构基础**：建立在判别式DINO-world架构上，但操作对象从空间特征网格变为增量令牌序列 `Z_{1:t}`。
        *   **生成机制 - Best-of-Many (BoM)训练**：
            *   训练时：从高斯分布中采样K个不同的噪声查询向量 `q_k`。对于每个查询，预测器并行地预测出K个未来的增量令牌候选。仅对与真实未来最接近的那个候选进行监督和反向传播。
            *   推理时：不同的噪声查询会映射到不同的合理未来，所有多样性均在单次前向传递中产生。
        *   **自回归展开**：对于中长期预测，将预测出的增量令牌追加到上下文序列中，迭代地进行多步预测。

## 3. 实验设计
*   **评估基准**：采用密集预测基准 ，评估短期（~0.2秒）和中期（~0.6秒）预测精度。
*   **下游任务与数据集**：
    *   **语义分割**: VSPW (多种场景)、Cityscapes (驾驶场景)。
    *   **深度估计**: KITTI (驾驶场景)。
    *   所有评估数据集均未包含在训练集中。
*   **对比方法**:
    1.  **判别式基线**: DINO-world (作者复现版)。
    2.  **生成式基线**: Cosmos-4B 和 Cosmos-12B (在像素重建潜在空间中操作的扩散模型)。
    3.  **上下界**: “复制最后一帧” (下界) 和 “使用真实未来帧” (上界)。
*   **评估指标**: 
    *   对于生成式模型，报告20个样本中的最佳性能 (`best`)和平均性能 (`mean`)。
    *   分割任务用mIoU，深度任务用RMSE。

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量和总训练时长。但提供了以下效率相关的量化信息：
*   **计算量对比**: DeltaWorld的推理FLOPs比Cosmos少约2000倍。
*   **参数量对比**: DeltaWorld的参数量比Cosmos少35倍以上（DeltaWorld约0.3B参数）。
*   **训练配置提及**: 
    *   主要实验使用512x512输入分辨率训练300K次迭代。
    *   消融实验使用256x256输入分辨率训练100K次迭代。
    *   批量大小为1024。

## 5. 实验数量与充分性
实验设计较为系统和充分：
1.  **渐进式构建分析 (表2)**：通过三个步骤清晰地展示了从判别式模型到高效生成式模型的演变过程及其对性能/效率的影响：(0)判别式基线, (1)+BoM训练, (2)+全帧压缩, (3)+增量压缩(DeltaWorld)。这构成了有力的消融研究。
2.  **超参数研究 (图5)**：探究了BoM训练中查询数量K对最终`best`和`mean`性能的影响。
3.  **主基准对比 (表3)**：在三个不同数据集、两个预测时域、两个任务上全面对比了主要基线方法。
4.  ***泛化性验证***: 
    *   将DeltaTok思想应用于另一个判别式架构DINO-Foresight ，同样取得了有竞争力的结果(附录C)。
5. ***定性分析***: 
提供了丰富的可视化样例(正文及附录E)，展示了模型生成的多样性及质量。

总体而言，实验覆盖了方法有效性、效率优势、组件贡献分析和跨架构泛化性验证，设计客观公平。

##6\.主要结论与发现

- DeltaTok能够将视频高效地压缩为一维增量令牌序列,为高效的世界建模奠定了基础。- DeltaWorld结合DeltaTok和BoM目标,能够以极低的计算成本(单次前向传递)生成多样且合理的未来预测。-在密集预测基准上,DeltaWorld的“最佳”样本性能显著优于大型生成式基线Cosmos,其“平均”性能也与优化的判别式基线相当甚至更优。-该方法实现了显著的效率提升:相比Cosmos,参数量减少>35倍,FLOPs减少约2000倍。

##7\.优点

-创新性强:提出了“一帧一令”的增量编码思想,将视频建模维度从3D降至1D,概念简洁而有效。-效率卓越:通过紧凑表示和单次前向生成的BoM策略,实现了数量级级别的计算开销降低,使实时或大规模生成应用成为可能。-实用性高:工作在预训练的VFM特征空间,直接关联高级语义信息,避免了不必要的像素级细节建模。-实验扎实:通过逐步构建、充分对比和多维度评估,全面验证了方法的有效性和优势。

##8\.不足与局限

-分布建模的理论保证较弱:BoM目标缺乏像扩散模型那样明确的概率分布建模依据。模型的多样性覆盖受限于训练时探索的噪声查询数量K,且无法保证采样分布反映真实结果的概率。-误差累积风险:自回归展开过程中,增量令牌的解码误差可能逐步累积,导致长期预测的特征漂移(文中已指出此通用挑战)。-依赖预训练VFM:模型的性能上限受限于所用VFM的特征质量和对下游任务的适配性。-应用场景限制:目前主要针对固定视角的视频预测。对于需要处理复杂相机运动或全新物体出现的极端动态场景,“变化”可能过于复杂而难以被单个令牌充分编码。(完)

## 6. 主要结论与发现（续）

- **效率与性能的平衡**：DeltaWorld 在保持与判别式模型相当的“平均”性能的同时，其“最佳”样本性能显著超越了大型生成式基线 Cosmos。这证明了其单次前向生成多样高质量预测的能力。
- **泛化能力**：DeltaTok 的核心思想（编码变化）被证明可以泛化到不同的主干架构（如 DINO-Foresight），提升了其他模型的性能，说明了其作为通用视频表示模块的潜力。
- **计算革命**：该方法实现了从传统三维视频建模到一维序列建模的范式转变，带来了惊人的效率提升：模型参数量减少超过35倍，推理FLOPs降低约2000倍。

## 7. 优点（续）

- **可解释性与简洁性**：“一帧一令”和“增量编码”的概念非常直观，将复杂的时空预测问题转化为对连续状态间“差异”的建模，降低了问题复杂度。
- **训练稳定性**：由于工作在稳定的、预训练好的视觉特征空间，并采用 MSE 重建损失进行训练，避免了在原始像素空间或复杂潜在空间中训练生成模型的不稳定性和模式坍缩风险。
- **为下游任务量身定制**：直接在高语义特征空间进行预测，使得预测结果天然适配于需要高级理解的下游任务（如分割、深度估计），无需额外的转换或适配层。

## 8. 不足与局限（续）

- **对静态场景的冗余**：在场景几乎静止或变化极小时，增量令牌可能编码的信息很少，但模型仍需为其分配计算资源。虽然高效，但可能存在针对极端静态场景的进一步优化空间。
- **长期依赖建模挑战**：虽然通过自回归展开进行多步预测，但对于非常长期的序列，模型仍然需要处理累积误差和远距离依赖问题。论文中的实验主要聚焦于短期和中期预测（数秒内）。
- **动态范围限制**：增量令牌可能难以捕捉帧与帧之间发生的、超出预训练 VFM 特征表达能力的剧烈或异常变化（例如，突然的镜头切换、爆炸等罕见事件）。

## 9. 未来展望

基于本论文的工作，未来可能的研究方向包括：
1.  **更强大的分布建模**：探索如何为 BoM 训练提供更坚实的概率理论基础，或者集成更先进的生成式先验。
2.  **多模态预测**：将当前的视觉世界模型扩展到多模态输入（如文本指令、音频）和输出（如控制动作），构建具身智能体。
3.  **层次化增量编码**：研究多尺度或层次化的增量令牌体系结构，以同时高效地捕获局部细微运动和全局场景变化。
4.  **在线适应与学习**：使模型能够在部署中持续学习并适应新的环境或物体动态。

总而言之，《A Frame is Worth One Token》提出了一种颠覆性的高效视频生成建模框架。它通过创新的“增量令牌”将视频压缩为紧凑的一维序列，并结合“Best-of-Many”训练策略，首次在单次前向传递中实现了高质量、多样化的未来预测。其在计算效率上的巨大优势（参数量减少35倍以上，FLOPs降低约2000倍）与强大的实证性能相结合，为在机器人、自动驾驶等实时系统中部署大规模生成式世界模型铺平了道路。

（完）
