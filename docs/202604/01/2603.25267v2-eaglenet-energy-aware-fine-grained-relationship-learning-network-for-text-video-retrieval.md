---
title: "EagleNet: Energy-Aware Fine-Grained Relationship Learning Network for Text-Video Retrieval"
title_zh: EagleNet：用于文本-视频检索的能量感知细粒度关系学习网络
authors: "Yuhan Chen, Pengwen Dai, Chuan Wang, Dayan Wu, Xiaochun Cao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25267v2"
tags: ["query:vlm"]
score: 10.0
evidence: 用于文本-视频检索的能量感知关系学习
tldr: EagleNet是一个针对文本-视频检索任务的能量感知细粒度关系学习网络。针对现有方法在扩展文本语义时忽略视频帧间上下文信息的问题，该模型通过构建文本-帧图并利用FRL机制学习细粒度关系，结合EAM能量感知匹配捕捉真实分布，并采用Sigmoid损失优化对齐。实验证明其在MSRVTT等多个主流数据集上达到了领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25267v2/fig-001.webp\", \"caption\": \"Table 1. Performance on MSRVTT, boldface is the best results and underline is the runner-ups. Note that we have resolved the data-leakage problem in the original TMASS codes, and re-implemented XPool†, CLIP-VIP†, TMASS†, TV-ProxyNet†, and Video-ColBERT† using the CLIP4Clip codebase to align with other methods. ∗ denotes the reproduced results, all of which are fine-tuned on ViT-B/32 or ViT-B/16.\", \"page\": 6, \"index\": 1, \"width\": 822, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2603.25267v2/fig-002.webp\", \"caption\": \"Table 2. Text-to-video results on DiDeMo, MSVD, and VATEX. Note that we have resolved the data-leakage problem in the original TMASS codes, and re-implemented XPool†, CLIP-VIP†, TMASS†, TV-ProxyNet†, and Video-ColBERT† using CLIP4Clip codebase to align with other methods. ∗ denotes the reproduced results, all of which are fine-tuned on ViT-B/32 or ViT-B/16.\", \"page\": 7, \"index\": 2, \"width\": 768, \"height\": 581}, {\"url\": \"assets/figures/arxiv/2603.25267v2/fig-003.webp\", \"caption\": \"Figure 5. Visualization of text-to-video retrieval results by different components of EagleNet. Green denotes the correct retrieval result, and red is the wrong result.\", \"page\": 18, \"index\": 3, \"width\": 1024, \"height\": 1360}, {\"url\": \"assets/figures/arxiv/2603.25267v2/fig-004.webp\", \"caption\": \"Figure 2. Diagram of EagleNet. (a) Overview of the training process. (b) Fine-Grained Relationship Learning (FRL) first samples multiple text candidates, then constructs a text-frame graph to learn both text-frame and frame-frame relationships, which are then used to aggregate text candidates into a final enriched text embedding aware of video context information. (c) Energy-Aware Matching (EAM) improves the relationships learning in FRL by capturing detailed text-frame interactions and accurately models true text-video pairs distribution, thereby also enhancing the final matching performance from a fine-grained perspective.\", \"page\": 4, \"index\": 4, \"width\": 881, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2603.25267v2/fig-005.webp\", \"caption\": \"Figure 4. Visualization of text-to-video retrieval results by our proposed EagleNet, TMASS [43], and TV-ProxyNet [51]. Green denotes the correct retrieval result, and red is the wrong result.\", \"page\": 17, \"index\": 5, \"width\": 1031, \"height\": 871}]"
motivation: 现有文本扩展方法忽略了视频内部帧之间的丰富交互，导致生成的增强文本无法准确捕捉视频帧的上下文信息。
method: 提出细粒度关系学习（FRL）机制构建文本-帧图，并结合能量感知匹配（EAM）建模交互能量，同时采用Sigmoid损失替代传统对比损失。
result: 在MSRVTT、DiDeMo、MSVD和VATEX等多个权威数据集上的实验结果均证明了EagleNet的优越性。
conclusion: EagleNet通过建模细粒度文本-帧关系和能量分布，有效缩小了文本与视频语义之间的差距，提升了检索精度。
---

## 摘要
由于大规模视觉-语言预训练模型的近期发展，文本-视频检索任务取得了显著进步。传统方法主要侧重于视频表示或跨模态对齐，而近期研究转向增强文本的表达能力，以更好地匹配视频中的丰富语义。然而，这些方法仅利用文本与帧/视频之间的交互，忽略了视频内部帧之间的丰富交互，导致最终扩展的文本无法捕捉帧的上下文信息，从而造成文本与视频之间的差异。为此，我们引入了能量感知细粒度关系学习网络（EagleNet），以生成准确且具有上下文感知能力的增强文本嵌入。具体而言，所提出的细粒度关系学习机制（FRL）首先通过生成的候选文本和视频帧构建文本-帧图，随后学习文本与帧之间的关系，并最终利用这些关系将候选文本聚合为包含帧上下文信息的增强文本嵌入。为了进一步改进 FRL 中的细粒度关系学习，我们设计了能量感知匹配（EAM）来建模文本-帧交互的能量，从而准确捕捉真实文本-视频对的分布。此外，为了实现更有效的跨模态对齐和稳定的训练，我们用 sigmoid 损失替换了传统的基于 softmax 的对比损失。在 MSRVTT、DiDeMo、MSVD 和 VATEX 上的广泛实验证明了 EagleNet 的优越性。代码可在 https://github.com/draym28/EagleNet 获取。

## Abstract
Text-video retrieval tasks have seen significant improvements due to the recent development of large-scale vision-language pre-trained models. Traditional methods primarily focus on video representations or cross-modal alignment, while recent works shift toward enriching text expressiveness to better match the rich semantics in videos. However, these methods use only interactions between text and frames/video, and ignore rich interactions among the internal frames within a video, so the final expanded text cannot capture frame contextual information, leading to disparities between text and video. In response, we introduce Energy-Aware Fine-Grained Relationship Learning Network (EagleNet) to generate accurate and context-aware enriched text embeddings. Specifically, the proposed Fine-Grained Relationship Learning mechanism (FRL) first constructs a text-frame graph by the generated text candidates and frames, then learns relationships among texts and frames, which are finally used to aggregate text candidates into an enriched text embedding that incorporates frame contextual information. To further improve fine-grained relationship learning in FRL, we design Energy-Aware Matching (EAM) to model the energy of text-frame interactions and thus accurately capture the distribution of real text-video pairs. Moreover, for more effective cross-modal alignment and stable training, we replace the conventional softmax-based contrastive loss with the sigmoid loss. Extensive experiments have demonstrated the superiority of EagleNet across MSRVTT, DiDeMo, MSVD, and VATEX. Codes are available at https://github.com/draym28/EagleNet.

---

## 论文详细总结（自动生成）

以下是对论文《EagleNet: Energy-Aware Fine-Grained Relationship Learning Network for Text-Video Retrieval》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在文本-视频检索（TVR）任务中，视频包含丰富的时空语义，而文本查询通常非常简短。现有的方法尝试通过“文本增强”来丰富文本语义，但它们大多只关注文本与视频/帧之间的交互，忽略了**视频内部帧与帧之间的上下文关系**。
*   **核心问题**：由于缺乏对视频内部时序和上下文逻辑的理解，生成的增强文本往往无法准确反映视频的真实语义，导致跨模态对齐存在偏差。
*   **研究目标**：提出一种能够感知视频内部细粒度关系和能量分布的网络，生成更具上下文感知能力的增强文本嵌入。

### 2. 方法论
EagleNet 主要由三个核心组件构成：
*   **细粒度关系学习（FRL）**：
    *   **随机文本建模**：首先对原始文本进行随机采样，生成多个候选文本。
    *   **文本-帧图构建**：将原始文本、候选文本和视频帧作为节点构建异质图，定义了“文本-文本”、“帧-帧”和“文本-帧”三种关系。
    *   **关系图注意力网络（RGAT）**：利用多层 RGAT 学习节点间的交互，特别是通过帧与帧的关系捕捉视频上下文，最后根据学习到的权重将候选文本聚合为“增强文本嵌入”。
*   **能量感知匹配（EAM）**：
    *   **核心思想**：引入能量模型（EBM）来建模真实文本-视频对的分布。
    *   **技术细节**：定义能量函数（如双线性函数或 MLP）来计算文本与各帧的交互能量。通过马尔可夫链蒙特卡洛（MCMC）采样生成“负样本（假数据）”，使模型学习到真实配对具有低能量，误匹配具有高能量。
*   **Sigmoid 损失优化**：
    *   用 Sigmoid 损失替换传统的 Softmax 对比损失。Sigmoid 损失独立处理每个正负对，对批次大小不敏感，且更适合 TVR 中“一对多”或“多对一”的语义重叠场景。

### 3. 实验设计
*   **数据集**：在四个主流基准数据集上进行了验证：**MSRVTT**、**DiDeMo**、**MSVD** 和 **VATEX**。
*   **Benchmark 与对比方法**：
    *   对比了包括 CLIP4Clip、XPool、TS2Net、CLIP-VIP、ProST、TMASS、TV-ProxyNet 和 Video-ColBERT 等在内的十余种最先进（SOTA）模型。
    *   **客观性处理**：作者特别指出修复了前序工作 TMASS 代码中的“数据泄露”问题，并在统一的 CLIP4Clip 框架下重新实现了多个基准模型，确保对比的公平性。

### 4. 资源与算力
*   **硬件环境**：训练使用了 **2 台 NVIDIA A100 GPU**；推理阶段使用 **1 台 A100 GPU**。
*   **训练参数**：训练 5 个 epoch，batch size 为 64。
*   **耗时**：论文附录提供了详细的时间开销表。例如在 MSRVTT 数据集上，EagleNet 的单轮训练时间约为 5207 秒，推理总时间约为 141.7 秒（略高于基础模型，但处于合理范围）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同规模和特征的数据集，实验结果具有普适性。
*   **消融实验**：非常充分。作者分别验证了 FRL、EAM、Sigmoid 损失的有效性，并深入探讨了能量函数的选择（CosSim vs Bilinear vs MLP）、池化方式（Max/Min/Avg）以及图编码器（GAT vs RGAT）的影响。
*   **可视化分析**：提供了检索结果的定性对比图，直观展示了 EagleNet 在处理复杂上下文时的优势。

### 6. 主要结论与发现
*   **性能领先**：EagleNet 在所有四个数据集上均刷新了 SOTA 记录，尤其在 R@1 指标上提升显著（如 MSRVTT 提升了 2.0%）。
*   **上下文的重要性**：实验证明，建模“帧与帧”之间的关系对于生成准确的增强文本至关重要。
*   **能量模型的有效性**：EAM 能够显著提升细粒度对齐的精度，帮助模型在语义相似的视频中做出更准确的区分。

### 7. 优点
*   **创新性**：首次将能量模型（EBM）引入文本-视频检索领域，并结合图神经网络处理异质关系。
*   **严谨性**：主动发现并修复了前序工作的实验漏洞（数据泄露），提供了更可信的 Benchmark。
*   **端到端**：模型设计简洁高效，不依赖外部复杂的知识图谱或预处理工具。

### 8. 不足与局限
*   **计算开销**：由于引入了 RGAT 和 MCMC 采样，训练阶段的计算复杂度比基础 CLIP 模型有所增加。
*   **超参数依赖**：模型涉及多个损失函数的权重分配（如 $\lambda_{sup}, \lambda_{eam}$）以及 MCMC 的步数，可能需要针对不同数据集进行微调。
*   **推理延迟**：虽然 EAM 仅在训练时使用，但 FRL 阶段的图计算仍会略微增加推理时的延迟。

（完）
