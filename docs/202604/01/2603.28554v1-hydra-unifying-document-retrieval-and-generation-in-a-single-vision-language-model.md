---
title: "Hydra: Unifying Document Retrieval and Generation in a Single Vision-Language Model"
title_zh: Hydra：在单一视觉语言模型中统一文档检索与生成
authors: Athos Georgiou
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28554v1"
tags: ["query:vlm"]
score: 9.0
evidence: 在单一视觉语言模型中统一文档检索与生成
tldr: "视觉文档理解通常需要独立的检索和生成模型，导致内存占用和系统复杂度翻倍。本文提出 Hydra，一种基于视觉语言模型（VLM）的双头架构，通过单个 LoRA 适配器实现了 ColBERT 式检索与自回归生成的统一。在推理时通过切换适配器，既能产生多向量嵌入进行检索，又能完全恢复基础模型的生成能力。该方法将峰值显存降低了 41%，并在多个基准测试中展现了与独立模型相当的性能，且能扩展至音频和视频等多模态任务。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28554v1/fig-001.webp\", \"caption\": \"Table 7: Hydra-Omni image retrieval on ViDoRe V1, V2, and V3. Model: vidore/colqwen-omni-v0.1 (Qwen2.5-Omni-3B backbone, 4.4B total parameters). No Hydraspecific training.\", \"page\": 15, \"index\": 1, \"width\": 992, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2603.28554v1/fig-002.webp\", \"caption\": \"Table 3: Summary of Hydra (4B) across all evaluation dimensions. Efficiency measured on a single GPU.\", \"page\": 10, \"index\": 2, \"width\": 744, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.28554v1/fig-003.webp\", \"caption\": \"Table 5: Per-task retrieval performance (nDCG@5) on ViDoRe V1, V2, and V3. Baseline: singlehead ColQwen3.5 trained under the same regime as Hydra (same data, hyperparameters, 1 epoch).\", \"page\": 14, \"index\": 3, \"width\": 1009, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.28554v1/fig-004.webp\", \"caption\": \"Figure 1: Hydra architecture. A single VLM serves two modes by toggling a LoRA adapter at inference time. Left: Retrieval mode (LoRA-on, bidirectional attention) produces 320-dim multivector embeddings via custom_text_proj. Right: Generation mode (LoRA-off, causal attention) produces autoregressive text via the base lm_head with KV-cache. The vision encoder is frozen and shared. No weight copying or model reloading occurs between modes. Solid arrows = retrieval path; dashed arrows = generation path.\", \"page\": 3, \"index\": 4, \"width\": 785, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2603.28554v1/fig-005.webp\", \"caption\": \"Figure 2: RAG pipeline comparison. Top: ColPali retrieves relevant pages, but a separate LLM is needed for generation at query time—requiring two models in GPU memory (8B+ parameters, 17,913 MB peak VRAM). Bottom: Hydra uses a single 4B-parameter model for both indexing (retrieval head for embeddings) and querying (retrieval head finds top-k pages, generation head answers from them). Both heads share one model in GPU memory, reducing peak VRAM to 10,496 MB (41% savings). Solid blue borders = retrieval; red borders = generation.\", \"page\": 4, \"index\": 5, \"width\": 976, \"height\": 599}, {\"url\": \"assets/figures/arxiv/2603.28554v1/fig-006.webp\", \"caption\": \"Table 2: Three-mode comparison of Hydra (retrieval-only training) vs. GritLM-style joint training. Retrieval: 9 ViDoRe V1 tasks. Generation: DocVQA validation (n=200). Both models use the same base and LoRA config (batch size differs; see text). Despite different adapter weights (max element-wise diff: 0.50), the two functional modes are equivalent; the mode that joint training was designed to unlock (LoRA-on generation) fails.\", \"page\": 9, \"index\": 6, \"width\": 962, \"height\": 182}]"
motivation: 旨在解决视觉文档理解中检索与生成模型分离导致的显存占用高和系统复杂度大的问题。
method: 提出 Hydra 架构，利用单个 LoRA 适配器在推理时动态切换，实现 ColBERT 式检索头与原始生成头的统一。
result: "实验表明 Hydra 在保持生成质量几乎不变的同时，将峰值显存降低了 41%，且检索性能与独立模型相当。"
conclusion: Hydra 证明了通过轻量级适配器在单一 VLM 中高效集成检索与生成功能的可行性，并具有良好的多模态扩展潜力。
---

## 摘要
视觉文档理解通常需要独立的检索和生成模型，这导致内存占用和系统复杂度翻倍。我们提出了 Hydra，这是一种双头方法，通过单一视觉语言模型（VLM）同时提供 ColBERT 风格的后期交互（late-interaction）检索和自回归生成。一个仅针对检索训练的 LoRA 适配器在推理时进行切换：启用它可产生多向量嵌入；禁用它则可恢复基础模型的生成质量——在 10,500 个贪婪和随机采样样本中，输出与基础模型 100% 逐字节一致；在四个 VQA 基准测试的 15,301 个样本中（其中三个具有信息量；在贪婪解码下，两个模型的 ChartQA 结果均接近于零），与独立的基础模型流水线相比，最大 delta-ANLS 仅为 0.0044。我们确定了三个工程要求（注意力模式恢复、lm_head 保留、KV 缓存感知解码），如果忽略这些要求，尽管权重恢复正确，也会在无声中破坏生成性能。在 ViDoRe V1 上，Hydra (4B) 在单次训练运行中与受控的单头基准线差距在 1 个百分点以内，在 V2 和 V3 上取得了更高的综合评分（集中在部分任务上）；需要多种子实验来确认这些趋势。这种单模型设计将 GPU 显存峰值降低了 41%，尽管在并发服务负载下，适配器切换会引入吞吐量开销。消融实验表明，在基于 LoRA (r=16) 的训练方案中，GritLM 风格的联合训练没有带来收益。对 Qwen2.5-Omni-3B 的概念验证扩展表明，该机制可推广到音频检索和视频嵌入，并支持语音生成。

## Abstract
Visual document understanding typically requires separate retrieval and generation models, doubling memory and system complexity. We present Hydra, a dual-head approach that provides both ColBERT-style late-interaction retrieval and autoregressive generation from a single vision-language model (VLM). A single LoRA adapter, trained only for retrieval, is toggled at inference: enabling it produces multi-vector embeddings; disabling it recovers the base model's generation quality -- byte-identical outputs in 100% of 10,500 greedy and stochastic samples, with max delta-ANLS = 0.0044 across 15,301 samples on four VQA benchmarks (three informative; ChartQA is near-zero for both models under greedy decoding) when compared against an independent base-model pipeline. We identify three engineering requirements (attention-mode restoration, lm_head preservation, KV-cache-aware decoding) whose omission silently breaks generation despite correct weight recovery. On ViDoRe V1, Hydra (4B) is within 1 percentage point of a controlled single-head baseline in a single training run, with higher aggregate scores on V2 and V3 that are concentrated on a subset of tasks; multi-seed experiments are needed to confirm these trends. The single-model design reduces peak GPU memory by 41%, though adapter switching introduces throughput overhead under concurrent serving loads. An ablation shows that GritLM-style joint training provides no benefit within the LoRA-based (r=16) training regime. A proof-of-concept extension to Qwen2.5-Omni-3B demonstrates that the mechanism generalizes to audio retrieval and video embedding, with speech generation.

---

## 论文详细总结（自动生成）

以下是对论文《Hydra: Unifying Document Retrieval and Generation in a Single Vision-Language Model》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现代视觉文档理解（Visual Document Understanding）系统通常采用“双模型”架构：一个检索模型（如 ColPali）负责寻找相关页面，一个生成模型（如 Qwen2-VL）负责理解并回答问题。
*   **核心问题**：这种架构极其浪费资源，因为两个模型共享相似的视觉语言模型（VLM）骨干，却需要独立加载，导致 GPU 显存占用翻倍，系统部署复杂度增加。
*   **整体含义**：论文提出 **Hydra** 架构，旨在通过单个 VLM 和单个 LoRA 适配器，在推理时动态切换“检索”和“生成”两种模式，从而在不损失性能的前提下大幅降低显存开销。

### 2. 论文提出的方法论
*   **核心思想**：利用 LoRA 的加性特性（$W_{adapted} = W_{base} + BA$）。在检索模式下启用 LoRA 适配器；在生成模式下禁用适配器，从而精确恢复基础模型的原始权重。
*   **关键技术细节**：
    *   **双头设计**：
        1.  **检索头**：启用 LoRA，使用双向注意力机制（Bidirectional Attention），通过线性投影层产生 320 维的多向量嵌入（ColBERT 风格）。
        2.  **生成头**：禁用 LoRA，恢复因果注意力机制（Causal Attention），使用原始的 `lm_head` 进行自回归文本生成。
    *   **三大工程要求**（确保生成能力不被破坏）：
        1.  **注意力模式恢复**：检索训练通常会将注意力改为双向，生成时必须手动切回因果模式。
        2.  **lm_head 保留**：防止在 LoRA 训练期间由于权重共享或 DDP 梯度同步导致原始生成头被污染。
        3.  **KV-cache 感知解码**：实现 KV 缓存支持，使生成速度提升约 38 倍。
*   **算法流程**：推理时根据任务需求切换 LoRA 开关和注意力掩码，无需重新加载模型或复制权重。

### 3. 实验设计
*   **数据集/场景**：
    *   **检索评估**：使用 ViDoRe（视觉文档检索基准）的 V1（9个任务）、V2（4个任务）和 V3（8个任务），涵盖论文、表格、财务报告等。
    *   **生成评估**：使用 DocVQA、ChartQA、InfoVQA 和 TextVQA 四个主流 VQA 基准，共 15,301 个样本。
*   **Benchmark 与对比方法**：
    *   **检索基准**：与受控的单头 ColQwen3.5 模型（相同训练配置但无生成能力）对比。
    *   **生成基准**：与原始预训练 VLM（Qwen3.5-4B）对比，验证输出的一致性。
    *   **消融实验**：对比了 GritLM 风格的“联合训练（检索+生成数据混合）”与 Hydra 的“仅检索训练”。

### 4. 资源与算力
*   **硬件使用**：论文在效率测量中使用了 **NVIDIA B200 GPU**。
*   **训练配置**：有效 Batch Size 为 112，使用 AdamW 优化器，学习率 5e-5，采用 bf16 混合精度训练 1 个 epoch。
*   **算力说明**：文中未明确给出总训练时长（GPU hours），但详细记录了推理延迟（模式切换仅需 5.9ms）和显存占用（从 17.9GB 降至 10.5GB）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   检索实验覆盖了 21 个不同的子任务。
    *   生成实验进行了超过 1.5 万次样本测试，包括 10,500 个样本的逐字节一致性验证。
    *   进行了多模态扩展实验（Qwen2.5-Omni），验证了在音频和视频检索上的泛化能力。
*   **充分性评价**：实验设计非常严谨，特别是针对“生成能力是否受损”进行了统计学上的等效性检验（TOST）。消融实验有力地证明了在 LoRA 框架下，联合训练并无必要。但作者也指出，检索实验多为单次运行，未来需要多种子实验来排除随机性。

### 6. 论文的主要结论与发现
*   **显存优化**：Hydra 将 RAG 流水线的峰值显存降低了 **41%**。
*   **性能保持**：检索性能与专门的单头模型相当（差距在 1% 以内）；生成结果在贪婪解码下与原始模型 **100% 逐字节一致**。
*   **训练发现**：在 LoRA (r=16) 训练模式下，GritLM 风格的联合训练无法让模型在开启 LoRA 时同时支持双向和因果注意力，因此“开关切换”是结构性必须的。
*   **泛化性**：该机制成功扩展到了全模态模型（Omni-modal），实现了零样本的音频检索。

### 7. 优点
*   **极简性**：无需任何生成数据的微调，仅靠检索训练即可实现双重功能。
*   **工程严谨**：识别并解决了注意力模式、权重污染和 KV 缓存等三个容易被忽视的工程陷阱。
*   **实用性强**：直接解决了工业界部署 VLM RAG 时显存不足的痛点。

### 8. 不足与局限
*   **吞吐量开销**：在并发服务环境下，频繁切换 LoRA 适配器可能会引入额外的调度开销。
*   **模型族限制**：主要在 Qwen 系列模型上验证，尚未在 InternVL 或 LLaVA 等其他架构上广泛测试。
*   **LoRA Rank 依赖**：结论可能受限于 LoRA 的秩（r=16），更高秩的情况下联合训练是否有效尚不明确。
*   **端到端评估缺失**：虽然分别验证了检索和生成，但未对完整的“检索后生成”端到端准确率进行深度评估。

（完）
