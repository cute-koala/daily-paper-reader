---
title: "COVTrack++: Learning Open-Vocabulary Multi-Object Tracking from Continuous Videos via a Synergistic Paradigm"
title_zh: COVTrack++：通过协同范式从连续视频中学习开放词汇多目标跟踪
authors: "Zekun Qian, Wei Feng, Ruize Han, Junhui Hou"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24016v1"
tags: ["query:vot"]
score: 9.0
evidence: 连续视频中的开放词汇多目标跟踪
tldr: 针对开放词汇多目标跟踪（OVMOT）中连续标注数据匮乏及检测与关联协同不足的问题，本文构建了首个连续标注训练集C-TAO，并提出COVTrack++框架。该框架通过多线索自适应融合、多粒度层次聚合及时间置信度传播三个模块，实现了检测与关联的协同增强。实验表明，该方法在TAO数据集上达到SOTA水平，并具有极强的零样本泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24016v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF TRACKING PERFORMANCE ON VALIDATION AND TEST SETS. WE COMPARE THE METHODS ON OPEN-VOCABULARY TAO BENCHMARK [4]. ALL METHODS USE THE SAME BACKBONE. † REPRESENTS USING THE SAME DETECTOR. (%)\", \"page\": 11, \"index\": 1, \"width\": 972, \"height\": 569}, {\"url\": \"assets/figures/arxiv/2603.24016v1/fig-002.webp\", \"caption\": \"TABLE III COMPONENT-LEVEL ABLATION OF THE ADAPTIVE MULTI-CUE FUSION MODULE ON THE TAO VALIDATION SET (WITHOUT MGA AND TCP). (%)\", \"page\": 13, \"index\": 2, \"width\": 768, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2603.24016v1/fig-003.webp\", \"caption\": \"Fig. 8. Visualization of TCP recovering the missed detection boxes (middle frame omitted).\", \"page\": 16, \"index\": 3, \"width\": 524, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.24016v1/fig-004.webp\", \"caption\": \"Fig. 9. Visualization results of COVTrack++ on real-world videos.\", \"page\": 16, \"index\": 4, \"width\": 524, \"height\": 431}]"
motivation: 现有的OVMOT研究受限于缺乏连续标注的视频数据以及缺乏能协同处理检测与关联的定制化框架。
method: 提出了COVTrack++框架，包含多线索融合(MCF)、多粒度层次聚合(MGA)和时间置信度传播(TCP)三个核心模块。
result: 在TAO数据集上刷新了SOTA记录，显著提升了新类别的关联和定位准确度，并在BDD100K上展现了出色的零样本泛化性能。
conclusion: 通过构建高质量连续数据集和协同学习框架，有效解决了开放词汇多目标跟踪中的数据瓶颈和特征关联难题。
---

## 摘要
多目标跟踪（MOT）传统上侧重于少数特定类别，限制了其在涉及多样化对象的现实场景中的适用性。开放词汇多目标跟踪（OVMOT）通过实现对任意类别（包括训练期间未见的新对象）的跟踪来解决这一问题。然而，目前的进展受到两个挑战的制约：缺乏用于训练的连续标注视频数据，以及缺乏能够协同处理检测与关联的定制化 OVMOT 框架。针对数据瓶颈，我们构建了 C-TAO，这是首个用于 OVMOT 的连续标注训练集，其标注密度比原始 TAO 增加了 26 倍，并捕捉了平滑的运动动力学和中间对象状态。针对框架瓶颈，我们提出了 COVTrack++，这是一个协同框架，通过三个模块实现了检测与关联之间的双向互惠机制：（1）多线索自适应融合（MCF）动态平衡外观、运动和语义线索，用于关联特征学习；（2）多粒度分层聚合（MGA）利用密集检测中的分层空间关系，其中可见的子节点（如对象部件）辅助被遮挡的父对象（如整体）进行关联特征增强；（3）时间置信度传播（TCP）通过高置信度跟踪对象提升跨帧的低置信度候选对象，从而恢复闪烁的检测结果并稳定轨迹。在 TAO 上的广泛实验证明了其最先进的性能，新类别的 TETA 在验证集和测试集上分别达到 35.4% 和 30.5%，相比之前的方法，新类别的 AssocA 提高了 4.8%，LocA 提高了 5.8%，并在 BDD100K 上表现出强大的零样本泛化能力。代码和数据集将公开。

## Abstract
Multi-Object Tracking (MOT) has traditionally focused on a few specific categories, restricting its applicability to real-world scenarios involving diverse objects. Open-Vocabulary Multi-Object Tracking (OVMOT) addresses this by enabling tracking of arbitrary categories, including novel objects unseen during training. However, current progress is constrained by two challenges: the lack of continuously annotated video data for training, and the lack of a customized OVMOT framework to synergistically handle detection and association. We address the data bottleneck by constructing C-TAO, the first continuously annotated training set for OVMOT, which increases annotation density by 26x over the original TAO and captures smooth motion dynamics and intermediate object states. For the framework bottleneck, we propose COVTrack++, a synergistic framework that achieves a bidirectional reciprocal mechanism between detection and association through three modules: (1) Multi-Cue Adaptive Fusion (MCF) dynamically balances appearance, motion, and semantic cues for association feature learning; (2) Multi-Granularity Hierarchical Aggregation (MGA) exploits hierarchical spatial relationships in dense detections, where visible child nodes (e.g., object parts) assist occluded parent objects (e.g., whole body) for association feature enhancement; (3) Temporal Confidence Propagation (TCP) recovers flickering detections through high-confidence tracked objects boosting low-confidence candidates across frames, stabilizing trajectories. Extensive experiments on TAO demonstrate state-of-the-art performance, with novel TETA reaching 35.4% and 30.5% on validation and test sets, improving novel AssocA by 4.8% and novel LocA by 5.8% over previous methods, and show strong zero-shot generalization on BDD100K. The code and dataset will be publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《COVTrack++: Learning Open-Vocabulary Multi-Object Tracking from Continuous Videos via a Synergistic Paradigm》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：传统的检测与跟踪（MOT）局限于预定义的封闭类别。开放词汇多目标跟踪（OVMOT）旨在跟踪视频中任意类别的对象（包括训练中未见的新类别）。
*   **核心问题**：
    1.  **数据瓶颈**：现有的多样化类别数据集（如 TAO）标注极其稀疏（每30帧标注1帧），导致模型难以学习连续的运动动力学；而连续标注的数据集（如 MOT20）类别过于单一。
    2.  **框架瓶颈**：缺乏专门为 OVMOT 设计的协同框架，无法有效处理定位、分类和关联这三个子任务之间的相互依赖关系，尤其是在处理嵌套对象（如“人”与“衣服”）和检测闪烁（Flickering）时表现不佳。

### 2. 方法论：核心思想与关键技术
论文提出了 **COVTrack++** 框架，通过“检测与关联的双向互惠机制”来提升性能：
*   **C-TAO 数据集构建**：对 TAO 训练集进行了全帧标注，标注密度提升了 26 倍。这为学习平滑的运动轨迹和中间状态（如遮挡、模糊）提供了基础。
*   **多线索自适应融合 (MCF)**：
    *   整合外观（App）、运动/位置（Loc）和语义（Sem）三种线索。
    *   引入**双视角置信度估计**：**帧内置信度**（通过 SGN 网络学习线索间的互补性）和**帧间置信度**（利用循环一致性 Cycle Consistency 评估特征的稳定性），实现动态加权融合。
*   **多粒度层次聚合 (MGA)**：
    *   利用 OVMOT 中常见的嵌套框关系（父子结构）。
    *   通过**空间包含掩码**识别父子关系，并利用**时间关联质量 (TAQ)** 评分过滤不可靠的子节点，使用交叉注意力机制将可见子部件的特征聚合到可能被遮挡的父对象上。
*   **时间置信度传播 (TCP)**：
    *   针对检测不稳定的问题，构建跨帧二分图。
    *   利用前一帧的高置信度目标，通过**类条件置信度传播**提升当前帧低置信度候选框的分数，从而恢复断裂的轨迹。

### 3. 实验设计
*   **数据集**：主要在 **TAO** (Track Any Object) 的验证集和测试集上进行评估。
*   **Benchmark**：使用 **TETA** 指标（包含定位 LocA、关联 AssocA 和分类 ClsA）。
*   **对比方法**：对比了包括 OVTrack、MASA、SLAck、OVSORT、TRACT、OVTR 等在内的多种 SOTA 方法。
*   **泛化性测试**：在 **BDD100K** 自动驾驶数据集上进行了零样本（Zero-shot）迁移实验。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 3090 GPU**。
*   **训练细节**：在 C-TAO 数据集上训练了 **10 个 epoch**。
*   **模型配置**：采用 Faster R-CNN (ResNet-50) 作为检测器骨干网络，且在训练关联模块时冻结了检测器参数。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   在 TAO 验证集和测试集上的全指标对比。
    *   **模块级消融实验**：分别验证了 MCF、MGA、TCP 对性能的贡献。
    *   **深入分析实验**：对 MCF 内部的各种置信度、MGA 的聚合策略、TCP 的传播机制进行了细粒度拆解。
    *   **数据密度分析**：对比了原始 TAO、部分标注（P-TAO）和全标注（C-TAO）对不同模型性能的影响。
*   **客观性**：实验采用了统一的检测器底座，确保了对比的公平性；跨数据集（BDD100K）的测试验证了其非过拟合的鲁棒性。

### 6. 主要结论与发现
*   **性能突破**：在 TAO 验证集上，新类别的 TETA 达到 **35.4%**，相比之前最好的方法提升了约 4%。
*   **数据价值**：证明了连续标注对于 OVMOT 至关重要，即使是其他模型（如 OVTrack）在 C-TAO 上训练后，性能也有显著提升。
*   **协同效应**：关联特征的增强（MCF+MGA）能显著提升检测恢复（TCP）的准确性，而检测质量的改善又反过来提供了更好的跟踪源。
*   **分类瓶颈**：尽管定位和关联大幅提升，但新类别的分类准确率（ClsA）依然普遍较低（约 3.5%），这反映了开放词汇识别本身的固有难度。

### 7. 优点与亮点
*   **首创性数据集**：填补了 OVMOT 领域缺乏高质量连续视频标注数据的空白。
*   **层次化建模**：MGA 模块敏锐地捕捉到了 OVMOT 中“整体与部分”的特殊空间关系，这在传统 MOT 中较少被讨论。
*   **自适应性强**：MCF 模块不依赖硬编码权重，而是通过循环一致性自发学习特征可靠性，具有很强的鲁棒性。
*   **即插即用**：TCP 模块作为一种后处理增强手段，无需重新训练检测器即可改善检测闪烁问题。

### 8. 不足与局限
*   **计算开销**：引入了多线索融合、交叉注意力和图匹配，虽然提升了精度，但在实时性要求极高的场景下可能存在推理延迟。
*   **检测器依赖**：模型性能仍高度依赖于预训练的开放词汇检测器（如 Faster R-CNN + CLIP），如果底层检测器无法生成任何候选框，TCP 也无法发挥作用。
*   **分类精度**：论文虽然在关联和定位上取得了巨大进步，但对于如何提升新类别的语义分类精度（ClsA）探讨相对较少。

（完）
