---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一标记：基于增量标记的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:q1"]
score: 10.0
evidence: 使用delta token的高效生成式世界模型
tldr: 视频世界建模的核心挑战是高效预测多样化的未来状态。本文提出 DeltaTok 和 DeltaWorld：DeltaTok 将连续帧间视觉基础模型的特征差异编码为单个“delta”令牌，将三维时空视频大幅压缩为一维时序序列；基于此，DeltaWorld 作为生成式世界模型，通过可并行处理的多假设训练策略高效生成多样化的合理未来。该方法在保持预测准确性和多样性的同时，参数量和计算复杂度相比现有方法显著降低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 视频世界建模中，现有方法要么预测单一未来（判别式模型），要么计算成本高昂（生成式模型），需要一种能高效生成多样化未来状态的新方法。
method: 该方法引入 DeltaTok 将连续帧间的特征差异编码为单个“delta”令牌，并构建生成式世界模型 DeltaWorld 在这些令牌序列上进行高效的多假设未来生成。
result: 在密集预测任务上的实验表明，DeltaWorld 的预测更接近真实结果，同时参数量比现有生成式世界模型少35倍以上，计算量（FLOPs）减少2000倍。
conclusion: DeltaWorld 通过创新的 Delta 令牌和生成式建模，在保持预测多样性的同时，实现了参数和计算量的大幅降低，为高效视频世界建模提供了新方案。
---

## 摘要
预测多样化的未来状态是视频世界建模的核心挑战。判别式世界模型生成确定性预测，隐式地平均了可能的未来，而现有的生成式世界模型在计算上仍然昂贵。近期研究表明，在视觉基础模型（VFM）的特征空间中预测未来，而非在优化像素重建的潜在空间中预测，所需的世界模型参数量显著减少。然而，大多数此类方法仍是判别式的。本文中，我们引入了 DeltaTok——一种将连续帧间的 VFM 特征差异编码为单个连续的“增量”标记的标记器，以及 DeltaWorld——一种在这些标记上运行的生成式世界模型，用于高效生成多样且合理的未来。增量标记将视频从三维时空表示简化为一维时间序列，例如对于 512x512 帧的视频可实现 1,024 倍的标记缩减。这种紧凑表示使得可处理的多假设训练成为可能：并行生成多个未来序列并仅监督最佳结果。在推理时，这能在单次前向传播中产生多样化的预测。在密集预测任务上的实验表明，DeltaWorld 生成的未来预测与真实结果更吻合，同时参数量比现有生成式世界模型少 35 倍以上、计算量（FLOPs）减少 2,000 倍以上。代码与权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：在自动驾驶、机器人等应用中，预测未来世界的多种可能状态（世界建模）至关重要。现有方法面临两难：
    *   **判别式模型**（如 DINO-world）：输出单一确定性预测，在不确定性下会趋向于平均未来，无法捕捉多样化的合理未来。
    *   **生成式模型**（如 Cosmos）：能生成多种未来，但通常计算成本高昂，需要多次前向传播（如扩散模型）或处理大量空间标记。
*   **核心目标**：开发一个既能高效生成多样化、合理的未来状态，又保持较低计算成本的生成式世界模型。

## 2. 方法论
*   **核心思想**：利用视频帧间的高度时空冗余性。与其完整编码或重建每一帧，不如只编码连续帧之间的“变化”。
*   **关键技术细节**：
    1.  **DeltaTok (增量标记器)**：
        *   输入：一个冻结的视觉基础模型（VFM，如 DINOv3）提取的连续两帧特征图 `x_{t-1}` 和 `x_t`。
        *   过程：通过一个基于 Vision Transformer (ViT) 的编码器-解码器架构，将两帧特征图的差异压缩为单个连续的“增量标记” `z_t`。
        *   解码：给定前一帧特征 `x_{t-1}` 和增量标记 `z_t`，解码器可以重建出当前帧特征 `x_t`。
        *   **效果**：将视频从三维（空间 x 空间 x 时间）表示压缩为一维（时间）序列。例如，对于512x512分辨率的帧（对应32x32=1024个空间标记），每帧仅用1个增量标记表示，实现了1024倍的压缩。
    2.  **DeltaWorld (世界模型)**：
        *   架构：基于 DINO-world 的预测器架构进行改造。
        *   操作对象：不再直接预测空间特征图，而是预测序列化的增量标记 `Z_{1:t}`。
        *   **训练策略 - Best-of-Many (BoM)**：
            *   从高斯分布中采样 K 个不同的噪声查询向量 `q_k`。
            *   用每个查询向量并行生成 K 个未来的增量标记候选。
            *   仅对与真实未来最接近的那个候选进行监督和反向传播。
            *   **优势**：在单次前向传播中即可实现多假设生成训练；推理时也只需一次前向传播即可获得多个不同未来样本。
    3.  **工作流程**：
        *   训练时：上下文帧通过 VFM + DeltaTok编码器得到增量标记序列；预测器接收该序列和噪声查询，并行生成多个未来的增量标记；BoM损失选择最佳者更新模型。
        *   推理时：给定上下文和不同噪声查询，单次前向传播即可得到多个不同的未来增量标记序列；通过 DeltaTok解码器可逐步重建出对应的空间特征图。

## 3. 实验设计
*   **主要任务与基准**：“密集预测基准”（Dense Forecasting Benchmark），评估对未来帧的语义分割和深度估计能力。包含短时（~0.2秒）直接预测和中时（~0.6秒）自回归 rollout预测。
*   **评估数据集**：
    1.  VSPW: 多样化场景的视频语义分割数据集。
    2. Cityscapes: 驾驶场景语义分割数据集。
    3. KITTI: 驾驶场景深度估计数据集。所有评估集均未出现在训练集中。
*   **对比方法**：
    1.  判别式基线：DINO-world （作者复现版）。
    2.  生成式基线：Cosmos-4B / Cosmos-12B （基于像素重建潜在空间的扩散世界模型）。
    3.  上下界：“复制最后一帧”作为下界，“使用真实未来”作为上界。

## 4. 资源与算力
论文未明确说明具体使用的GPU型号、数量和总训练时长等硬件细节。但提供了以下效率指标：
*   FLOPs对比显示 DeltaWorld比 Cosmos-12B节省了约2000倍的计算量（31 TFLOPs vs ~64,000 TFLOPs）。
*   在消融实验中提到使用8块 NVIDIA H200 GPU进行训练时间和内存测量。

## 5.实验数量与充分性
实验设计较为系统和充分：
*   **主实验 (表3)** ：在两个分辨率设置下（256x256用于消融、512x512用于主结果），全面对比了所有基线方法在三个数据集、两个任务、两个时间尺度上的性能及计算效率。报告了“最佳样本”和“平均样本”两种指标以衡量多样性和平均质量。
*   **消融实验 (表2, C, D)** ：系统地展示了从判别式基线到最终DeltaWorld的演进过程：(0)判别式 -> (1)加入BoM -> (2)全帧压缩 -> (3)Delta压缩。清晰证明了每一步带来的性能、效率和多样性变化。此外还验证了Delta思想在其他判别式架构上的有效性。
*   **参数分析 (图5)** ：研究了BoM训练中查询数量K对最终性能的影响趋势。
*   **定性分析 (附录E)** ：提供了丰富的可视化样例展示生成的多样性及质量。

总体而言，实验覆盖了方法的核心主张（高效性、准确性、多样性），并与当前代表性工作进行了公平比较。评估协议遵循了先前工作的标准。

##6 .主要结论与发现
1. DeltaTok能够将视频高效压缩为单维度的增量令牌序列 ，大幅降低表示复杂度 。
2.DeltaWorld结合 BoM目标 ，能够在单次前向传播中高效生成多样且合理的未来样本 。
3 .在密集预测基准上 ，DeltaWorld的最佳样本质量超越了现有的生成式世界模型 Cosmos ，而其平均样本质量也与优化的判别式基线相当甚至更优 。
4 .关键优势在于极致的效率 ：相比 Cosmos -12B ，参数量减少超过35倍 ，计算量减少约2000倍 。

##7 .优点
1 .创新性思路 ：将经典的视频编码中的“差分压缩”思想创造性地应用于 VFM特征空间的世界建模 ，概念清晰且有效 。
2 .效率突破显著 ：通过消除空间维度的建模需求并采用单次前传的多假设生成策略 ，实现了参数量和计算量的数量级下降 。
3 .简单有效 ：BoM训练目标提供了一种简单而有效的将判别式模型转为生成式模型的途径 。
4 .泛化能力强 ：方法不依赖于特定任务或领域 ，在多个不同数据集上均表现良好 。

##8 .不足与局限
1 .分布建模的理论保证不足 ：BoM目标缺乏像扩散模型那样对数据分布的显式建模 。其覆盖范围受限于训练时探索的噪声查询数量 K ，且不能保证采样分布近似真实概率分布 。
2 .误差累积风险 ：由于采用自回归方式基于前一重建帧来解码当前增量令牌 ，令牌器和预测器的误差可能在多步 rollout过程中累积放大 。这是自回归模型的共性问题 。
3 .依赖预训练的 VFM :性能依赖于所选 VFM的质量和特性 。虽然使用了强大的 DINOv3 ，但更换 VFM可能需要重新调整或训练令牌器 。
4 .长时序建模未充分探索 ：论文主要验证了短/中期预测 （最多约0 .6秒 ）。对于需要更长程推理的场景 （如数秒后 ）其表现和误差累积情况尚不明确 。

(完)

## 9. 潜在影响与未来方向
1.  **对世界建模领域的推动**：为高效生成式世界建模提供了一个新的范式，证明了通过压缩表示和高效采样策略，可以在不牺牲多样性的前提下，极大地降低计算成本。这可能促使更多研究关注于如何更有效地利用视频的时空冗余性。
2.  **对下游应用的启示**：其高效性使得在资源受限的边缘设备（如车载计算单元、机器人）上部署复杂的多假设预测模型成为可能。这可以显著提升自动驾驶、机器人导航等系统在不确定性环境下的决策鲁棒性。
3.  **方法论的扩展**：
    *   **更复杂的预测目标**：当前工作主要预测视觉特征。未来可将“增量标记”的思想扩展到其他模态（如LiDAR点云、雷达信号）或更高层次的表示（如场景图、行为意图），构建多模态、多层次的世界模型。
    *   **改进的分布建模**：可以探索将BoM目标与更严格的概率生成模型（如归一化流、变分自编码器）相结合，以提供更好的分布覆盖和似然估计。
    *   **缓解误差累积**：引入非自回归解码、迭代修正机制或引入规划目标作为正则化，可能有助于改善长时序rollout的稳定性。
4.  **基础模型的协同进化**：这项工作展示了如何将强大的静态视觉基础模型（VFM）动态化为一个世界模型。随着VFM能力的持续增强（如更好的几何和物理理解），此类世界模型的性能上限也将随之提高。

## 10. 总结评价
本文《A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens》是一项在概念和方法上都具有显著贡献的工作。它敏锐地抓住了视频数据高冗余的特性，并巧妙地将其转化为计算效率上的巨大优势。

*   **核心贡献明确**：提出了“Delta Token”这一紧凑的视频表示方法，以及结合“Best-of-Many”训练策略的生成式世界模型架构。这两者的结合是其实现“高效且多样”预测的关键。
*   **实验验证扎实**：通过在标准密集预测基准上与强基线进行广泛对比，并从性能、多样性、效率等多个维度提供定量和定性证据，有力地支撑了其主张。
*   **实用价值突出**：所展示的计算效率提升是数量级的，这使其研究成果具有很高的实际应用潜力和吸引力。

尽管存在对理论分布保证不足、长时序误差累积等局限性的讨论，但这些并未削弱其核心贡献的价值，反而为后续研究指明了清晰的改进方向。总体而言，该论文为生成式世界建模领域提供了一个简洁、强大且极具效率的新解决方案。

（完）
