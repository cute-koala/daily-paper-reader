---
title: "Mine-JEPA: In-Domain Self-Supervised Learning for Mine-Like Object Classification in Side-Scan Sonar"
title_zh: Mine-JEPA：用于侧扫声呐中类似水雷物体分类的领域内自监督学习
authors: "Taeyoun Kwon, Youngwon Choi, Hyeonyu Kim, Myeongkyun Cho, Junhyeok Choi, Moon Hwan Kim"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00383v1"
tags: ["query:q2"]
score: 10.0
evidence: 在声纳中使用联合嵌入预测架构的 Mine-JEPA
tldr: 本研究针对侧扫声纳图像中水雷样物体分类的数据稀缺和域差异问题，提出了首个域内自监督学习管道Mine-JEPA。该方法仅使用少量未标记声纳图像进行预训练，在二分类和三分类任务上均超越了微调的大型基础模型DINOv3的性能，同时参数更少。结果表明在特定领域数据有限时，精心设计的域内自监督学习是比依赖大规模预训练基础模型更有效的策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00383v1/fig-001.webp\", \"caption\": \"Figure 1. Performance comparison across model scales. Using only 1,170 sonar images for pretraining, Mine-JEPA outperforms DINOv3, pretrained on 1.7B images, at both backbone scales. ViT-Tiny remains competitive with 4× fewer parameters.\", \"page\": 1, \"index\": 1, \"width\": 462, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-002.webp\", \"caption\": \"Table 4. Ablation on initialization, augmentation, and data composition. Real∗ denotes natural-image augmentation (saturation/hue jitter, solarization, grayscale). Rows 1–2 compare against Random Init, rows 3–4 against DINOv3, and rows 5–8 show cumulative gains; each ∆ is relative to the preceding row.\", \"page\": 6, \"index\": 2, \"width\": 952, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Mine-JEPA pipeline. Stage 1 extracts unlabeled 96×96 patches from full SSS images using a sliding window. Stage 2 performs in-domain self-supervised pretraining with SSS-adapted multi-view augmentation and the SIGReg objective. Stage 3 evaluates the pretrained backbone with frozen or fine-tuned probes for downstream patch classification.\", \"page\": 3, \"index\": 3, \"width\": 1014, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-004.webp\", \"caption\": \"Figure 3. Representative samples from the public side-scan sonar dataset. Top: a full SSS image with annotated mine-like contacts (MILCO) and non-mine bottom objects (NOMBO). Bottom: 96×96 patches from the three classes used in our evaluation: MILCO, background (BG), and NOMBO.\", \"page\": 4, \"index\": 4, \"width\": 475, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-005.webp\", \"caption\": \"Table 1. 3-class patch classification on BG/MILCO/NOMBO. For each method, we report the best probe mode and the corresponding macro-F1, NOMBO F1, and accuracy. Probe modes: linear and mlp (frozen backbone), finetune and ft mlp (fine-tuned backbone).\", \"page\": 5, \"index\": 5, \"width\": 994, \"height\": 223}]"
motivation: 侧扫声纳水雷分类面临数据稀缺和与自然图像存在巨大域鸿沟的挑战，而现有基础模型在该领域的应用尚未充分探索。
method: 提出了Mine-JEPA，一个基于SIGReg正则化自监督损失的域内学习管道，仅使用1170张未标记声纳图像进行预训练。
result: 在二分类任务中F1分数达0.935，优于微调的DINOv3；在三分类任务中达0.820，且使用更少的参数实现了有竞争力的性能。
conclusion: 研究表明，在数据稀缺的侧扫声纳领域，精心设计的域内自监督学习是比大型基础模型更有效的替代方案。
---

## 摘要
侧扫声呐（SSS）水雷分类是一个具有挑战性的海洋视觉问题，其特点是数据极度稀缺且与自然图像存在巨大的领域差异。虽然自监督学习（SSL）和通用视觉基础模型在通用视觉及多个专业领域表现出色，但它们在SSS中的应用在很大程度上仍未得到探索。我们提出了Mine-JEPA，这是首个用于SSS水雷分类的领域内SSL流程，它使用基于正则化的SSL损失函数SIGReg，仅对1,170张未标记的声呐图像进行预训练。在二分类（水雷与非水雷）设置中，Mine-JEPA取得了0.935的F1分数，优于经过微调的DINOv3（0.922），后者是在17亿张图像上预训练的基础模型。对于3类类似水雷物体的分类任务，Mine-JEPA通过合成数据增强达到了0.820的分数，再次优于经过微调的DINOv3（0.810）。我们进一步观察到，将领域内SSL应用于基础模型会使性能下降10-13个百分点，这表明更强的预训练模型并不总能从额外的领域适应中受益。此外，采用紧凑ViT-Tiny骨干网络的Mine-JEPA实现了有竞争力的性能，同时使用的参数比DINOv3少4倍。这些结果表明，在数据稀缺的海洋声呐图像处理中，精心设计的领域内自监督学习是替代庞大基础模型的一个可行方案。

## Abstract
Side-scan sonar (SSS) mine classification is a challenging maritime vision problem characterized by extreme data scarcity and a large domain gap from natural images. While self-supervised learning (SSL) and general-purpose vision foundation models have shown strong performance in general vision and several specialized domains, their use in SSS remains largely unexplored. We present Mine-JEPA, the first in-domain SSL pipeline for SSS mine classification, using SIGReg, a regularization-based SSL loss, to pretrain on only 1,170 unlabeled sonar images. In the binary mine vs. non-mine setting, Mine-JEPA achieves an F1 score of 0.935, outperforming fine-tuned DINOv3 (0.922), a foundation model pretrained on 1.7B images. For 3-class mine-like object classification, Mine-JEPA reaches 0.820 with synthetic data augmentation, again outperforming fine-tuned DINOv3 (0.810). We further observe that applying in-domain SSL to foundation models degrades performance by 10--13 percentage points, suggesting that stronger pretrained models do not always benefit from additional domain adaptation. In addition, Mine-JEPA with a compact ViT-Tiny backbone achieves competitive performance while using 4x fewer parameters than DINOv3. These results suggest that carefully designed in-domain self-supervised learning is a viable alternative to much larger foundation models in data-scarce maritime sonar imagery.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Mine-JEPA: In-Domain Self-Supervised Learning for Mine-Like Object Classification in Side-Scan Sonar》的论文进行结构化、深入且客观的总结。

---

### **论文总结：Mine-JEPA**

#### **1. 核心问题与整体含义**
*   **研究背景**：侧扫声呐（SSS）水雷分类是海洋安全领域的关键任务，但面临两大核心挑战：
    1.  **数据极度稀缺**：标注数据获取成本高昂，数量有限。
    2.  **巨大的领域鸿沟**：声呐图像（基于声学回波的亮区-阴影结构）与自然图像（光学成像）在视觉特征上存在根本性差异。
*   **研究动机**：尽管自监督学习（SSL）和通用视觉基础模型（如DINOv3）在其他领域取得了巨大成功，但它们在SSS领域的应用潜力尚未被充分探索。一个自然的思路是进行“领域内”自监督预训练。然而，在数据量极小且领域特殊的场景下，如何设计稳定有效的SSL方法是一个开放性问题。
*   **整体含义**：本文旨在探索并证明，在数据稀缺的特定领域（如SSS），一个精心设计的、轻量级的“领域内”SSL流程可以媲美甚至超越在数十亿自然图像上预训练的大型基础模型，为资源受限的边缘部署提供了高效替代方案。

#### **2. 方法论**
*   **核心思想**：提出Mine-JEPA管道，其核心是在极小的声呐数据集上执行“领域内”自监督预训练，而非直接依赖大规模预训练的基础模型。
*   **关键技术细节**：
    *   **三阶段流程**：
        1.  **数据准备**：从1170张全尺寸SSS图像中滑动提取约15.3万个96x96的无标签图像块。
        2.  **域内SSL预训练**：使用经过SSS适配的数据增强和SIGReg损失函数对Vision Transformer骨干网络进行预训练。
        3.  **下游评估**：将预训练的骨干网络冻结或微调，用于有标签的图像块分类任务。
    *   **SSL目标函数 - SIGReg (Sketched Isotropic Gaussian Regularization)**：
        *   源自LeJEPA框架。它结合了视图不变性损失和分布正则化损失。
        *   `L = (1 - λ)Linv + λLsig`
        *   `Linv`促使同一图像的不同增强视图的表示相互靠近。
        *   `Lsig`通过随机切片计算Epps-Pulley拟合优度统计量，将嵌入分布正则化为标准正态分布。这种方法无需负样本对、动量编码器或复杂的防坍缩启发式方法，在小数据场景下更稳定、超参数负担低。
    *   **针对SSS领域的三大关键适配**：
        1.  **SSS适配的数据增强**：移除了对自然图像有效但对声呐无效的增强（如色相/饱和度抖动、灰度化），增加了符合声呐扫描几何特性的垂直翻转和随机旋转(±15°)。
        2.  **初始化策略**：使用ImageNet-1K预训练的权重初始化ViT骨干网络（而非随机或DINOv3），为模型提供基础的边缘/纹理先验知识，同时保留足够的适应空间。
        3.  **数据构成**：探索了混合真实声呐块与约25.6万个合成灰度声呐块进行预训练的效果。

#### **3. 实验设计**
*   **数据集与Benchmark**：
    *   使用公开的侧扫声呐水雷检测基准数据集 [24]。
    *   包含1170张图像，其中304张包含668个标注目标（437个MILCO类水雷接触物, 231个NOMBO类非水雷海底物体）。
    *   采用80/10/10的分层划分作为训练/验证/测试集。下游任务评估使用从标注区域提取的图像块进行分类。
*   **对比方法**：
    *   **基线模型**: Random Init, ImageNet-1K (IN1K) Only, DINOv3 (微调)。
    *   **其他SSL方法**: SimCLR, VICReg, BYOL (均在相同IN1K初始化下于Real+Syn数据上训练)。
    *   Mine-JEPA本身也进行了不同配置的消融实验。

#### **4. 资源与算力**
论文中未明确说明具体的GPU型号、数量及总训练时长等算力细节。仅提及了部分超参数设置（如Batch Size=1024, Epochs=100）。这表明实验的重点在于方法有效性的验证和比较，而非计算效率的详细报告。

#### **5. 实验数量与充分性**
实验设计较为系统和充分：
*   ***主实验***: 分别在二分类（水雷 vs. 非水雷）和三分类（背景/MILCO/NOMBO）任务上对比了所有方法的最佳性能。
*   ***消融分析***:
    1.  ***初始化与适应行为***: 系统分析了不同初始化策略下应用域内SSL的效果。关键发现是：在DINOv3上进行域内SSL会导致性能显著下降；而在IN1K上进行则能获得累积增益。这揭示了“更强的预训练模型不一定能从额外的域适应中受益”。
    2.  ***SSL方法与数据可扩展性对比***: 在相同条件下对比了不同家族（正则化、对比式、蒸馏式）的SSL目标函数对异构合成数据的利用能力。发现正则化方法能从合成数据中获益最多且最稳定。
    3. ***组件消融***: （见表4）逐步展示了从随机初始化开始，依次加入SSS适配增强、IN1K初始化、λ调优、添加Real+Syn数据所带来的性能提升链。
*   ***公平性与客观性***:
    *   所有对比方法均使用相同的骨干网络架构(ViT-S/16)、相同的初始化和相同的数据集进行公平比较。
    *   结果报告为10次随机种子的均值±标准差，增强了统计可靠性。

#### **6. 主要结论与发现**
1.  Mine-JEPA在三分类和二分类任务上均超越了微调的DINOv3基础模型 (**F1:0.820 vs DINOv3’s0 .810; F1:0 .935 vs DINOv3’s0 .922**) ，证明了在小规模特定领域中精心设计的域内SSL的有效性。
2.  一个重要的反直觉发现是：将域内SSL应用于已经非常强大的基础模型(DINOv3)会使其性能下降10-13个百分点。这表明过强的先验知识可能阻碍其在显著不同的新领域中有效适应(**特征扭曲**) 。
3.SIGReg等基于正则化的SSL目标比对比式或蒸馏式方法更能稳健地利用异构合成数据进行学习 。
4.Mine-JEPA采用轻量级ViT-Tiny骨干时仍能保持竞争力 ，同时参数量仅为DINOv3的四分之一 ，显示出其在计算资源受限平台上的应用潜力 。

####7.**优点**
* ***问题定位精准*** ：针对SSS领域的两个核心痛点——小数据和域差异——提出了针对性解决方案 。
* ***方法论设计精巧且务实*** ：没有盲目追求复杂架构 ，而是选择了在小数据场景下更稳定的LeJEPA/SIGReg框架 ，并围绕该框架进行了三项关键的领域适配 ，形成了完整高效的解决方案 。
* ***分析深入且有洞察力*** ：不仅展示了方法的有效性 ，还通过详尽的消融实验揭示了“强基础模型+域适应不一定更好”这一重要现象 ，并探讨了不同类别SSL目标函数对异构数据的鲁棒性差异 。这些分析具有普遍参考价值 。
* ***结果具有启发性与实践意义*** ：证明了在特定小规模专业领域中，“小而精”的自监督学习可以成为替代“大而全”基础模型的可行路径 。

####8.**不足与局限**
* ***数据集规模有限*** ：所有结论均基于一个仅包含1170张图像的单一公开数据集得出 。虽然这反映了该领域的现实困难 ，但结果的普适性和泛化能力有待在更大规模 、更多样化的数据集上进行验证 。
* ***任务范围较窄*** ：研究聚焦于已裁剪好的小块图像的分类任务 。对于实际应用中更常见的全图滑动窗口检测或端到端检测任务的有效性尚未验证 。
* ***未考虑更广泛的迁移场景*** ：论文主要探讨了从自然图像到声纳图像的迁移 。对于在不同类型 、不同环境采集的真实世界侧扫声纳系统之间的跨传感器迁移能力未做评估 。
*(完)*

好的，我将从“不足与局限”部分继续补全。

#### **8. 不足与局限（续）**
*   ***合成数据质量与融合方式***：虽然探索了合成数据的利用，但论文中使用的合成数据是简单的灰度图像块，其逼真度和多样性有限。如何生成更高质量的声呐模拟数据，以及如何更有效地融合真实与合成数据进行预训练，仍有待深入研究。
*   ***计算成本分析不足***：论文强调了方法的轻量化和边缘部署潜力，但未提供完整的端到端计算成本（如训练时间、推理速度、内存占用）对比分析。这对于实际工程应用中的选型至关重要。
*   ***理论解释的深度***：对于“为何DINOv3进行域内SSL会失败”这一关键现象，论文主要从经验观察和“特征扭曲”的角度进行解释。若能结合表示学习理论或进行更深入的表示空间可视化分析（如特征分布、注意力图变化），将能提供更具说服力的机理阐释。

#### **9. 未来工作方向**
基于本研究的发现和局限，未来工作可以从以下几个方向展开：
1.  **扩展到更复杂的下游任务**：将Mine-JEPA预训练模型应用于目标检测、实例分割或像素级分类任务，验证其在完整SSS图像分析流程中的价值。
2.  **跨域与持续学习**：研究该方法在不同声呐系统、不同海底环境或不同分辨率数据集间的迁移能力。探索在持续获取新数据场景下的在线或增量自监督学习策略。
3.  **高级合成数据生成**：结合物理声学模型或高质量的生成对抗网络（GANs）来创建更逼真的合成SSS数据，以进一步提升预训练模型的鲁棒性和泛化性。
4.  **架构与目标函数优化**：针对SSS图像的独特结构（如阴影-亮区配对），设计更专用的神经网络架构或自监督学习目标函数，以更好地捕获领域特有的不变性特征。
5.  **部署导向的优化**：对Mine-JEPA管道进行彻底的效率评估和优化，包括模型量化、剪枝和硬件加速，以真正满足水下无人平台等边缘设备的严苛资源限制。

#### **10. 总结评价**
总体而言，《Mine-JEPA》是一篇高质量、务实且富有洞察力的应用型研究论文。它没有追逐最前沿但复杂的基础模型架构，而是敏锐地抓住了特定垂直领域（侧扫声呐水雷分类）的实际瓶颈——**小数据和领域差异**。通过系统性地将一个简洁稳定的自监督学习框架（LeJEPA/SIGReg）与该领域的先验知识相结合，作者成功地构建了一个高效且有效的解决方案。

其最大的贡献在于通过严谨的实验揭示了 **“预训练强度”与“领域适应能力”之间并非简单的正相关关系**这一反直觉发现，挑战了“直接使用最强基础模型总没错”的惯性思维。这为资源受限的专业领域提供了一个清晰的范例：即通过精心设计的、轻量级的域内自监督学习，可以以更低的成本获得媲美甚至超越巨型通用模型的性能。

尽管存在数据集规模单一、任务范围较窄等局限性，但其方法论框架和分析结论具有很好的可迁移性和启发性，不仅适用于声呐图像分析，也为其他数据稀缺、领域特殊的视觉任务（如医学影像、工业检测、遥感等）提供了有价值的参考路径。

（完）
