---
title: "Mine-JEPA: In-Domain Self-Supervised Learning for Mine-Like Object Classification in Side-Scan Sonar"
title_zh: Mine-JEPA：用于侧扫声呐中类似水雷物体分类的领域内自监督学习
authors: "Taeyoun Kwon, Youngwon Choi, Hyeonyu Kim, Myeongkyun Cho, Junhyeok Choi, Moon Hwan Kim"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00383v1"
tags: ["query:q2"]
score: 9.0
evidence: Mine-JEPA 使用联合嵌入预测架构进行声纳分类
tldr: 针对侧扫声纳图像中水雷样物体分类的数据稀缺和领域差异问题，本研究提出了首个域内自监督学习框架Mine-JEPA。该方法仅使用1170张未标记声纳图像进行预训练，在二元和三元分类任务上均超越了基于17亿图像预训练的DINOv3基础模型的微调结果。主要贡献在于证明了在特定数据稀缺领域，精心设计的域内自监督学习可以成为比依赖大规模通用基础模型更有效的替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00383v1/fig-001.webp\", \"caption\": \"Figure 1. Performance comparison across model scales. Using only 1,170 sonar images for pretraining, Mine-JEPA outperforms DINOv3, pretrained on 1.7B images, at both backbone scales. ViT-Tiny remains competitive with 4× fewer parameters.\", \"page\": 1, \"index\": 1, \"width\": 462, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-002.webp\", \"caption\": \"Table 4. Ablation on initialization, augmentation, and data composition. Real∗ denotes natural-image augmentation (saturation/hue jitter, solarization, grayscale). Rows 1–2 compare against Random Init, rows 3–4 against DINOv3, and rows 5–8 show cumulative gains; each ∆ is relative to the preceding row.\", \"page\": 6, \"index\": 2, \"width\": 952, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Mine-JEPA pipeline. Stage 1 extracts unlabeled 96×96 patches from full SSS images using a sliding window. Stage 2 performs in-domain self-supervised pretraining with SSS-adapted multi-view augmentation and the SIGReg objective. Stage 3 evaluates the pretrained backbone with frozen or fine-tuned probes for downstream patch classification.\", \"page\": 3, \"index\": 3, \"width\": 1014, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-004.webp\", \"caption\": \"Figure 3. Representative samples from the public side-scan sonar dataset. Top: a full SSS image with annotated mine-like contacts (MILCO) and non-mine bottom objects (NOMBO). Bottom: 96×96 patches from the three classes used in our evaluation: MILCO, background (BG), and NOMBO.\", \"page\": 4, \"index\": 4, \"width\": 475, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.00383v1/fig-005.webp\", \"caption\": \"Table 1. 3-class patch classification on BG/MILCO/NOMBO. For each method, we report the best probe mode and the corresponding macro-F1, NOMBO F1, and accuracy. Probe modes: linear and mlp (frozen backbone), finetune and ft mlp (fine-tuned backbone).\", \"page\": 5, \"index\": 5, \"width\": 994, \"height\": 223}]"
motivation: 侧扫声纳水雷分类面临数据极度稀缺且与自然图像存在巨大领域差异的挑战，而自监督学习和通用视觉基础模型在此领域的应用尚未充分探索。
method: 提出了Mine-JEPA，首个用于侧扫声纳水雷分类的域内自监督学习流程，使用基于正则化的SIGReg损失函数，仅需1170张未标记声纳图像进行预训练。
result: 在二元分类任务中F1分数达0.935，优于微调的DINOv3；在三分类任务中达0.820（使用数据增强），同样优于DINOv3；且发现对强基础模型进行域内自监督学习反而会降低其性能。
conclusion: 研究表明，在数据稀缺的侧扫声纳图像领域，精心设计的域内自监督学习是比依赖大规模预训练基础模型更可行的替代方案。
---

## 摘要
侧扫声呐（SSS）水雷分类是一个具有挑战性的海洋视觉问题，其特点是数据极度稀缺且与自然图像存在巨大的领域差异。虽然自监督学习（SSL）和通用视觉基础模型在通用视觉和多个专业领域表现出色，但它们在SSS中的应用在很大程度上仍未得到探索。我们提出了Mine-JEPA，这是首个用于SSS水雷分类的领域内SSL流程，它使用基于正则化的SSL损失函数SIGReg，仅用1,170张未标记的声呐图像进行预训练。在二分类（水雷与非水雷）设置中，Mine-JEPA取得了0.935的F1分数，优于经过微调的DINOv3（0.922），后者是在17亿张图像上预训练的基础模型。对于3类类似水雷物体分类，Mine-JEPA通过合成数据增强达到了0.820的分数，再次优于经过微调的DINOv3（0.810）。我们进一步观察到，将领域内SSL应用于基础模型会使性能下降10-13个百分点，这表明更强的预训练模型并不总能从额外的领域适应中受益。此外，采用紧凑型ViT-Tiny骨干网络的Mine-JEPA实现了有竞争力的性能，同时使用的参数比DINOv3少4倍。这些结果表明，在数据稀缺的海洋声呐图像中，精心设计的领域内自监督学习是替代庞大基础模型的一个可行方案。

## Abstract
Side-scan sonar (SSS) mine classification is a challenging maritime vision problem characterized by extreme data scarcity and a large domain gap from natural images. While self-supervised learning (SSL) and general-purpose vision foundation models have shown strong performance in general vision and several specialized domains, their use in SSS remains largely unexplored. We present Mine-JEPA, the first in-domain SSL pipeline for SSS mine classification, using SIGReg, a regularization-based SSL loss, to pretrain on only 1,170 unlabeled sonar images. In the binary mine vs. non-mine setting, Mine-JEPA achieves an F1 score of 0.935, outperforming fine-tuned DINOv3 (0.922), a foundation model pretrained on 1.7B images. For 3-class mine-like object classification, Mine-JEPA reaches 0.820 with synthetic data augmentation, again outperforming fine-tuned DINOv3 (0.810). We further observe that applying in-domain SSL to foundation models degrades performance by 10--13 percentage points, suggesting that stronger pretrained models do not always benefit from additional domain adaptation. In addition, Mine-JEPA with a compact ViT-Tiny backbone achieves competitive performance while using 4x fewer parameters than DINOv3. These results suggest that carefully designed in-domain self-supervised learning is a viable alternative to much larger foundation models in data-scarce maritime sonar imagery.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Mine-JEPA: In-Domain Self-Supervised Learning for Mine-Like Object Classification in Side-Scan Sonar》的论文进行结构化、深入且客观的总结。

---

## 1. 核心问题与整体含义
*   **研究背景**：侧扫声呐（SSS）是探测水下威胁（如水雷）的关键传感器。传统上，其图像分析依赖人工判读，效率低下。尽管深度学习在计算机视觉领域取得了巨大成功，但其在SSS水雷分类中的应用面临两大核心挑战：
    1.  **数据极度稀缺**：标注的声呐图像非常稀少。
    2.  **巨大的领域差异**：声呐图像（基于声学回波、呈现高光-阴影结构）与自然RGB图像在统计特性上存在根本性差异。
*   **研究动机**：通用视觉基础模型（如DINOv3）虽强大，但其在大规模自然图像上预训练的特征可能无法直接适应SSS这一特殊领域。同时，主流自监督学习（SSL）方法通常需要大量数据才能稳定工作。因此，本文旨在探索在**数据稀缺且领域差异大**的条件下，如何通过精心设计的**领域内自监督学习**来获得有效的特征表示。

## 2. 方法论
*   **核心思想**：提出一个名为 **Mine-JEPA** 的三阶段流程，利用少量未标记的SSS图像进行领域内自监督预训练，以学习适用于下游水雷分类任务的特征表示。
*   **关键技术细节与流程**：
    1.  **数据准备 (Stage 1)**：从1170张全尺寸SSS图像中，以滑动窗口方式提取约15.3万个96x96像素的图像块作为无标签预训练数据。
    2.  **领域内SSL预训练 (Stage 2)**：
        *   **骨干网络与初始化**：使用Vision Transformer (ViT)，并采用ImageNet-1K预训练的权重进行初始化（而非随机或更强的DINOv3）。
        *   **SSL目标函数 - SIGReg**: 采用LeJEPA框架中的“草图化各向同性高斯正则化”损失。该损失结合了视图不变性损失和分布正则化项，旨在将嵌入向量分布推向标准正态分布。其优势在于结构简单（无需动量编码器、负样本对或复杂的防坍缩机制），非常适合小数据场景。
        *   **SSS适配的数据增强**: 摒弃了针对自然图像的增强策略（如色相/饱和度抖动、灰度化），转而采用更适合声呐图像的增强组合：仅保留亮度/对比度抖动、水平/垂直翻转、随机旋转(±15°)、随机裁剪和高斯模糊。
        *   **低维投影头**: 有意使用极低的投影维度（d=16），作为结构瓶颈以防止在小数据上过拟合。
        *   **混合数据策略**: 除了真实声呐块外，还探索了加入约25.6万个合成声呐块进行混合预训练。
    3.  **下游评估 (Stage 3)**：将预训练的骨干网络冻结或微调，并附加线性或MLP分类头，在标注的SSS图像块上进行分类性能评估。

## 3. 实验设计
*   **数据集与Benchmark**: 
    *   使用公开的侧扫声呐水雷检测数据集 [24]，包含1170张图像。
    *   任务设定为两种：
        1.  三分类：背景(BG)、类似水雷接触物(MILCO)、非水雷海底物体(NOMBO)。
        2.  二分类：水雷(MILCO) vs. 非水雷(BG+NOMBO)。
    *   采用分层划分的80/10/10训练/验证/测试集分割。
*   **对比方法**:
    *   **基线模型**: 
        *   随机初始化
        *   仅ImageNet-1K预训练 (IN1K Only)
        *   强大的通用基础模型 DINOv3 (ViT-S)
    *   **其他SSL方法**: 
        *   对比学习方法 SimCLR
        *   正则化方法 VICReg
        *   自蒸馏方法 BYOL
        所有对比SSL方法均使用相同的IN1K初始化和实验设置。

## 4. 资源与算力
论文中未明确说明具体的GPU型号、数量及总训练时长等算力细节。仅提及使用了Vision Transformer架构和AdamW优化器等配置。

## 5. 实验数量与充分性
实验设计较为系统和充分：
*   **主实验**: 
    1.  在三分类和二分类任务上全面比较了Mine-JEPA与多个基线及其他SSL方法的性能（F1分数、准确率）。
    2.  测试了不同规模的骨干网络(ViT-Tiny vs ViT-S)。
*   **消融实验与分析 (关键部分)**:
    1.  **(表4)初始化策略影响分析**: 
        *   验证了SSS适配增强的必要性（使用自然图像增强会严重损害性能）。
        *   揭示了重要发现：对强基础模型(DINOv3)应用领域内SSL反而导致性能显著下降(10-13个百分点)，而从IN1K初始化开始则能获得累积提升。
    2. **(表5)SSL方法与数据可扩展性分析**: 
        *   比较了不同家族SSL方法(SIGReg, VICReg, SimCLR, BYOL)在纯真实数据和混合(真实+合成)数据上的表现。
        *   发现正则化类方法(SIGReg, VICReg)能从混合数据中受益，而对比和蒸馏类方法则效果不佳甚至下降。

这些实验从多个维度验证了方法的有效性、关键组件的必要性以及一些反直觉的现象（如强模型不适应额外域适应），设计较为客观和公平。

## 6.主要结论与发现
1. Mine-JEPA在三分类和二分类任务上均超越了基于17亿张自然图像预训练的DINOv3基础模型的微调结果。这证明了在特定小规模专业领域中，“小而精”的域内自监督学习可以成为“大而全”的基础模型的可行替代方案。
2. ViT-Tiny骨干网络能以少4倍的参数量达到有竞争力的性能，表明该方法适合部署在资源受限的平台（如自主水下航行器AUVs）上。
3. **(关键反直觉发现)** ：对已经很强的基础模型(DINOv3)应用额外的域内自监督学习会损害其表征质量。这表明更强的预训练模型并不总能从额外的域适应中受益，尤其是在目标域较小且与原域差异巨大的情况下。相比之下，“温和”的ImageNet-1K初始化是更好的起点。
4.SIGReg这种简单的正则化式SSL目标在小数据和异构数据场景下表现出比更复杂的对比或蒸馏方法更好的鲁棒性和有效性。

##7 .优点
*   **问题聚焦明确**:精准针对了声呐图像处理中“数据少、差异大”的核心痛点展开研究。
*   **方法论系统性强**:构建了一个完整的三阶段流程(数据处理→域内SSL→下游评估)，并对每个环节(增强策略、初始化选择、损失函数设计)都进行了针对性的适配和深入分析。
*	**实证分析深入透彻**:不仅报告了最终性能超越SOTA的结果更重要的是通过详实的消融实验揭示了“为何有效”(如适配增强的必要性)以及“为何有时无效”(如强基础模型的负迁移现象)，提供了深刻的洞见而非仅仅是经验性结果展示.
*	**实践指导意义强:**指出了在小众专业领域中盲目追求大规模基础模型并非最佳路径为类似场景的研究提供了清晰的技术路线图.

##8 .不足与局限  
*	数据集规模有限:所有实验均基于单一公开数据集仅包含1170张原始图片测试集规模也较小限制了结论的一般性和统计显著性.
*	泛化能力未充分验证:未在不同类型的水雷不同型号的声纳系统或更广泛多变的环境条件下进行评估因此尚不清楚该方法在其他操作场景下的鲁棒性.
*	合成数据的普适性问题:虽然加入合成数据带来了增益但合成数据的生成方式及其与实际数据的模态差距如何影响学习过程仍需进一步探究.
*(完)*

好的，我将从您提供的“不足与局限”部分继续，并补充其他必要的分析维度。

---

## 8. 不足与局限（续）
*   **方法创新性有限**：Mine-JEPA的核心框架（JEPA/SIGReg）并非本文原创，而是对现有方法的领域适配。其主要贡献在于验证了该框架在特定领域的有效性，并提供了详细的实验分析，而非提出全新的SSL范式。
*   **计算成本分析缺失**：虽然强调了ViT-Tiny的轻量级优势，但论文未提供完整的预训练和微调阶段所需的计算资源（如GPU小时）对比。对于实际应用而言，额外的领域内预训练所增加的计算开销是否合理，需要权衡。
*   **解释性与可视化不足**：论文主要依赖分类指标进行评估，缺乏对所学特征表示的可视化分析（如t-SNE图）或注意力图的可视化。这使得难以直观理解模型究竟学到了什么声呐图像特有的模式。

## 9. 潜在影响与未来方向
*   **潜在影响**：
    1.  **为小众领域AI应用提供范例**：本研究为数据稀缺、领域差异大的专业传感器数据处理提供了一个清晰、可复现的技术路径——即利用温和的通用初始化+精心设计的域内自监督学习。
    2.  **挑战“越大越好”的预设**：其实验结果挑战了直接使用最强基础模型的惯性思维，强调了在特定场景下进行针对性模型设计的重要性。
    3.  **推动水下感知自动化**：该方法有望集成到AUV系统中，实现更高效、自动化的水下目标识别与分类。
*   **未来研究方向**：
    1.  **跨域/多模态融合**：探索结合合成孔径声呐(SAS)、前视声呐等其他水下传感器数据，或融合物理模型知识来增强特征学习。
    2.  **主动学习与半监督学习**：将自监督预训练与主动学习策略结合，以更高效地利用有限的人工标注预算。
    3.  **更具解释性的模型设计**：开发能提供决策依据（如高亮关键声学特征）的模型，以增强操作员对AI系统的信任。
    4.  **扩展到检测与分割任务**：将所提框架应用于更复杂的水下目标检测和像素级分割任务。

## 10. 总结评价
本文是一项扎实、严谨且富有洞察力的应用研究。它没有追求最前沿的SSL算法创新，而是聚焦于一个具体且具有挑战性的实际问题——侧扫声呐图像中的水雷分类。通过系统性的实验设计和深入的消融分析，论文有力地论证了：
1.  在数据稀缺的专业领域，“温和初始化 + 领域适配的自监督学习”是一条比直接微调强大基础模型更有效的路径。
2.  简单的正则化式SSL目标（SIGReg）在此类场景下可能比复杂的对比学习方法更具鲁棒性。
3.  技术选择（如数据增强、初始化点）必须紧密结合领域特性。

尽管存在数据集单一、泛化能力验证不足等局限性，但论文因其清晰的问题定义、严谨的方法论和具有启发性的发现（尤其是关于强基础模型负迁移的现象），为类似的小众、高价值AI应用场景提供了宝贵的经验指导和实践框架。

（完）
