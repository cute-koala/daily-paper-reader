---
title: Scaling Video Pretraining for Surgical Foundation Models
title_zh: 面向手术基础模型的规模化视频预训练
authors: "Sicheng Lu, Zikai Xiao, Jianhui Wei, Danyu Sun, Qi Lu, Keli Hu, Yang Feng, Jian Wu, Zongxin Yang, Zuozhu Liu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29966v1"
tags: ["query:q2"]
score: 9.0
evidence: 实例化为SurgRec-JEPA变体
tldr: "针对计算机辅助干预中至关重要的手术视频理解任务，现有基础模型因数据规模有限、程序多样性不足及评估不一致而受限。本研究提出了一个名为SurgRec的可扩展、可复现的预训练方案（含MAE和JEPA两种变体），并构建了涵盖内窥镜、腹腔镜等多种术式的大型多源视频数据集（10,535个视频）。通过统一的预训练流程与平衡采样策略，并在16个下游数据集上建立标准化基准进行评估。结果表明，SurgRec性能全面优于现有自监督学习及视觉语言模型基线，后者在细粒度时序识别任务上表现不稳定。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29966v1/fig-001.webp\", \"caption\": \"Table 2. Main comparison on Acc@1 (%) against SSL baselines. SR and V-MAE denote SurgRec and VideoMAE, respectively. SR-MAE (w/o bal.) refers to SurgRecMAE trained without balanced sampling. Dataset names are abbreviated (e.g., Cat: Cataract, Colono: Colonoscopic).\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-002.webp\", \"caption\": \"Table 3. VLM Acc@1 (%). SurgRec-MAE is the supervised reference. P1: baseline prompt; P2: stricter output constraint. ∆ = P2−P1 (points). Dataset names are abbreviated as in Table 2.\", \"page\": 7, \"index\": 2, \"width\": 716, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-003.webp\", \"caption\": \"Table 1. Pretraining data scale comparison across recent medical foundation models.\", \"page\": 3, \"index\": 3, \"width\": 725, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed medical video pretraining and evaluation framework.\", \"page\": 3, \"index\": 4, \"width\": 581, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2603.29966v1/fig-005.webp\", \"caption\": \"Table 4. Generalization analysis on the 4-domain benchmark (Acc@1, %). Domain scores are macro-averaged; Overall Macro and Worst Domain denote the mean and minimum across domains, respectively.\", \"page\": 8, \"index\": 5, \"width\": 731, \"height\": 304}]"
motivation: 现有手术基础模型受限于数据规模小、手术程序多样性不足和评估不一致，且缺乏可复现的训练流程。
method: "提出了一个名为SurgRec的可扩展且可复现的预训练方案，包含SurgRec-MAE和SurgRec-JEPA两个变体，并构建了一个包含10,535个视频的大型多源数据集。"
result: 在16个下游数据集上的广泛评估表明，SurgRec始终优于自监督学习和视觉语言模型基线。
conclusion: 本研究为社区提供了一个可复现、可扩展的基础，以构建更通用的手术视频模型。
---

## 摘要
手术视频理解对于计算机辅助干预至关重要，然而现有的手术基础模型仍受限于数据规模有限、手术流程多样性不足以及评估标准不一致等问题，且往往缺乏可复现的训练流程。我们提出了SurgRec，一个用于手术视频理解的可扩展且可复现的预训练方案，并实例化为两个变体：SurgRec-MAE和SurgRec-JEPA。我们构建了一个大规模多来源数据集，包含10,535个视频和2.145亿帧图像，涵盖内窥镜、腹腔镜、白内障及机器人手术。基于此数据集，我们开发了一个统一的预训练流程，采用平衡采样策略，并在16个下游数据集和四个临床领域中建立了标准化的、具有一致数据划分的可复现基准测试。在与自监督学习基线模型和视觉语言模型的广泛比较中，SurgRec在所有下游数据集上均表现出卓越的性能。相比之下，视觉语言模型在细粒度时序识别任务上表现不可靠，既存在性能差距也对提示词措辞敏感。我们的工作为研究社区提供了一个可复现、可扩展的基础框架，以构建更通用的手术视频模型。所有代码、模型和数据都将公开发布。

## Abstract
Surgical video understanding is essential for computer-assisted interventions, yet existing surgical foundation models remain constrained by limited data scale, procedural diversity, and inconsistent evaluation, often lacking a reproducible training pipeline. We propose SurgRec, a scalable and reproducible pretraining recipe for surgical video understanding, instantiated with two variants: SurgRec-MAE and SurgRec-JEPA. We curate a large multi-source corpus of 10,535 videos and 214.5M frames spanning endoscopy, laparoscopy, cataract, and robotic surgery. Building on this corpus, we develop a unified pretraining pipeline with balanced sampling and standardize a reproducible benchmark across 16 downstream datasets and four clinical domains with consistent data splits. Across extensive comparisons against SSL baselines and vision-language models, SurgRec consistently achieves superior performance across downstream datasets. In contrast, VLMs prove unreliable for fine-grained temporal recognition, exhibiting both performance gaps and sensitivity to prompt phrasing. Our work provides a reproducible, scalable foundation for the community to build more general surgical video models. All code, models, and data will be publicly released.

---

## 论文详细总结（自动生成）

# 论文总结：Scaling Video Pretraining for Surgical Foundation Models

## 1. 核心问题与整体含义
*   **研究背景**：手术视频理解是计算机辅助干预的核心，但现有手术基础模型面临三大挑战：
    1.  **数据规模有限**：现有模型训练数据量不足。
    2.  **程序多样性不足**：覆盖的手术类型和场景不够广泛。
    3.  **评估不一致且不可复现**：不同研究使用不同的数据集划分和评估标准，导致结果难以比较和复现。
*   **研究动机**：为了克服上述限制，本研究旨在建立一个**可扩展、可复现的预训练方案**，通过整合大规模、多样化的手术视频数据，并建立统一的评估基准，为构建更通用、鲁棒的手术视频基础模型提供坚实基础。

## 2. 方法论
*   **核心思想**：提出一个名为 **SurgRec** 的统一预训练方案，其核心是通过大规模、多来源的手术视频数据进行自监督学习（SSL），并强调数据的平衡性以提升跨领域泛化能力。
*   **关键技术细节**：
    *   **数据集构建 (SurgRec Corpus)**：
        *   整合了来自32个公开数据集（2,790个视频）和公开网络资源（7,745个视频）的共10,535个手术视频（2.145亿帧）。
        *   涵盖四大临床领域：腹腔镜手术、内窥镜检查、白内障手术、机器人辅助手术。
    *   **平衡采样策略 (Balanced Sampling)**：
        *   为解决数据源和术式分布不均的问题，采用基于聚类的平衡采样方法。具体流程为：
            1.  使用DINOv3提取所有视频片段的特征嵌入。
            2.  应用分层K-means聚类（25k → 5k → 1k）。
            3.  从每个最终簇的质心附近选取样本，确保从长尾分布中均匀采样。
        *   在训练中采用混合批次策略：85%来自平衡后的未标注池（其中70%为网络视频，30%为临床核心数据），15%仅来自临床核心数据。
    *   **模型变体 (SurgRec Variants)**：
        *   基于两种主流的自监督学习范式实例化了两个模型变体：
            1.  **SurgRec-MAE**: 基于掩码自动编码器（Masked Autoencoder）架构进行重建预训练。
            2.  **SurgRec-JEPA**: 基于联合嵌入预测架构（Joint-Embedding Predictive Architecture）进行潜在空间预测预训练。

## 3. 实验设计
*   **下游基准测试 (Benchmark)**：
    *   建立了一个标准化的多程序基准测试，涵盖16个下游数据集，分属上述四个临床领域。
    *   为确保可复现性，制定了统一的数据划分规则：（1）优先使用官方划分；（2）其次使用社区公认划分；（3）若无则采用7:2:1的视频级随机划分。所有划分清单均公开。
*   **对比方法 (Baselines)**：
    1.  **自监督学习基线 (SSL Baselines)**:
        *   通用域模型：VideoMAE, JEPA, DINOv3.
        *   领域适应基线：在本文临床子集上继续预训练的 DINOv3-SurgeNetXL.
    2.  **视觉语言模型基线 (VLM Baselines)**:
        *   Qwen3-VL-8B, LLaVA-NeXT-7b, Qwen2.5-VL-7B。这些模型进行零样本推理测试。

## 4. 资源与算力
*   **预训练阶段**：使用了8块 NVIDIA A100 GPU。SurgRec-MAE训练耗时约8天，SurgRec-JEPA耗时约15天。
*   **下游微调阶段**：在8块 RTX 3090 GPU上进行，每个数据集微调耗时约1至12小时。

## 5. 实验数量与充分性
*   **实验数量充足且系统化**：
    1.  **主实验**:在两个自监督变体上进行了16个下游数据集的全面性能评估（表2）。
    2.  **VLM对比实验**:对三个主流VLM进行了零样本测试，并分析了提示词敏感性（表3）。
    3.  **泛化分析**:将结果按四个临床领域进行宏平均聚合分析，并计算了“最差领域”性能以评估鲁棒性（表4）。
    4.  **消融实验**:通过对比有无平衡采样的 SurgRec-MAE (`SR-MAE w/o bal`)，验证了平衡采样策略的有效性及其对泛化能力的关键作用（表4）。
*   **客观性与公平性**:
    *   所有对比方法均在作者建立的统一基准上进行评估，确保了公平比较的基础。
    *   对VLM的评估考虑了提示词措辞的影响，揭示了其不稳定性。

## -6-.主要结论与发现
1.	规模化与多样性是关键:大规模、多来源的手术视频预训练能有效提升模型的跨域泛化能力。SurgRec-MAE/JEPA在所有16个下游任务上均优于通用域SSL基线及VLM基线。
2.**平衡采样至关重要**:仅增加数据量而不进行平衡采样会导致性能下降。消融实验表明，“无平衡”版本的性能显著低于完整版本。这表明数据的多样性分布与总体规模同等重要。
3.**VLMs在细粒度手术任务上存在局限**:尽管VLMs具有开放词汇能力，但在细粒度的手术时序识别任务上表现不稳定且性能较差。其输出对提示词措辞敏感(如表3所示)，限制了其在要求高可靠性的临床应用中的直接可用性。

## -7-.优点
1.**贡献全面且实用**:不仅提出了新的预训练方案(SurgRec)和模型变体(MAE/JEPA)，还构建了大规模数据集(SurgRec Corpus)和标准化的可复现基准(Surgical Benchmark)。代码、模型和数据将全部开源极大地促进了社区发展。

2.**强调可复现性与标准化**:论文的核心贡献之一是建立了一个具有明确、一致数据划分的标准化评测框架解决了该领域长期存在的评测不一致问题。

3.**深入的分析视角**:不仅报告了总体性能还进行了细致的领域级宏平均分析、“最差领域”性能分析以及针对VLM的提示词敏感性分析提供了更深入的见解。

4.**有效的技术策略**:提出的基于聚类的平衡采样方法简单有效显著提升了模型在数据稀缺领域的表现证明了处理长尾分布的重要性。

## -8-.不足与局限

1.**网络来源数据的域鸿沟(domain gap)**:用于扩充数据的网络教学/演示视频通常只包含“高光片段”缺乏真实临床场景中的并发症、意外情况等可能导致模型在实际部署时遇到未见过的场景泛化能力下降。

2.**部分基准数据集规模小或噪声大**:尽管建立了16个数据集的基准但其中一些子集本身规模较小或标注质量不高可能影响最终性能评估的统计可靠性。

3.**未来工作方向明确但尚未实现:**论文指出了未来需要在更真实的多中心临床环境中进行评估但当前工作尚未完成这一步验证模型的最终临床应用潜力仍需后续研究。


(完)

好的，我们继续从“不足与局限”部分开始，补充更深入的讨论和总结。

## 8. 不足与局限（续）

4.  **模型架构的探索有限**：研究主要基于两种主流的自监督学习范式（MAE和JEPA）进行实例化。虽然这两种方法在通用计算机视觉领域表现出色，但可能并非针对手术视频特有的时空特性（如工具-组织交互、长程依赖关系）的最优设计。未来可以探索更多专门为手术视频设计的预训练目标或架构。

5.  **评估任务集中于分类**：下游基准测试主要聚焦于动作/阶段识别、工具存在性检测等分类任务。对于手术视频理解同样重要的其他任务，如密集预测（如器械分割）、时序定位、报告生成等，尚未进行评估。这限制了对其作为“基础模型”通用能力的全面判断。

6.  **计算资源门槛**：尽管论文详细说明了所需算力（8块A100 GPU训练8-15天），但对于许多研究团队而言，复现或扩展此规模的工作仍具有较高的资源门槛。这在一定程度上影响了其方法的可及性。

## 9. 总结与展望

本研究是一项在构建**大规模、可复现的手术视频基础模型**道路上里程碑式的工作。它系统性地解决了该领域长期存在的三大痛点——数据规模小、程序单一、评估不一致。

*   **核心贡献总结**：
    1.  **数据集 (SurgRec Corpus)**：整合了超万级视频的大规模、多领域手术视频语料库。
    2.  **方法 (SurgRec Framework)**：提出了包含平衡采样策略的统一预训练框架，并实例化了两个高性能模型变体。
    3.  **基准 (Standardized Benchmark)**：建立了首个覆盖四大临床领域、包含16个数据集、具有明确公开划分的标准化评测基准。
    4.  **洞见 (Key Insights)**：通过严谨的实验揭示了：（a）数据规模与多样性对泛化能力同等重要；（b）当前通用视觉语言模型在细粒度手术任务上存在局限；（c）平衡采样是处理长尾临床数据的关键技术。

*   **未来工作展望**：
    1.  **向多模态扩展**：未来的手术基础模型应整合来自机器人传感器（如力反馈）、生理信号、外科医生注释等多模态信息，以构建更全面的情境理解能力。
    2.  **探索更高效的架构与目标**：设计更能捕捉手术特定时空动态和因果关系的预训练任务与模型架构。
    3.  **迈向真实世界验证与闭环应用**：在更具挑战性的多中心临床环境中进行评估，并探索模型在实时决策支持、技能评估、自动化报告等闭环应用中的潜力。
    4.  **推动开源生态建设**：本研究承诺开源所有资源（代码、模型、基准），有望成为社区发展的基石，激励更多研究者在此基础上进行创新和验证。

总而言之，这项工作为手术人工智能社区提供了一个坚实的数据基础、一套有效的预训练方案和一个公平的评测标准，显著推动了构建通用且鲁棒的手术视频理解模型的进程。

（完）
