---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一标记：基于增量标记的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:profile-4"]
score: 10.0
evidence: 使用增量令牌的高效生成式世界模型
tldr: 本文针对视频世界建模中未来状态多样性的挑战，提出了一种高效的生成式世界模型框架。现有方法要么是确定性预测（缺乏多样性），要么计算代价高昂。作者引入DeltaTok，将连续帧间的视觉基础模型特征差异编码为单个“delta”token，从而将视频从三维时空表示压缩为一维时序序列，大幅降低计算负担。基于此构建的DeltaWorld模型支持多假设并行训练与推理，能在单次前向传播中生成多样化未来预测。实验表明，该模型在密集预测任务上比现有生成式世界模型参数少35倍、FLOPs低2000倍，且预测结果更贴合真实情况。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 现有生成式世界模型计算成本高且预测多样性有限，需要一种能高效生成多样化未来状态的方法。
method: 提出DeltaTok将连续帧间特征差异编码为delta token，并基于此构建生成式世界模型DeltaWorld进行多假设训练。
result: 在密集预测任务中，模型参数减少35倍以上、FLOPs降低2000倍，同时预测结果更贴近真实世界。
conclusion: DeltaTok与DeltaWorld通过创新的delta token表示和高效生成建模，显著提升了世界模型的预测多样性与计算效率。
---

## 摘要
预测多样化的未来状态是视频世界建模的核心挑战。判别式世界模型生成确定性预测，隐式地平均了可能的未来，而现有的生成式世界模型计算成本仍然高昂。近期研究表明，在视觉基础模型的特征空间中预测未来（而非针对像素重建优化的潜在空间），所需的世界模型参数量显著减少。然而，此类方法大多仍为判别式。本文中，我们引入了DeltaTok——一种将连续帧间的VFM特征差异编码为单个连续“增量”标记的标记器，以及DeltaWorld——一种在这些标记上运行的生成式世界模型，用于高效生成多样化的合理未来。增量标记将视频从三维时空表示简化为一维时间序列，例如在512x512分辨率帧下可实现1,024倍的标记缩减。这种紧凑表示使得可处理的多假设训练成为可能：并行生成多个未来序列并仅监督最优结果。在推理阶段，该方法通过单次前向传播即可实现多样化预测。密集预测任务的实验表明，DeltaWorld生成的未来预测与真实结果更吻合，同时参数量比现有生成式世界模型减少35倍以上，计算量降低2,000倍以上。代码与权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：在自动驾驶等场景中，世界模型需要预测未来环境的多种可能状态以支持可靠决策。现有方法存在两大瓶颈：
    1.  **判别式模型**（如DINO-world）仅输出单一确定性预测，在不确定性下会坍缩为条件均值，无法捕捉多样化的未来。
    2.  **生成式模型**（如Cosmos）虽然能产生多样结果，但计算成本极高。它们通常在像素或重建优化的潜在空间中操作，需要多次前向传播（如扩散去噪），且建模了大量对决策无关的时空冗余细节。
*   **整体目标**：提出一种**高效且能生成多样化未来**的世界模型。核心思路是结合两个关键创新：1) 在预训练的视觉基础模型特征空间中操作，避免建模像素级细节；2) 将视频压缩为极简的“增量标记”序列，大幅降低建模复杂度。

## 2. 方法论
*   **核心思想**：利用视频帧间变化的低维性和结构性，仅编码和预测连续帧之间的“差异”，而非完整的每一帧。
*   **关键技术细节**：
    1.  **DeltaTok (增量标记器)**：
        *   输入：通过冻结的VFM（如DINOv3）提取的连续两帧特征网格 `x_{t-1}` 和 `x_t`。
        *   编码器：一个基于ViT的编码器，将两个特征网格压缩为单个连续的“增量标记” `z_t`。该标记代表了从 `x_{t-1}` 到 `x_t` 所需的变化。
        *   解码器：接收前一帧特征 `x_{t-1}` 和增量标记 `z_t`，重构出当前帧特征 `\hat{x}_t`。
        *   效果：将视频从 `(H, W, T)` 的三维时空表示压缩为仅包含 `T`个一维标记的序列（例如512x512帧下减少1024倍）。
    2.  **DeltaWorld (世界模型)**：
        *   **架构基础**：基于判别式DINO-world架构的未来预测器 `f`。
        *   **生成化改造 - Best-of-Many (BoM)训练**：
            *   训练时，从高斯分布采样K个噪声查询向量 `q_k`，替换原模型的单一查询。
            *   每个查询独立地通过预测器生成一个未来的增量标记候选 `\hat{z}_{t+1}^k`。
            *   计算所有候选与真实未来增量标记的距离，**仅对最佳候选进行反向传播监督**。这鼓励模型将不同的噪声映射到不同的合理未来。
            *   推理时，输入不同的噪声查询即可在单次前向传播中并行得到多个未来假设。
        *   **工作流程**：给定历史帧 -> VFM提取特征 -> DeltaTok编码为历史增量标记序列 -> DeltaWorld预测器基于噪声查询和该序列预测下一个增量标记 -> DeltaTok解码器结合上一帧特征重构出未来空间特征图。

## 3. 实验设计
*   **主要任务与Benchmark**：遵循DINO-world提出的密集预测基准。评估短期（~0.2秒，直接预测）和中期（~0.6秒，三步自回归展开）的未来预测质量。
*   **评估指标与下游任务**：
    1.  语义分割mIoU（平均交并比）：在VSPW和Cityscapes数据集上评估。
    2.  深度估计RMSE（均方根误差）：在KITTI数据集上评估。
    3.  对于生成式模型，报告“最佳”（20个样本中最接近真实值的）和“平均”（20个样本的特征平均后计算任务指标）两种分数。
*   **对比方法**：
    1.  **判别式基线**: DINO-world (作者复现版)。
    2.  **生成式基线**: Cosmos-4B, Cosmos-12B (基于像素重建潜在空间的扩散世界模型)。
    3.  上下界: “复制最后一帧” (下界)，“使用真实未来帧” (上界)。

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量和总训练时长。但提供了以下效率相关信息：
*   FLOPs对比显示DeltaWorld比Cosmos节省超过2000倍的计算量（31 TFLOPs vs ~64,000 TFLOPs）。
*   表2中的消融实验提到在8张NVIDIA H200 GPU上测量了每迭代的训练时间和GPU内存占用相对值。

## 5. 实验数量与充分性
实验设计较为系统和充分：
*   **主实验 (表3)**：在两个分割数据集和一个深度数据集上对比了所有基线方法在短/中期预测的性能、参数量和FLOPs。
*   **消融研究 (表2)**：清晰地展示了从判别式基线到最终DeltaWorld的三步演进过程：(0)判别式、(1)+BoM、(2)+全帧压缩、(3)+增量压缩。每一步都分析了性能、计算量和内存的变化趋势。
*   **BoM超参数分析 (图5)**：研究了训练和推理时不同采样数量K对性能的影响。
*   **跨架构验证 (附录C)**：将DeltaTok思想应用于另一个判别式世界模型DINO-Foresight，证明了其通用性。
*   **效率分解分析 (附录B表B)**：详细拆解了各组件（骨干网络、DeltaTok编解码器、预测器）在不同阶段的FLOPs贡献度。
*   实验覆盖了性能、效率、内存、多样性等多个维度。对比对象选择了领域内相关的最新工作。评估协议遵循公开基准。

## 6.主要结论与发现
1. DeltaWorld能够高效地生成多样化的合理未来场景。其“最佳”样本在所有任务和数据集上均超越了参数量大得多的Cosmos模型，“平均”样本也与优化的判别式基线相当或更优。
2.DeltaToken表示法极其高效。相比现有生成式世界模型(如Cosmos)，DeltaWorld实现了超过35倍的参数量减少和超过2000倍的FLOPs降低(图2)。
3.BoM目标函数结合delta压缩是构建高效生成式世界模型的有效途径(表2)。它恢复了因BoM引入而下降的平均预测质量同时保持了多样性优势归因于delta公式的自然先验:不变化即保持前一帧使得模型只需学习变化的部分。

##7优点
创新性强:提出了“delta token”这一新颖的视频表示概念将三维时空视频压缩为一维时序差异序列思想简洁而有力极大地提升了建模效率解决了高冗余问题。

工程贡献显著:通过结合VFM特征空间BoM训练目标和delta token成功构建了一个参数少计算量低且能单次前向生成多样未来的实用化世界模型框架为实际应用部署奠定了基础。

实验验证全面:不仅在主任务上表现出色还通过详尽的消融实验验证了每个组件的必要性以及方法的通用性(可迁移至其他架构)。对效率和性能的权衡分析透彻。

##8不足与局限
分布建模的理论保证不足:BoM目标缺乏像扩散模型中那样的显式分布匹配目标对未来分布的覆盖程度受限于训练时探索的噪声查询数量K没有机制保证采样到的未来概率分布逼近真实分布。

误差累积风险:由于采用自回归展开并使用前一时刻的重构特征来解码下一时刻任何步骤的重构或预测误差都可能随时间累积导致长期滚动的特征漂移这是自回归模型的共性问题文中提及但未完全解决。

应用场景限制:目前工作在静态相机视角的视频上进行测试未涉及智能体主动动作条件下的复杂交互环境模拟这对于机器人等需要规划的应用至关重要是一个重要的未来方向文中已指出此局限性。

数据依赖性与泛化性:方法依赖于预训练的VFM(DINOv3)的质量其性能上限受限于该基础模型的表征能力虽然使用了多领域数据训练但未在更极端或未见过的动态场景中进行充分测试泛化能力有待进一步验证。

---
**(完)**

## 9. 潜在影响与未来方向
*   **对世界模型领域的影响**：
    *   **效率范式转变**：论文证明了在高度压缩的“增量”表示上进行生成式建模的可行性，为构建轻量级、可部署的世界模型开辟了新路径，可能推动该技术从研究走向实际应用。
    *   **解耦表示学习与动态建模**：通过利用冻结的、通用的视觉基础模型（VFM）作为特征提取器，该方法将“理解当前场景”和“预测未来动态”两个任务解耦。这允许研究者专注于改进动态预测模块，而无需从头训练庞大的视觉编码器。
    *   **促进决策研究**：高效生成多样化未来假设的能力，可以直接服务于需要基于不确定性进行规划的算法（如自动驾驶中的风险感知规划、蒙特卡洛树搜索等），为下游决策模块提供了更丰富的输入。
*   **对未来工作的启发**：
    *   **改进分布建模**：可以探索更严格的生成式目标（如基于流的模型、GANs或改进的BoM变体）来更好地匹配真实未来分布，并提供似然估计。
    *   **处理长期依赖与误差累积**：引入更强的时序模型（如状态空间模型）、周期性刷新机制或潜在空间校正技术，以缓解自回归展开中的特征漂移问题。
    *   **融入动作条件预测**：扩展框架以接受智能体动作作为输入，实现对可控交互环境的模拟，这是迈向具身智能和机器人规划的关键一步。
    *   **探索更优的Delta表示**：当前增量标记基于连续帧，可研究跨更长时窗或基于关键帧的增量编码，以捕捉更复杂的运动模式。

## 10. 总结评价
本文是一项在生成式世界建模领域具有显著贡献的工作。它精准地抓住了现有方法在“多样性”与“效率”之间的核心矛盾，并提出了一个巧妙而有效的解决方案——“增量标记”（Delta Token）。通过将视频压缩为帧间差异序列，并在强大的预训练视觉特征空间中进行操作，该方法实现了惊人的计算效率提升（超过2000倍FLOPs减少），同时利用Best-of-Many训练策略恢复了生成多样化合理未来的能力。

实验设计严谨、对比充分，不仅证明了其性能优于庞大的基线模型（Cosmos），还通过系统的消融研究清晰地揭示了每个技术组件的贡献。尽管存在对预训练模型的依赖、长期预测误差累积等局限性，但论文明确指出了这些点并提出了未来方向。

总体而言，这项工作不仅在学术上提出了新颖的观点，其极高的效率也使其具备了实际应用的潜力，为高效、实用的世界模型的开发树立了一个有影响力的标杆。

---
（完）
