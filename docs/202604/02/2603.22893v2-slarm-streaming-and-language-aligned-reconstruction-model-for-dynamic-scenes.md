---
title: "SLARM: Streaming and Language-Aligned Reconstruction Model for Dynamic Scenes"
title_zh: SLARM：用于动态场景的流式语言对齐重建模型
authors: "Zhicheng Qiu, Jiarui Meng, Tong-an Luo, Yican Huang, Xuan Feng, Xuanfu Li, ZHan Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22893v2"
tags: ["query:vlm"]
score: 8.0
evidence: 语言对齐的重建与语义查询
tldr: SLARM是一个统一的动态场景重建、语义理解和实时流式推理的前馈模型。它通过高阶运动建模捕捉复杂非均匀运动，并蒸馏LSeg语义特征实现语言对齐，支持自然语言查询。采用窗口因果注意力机制实现低延迟流式处理，在动态估计、渲染质量和场景解析方面均达到SOTA水平，显著提升了运动精度和重建效果。
selection_source: carryover_cache
motivation: 旨在解决动态场景重建中复杂运动捕捉难、语义理解缺失以及实时流式处理内存开销大的问题。
method: 结合高阶运动建模、LSeg语义蒸馏以及窗口化因果注意力机制，构建了一个端到端的语言对齐流式重建框架。
result: "在动态估计精度上提升了21%，重建PSNR提高1.6 dB，且场景分割mIoU相比现有方法提升了20%。"
conclusion: SLARM通过语义与几何的紧密耦合，实现了高效、高精度的动态场景实时重建与语义解析。
---

## 摘要
我们提出了 SLARM，这是一种统一了动态场景重建、语义理解和实时流式推理的前馈模型。SLARM 通过高阶运动建模捕捉复杂的非均匀运动，且仅在可微分渲染上进行训练，无需任何光流监督。此外，SLARM 从 LSeg 中蒸馏语义特征，以获得语言对齐的表示。这种设计实现了通过自然语言进行语义查询，且语义与几何之间的紧密耦合进一步增强了动态重建的准确性和鲁棒性。此外，SLARM 使用基于窗口的因果注意力机制处理图像序列，在不累积内存开销的情况下实现了稳定、低延迟的流式推理。在这一统一框架内，SLARM 在动态估计、渲染质量和场景解析方面均达到了最先进的结果，相比现有方法，其运动准确度提升了 21%，重建 PSNR 提升了 1.6 dB，分割 mIoU 提升了 20%。

## Abstract
We propose SLARM, a feed-forward model that unifies dynamic scene reconstruction, semantic understanding, and real-time streaming inference. SLARM captures complex, non-uniform motion through higher-order motion modeling, trained solely on differentiable renderings without any flow supervision. Besides, SLARM distills semantic features from LSeg to obtain language-aligned representations. This design enables semantic querying via natural language, and the tight coupling between semantics and geometry further enhances the accuracy and robustness of dynamic reconstruction. Moreover, SLARM processes image sequences using window-based causal attention, achieving stable, low-latency streaming inference without accumulating memory cost. Within this unified framework, SLARM achieves state-of-the-art results in dynamic estimation, rendering quality, and scene parsing, improving motion accuracy by 21%, reconstruction PSNR by 1.6 dB, and segmentation mIoU by 20% over existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《SLARM: Streaming and Language-Aligned Reconstruction Model for Dynamic Scenes》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D/4D 场景重建方法（如 NeRF 或 3DGS）通常面临三个主要挑战：
    1.  **优化耗时**：大多依赖于单场景过拟合优化，缺乏泛化能力。
    2.  **运动建模简单**：现有前馈模型（如 STORM）通常假设匀速运动，难以捕捉现实中复杂的非线性动力学（如人体动作）。
    3.  **功能单一**：缺乏高层语义理解，且难以在不累积内存开销的情况下实现长序列的实时流式推理。
*   **研究背景**：随着自动驾驶和具身智能的发展，迫切需要一种能够同时实现动态重建、语义对齐和流式输出的通用 4D 基础模型。

### 2. 方法论
SLARM 采用前馈 Transformer 架构，通过单次前向传播同时预测几何、运动和语义。
*   **高阶运动建模**：
    *   不同于以往的位移或速度模型，SLARM 使用**三阶泰勒展开（Taylor Expansion）**来建模位移 $\Gamma(\Delta t)$。
    *   网络预测速度（Velocity）、加速度（Acceleration）和加加速度（Jerk）的系数。这使得模型能够通过自监督渲染损失（无需光流标注）学习复杂的非均匀运动。
*   **语言对齐的 4D 语义**：
    *   **特征蒸馏**：从 2D 视觉语言模型 **LSeg** 中蒸馏语义知识，将语言对齐的特征向量赋予每个 3D 高斯基元。
    *   **语义增强**：通过将语义一致性作为正则项，辅助几何重建，并支持通过自然语言（如“查询行人”）直接进行 3D 空间搜索。
*   **流式推理架构**：
    *   采用**窗口化因果注意力机制（Window-based Causal Attention）**，仅依赖当前和过去的观察。
    *   通过维护一个紧凑的隐藏状态和内存队列，实现恒定的低延迟和内存占用，支持长序列处理。
*   **统一多任务解码**：使用共享权重的 ViT 提取特征，通过不同的 Head 分别输出高斯参数（位置、旋转、缩放、不透明度、颜色）、运动属性和语义特征。

### 3. 实验设计
*   **数据集**：主要在 **Waymo Open Dataset (WOD)** 上进行实验，并扩展到了 **NuScenes** 和 **Argoverse2** 等自动驾驶主流数据集。
*   **Benchmark 与对比方法**：
    *   **动态重建**：对比了 LGM、GS-LRM、MapAnything 和 STORM。指标包括 PSNR、SSIM 和深度 RMSE。
    *   **场景流估计**：对比了 NSFP、NSFP++ 和 STORM。指标包括 EPE3D、Acc 5/10 和角度误差。
    *   **语义分割**：对比了 SegFormer、Mask2Former、LSeg 等 SOTA 2D 分割模型。

### 4. 资源与算力
*   **硬件**：使用了 **64 台华为昇腾 (Huawei Ascend) 910B NPU**。
*   **训练配置**：Batch size 为 64，使用 AdamW 优化器。
*   **时长**：训练总时长为 **4 天**，迭代次数为 200k 次。

### 5. 实验数量与充分性
*   **实验规模**：使用了 1000 个 Waymo 驾驶序列（约 20 万帧图像），涵盖了复杂的城市交通场景。
*   **消融实验**：详细探讨了运动建模阶数（1-4 阶对比）、语义损失项的影响、以及在线（流式）与离线模式的性能/速度权衡。
*   **客观性**：实验不仅评估了全图指标，还专门针对“仅动态区域”进行了掩码评估，证明了其在处理运动物体上的显著优势。对比基准涵盖了从传统优化到最新前馈模型的多种范式，实验设计较为全面、公平。

### 6. 主要结论与发现
*   **性能领先**：SLARM 在动态估计精度上提升了 **21%**，重建 PSNR 提升了 **1.6 dB**，语义分割 mIoU 提升了 **20%**。
*   **运动建模的重要性**：三阶运动模型在捕捉人体步态等复杂动作时远优于匀速模型。
*   **语义与几何的互补**：引入语义蒸馏不仅实现了语言查询，还通过语义一致性约束显著降低了场景流的估计误差（EPE）。
*   **流式优势**：SLARM-W（窗口模式）在保持高性能的同时，实现了线性的推理时间和稳定的内存消耗。

### 7. 优点
*   **统一性**：在一个框架内解决了重建、运动和语义三个核心任务，且互有增益。
*   **无需监督**：运动学习完全基于渲染一致性，不依赖昂贵的 3D 场景流标注。
*   **实时潜力**：流式架构设计使其非常适合部署在自动驾驶和机器人等对延迟敏感的设备上。
*   **语言交互**：通过 LSeg 蒸馏，赋予了 3D 场景“可被 LLM 理解”的能力。

### 8. 不足与局限
*   **位姿依赖**：模型目前仍需要准确的相机位姿（Camera Poses）作为输入，无法处理位姿未知的视频。
*   **材质限制**：由于依赖光度一致性（Photometric Consistency），在处理玻璃、镜面等复杂反射材质时效果受限。
*   **计算开销**：虽然推理是前馈的，但训练过程仍需要大规模计算资源（64 块高性能 NPU）。
*   **长程依赖**：虽然使用了窗口注意力，但在处理极长时间跨度的遮挡恢复时，可能仍不如全局优化方法稳定。

（完）
