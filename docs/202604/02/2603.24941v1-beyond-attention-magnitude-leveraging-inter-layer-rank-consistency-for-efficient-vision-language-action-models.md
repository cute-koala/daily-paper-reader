---
title: "Beyond Attention Magnitude: Leveraging Inter-layer Rank Consistency for Efficient Vision-Language-Action Models"
title_zh: 超越注意力幅度：利用层间排名一致性实现高效视觉-语言-动作模型
authors: "Peiju Liu, Jinming Liu, Xipeng Qiu, Xuanjing Huang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24941v1"
tags: ["query:vlm"]
score: 8.0
evidence: 用于机器人操作的高效视觉-语言-动作（VLA）模型
tldr: "针对视觉-语言-动作（VLA）模型推理延迟高的问题，本文挑战了仅依赖注意力权重进行Token缩减的传统做法。提出TIES框架，利用跨层Token排名一致性动态选择关键视觉Token。该方法无需额外训练，在减少78% Token使用的同时，将机器人操作成功率提升了6%，实现了效率与性能的双重提升。"
selection_source: carryover_cache
motivation: 现有的Token缩减方法主要依赖静态的注意力权重，但这在VLA任务中可能导致性能下降且无法适应动态任务需求。
method: 提出TIES框架，通过结合注意力权重与跨层排名一致性（Tau引导），动态且自适应地筛选视觉Token。
result: "在CogACT + SIMPLER基准测试中，TIES在减少78% Token消耗的情况下，将平均成功率提高了6%。"
conclusion: TIES证明了利用层间一致性进行Token筛选的有效性，为构建高效且鲁棒的VLA模型提供了无需训练的新方案。
---

## 摘要
视觉-语言-动作（VLA）模型在机器人操控方面表现出色，但由于处理密集的视觉标记（tokens），面临显著的推理延迟。现有的标记缩减方法主要依赖注意力幅度作为静态选择标准。在这项工作中，我们对这一假设提出了挑战，揭示了高注意力标记是任务相关的，甚至可能降低策略性能。为解决这一问题，我们引入了 TIES（Tau 引导的层间高效选择），这是一个由层间标记排名一致性引导的动态框架。通过自适应地平衡注意力幅度与排名一致性，TIES 在无需额外训练的情况下确保了稳健的标记选择。在 CogACT + SIMPLER 基准测试中，TIES 在减少 78% 标记使用的同时，将平均成功率提高了 6%，并在多种解码器和基准测试中展现出强大的泛化能力。

## Abstract
Vision-Language-Action (VLA) models excel in robotic manipulation but suffer from significant inference latency due to processing dense visual tokens. Existing token reduction methods predominantly rely on attention magnitude as a static selection. In this work, we challenge this assumption, revealing that high-attention tokens are task-dependent and can even degrade policy performance. To address this, we introduce \textbf{TIES} (\textbf{T}au-guided \textbf{I}nter-layer \textbf{E}fficient \textbf{S}election), a dynamic framework guided by inter-layer token ranking consistency. By adaptively balancing attention magnitude with ranking consistency, TIES ensures robust token selection without requiring additional training. On the CogACT + SIMPLER benchmark, TIES improves average success rates by 6\% while reducing token usage by 78\%, and demonstrate strong generalization across diverse decoders and benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Attention Magnitude: Leveraging Inter-layer Rank Consistency for Efficient Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型在机器人操控中表现优异，但由于Transformer对大量视觉Token处理的平方级复杂度，导致推理延迟高，难以实时部署。
*   **研究动机**：现有Token缩减方法大多假设“注意力权重（Attention Magnitude）越大越重要”。作者通过实验挑战了这一假设，发现：
    1.  高注意力Token具有任务依赖性。
    2.  在某些任务（如开抽屉）中，保留高注意力Token反而会导致性能下降（性能反转），因为这些Token可能只是数值计算的“汇聚点（Sinks）”或伪相关噪声。
*   **整体含义**：论文旨在寻找一种比单纯注意力权重更可靠的指标，以实现动态、高效且鲁棒的视觉Token筛选。

### 2. 方法论：核心思想与关键技术
论文提出了 **TIES (Tau-guided Inter-layer Efficient Selection)** 框架，其核心思想是利用**层间排名一致性**来衡量Token的可靠性。

*   **核心指标（Kendall’s $\tau$）**：使用肯德尔等级相关系数（Kendall rank correlation coefficient）来计算相邻Transformer层之间Top-k Token排名的稳定性。
    *   **低 $\tau$（波动大）**：代表模型正在动态评估信息，此时高注意力Token通常是可靠的（Type 1）。
    *   **高 $\tau$（高度一致）**：作者提出“伪锁定假设（Spurious Locking Hypothesis）”，认为此时模型可能陷入了局部最优，固定在冗余或有害的特征（如背景纹理）上（Type 2）。
*   **算法流程**：
    1.  **离线校准**：从目标数据集中抽取少量样本（约100帧），计算 $\tau$ 的中位数和分布作为基准。
    2.  **在线推理**：
        *   **相似性触发**：利用视频流的时间冗余，仅在当前帧与锚点帧视觉差异较大时重新计算 $\tau$。
        *   **动态选择策略**：根据当前 $\tau$ 与基准的偏离程度，动态调整 **Top-k采样**（关注重要性）与 **均匀采样/多样性采样**（关注信息覆盖面）的比例。
    3.  **软/硬 TIES**：提供基于阈值的二选一（Hard-TIES）或基于线性插值的平滑过渡（Soft-TIES）两种模式。

### 3. 实验设计
*   **数据集与场景**：
    *   **SIMPLER Benchmark**：包含 Google Robot 和 WidowX 平台的模拟环境，分为“视觉匹配（VM）”和“变体聚合（VA，含光照、背景变化）”两种协议。
    *   **LIBERO Benchmark**：用于验证跨任务域的泛化能力。
*   **对比方法**：
    *   **FastV**：基于层级的静态Token剪枝。
    *   **VLA-Cache**：基于时间相似性的Token缓存。
    *   **EfficientVLA**：结合Token剪枝、层剪枝和解码器优化的综合方法。
*   **基础模型**：验证了 CogACT（基于扩散Transformer）、OpenVLA（自回归）和 OpenVLA-OFT（线性回归头）等多种架构。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A800 GPU** 上执行。
*   **训练/推理时长**：由于 TIES 是**无需训练（Training-free）**的即插即用方法，因此不存在大规模训练算力消耗。论文强调了其在推理阶段的效率，通过减少 78% 的 Token 使用显著降低了 FLOPs。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 大类机器人操控任务（PickCan, MoveNear, Drawer, DrawerApple）。
    *   进行了不同剪枝比例（28, 56, 112, 256 tokens）的对比实验。
    *   包含针对不同采样策略（Middle, Uniform, DivPrune）的消融实验。
*   **充分性与公平性**：实验在统一的 SIMPLER 框架下进行，对比了近年来最先进的加速方案。通过跨模型（CogACT vs OpenVLA）和跨基准（SIMPLER vs LIBERO）的测试，证明了结论的普适性。实验设计考虑了环境扰动（VA协议），体现了客观性。

### 6. 主要结论与发现
*   **性能提升**：在减少 78% Token 的情况下，TIES 在 SIMPLER 上的平均成功率比全 Token 基准提升了 **6%**。
*   **去噪作用**：在环境复杂的变体聚合（VA）任务中，TIES 的表现远超传统方法，证明了其动态策略能有效过滤误导性的高注意力噪声。
*   **效率**：即使在极端的 83.6% FLOPs 削减下，成功率依然优于或持平于原始模型。
*   **指标有效性**：Kendall’s $\tau$ 在预测注意力可靠性方面的 AUC 达到 0.91，远优于信息熵等静态指标。

### 7. 优点（亮点）
*   **即插即用**：无需对预训练模型进行任何微调或重新训练，部署成本极低。
*   **理论洞察**：挑战了“注意力即重要性”的直觉，提出了“伪锁定”这一深刻见解，为理解 Transformer 内部机制提供了新视角。
*   **动态自适应**：相比静态剪枝，TIES 能根据输入内容实时调整策略，兼顾了推理效率与操控精度。

### 8. 不足与局限
*   **启发式依赖**：虽然实验效果好，但 $\tau$ 与 Token 重要性之间的关联仍属于启发式观察，缺乏严格的因果推导和理论证明。
*   **空间冗余局限**：目前主要针对视觉空间维度的冗余，尚未深入探讨视觉与复杂语言指令之间的跨模态细粒度对齐优化。
*   **校准依赖**：虽然校准过程很轻量，但仍需要少量目标域数据来确定 $\tau$ 的分布，对于完全未知的极端环境可能存在泛化风险。

（完）
