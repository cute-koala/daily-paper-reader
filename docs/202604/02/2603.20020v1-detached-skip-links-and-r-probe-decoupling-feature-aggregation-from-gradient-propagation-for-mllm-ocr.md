---
title: "Detached Skip-Links and $R$-Probe: Decoupling Feature Aggregation from Gradient Propagation for MLLM OCR"
title_zh: 分离式跳跃连接与 $R$-Probe：为多模态大语言模型 OCR 任务解耦特征聚合与梯度传播
authors: "Ziye Yuan, Ruchang Yao, Chengxin Zheng, Yusheng Zhao, Daxiang Dong, Ming Zhang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20020v1"
tags: ["query:vlm"]
score: 8.0
evidence: 优化多模态大模型在OCR任务中的特征融合
tldr: 针对多模态大模型在OCR任务中细粒度视觉细节丢失的问题，本文发现传统的跳跃连接会导致高层语义梯度干扰底层视觉信号。为此，作者提出了“分离式跳跃连接”（Detached Skip-Links），在保留前向特征融合的同时阻断反向梯度传播，并引入R-Probe评估视觉特征的可重建性。该方法在不增加参数的情况下，显著提升了模型在OCR及通用多模态任务上的表现。
selection_source: carryover_cache
motivation: 传统的跳跃连接在多层特征融合时会产生梯度干扰，导致底层视觉信号被高层语义目标覆盖，从而损害OCR性能。
method: 提出了Detached Skip-Links以解耦特征聚合与梯度传播，并设计了R-Probe工具来量化LLM对视觉特征的细粒度重建能力。
result: 在多个ViT骨干网络和多模态基准测试中，该方法在不增加可学习参数的情况下，一致性地提高了OCR和通用任务的性能。
conclusion: 通过解耦梯度传播，可以有效保护底层视觉细节，从而提升MLLM处理细粒度视觉任务的能力。
---

## 摘要
多模态大语言模型 (MLLM) 在高层推理方面表现出色，但在细粒度视觉细节受损或对齐不良的 OCR 任务中表现不佳。我们发现了一个在多层特征融合中被忽视的优化问题。跳跃路径引入了从高层语义目标到早期视觉层的直接反向传播路径。这种机制会覆盖低层信号并导致训练不稳定。为了减轻这种梯度干扰，我们提出了分离式跳跃连接 (Detached Skip-Links)，这是一种极简的修改，在正向传播中重用浅层特征，同时在联合训练期间阻断通过跳跃分支的梯度。这种非对称设计减少了梯度干扰，在不增加可学习参数的情况下提高了稳定性和收敛性。为了诊断细粒度信息是否被保留并可供 LLM 使用，我们引入了 $R$-Probe，它使用从 LLM 前四分之一层初始化的浅层解码器来衡量投影视觉 token 的像素级可重建性。在多个 ViT 骨干网络和多模态基准测试中，以及在高达 700 万个训练样本的规模下，我们的方法持续改进了以 OCR 为中心的基准测试，并在通用多模态任务上带来了明显的提升。

## Abstract
Multimodal large language models (MLLMs) excel at high-level reasoning yet fail on OCR tasks where fine-grained visual details are compromised or misaligned. We identify an overlooked optimization issue in multi-layer feature fusion. Skip pathways introduce direct back-propagation paths from high-level semantic objectives to early visual layers. This mechanism overwrites low-level signals and destabilizes training. To mitigate this gradient interference, we propose Detached Skip-Links, a minimal modification that reuses shallow features in the forward pass while stopping gradients through the skip branch during joint training. This asymmetric design reduces gradient interference, improving stability and convergence without adding learnable parameters. To diagnose whether fine-grained information is preserved and usable by an LLM, we introduce $R$-Probe, which measures pixel-level reconstructability of projected visual tokens using a shallow decoder initialized from the first quarter of the LLM layers. Across multiple ViT backbones and multimodal benchmarks, and at scales up to 7M training samples, our approach consistently improves OCR-centric benchmarks and delivers clear gains on general multimodal tasks.

---

## 论文详细总结（自动生成）

这篇论文针对多模态大语言模型（MLLM）在光学字符识别（OCR）任务中的性能瓶颈提出了创新的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 MLLM 在高层推理上表现出色，但在 OCR 和细粒度视觉感知任务中经常出现“幻觉”或识别错误。
*   **核心矛盾**：研究发现，为了引入细粒度信息，研究者常采用多层特征融合（跳跃连接）。然而，这建立了一条从 LLM 语义目标到视觉编码器浅层块的直接反向传播路径。
*   **发现问题**：高层的语义梯度（如预测下一个词的损失）会干扰并“覆盖”浅层原本用于捕捉几何结构、笔画等低层视觉信号的参数，导致训练不稳定和特征退化。

### 2. 方法论
*   **分离式跳跃连接 (Detached Skip-Links)**：
    *   **核心思想**：在正向传播中保留浅层特征的聚合，但在反向传播中通过“停止梯度”（stop-gradient）操作阻断语义梯度流向视觉编码器的浅层。
    *   **技术细节**：将 ViT 的中间特征分为浅层组（$h_{shallow}$）和深层组（$h_{deep}$）。融合公式为：$z = MLP([h_{main}; h_{deep}; sg(h_{shallow})])$，其中 $sg$ 代表梯度截断。
    *   **逻辑**：深层特征与语义目标对齐，受益于联合优化；浅层特征负责几何细节，通过截断梯度来保护其预训练的视觉先验不被破坏。
*   **R-Probe 诊断工具**：
    *   **功能**：衡量视觉 token 是否保留了足够的像素级细节，以及这些信息是否能被 LLM 架构有效利用。
    *   **实现**：使用一个浅层 Transformer 解码器（初始化自 LLM 的前 1/4 层）尝试从投影后的视觉 token 中重建原始像素。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：500 万（5M）多模态样本（包括 InternVL, GRIT, LLaVAR 等开源数据集）。
    *   **微调 (SFT)**：200 万（2M）任务特定样本（涵盖 OCR、文档理解、数学、图表等）。
*   **基准测试 (Benchmark)**：涵盖 22 个基准测试，分为四类：STEM 难题（MMMU, MathVista）、通用感知（SEEDBench, MMVet）、对齐（POPE）和 OCR（DocVQA, OCRBench, TextVQA 等）。
*   **对比方法**：对比了 DenseConnector (DC)、DeepStack 和 Multi-Layer Fusion (ML) 等主流的多层融合策略。

### 4. 资源与算力
*   **算力配置**：文中提到了使用 DeepSpeed ZeRO-3、FlashAttention-2 和 bf16 精度进行训练。
*   **训练细节**：全局 Batch Size 为 256，预训练序列长度 16k，SFT 序列长度 8k。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）以及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在高达 700 万样本的规模下验证了方法。
*   **消融实验**：针对特征采样步长（Stride）和截断层数（Detach Layers）做了详细的网格搜索。
*   **泛化性验证**：在四种不同的视觉骨干网络（InternViT-300M, AimV2-L, SigLip2-So400M, Perception Encoder）上进行了重复实验。
*   **充分性评价**：实验设计非常充分且客观。通过梯度统计分析（SNR、正交性测量）从理论层面支撑了实验结果，并对比了多种 SOTA 融合方案，证明了该方法的普适性。

### 6. 主要结论与发现
*   **梯度解耦的必要性**：截断浅层梯度能显著提升训练稳定性和收敛速度，防止视觉先验被语义噪声破坏。
*   **性能提升**：在 OCR 相关任务上提升显著（如在 PE 骨干网络上 OCR 平均分提升 3.1），同时在通用多模态任务上也保持了增长。
*   **R-Probe 的有效性**：实验证明重建损失越低，模型在下游感知任务中的表现通常越好，R-Probe 可作为模型设计的有效诊断指标。

### 7. 优点
*   **极简且高效**：不需要增加任何额外的可学习参数，仅通过修改计算图即可获得性能增益。
*   **理论与实践结合**：不仅有大规模实验，还提供了贝叶斯风险分解和梯度动力学分析，解释了“为什么有效”。
*   **诊断创新**：R-Probe 提供了一种不依赖最终文本输出、直接评估视觉 token 质量的新手段。

### 8. 不足与局限
*   **推理权衡**：将 R-Probe 作为辅助损失函数训练时，虽然提升了 OCR 性能，但在某些高层推理任务（如 CharXiv RQ）上出现了轻微的性能下降。
*   **超参数依赖**：虽然方法稳健，但仍需针对不同的 ViT 架构手动选择截断哪些层以及融合的步长。
*   **应用范围**：主要针对 OCR 和细粒度感知，对于不依赖局部细节的纯语义理解任务，该方法的边际收益可能有限。

（完）
