---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧即一标记：基于增量标记的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:q1"]
score: 10.0
evidence: 在特征空间中使用增量标记的生成式世界建模
tldr: 本文针对视频世界建模中预测多样未来的挑战，提出DeltaTok和DeltaWorld方法。研究背景是现有判别式模型仅输出确定性预测，而生成式模型计算昂贵。方法核心是将连续帧间的视觉基础模型特征差异编码为紧凑的delta token序列，从而大幅降低表示维度，并基于此构建高效生成模型进行多假设训练与推理。主要贡献是实现参数和计算量显著减少（分别超35倍和2000倍）的同时，生成更贴合真实世界的多样未来预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 现有生成式世界模型计算成本高，而判别式模型无法捕捉未来多样性，需高效生成多假设预测。
method: 提出DeltaTok将连续帧间的特征差异编码为单个delta token，并构建DeltaWorld模型在这些token上生成多样未来。
result: 在密集预测任务中，DeltaWorld比现有生成模型参数少35倍、FLOPs少2000倍，且预测更贴近真实结果。
conclusion: DeltaTok与DeltaWorld通过创新的delta token表示和高效生成建模，显著提升了视频世界建模的效率和多样性。
---

## 摘要
预测多样化的未来状态是视频世界建模的核心挑战。判别式世界模型生成确定性预测，隐式地平均了可能的未来，而现有的生成式世界模型计算成本仍然高昂。近期研究表明，在视觉基础模型的特征空间中预测未来（而非针对像素重建优化的潜在空间），所需的世界模型参数量显著减少。然而，此类方法大多仍为判别式。本文提出DeltaTok——一种将连续帧间的VFM特征差异编码为单个连续“增量”标记的标记器，以及DeltaWorld——一种基于这些标记运行的生成式世界模型，能高效生成多样且合理的未来。增量标记将视频从三维时空表示简化为一维时间序列，例如对于512x512帧的视频可实现1,024倍的标记缩减。这种紧凑表示使得可处理的多假设训练成为可能：并行生成多个未来序列并仅监督最优结果。在推理阶段，该方法能在单次前向传播中实现多样化预测。密集预测任务的实验表明，DeltaWorld生成的未来预测与真实结果更吻合，同时参数量比现有生成式世界模型减少35倍以上，计算量降低2,000倍以上。代码与权重：https://deltatok.github.io。

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 论文总结：A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens

## 1. 核心问题与整体含义
*   **研究动机**：在自动驾驶等场景中，世界模型需要预测未来环境状态以辅助决策。由于未来具有不确定性，模型必须能够生成**多样且合理**的未来假设。现有方法存在两大痛点：
    *   **判别式模型**（如DINO-world）：仅输出单一确定性预测，在不确定性下会坍缩为条件均值，无法捕捉多样的未来模式。
    *   **生成式模型**（如Cosmos）：虽然能生成多样结果，但通常计算成本极高。它们通常在为像素重建优化的潜在空间中操作，需要多次前向传播（如扩散模型的迭代去噪），且表示中存在大量时空冗余。
*   **研究目标**：开发一个能够高效生成多样化、高质量未来预测的生成式世界模型。

## 2. 方法论
*   **核心思想**：利用视频帧间变化的低维特性，将建模重点从“重建每一帧”转移到“预测帧间变化”。
*   **关键技术细节**：
    1.  **DeltaTok（增量标记器）**：
        *   输入：由冻结的视觉基础模型（VFM，本文使用DINOv3）提取的连续两帧特征图 `x_{t-1}` 和 `x_t`。
        *   过程：通过一个基于Vision Transformer的编码器-解码器架构，将两帧特征图的差异压缩编码为一个单一的、连续的“增量标记” `z_t`。
        *   输出：解码器利用前一帧特征 `x_{t-1}` 和增量标记 `z_t` 来重建当前帧特征 `\hat{x}_t`。
        *   效果：将视频表示从三维时空网格（H×W×T）压缩为一维时间序列（T个标记），大幅减少计算量。
    2.  **DeltaWorld（增量世界模型）**：
        *   **架构基础**：基于判别式世界模型DINO-world的预测器架构进行改造。
        *   **操作空间**：预测器不再直接处理空间特征图，而是对由DeltaTok生成的增量标记序列 `Z_{1:t}` 进行操作，直接预测下一个增量标记 `\hat{z}_{t+1}`。
        *   **生成机制 - Best-of-Many (BoM)训练**：
            *   训练时：从高斯分布中采样K个不同的噪声查询向量 `q_k`，输入预测器得到K个候选的未来增量标记。仅对与真实未来最接近的那个候选进行监督和反向传播。
            *   推理时：输入不同的噪声查询向量，即可在单次前向传播中并行生成K个不同的未来假设序列。
        *   **优势结合**：“增量表示”极大压缩了建模对象，“BoM训练”实现了单次前向的多样化生成。

## 3. 实验设计
*   **评估基准与任务**：遵循密集预测基准 Dense Forecasting Benchmark。任务包括语义分割和单目深度估计。
*   **主要数据集**：
    *   VSPW (语义分割)
    *   Cityscapes (语义分割)
    *   KITTI (深度估计)
*   **评估设置**：
    *   短时预测 (~0.2秒)：直接一步预测。
    *   中时预测 (~0.6秒)：三步自回归展开推理。
    *   指标：“最佳”（20个样本中最接近真实值的）和“平均”（20个样本特征平均后计算）。
*   **对比方法**：
    1.  Copy Last / Present (下界/上界)。
    2.  DINO-world (判别式基线)。
    3.  Cosmos-4B / Cosmos-12B (基于像素重建潜在空间的生成式世界模型)。

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量和总训练时长。但提供了以下效率相关的量化信息：
*   FLOPs对比显示DeltaWorld比Cosmos少用约2000倍的计算量进行推理（见表3）。
*   在消融实验中报告了相对训练时间和GPU内存使用量（见表2）。
*   实现细节中提到使用8张NVIDIA H200 GPU进行训练时间/内存测量。

## 5. 实验数量与充分性
实验设计较为系统和充分：
1.  **渐进式消融实验 (表2)**：清晰地展示了从判别式基线到最终DeltaWorld的演变过程：(0)判别式 -> (1) BoM训练 -> (2)全帧压缩 -> (3)增量压缩。每一步都评估了性能、计算量和内存的变化，论证了每个组件的必要性及效果。
2.  **超参数分析实验 (图5)**：研究了BoM训练中查询数量K对最终“最佳”和“平均”性能的影响趋势。
3.  **主实验结果对比 (表3)**：在多个数据集和任务上全面对比了所有基线方法，证明了DeltaWorld在效率和质量上的综合优势。
4.  **扩展验证实验(附录)**：
    *   验证了delta token思想在其他判别式架构(DINO-Foresight)上的有效性(附录C)。
    *   提供了详细的FLOPs分解分析(附录B)。
    *   展示了丰富的定性结果(附录E)，包括多样性样本可视化。

总体而言，实验覆盖了定量性能、效率分析、消融研究和定性展示，对比对象选择合理（包括SOTA生成式和判别式方法），评估指标标准且全面（兼顾多样性和准确性），具有较好的客观性和公平性。

## 6.主要结论与发现
1. DeltaTok能够将视频高效地表示为紧凑的一维增量标记序列，显著降低建模复杂度。
2.DeltaWorld结合BoM目标函数,能够在单次前向传播中高效地生成多样化的未来假设,其最佳样本质量超越了现有的计算密集型生成模型(Cosmos)。
3.DeltaWorld的平均预测质量与针对平均优化过的判别式基线(DINO-world)相当甚至更优,表明其生成的多样性是围绕合理模式的,而非随机噪声.
4.DeltaWorld实现了显著的效率提升:相比Cosmos,参数量减少35倍以上,推理FLOPs减少2000倍以上.

##7优点
1.**创新性的表示学习**:提出“delta token”概念,巧妙利用视频的时间连续性,将建模目标从绝对状态转变为相对变化,极大地压缩了表示空间.
2.**高效的生成范式**:通过BoM训练策略,避免了扩散模型等需要的多次迭代采样,实现了单次前向传播的多样化生成.
3.**显著的效率优势**:方法在保持甚至提升预测质量的同时,带来了数量级的参数量和计算量下降.
4.**系统严谨的实验**:通过逐步消融、详尽对比和多维度评估(最佳/平均),有力地支撑了核心论点.

##8不足与局限
1.**分布建模的理论保证不足**:BoM目标函数缺乏像扩散模型那样明确的概率分布建模连接。模型的多样性覆盖受限于训练时探索的噪声查询数量K,没有机制确保采样分布能准确反映不同未来的真实发生概率.
2.**误差累积风险**:由于采用自回归展开并在前一帧重建基础上应用新的delta token进行解码,编码器和预测器的误差可能在多步推理中累积放大.
3.**依赖预训练的VFM**:整个框架的性能依赖于冻结的视觉基础模型(DINOv3)所提供的特征质量.
4.**应用场景限制**:目前主要针对短中期视觉状态预测进行了验证。对于需要更长时序推理或涉及复杂物理交互的场景,其有效性有待进一步检验.

(完)

（接上文）

## 9. 未来展望
1.  **概率建模的深化**：探索将BoM训练与更严格的概率生成模型（如归一化流、变分自编码器）相结合，为生成的多样性提供可解释的概率分布。
2.  **长时序预测扩展**：研究如何将delta token序列与更强大的时序模型（如状态空间模型）结合，以应对更长、更复杂的未来预测任务。
3.  **多模态与决策集成**：将高效的视觉世界模型输出作为智能体（如自动驾驶系统）的感知输入，探索其在闭环决策和规划中的应用潜力。
4.  **基础模型的协同进化**：随着视觉基础模型能力的不断提升，DeltaTok的压缩效率和表征能力有望进一步增强。

## 10. 总结
本文提出的DeltaWorld框架，通过创新的“增量标记”表示和“最佳候选”训练策略，在生成式世界建模领域实现了质量与效率的突破。它成功地将视频的未来预测问题转化为对低维、连续变化的建模，并通过单次前向传播即可生成多样化的合理未来假设。实验表明，该方法在显著降低计算成本的同时，其预测质量媲美甚至超越了需要巨大算力的现有方法。尽管在概率解释性和长程推理方面存在局限，但其核心思想——利用连续性先验进行高效表示——为构建实用、轻量的生成式世界模型提供了富有前景的新方向。

（完）
