---
title: Efficient Spatio-Temporal Grounding with Multimodal Large Models via Second-Level Tracking and RL Verification
title_zh: 通过二级跟踪和强化学习验证的高效时空定位与多模态大模型
authors: "Tianshu Zhang, Yan Wang, Ji Qi, Lijie Wen"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.29023v1"
tags: ["query:vt"]
score: 9.0
evidence: 语言查询的时空定位与跟踪
tldr: 时空定位面临长视频计算开销大和跟踪不稳定的挑战。本文提出将帧级推理转为秒级跟踪并跨秒平滑，利用多模态大模型合成链式思维轨迹，并用真实标注替换以避免噪声。通过强化学习基于t_IoU+mv_IoU验证器优化策略。实验表明该方法在多种帧率设置下兼顾效率与定位质量，为长视频时空定位提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29023v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1340, \"height\": 664, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29023v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1289, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29023v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1461, \"height\": 1349, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29023v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1427, \"height\": 509, \"label\": \"Figure\"}]"
motivation: 长视频时空定位中帧级推理计算昂贵且不稳定，现有方法难以平衡效率与精度。
method: 采用秒级跟踪与跨秒平滑压缩序列，合成链式思维轨迹并用真实标注替代，结合强化学习优化策略。
result: 在多种FPS设置下，方法实现效率与定位质量的有效权衡，优于直接帧级推断。
conclusion: 该方法通过近似推理和噪声规避，显著降低了计算成本同时保持高定位准确性。
---

## 摘要
长时间视频中的时空定位需要精确的时间定位和基于自然语言查询的鲁棒目标跟踪。虽然最近的视觉语言模型展现出强大的推理能力，但直接对长序列逐帧推理计算成本高且不稳定。我们提出了一种实用流程，将帧级跟踪转换为二级跟踪，并通过跨二级平滑保持连续性同时减少序列长度。为改进推理监督，我们利用先进多模态模型合成思维链式轨迹用于时间定位和目标选择，并用真实标注替换生成的时空坐标以避免噪声监督。我们进一步使用基于t_IoU+mv_IoU的验证器通过强化学习优化策略。跨多帧率设置的实验表明，我们的方法在效率与定位质量之间实现了强平衡。

## Abstract
Spatio-temporal grounding in long videos requires precise temporal localization and robust object tracking conditioned on natural-language queries. While recent vision-language models (VLMs) show strong reasoning ability, directly applying frame-by-frame inference to long sequences is computationally expensive and unstable. We propose a practical pipeline that shifts from frame-level to second-level tracking and performs cross-second smoothing to preserve continuity while reducing sequence length. To improve reasoning supervision, we synthesize chain-of-thought style trajectories using advanced multimodal models for temporal localization and target selection, and replace generated spatio-temporal coordinates with ground-truth annotations to avoid noisy supervision. We further optimize the policy with reinforcement learning using a verifier based on $t\_\mathrm{IoU}+mv\_\mathrm{IoU}$. Experiments across multiple FPS settings show that our method achieves a strong trade-off between efficiency and localization quality.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **任务定义**：时空视频定位（STVG）要求模型根据自然语言查询在视频中同时定位事件的时间区间和空间轨迹，例如“穿红色衣服的人拿起包”需要找到事件发生的时间段以及该目标在整个时段内的边界框序列。
- **现有挑战**：
  - 长视频中帧级推理计算成本高、不稳定；密集采样导致视觉令牌爆炸，稀疏采样可能错过关键帧。
  - 多模态大语言模型（MLLM）虽有强大推理能力，但直接应用于STVG面临长视觉上下文、细粒度时间推理和精确坐标生成的困难。
  - 生成坐标易产生抖动和身份漂移，SFT仅优化令牌似然而非任务指标。
- **动机**：需要一个MLLM-based STVG系统，能在长视频中既保留语义推理，又控制序列长度和接地噪声，平衡效率与精度。

## 2. 方法论：核心思想、关键技术细节、公式/算法流程

### 核心思想
- **二级跟踪（Second-Level Tracking）**：将视频按秒分组，以秒为基本推理单元代替帧，大幅减少序列长度，同时保持事件结构。
- **跨秒平滑（Cross-Second Smoothing）**：后处理中利用相邻有效框稳定预测，减少抖动。
- **三阶段训练流水线**：CPT（连续预训练）→ SFT（监督微调）→ RL（强化学习），逐步提升时空接地能力。
- **推理监督修正**：使用Gemini生成思维链（CoT）风格的推理轨迹，但最终坐标和分段替换为真实标注，避免几何噪声监督。
- **RL验证器**：基于 tIoU + mvIoU 的奖励函数，直接优化与评估指标对齐的接地质量。

### 关键技术与公式

1. **二级表示转换**：
   - 视频 V = {f₁,…,f_T}（帧率 F）→ 秒级单元 S = {S₁,…,S_K}，K = ⌈T/F⌉。
   - 模型输出：时间区间 [t̂_s, t̂_e] 和每个秒级的边界框序列 B̂ = {b̂_k}。
   - 标注转换：聚合帧级可见框到秒级。

2. **SFT阶段的坐标修正**：
   - 使用Gemini生成推理轨迹（关注目标识别和粗糙时间窗口），但最终结构化输出中的时间区间和框替换为真实标注。
   - 损失函数：L_sft = –E[∑ log π_θ(τ_u | τ_{<u}, S, q)]。

3. **RL优化**：
   - 奖励函数：R = w_t·tIoU + w_m·mvIoU
     - tIoU = |[t̂_s, t̂_e] ∩ [t*_s, t*_e]| / |[t̂_s, t̂_e] ∪ [t*_s, t*_e]|
     - mvIoU = (1/|Ω|) Σ_{k∈Ω} IoU(b̂_k, b*_k)，Ω = [t̂_s, t̂_e] ∩ [t*_s, t*_e]
   - GRPO风格优化：从SFT模型初始化，采样一组输出，计算规范化优势Â_i，使用不对称裁剪（ϵ_low=0.20, ϵ_high=0.28）优化目标 J_RL。

4. **训练阶段组成**：
   - **CPT**：混合STVG数据、多目标跟踪（MOT）、单目标跟踪（SOT）、分割数据、通用视频理解数据，统一为秒级格式，多数样本1 FPS采样，少量高FPS。
   - **SFT**：20K示例来自VidSTG和HC-STVG，使用Gemini生成推理轨迹，坐标修正。
   - **RL**：使用tIoU+mvIoU验证器优化。

5. **推理过程**：
   - 输入语言查询和秒级视频单元 → 输出时间区间和二级轨迹 → 解析结构答案，丢弃无效状态 → 应用跨秒平滑 → 可插值回帧级。

## 3. 实验设计：数据集、Benchmark、对比方法

### 数据集与Benchmark
- **VidSTG**：基于VidOR的STVG基准，包含陈述句和疑问句形式，测试不同语言表达。
- **HC-STVG**：以人为中心的STVG，复杂多人场景，强调目标区分、交互理解和身份保持。
- **Video-MME-v2**：通用视频理解基准，用于检查训练是否过度适配坐标预测。

### 对比方法
- 通用MLLM基线：Qwen3.5 (397B)、Seed2.0-pro (参数量未公开)、Kimi-k2.5 (1T)、Gemini3-Flash (未公开)、Gemini3-Pro (未公开)。
- 自对比版本：Ours-SFT、Ours-RL (仅mvIoU)、Ours-RL (tIoU+mvIoU，最终版本)，均为9B参数量。

### 评估指标
- mtIoU：平均时间IoU。
- mvIoU：平均时空管IoU（主要完整性指标）。
- vIoU@R：管IoU超过阈值R（如0.3或0.5）的百分比。

## 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长等具体算力信息**。只提到模型是9B参数量（基于GLM-V风格架构），训练分为三个阶段。未提供硬件细节，可能出于篇幅或保密考虑。读者需关注未来项目页面获取复现材料。

## 5. 实验数量与充分性

- **主实验**：在VidSTG和HC-STVG两个基准上与五个强基线对比，报告了mtIoU、mvIoU、vIoU@0.5，三个运行取均值。
- **消融实验**：
  - RL奖励消融：对比仅mvIoU vs. tIoU+mvIoU，显示tIoU加速收敛并提升最终性能。
  - 训练FPS敏感性：对比0.5FPS、1FPS、动态FPS，1FPS最优。
  - 通用理解验证：在Video-MME-v2上对比GLM-4.1V-Thinking，显示提升。
- **定性案例**：两个典型样本（犯罪跟踪、超车跟踪），展示推理轨迹和性能。
- **充分性分析**：
  - 实验覆盖了主要STVG基准和通用理解基准，对比方法涵盖多种规模MLLM，消融较完整。
  - 但缺少与专门STVG模型（非MLLM基线）的对比，也未在更多长视频数据集上测试。
  - 实验公平性：所有对比均为公开基线，自对比版本分离了SFT和RL效果，设置合理。

## 6. 主要结论与发现

- **秒级跟踪+RL验证器使9B模型超越更大通用MLLM**：在VidSTG和HC-STVG上，最终RL模型mtIoU达31.35和60.04，mvIoU达23.79和40.99，远高对比方法。
- **三阶段训练有序提升性能**：SFT适应输出格式和推理行为，RL进一步优化接地指标。tIoU+mvIoU联合奖励比仅mvIoU收敛更快、最终更好。
- **时空定位训练反哺通用视频理解**：在Video-MME-v2上，定位训练后模型非线性推理和简单准确率均提升，表明任务专门化未损害通用能力。
- **1FPS训练在效率-质量权衡中表现最佳**，高于0.5FPS和动态FPS。

## 7. 优点：方法与实验设计的亮点

- **创新设计**：
  - 首次将MLLM-based STVG统一到秒级跟踪框架，有效压缩长序列。
  - 推理监督分离：保留CoT语义推理，但用真实坐标替换，避免生成几何噪声污染训练。
  - RL奖励函数tIoU+mvIoU直接对应评估指标，且tIoU较早时间提供密集信号。
- **实验设计**：
  - 分阶段分析SFT和RL贡献，清晰展示各阶段效果。
  - 包含通用视频理解验证，证明方法不牺牲泛化能力。
  - 定性案例展示推理链条，增强可解释性。
- **实用性**：模型仅9B，推理时序列长度约等于视频秒数，效率高，适于长视频。

## 8. 不足与局限

- **实验覆盖局限**：
  - 未与专门的STVG模型（如STVGFormer、TubeDETR等）对比，仅限MLLM基线。
  - 仅在VidSTG和HC-STVG上验证，未在更多长视频或开放世界数据集测试。
  - 训练FPS敏感性测试仅在Dancetrack上？原文提到“tested on Dancetrack”，但未解释与STVG的关系，令人困惑。
- **偏差风险**：
  - SFT数据仅20K示例，可能不足以覆盖复杂场景；Gemini生成的推理轨迹可能存在语义偏差。
  - RL阶段依赖VidSTG和HC-STVG的标注，若标注有噪声会影响优化。
- **应用限制**：
  - 方法假设秒级单元足够捕捉事件本质，但对于极短动作（<1秒）可能失效。
  - 跨秒平滑后处理可能模糊精细边界。
  - 未探索实时性要求下的延迟，仅提及“效率”，但无具体速度数据。
- **资源信息缺失**：无GPU、训练时长、内存等细节，影响可复现性评估。

（完）
